// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { StatusDetails } from "../types/index.ts";

/**
 * Get all statuses
 *
 * Returns a list of all statuses associated with active workflows.
 *
 * This operation can be accessed anonymously.
 *
 * [Permissions](#permissions) required: *Browse projects* [project permission](https://support.atlassian.com/jira-cloud-administration/docs/manage-project-permissions/) for the project.
 *
 * @returns Returned if the request is successful.
 */
export function getStatuses(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusDetails[]> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/status",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get status
 *
 * Returns a status. The status must be associated with an active workflow to be returned.
 *
 * If a name is used on more than one status, only the status found first is returned. Therefore, identifying the status by its ID may be preferable.
 *
 * This operation can be accessed anonymously.
 *
 * [Permissions](#permissions) required: *Browse projects* [project permission](https://support.atlassian.com/jira-cloud-administration/docs/manage-project-permissions/) for the project.
 *
 * @param params.idOrName - The ID or name of the status.
 * @returns Returned if the request is successful.
 */
export function getStatus(
  ctx: ClientContext,
  params: {
    /** The ID or name of the status. */
    idOrName: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusDetails> {
  const { idOrName } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/status/{idOrName}",
    pathParams: { idOrName },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents issue workflow statuses. Use it to obtain a list of all statuses associated with workflows and the details of a status. */
export function createWorkflowStatusesService(ctx: ClientContext) {
  return {
    /**
     * Get all statuses
     *
     * Returns a list of all statuses associated with active workflows.
     *
     * This operation can be accessed anonymously.
     *
     * [Permissions](#permissions) required: *Browse projects* [project permission](https://support.atlassian.com/jira-cloud-administration/docs/manage-project-permissions/) for the project.
     *
     * @returns Returned if the request is successful.
     */
    getStatuses(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<StatusDetails[]> {
      return getStatuses(ctx, options);
    },

    /**
     * Get status
     *
     * Returns a status. The status must be associated with an active workflow to be returned.
     *
     * If a name is used on more than one status, only the status found first is returned. Therefore, identifying the status by its ID may be preferable.
     *
     * This operation can be accessed anonymously.
     *
     * [Permissions](#permissions) required: *Browse projects* [project permission](https://support.atlassian.com/jira-cloud-administration/docs/manage-project-permissions/) for the project.
     *
     * @param params.idOrName - The ID or name of the status.
     * @returns Returned if the request is successful.
     */
    getStatus(
      params: {
        /** The ID or name of the status. */
        idOrName: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StatusDetails> {
      return getStatus(ctx, params, options);
    },
  };
}
