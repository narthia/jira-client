// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { Avatar, ProjectAvatars } from "../types/index.ts";

/**
 * Set project avatar
 *
 * Sets the avatar displayed for a project.
 *
 * Use [Load project avatar](#api-rest-api-2-project-projectIdOrKey-avatar2-post) to store avatars against the project, before using this operation to set the displayed avatar.
 *
 * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
 *
 * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
 * @returns Returned if the request is successful.
 */
export function updateProjectAvatar(
  ctx: ClientContext,
  params: Avatar & {
    /** The ID or (case-sensitive) key of the project. */
    projectIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { projectIdOrKey, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/project/{projectIdOrKey}/avatar",
    pathParams: { projectIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete project avatar
 *
 * Deletes a custom avatar from a project. Note that system avatars cannot be deleted.
 *
 * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
 *
 * @param params.projectIdOrKey - The project ID or (case-sensitive) key.
 * @param params.id - The ID of the avatar.
 * @returns Returned if the request is successful.
 */
export function deleteProjectAvatar(
  ctx: ClientContext,
  params: {
    /** The project ID or (case-sensitive) key. */
    projectIdOrKey: string;
    /**
     * The ID of the avatar.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { projectIdOrKey, id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/project/{projectIdOrKey}/avatar/{id}",
    pathParams: { projectIdOrKey, id },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Load project avatar
 *
 * Loads an avatar for a project.
 *
 * Specify the avatar's local file location in the body of the request. Also, include the following headers:
 *
 *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
 *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
 *
 * For example:
 * `curl --request POST `
 *
 * `--user email@example.com:<api_token> `
 *
 * `--header 'X-Atlassian-Token: no-check' `
 *
 * `--header 'Content-Type: image/< image_type>' `
 *
 * `--data-binary "<@/path/to/file/with/your/avatar>" `
 *
 * `--url 'https://your-domain.atlassian.net/rest/api/2/project/{projectIdOrKey}/avatar2'`
 *
 * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
 *
 * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
 *
 * After creating the avatar use [Set project avatar](#api-rest-api-2-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
 *
 * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
 *
 * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
 * @param params.x - The X coordinate of the top-left corner of the crop region.
 * @param params.y - The Y coordinate of the top-left corner of the crop region.
 * @param params.size - The length of each side of the crop region.
 * @returns Returned if the request is successful.
 */
export function createProjectAvatar(
  ctx: ClientContext,
  params: {
    /** The ID or (case-sensitive) key of the project. */
    projectIdOrKey: string;
    /**
     * The X coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    x?: number;
    /**
     * The Y coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    y?: number;
    /**
     * The length of each side of the crop region.
     *
     * @default 0
     * @format int32
     */
    size?: number;
    body: Blob;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Avatar> {
  const { projectIdOrKey, x, y, size, body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/project/{projectIdOrKey}/avatar2",
    pathParams: { projectIdOrKey },
    query: { x, y, size },
    headers: options?.headers,
    body: body,
    bodyType: "binary",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all project avatars
 *
 * Returns all project avatars, grouped by system and custom avatars.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
 *
 * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
 * @returns Returned if request is successful.
 */
export function getAllProjectAvatars(
  ctx: ClientContext,
  params: {
    /** The ID or (case-sensitive) key of the project. */
    projectIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectAvatars> {
  const { projectIdOrKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/project/{projectIdOrKey}/avatars",
    pathParams: { projectIdOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents avatars associated with a project. Use it to get, load, set, and remove project avatars. */
export function createProjectAvatarsService(ctx: ClientContext) {
  return {
    /**
     * Set project avatar
     *
     * Sets the avatar displayed for a project.
     *
     * Use [Load project avatar](#api-rest-api-2-project-projectIdOrKey-avatar2-post) to store avatars against the project, before using this operation to set the displayed avatar.
     *
     * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
     *
     * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
     * @returns Returned if the request is successful.
     */
    updateProjectAvatar(
      params: Avatar & {
        /** The ID or (case-sensitive) key of the project. */
        projectIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateProjectAvatar(ctx, params, options);
    },

    /**
     * Delete project avatar
     *
     * Deletes a custom avatar from a project. Note that system avatars cannot be deleted.
     *
     * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
     *
     * @param params.projectIdOrKey - The project ID or (case-sensitive) key.
     * @param params.id - The ID of the avatar.
     * @returns Returned if the request is successful.
     */
    deleteProjectAvatar(
      params: {
        /** The project ID or (case-sensitive) key. */
        projectIdOrKey: string;
        /**
         * The ID of the avatar.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteProjectAvatar(ctx, params, options);
    },

    /**
     * Load project avatar
     *
     * Loads an avatar for a project.
     *
     * Specify the avatar's local file location in the body of the request. Also, include the following headers:
     *
     *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
     *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
     *
     * For example:
     * `curl --request POST `
     *
     * `--user email@example.com:<api_token> `
     *
     * `--header 'X-Atlassian-Token: no-check' `
     *
     * `--header 'Content-Type: image/< image_type>' `
     *
     * `--data-binary "<@/path/to/file/with/your/avatar>" `
     *
     * `--url 'https://your-domain.atlassian.net/rest/api/2/project/{projectIdOrKey}/avatar2'`
     *
     * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
     *
     * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
     *
     * After creating the avatar use [Set project avatar](#api-rest-api-2-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
     *
     * **[Permissions](#permissions) required:** *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg).
     *
     * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
     * @param params.x - The X coordinate of the top-left corner of the crop region.
     * @param params.y - The Y coordinate of the top-left corner of the crop region.
     * @param params.size - The length of each side of the crop region.
     * @returns Returned if the request is successful.
     */
    createProjectAvatar(
      params: {
        /** The ID or (case-sensitive) key of the project. */
        projectIdOrKey: string;
        /**
         * The X coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        x?: number;
        /**
         * The Y coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        y?: number;
        /**
         * The length of each side of the crop region.
         *
         * @default 0
         * @format int32
         */
        size?: number;
        body: Blob;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Avatar> {
      return createProjectAvatar(ctx, params, options);
    },

    /**
     * Get all project avatars
     *
     * Returns all project avatars, grouped by system and custom avatars.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
     *
     * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
     * @returns Returned if request is successful.
     */
    getAllProjectAvatars(
      params: {
        /** The ID or (case-sensitive) key of the project. */
        projectIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectAvatars> {
      return getAllProjectAvatars(ctx, params, options);
    },
  };
}
