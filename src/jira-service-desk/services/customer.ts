// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { CustomerCreateDto, UserDto } from "../types/index.ts";

/**
 * Create customer
 *
 * This method adds a customer to the Jira Service Management instance by passing a JSON file including an email address and display name. The display name does not need to be unique. The record's identifiers, `name` and `key`, are automatically generated from the request details.
 *
 * **[Permissions](#permissions) required**: Jira Administrator Global permission
 *
 * @param params.strictConflictStatusCode - Optional boolean flag to return 409 Conflict status code for duplicate customer creation request
 * @returns Returns the customer details.
 */
export function createCustomer(
  ctx: ClientContext,
  params: CustomerCreateDto & {
    /** Optional boolean flag to return 409 Conflict status code for duplicate customer creation request */
    strictConflictStatusCode?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<UserDto> {
  const { strictConflictStatusCode, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/customer",
    query: { strictConflictStatusCode },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Revoke portal only access for user
 *
 * This method revokes portal-only access for a particular user, removing their ability to log in to the Jira Service Management customer portal as a portal-only user. After revocation, the user cannot submit or view requests through the portal.
 *
 * **[Permissions](#permissions) required:** Site administration (that is, member of the *site-admin* [group](https://confluence.atlassian.com/x/24xjL)).
 *
 * @param params.accountId - The account ID of the user, which uniquely identifies the portal-only account. For example, *qm:a713c8ea-1075-4e30-9d96-891a7d181739:5ad6d3581db05e2a66fa80b*.
 * @returns Returned if the request is successful.
 */
export function revokePortalOnlyAccessForUser(
  ctx: ClientContext,
  params: {
    /** The account ID of the user, which uniquely identifies the portal-only account. For example, *qm:a713c8ea-1075-4e30-9d96-891a7d181739:5ad6d3581db05e2a66fa80b*. */
    accountId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { accountId } = params;
  return ctx.request({
    method: "put",
    path: "/rest/servicedeskapi/customer/user/{accountId}/revoke-portal-only-access",
    pathParams: { accountId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `customer` service. */
export function createCustomerService(ctx: ClientContext) {
  return {
    /**
     * Create customer
     *
     * This method adds a customer to the Jira Service Management instance by passing a JSON file including an email address and display name. The display name does not need to be unique. The record's identifiers, `name` and `key`, are automatically generated from the request details.
     *
     * **[Permissions](#permissions) required**: Jira Administrator Global permission
     *
     * @param params.strictConflictStatusCode - Optional boolean flag to return 409 Conflict status code for duplicate customer creation request
     * @returns Returns the customer details.
     */
    createCustomer(
      params: CustomerCreateDto & {
        /** Optional boolean flag to return 409 Conflict status code for duplicate customer creation request */
        strictConflictStatusCode?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<UserDto> {
      return createCustomer(ctx, params, options);
    },

    /**
     * Revoke portal only access for user
     *
     * This method revokes portal-only access for a particular user, removing their ability to log in to the Jira Service Management customer portal as a portal-only user. After revocation, the user cannot submit or view requests through the portal.
     *
     * **[Permissions](#permissions) required:** Site administration (that is, member of the *site-admin* [group](https://confluence.atlassian.com/x/24xjL)).
     *
     * @param params.accountId - The account ID of the user, which uniquely identifies the portal-only account. For example, *qm:a713c8ea-1075-4e30-9d96-891a7d181739:5ad6d3581db05e2a66fa80b*.
     * @returns Returned if the request is successful.
     */
    revokePortalOnlyAccessForUser(
      params: {
        /** The account ID of the user, which uniquely identifies the portal-only account. For example, *qm:a713c8ea-1075-4e30-9d96-891a7d181739:5ad6d3581db05e2a66fa80b*. */
        accountId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return revokePortalOnlyAccessForUser(ctx, params, options);
    },
  };
}
