// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";

/**
 * Submit DevOps Components
 *
 * Update / insert DevOps Component data.
 *
 * Components are identified by their ID, and existing Component data for the same ID will be replaced if it exists and the updateSequenceNumber of existing data is less than the incoming data.
 *
 * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getComponentById operation can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple Components being submitted in one request, each is validated individually prior to submission. Details of which Components failed submission (if any) are available in the response object.
 *
 * A maximum of 1000 components can be submitted in one request.
 *
 * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
 * This resource requires the 'WRITE' scope for Connect apps.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the DevOps Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Submission accepted. Each submitted Component that is of a valid format will be eventually available in Jira. Details of which Components were submitted and which failed submission (due to data format problems etc.) are available in the response object.
 */
export function submitComponents(
  ctx: ClientContext,
  params: {
    /**
     * Properties assigned to incidents/components/review data that can then be used for delete / query operations.
     *
     * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
     *
     * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
     *
     * @example
     * ```json
     * {
     *   "accountId": "account-234",
     *   "projectId": "project-123"
     * }
     * ```
     */
    properties?: Record<string, string>;
    devopsComponents: {
      /**
       * The DevOpsComponentData schema version used for this devops component data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the DevOps Component. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this DevOps Component in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each DevOps Component and increment that on each update to Jira).
       *
       * Updates for a DevOps Component that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The human-readable name for the DevOps Component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Galactus"
       * ```
       */
      name: string;
      /**
       * The human-readable name for the Provider that owns this DevOps Component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Marvel"
       * ```
       */
      providerName?: string;
      /**
       * A description of the DevOps Component in Markdown format. Will be shown in the UI.
       *
       * @example
       * ```json
       * null
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this devops component, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the component in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/MS/galactus/summary"
       * ```
       */
      url: string;
      /**
       * A URL to display a logo representing this devops component, if available.
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/MS/galactus/logo"
       * ```
       */
      avatarUrl: string;
      /**
       * The tier of the component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Tier 1"
       * ```
       */
      tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4";
      /**
       * The type of the component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Service"
       * ```
       */
      componentType:
        | "Service"
        | "Application"
        | "Library"
        | "Capability"
        | "Cloud resource"
        | "Data pipeline"
        | "Machine learning model"
        | "UI element"
        | "Website"
        | "Other";
      /**
       * The last-updated timestamp to present to the user the last time the DevOps Component was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
    }[];
    /**
     * Information about the provider. This is useful for auditing, logging, debugging,
     * and other internal uses. It is not considered private information. Hence, it may not contain personally
     * identifiable information.
     */
    providerMetadata?: {
      /**
       * An optional name of the source of the incidents.
       *
       * @example
       * ```json
       * "Atlassian Operations Platform 2.1.0"
       * ```
       */
      product?: string;
    };
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the DevOps Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Components that have been accepted for submission.
   *
   * A Component may be rejected if it was only associated with unknown project keys.
   *
   * Note that a Component that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedComponents?: string[];
  /**
   * Details of Components that have not been accepted for submission, usually due to a problem with the request data.
   *
   * The object (if present) will be keyed by Component ID and include any errors associated with that Component that have prevented it being submitted.
   */
  failedComponents?: Record<
    string,
    {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[]
  >;
  /**
   * Project keys that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF` is sometimes incorrectly identified as a Jira project key), or they may be for projects that no longer exist.
   *
   * If a Component has been associated with project keys other than those in this array it will still be stored against those valid keys.
   * If a Component was only associated with project keys deemed to be invalid it won't be persisted.
   */
  unknownProjectKeys?: string[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/devopscomponents/1.0/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete DevOps Components by Property
 *
 * Bulk delete all Components that match the given request.
 *
 * One or more query params must be supplied to specify Properties to delete by.
 * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
 * See the documentation for the submitComponents operation for more details.
 *
 * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
 *
 * Deletion is performed asynchronously. The getComponentById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteComponentsByProperty(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "delete",
    path: "/rest/devopscomponents/1.0/bulkByProperties",
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Component by ID
 *
 * Retrieve the currently stored Component data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
 * This resource requires the 'READ' scope for Connect apps.
 *
 * @param params.componentId - The ID of the Component to fetch.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns The Component data currently stored for the given ID.
 */
export function getComponentById(
  ctx: ClientContext,
  params: {
    /** The ID of the Component to fetch. */
    componentId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The DevOpsComponentData schema version used for this devops component data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion: "1.0";
  /**
   * The identifier for the DevOps Component. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * An ID used to apply an ordering to updates for this DevOps Component in the case of out-of-order receipt of update requests.
   *
   * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each DevOps Component and increment that on each update to Jira).
   *
   * Updates for a DevOps Component that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceNumber: number;
  /**
   * The human-readable name for the DevOps Component. Will be shown in the UI.
   *
   * @example
   * ```json
   * "Galactus"
   * ```
   */
  name: string;
  /**
   * The human-readable name for the Provider that owns this DevOps Component. Will be shown in the UI.
   *
   * @example
   * ```json
   * "Marvel"
   * ```
   */
  providerName?: string;
  /**
   * A description of the DevOps Component in Markdown format. Will be shown in the UI.
   *
   * @example
   * ```json
   * null
   * ```
   */
  description: string;
  /**
   * A URL users can use to link to a summary view of this devops component, if appropriate.
   *
   * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the component in that project).
   *
   * @format uri
   * @example
   * ```json
   * "https://example.com/project/MS/galactus/summary"
   * ```
   */
  url: string;
  /**
   * A URL to display a logo representing this devops component, if available.
   *
   * @format uri
   * @example
   * ```json
   * "https://example.com/project/MS/galactus/logo"
   * ```
   */
  avatarUrl: string;
  /**
   * The tier of the component. Will be shown in the UI.
   *
   * @example
   * ```json
   * "Tier 1"
   * ```
   */
  tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4";
  /**
   * The type of the component. Will be shown in the UI.
   *
   * @example
   * ```json
   * "Service"
   * ```
   */
  componentType:
    | "Service"
    | "Application"
    | "Library"
    | "Capability"
    | "Cloud resource"
    | "Data pipeline"
    | "Machine learning model"
    | "UI element"
    | "Website"
    | "Other";
  /**
   * The last-updated timestamp to present to the user the last time the DevOps Component was updated.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
}> {
  const { componentId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/devopscomponents/1.0/devopscomponents/{componentId}",
    pathParams: { componentId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Component by ID
 *
 * Delete the Component data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The getComponentById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * @param params.componentId - The ID of the Component to delete.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteComponentById(
  ctx: ClientContext,
  params: {
    /** The ID of the Component to delete. */
    componentId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { componentId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/devopscomponents/1.0/devopscomponents/{componentId}",
    pathParams: { componentId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * APIs related to integrating Dev Ops Components affected by Incident data with Jira Software.
 *
 * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraDevOpsComponentProvider` in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/devops-components/.
 */
export function createDevOpsComponentsService(ctx: ClientContext) {
  return {
    /**
     * Submit DevOps Components
     *
     * Update / insert DevOps Component data.
     *
     * Components are identified by their ID, and existing Component data for the same ID will be replaced if it exists and the updateSequenceNumber of existing data is less than the incoming data.
     *
     * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getComponentById operation can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple Components being submitted in one request, each is validated individually prior to submission. Details of which Components failed submission (if any) are available in the response object.
     *
     * A maximum of 1000 components can be submitted in one request.
     *
     * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
     * This resource requires the 'WRITE' scope for Connect apps.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the DevOps Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Submission accepted. Each submitted Component that is of a valid format will be eventually available in Jira. Details of which Components were submitted and which failed submission (due to data format problems etc.) are available in the response object.
     */
    submitComponents(
      params: {
        /**
         * Properties assigned to incidents/components/review data that can then be used for delete / query operations.
         *
         * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
         *
         * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
         *
         * @example
         * ```json
         * {
         *   "accountId": "account-234",
         *   "projectId": "project-123"
         * }
         * ```
         */
        properties?: Record<string, string>;
        devopsComponents: {
          /**
           * The DevOpsComponentData schema version used for this devops component data.
           *
           * Placeholder to support potential schema changes in the future.
           *
           * @default "1.0"
           * @example
           * ```json
           * "1.0"
           * ```
           */
          schemaVersion: "1.0";
          /**
           * The identifier for the DevOps Component. Must be unique for a given Provider.
           *
           * @example
           * ```json
           * "111-222-333"
           * ```
           */
          id: string;
          /**
           * An ID used to apply an ordering to updates for this DevOps Component in the case of out-of-order receipt of update requests.
           *
           * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each DevOps Component and increment that on each update to Jira).
           *
           * Updates for a DevOps Component that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
           *
           * @format int64
           * @example
           * ```json
           * 1523494301448
           * ```
           */
          updateSequenceNumber: number;
          /**
           * The human-readable name for the DevOps Component. Will be shown in the UI.
           *
           * @example
           * ```json
           * "Galactus"
           * ```
           */
          name: string;
          /**
           * The human-readable name for the Provider that owns this DevOps Component. Will be shown in the UI.
           *
           * @example
           * ```json
           * "Marvel"
           * ```
           */
          providerName?: string;
          /**
           * A description of the DevOps Component in Markdown format. Will be shown in the UI.
           *
           * @example
           * ```json
           * null
           * ```
           */
          description: string;
          /**
           * A URL users can use to link to a summary view of this devops component, if appropriate.
           *
           * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the component in that project).
           *
           * @format uri
           * @example
           * ```json
           * "https://example.com/project/MS/galactus/summary"
           * ```
           */
          url: string;
          /**
           * A URL to display a logo representing this devops component, if available.
           *
           * @format uri
           * @example
           * ```json
           * "https://example.com/project/MS/galactus/logo"
           * ```
           */
          avatarUrl: string;
          /**
           * The tier of the component. Will be shown in the UI.
           *
           * @example
           * ```json
           * "Tier 1"
           * ```
           */
          tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4";
          /**
           * The type of the component. Will be shown in the UI.
           *
           * @example
           * ```json
           * "Service"
           * ```
           */
          componentType:
            | "Service"
            | "Application"
            | "Library"
            | "Capability"
            | "Cloud resource"
            | "Data pipeline"
            | "Machine learning model"
            | "UI element"
            | "Website"
            | "Other";
          /**
           * The last-updated timestamp to present to the user the last time the DevOps Component was updated.
           *
           * Expected format is an RFC3339 formatted string.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          lastUpdated: string;
        }[];
        /**
         * Information about the provider. This is useful for auditing, logging, debugging,
         * and other internal uses. It is not considered private information. Hence, it may not contain personally
         * identifiable information.
         */
        providerMetadata?: {
          /**
           * An optional name of the source of the incidents.
           *
           * @example
           * ```json
           * "Atlassian Operations Platform 2.1.0"
           * ```
           */
          product?: string;
        };
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define the DevOps Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Components that have been accepted for submission.
       *
       * A Component may be rejected if it was only associated with unknown project keys.
       *
       * Note that a Component that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedComponents?: string[];
      /**
       * Details of Components that have not been accepted for submission, usually due to a problem with the request data.
       *
       * The object (if present) will be keyed by Component ID and include any errors associated with that Component that have prevented it being submitted.
       */
      failedComponents?: Record<
        string,
        {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[]
      >;
      /**
       * Project keys that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF` is sometimes incorrectly identified as a Jira project key), or they may be for projects that no longer exist.
       *
       * If a Component has been associated with project keys other than those in this array it will still be stored against those valid keys.
       * If a Component was only associated with project keys deemed to be invalid it won't be persisted.
       */
      unknownProjectKeys?: string[];
    }> {
      return submitComponents(ctx, params, options);
    },

    /**
     * Delete DevOps Components by Property
     *
     * Bulk delete all Components that match the given request.
     *
     * One or more query params must be supplied to specify Properties to delete by.
     * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
     * See the documentation for the submitComponents operation for more details.
     *
     * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
     *
     * Deletion is performed asynchronously. The getComponentById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteComponentsByProperty(options?: {
      headers?: {
        /**
         * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
         *
         * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
         *
         * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<void> {
      return deleteComponentsByProperty(ctx, options);
    },

    /**
     * Get a Component by ID
     *
     * Retrieve the currently stored Component data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
     * This resource requires the 'READ' scope for Connect apps.
     *
     * @param params.componentId - The ID of the Component to fetch.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns The Component data currently stored for the given ID.
     */
    getComponentById(
      params: {
        /** The ID of the Component to fetch. */
        componentId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The DevOpsComponentData schema version used for this devops component data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the DevOps Component. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this DevOps Component in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each DevOps Component and increment that on each update to Jira).
       *
       * Updates for a DevOps Component that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The human-readable name for the DevOps Component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Galactus"
       * ```
       */
      name: string;
      /**
       * The human-readable name for the Provider that owns this DevOps Component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Marvel"
       * ```
       */
      providerName?: string;
      /**
       * A description of the DevOps Component in Markdown format. Will be shown in the UI.
       *
       * @example
       * ```json
       * null
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this devops component, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the component in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/MS/galactus/summary"
       * ```
       */
      url: string;
      /**
       * A URL to display a logo representing this devops component, if available.
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/MS/galactus/logo"
       * ```
       */
      avatarUrl: string;
      /**
       * The tier of the component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Tier 1"
       * ```
       */
      tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4";
      /**
       * The type of the component. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Service"
       * ```
       */
      componentType:
        | "Service"
        | "Application"
        | "Library"
        | "Capability"
        | "Cloud resource"
        | "Data pipeline"
        | "Machine learning model"
        | "UI element"
        | "Website"
        | "Other";
      /**
       * The last-updated timestamp to present to the user the last time the DevOps Component was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
    }> {
      return getComponentById(ctx, params, options);
    },

    /**
     * Delete a Component by ID
     *
     * Delete the Component data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The getComponentById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * Only Connect apps that define the `jiraDevOpsComponentProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * @param params.componentId - The ID of the Component to delete.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteComponentById(
      params: {
        /** The ID of the Component to delete. */
        componentId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteComponentById(ctx, params, options);
    },
  };
}
