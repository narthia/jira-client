// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  JiraStatus,
  PageOfStatuses,
  StatusCreateRequest,
  StatusProjectIssueTypeUsageDto,
  StatusProjectUsageDto,
  StatusUpdateRequest,
  StatusWorkflowUsageDto,
} from "../types/index.ts";

/**
 * Bulk get statuses
 *
 * Returns a list of the statuses specified by one or more status IDs.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @param params.id - The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001. Min items `1`, Max items `50`
 * @returns Returned if the request is successful.
 */
export function getStatusesById(
  ctx: ClientContext,
  params: {
    /**
     * The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001.
     *
     * Min items `1`, Max items `50`
     */
    id: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<JiraStatus[]> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses",
    query: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Bulk update statuses
 *
 * Updates statuses by ID.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @returns Returned if the request is successful.
 */
export function updateStatuses(
  ctx: ClientContext,
  params: StatusUpdateRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/statuses",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Bulk create statuses
 *
 * Creates statuses for a global or project scope.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @returns Returned if the request is successful.
 */
export function createStatuses(
  ctx: ClientContext,
  params: StatusCreateRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<JiraStatus[]> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/statuses",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Bulk delete Statuses
 *
 * Deletes statuses by ID.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @param params.id - The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001. Min items `1`, Max items `50`
 * @returns Returned if the request is successful.
 */
export function deleteStatusesById(
  ctx: ClientContext,
  params: {
    /**
     * The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001.
     *
     * Min items `1`, Max items `50`
     */
    id: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/statuses",
    query: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Bulk get statuses by name
 *
 * Returns a list of the statuses specified by one or more status names.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Browse projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @param params.name - The list of status names. To include multiple names, provide an ampersand-separated list. For example, name=nameXX&name=nameYY. Min items `1`, Max items `50`
 * @param params.projectId - The project the status is part of or null for global statuses.
 * @returns Returned if the request is successful.
 */
export function getStatusesByName(
  ctx: ClientContext,
  params: {
    /**
     * The list of status names. To include multiple names, provide an ampersand-separated list. For example, name=nameXX&name=nameYY.
     *
     * Min items `1`, Max items `50`
     */
    name: string[];
    /** The project the status is part of or null for global statuses. */
    projectId?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<JiraStatus[]> {
  const { name, projectId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses/byNames",
    query: { name, projectId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Search statuses paginated
 *
 * Returns a [paginated](https://developer.atlassian.com/cloud/jira/platform/rest/v3/intro/#pagination) list of statuses that match a search on name or project.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
 *
 * @param params.projectId - The project the status is part of or null for global statuses.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.searchString - Term to match status names against or null to search for all statuses in the search scope.
 * @param params.statusCategory - Category of the status to filter by. The supported values are: `TODO`, `IN_PROGRESS`, and `DONE`.
 * @param params.includeGlobalStatuses - Whether to include global statuses (scope = null, not tied to any project) in the response. Defaults to false. Only relevant for project scoped queries.
 * @returns Returned if the request is successful.
 */
export function search(
  ctx: ClientContext,
  params?: {
    /** The project the status is part of or null for global statuses. */
    projectId?: string;
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 200
     * @format int32
     */
    maxResults?: number;
    /** Term to match status names against or null to search for all statuses in the search scope. */
    searchString?: string;
    /** Category of the status to filter by. The supported values are: `TODO`, `IN_PROGRESS`, and `DONE`. */
    statusCategory?: string;
    /** Whether to include global statuses (scope = null, not tied to any project) in the response. Defaults to false. Only relevant for project scoped queries. */
    includeGlobalStatuses?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageOfStatuses> {
  const { projectId, startAt, maxResults, searchString, statusCategory, includeGlobalStatuses } =
    params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses/search",
    query: { projectId, startAt, maxResults, searchString, statusCategory, includeGlobalStatuses },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue type usages by status and project
 *
 * Returns a page of issue types in a project using a given status.
 *
 * @param params.statusId - The statusId to fetch issue type usages for
 * @param params.projectId - The projectId to fetch issue type usages for
 * @param params.nextPageToken - The cursor for pagination
 * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
 * @returns Returned if the request is successful.
 */
export function getProjectIssueTypeUsagesForStatus(
  ctx: ClientContext,
  params: {
    /** The statusId to fetch issue type usages for */
    statusId: string;
    /** The projectId to fetch issue type usages for */
    projectId: string;
    /** The cursor for pagination */
    nextPageToken?: string;
    /**
     * The maximum number of results to return. Must be an integer between 1 and 200.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusProjectIssueTypeUsageDto> {
  const { statusId, projectId, nextPageToken, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses/{statusId}/project/{projectId}/issueTypeUsages",
    pathParams: { statusId, projectId },
    query: { nextPageToken, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get project usages by status
 *
 * Returns a page of projects using a given status.
 *
 * @param params.statusId - The statusId to fetch project usages for
 * @param params.nextPageToken - The cursor for pagination
 * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
 * @returns Returned if the request is successful.
 */
export function getProjectUsagesForStatus(
  ctx: ClientContext,
  params: {
    /** The statusId to fetch project usages for */
    statusId: string;
    /** The cursor for pagination */
    nextPageToken?: string;
    /**
     * The maximum number of results to return. Must be an integer between 1 and 200.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusProjectUsageDto> {
  const { statusId, nextPageToken, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses/{statusId}/projectUsages",
    pathParams: { statusId },
    query: { nextPageToken, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get workflow usages by status
 *
 * Returns a page of workflows using a given status.
 *
 * @param params.statusId - The statusId to fetch workflow usages for
 * @param params.nextPageToken - The cursor for pagination
 * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
 * @returns Returned if the request is successful.
 */
export function getWorkflowUsagesForStatus(
  ctx: ClientContext,
  params: {
    /** The statusId to fetch workflow usages for */
    statusId: string;
    /** The cursor for pagination */
    nextPageToken?: string;
    /**
     * The maximum number of results to return. Must be an integer between 1 and 200.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusWorkflowUsageDto> {
  const { statusId, nextPageToken, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/statuses/{statusId}/workflowUsages",
    pathParams: { statusId },
    query: { nextPageToken, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents statuses. Use it to search, get, create, delete, and change statuses. */
export function createStatusService(ctx: ClientContext) {
  return {
    /**
     * Bulk get statuses
     *
     * Returns a list of the statuses specified by one or more status IDs.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @param params.id - The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001. Min items `1`, Max items `50`
     * @returns Returned if the request is successful.
     */
    getStatusesById(
      params: {
        /**
         * The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001.
         *
         * Min items `1`, Max items `50`
         */
        id: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<JiraStatus[]> {
      return getStatusesById(ctx, params, options);
    },

    /**
     * Bulk update statuses
     *
     * Updates statuses by ID.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @returns Returned if the request is successful.
     */
    updateStatuses(
      params: StatusUpdateRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateStatuses(ctx, params, options);
    },

    /**
     * Bulk create statuses
     *
     * Creates statuses for a global or project scope.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @returns Returned if the request is successful.
     */
    createStatuses(
      params: StatusCreateRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<JiraStatus[]> {
      return createStatuses(ctx, params, options);
    },

    /**
     * Bulk delete Statuses
     *
     * Deletes statuses by ID.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @param params.id - The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001. Min items `1`, Max items `50`
     * @returns Returned if the request is successful.
     */
    deleteStatusesById(
      params: {
        /**
         * The list of status IDs. To include multiple IDs, provide an ampersand-separated list. For example, id=10000&id=10001.
         *
         * Min items `1`, Max items `50`
         */
        id: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteStatusesById(ctx, params, options);
    },

    /**
     * Bulk get statuses by name
     *
     * Returns a list of the statuses specified by one or more status names.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Browse projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @param params.name - The list of status names. To include multiple names, provide an ampersand-separated list. For example, name=nameXX&name=nameYY. Min items `1`, Max items `50`
     * @param params.projectId - The project the status is part of or null for global statuses.
     * @returns Returned if the request is successful.
     */
    getStatusesByName(
      params: {
        /**
         * The list of status names. To include multiple names, provide an ampersand-separated list. For example, name=nameXX&name=nameYY.
         *
         * Min items `1`, Max items `50`
         */
        name: string[];
        /** The project the status is part of or null for global statuses. */
        projectId?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<JiraStatus[]> {
      return getStatusesByName(ctx, params, options);
    },

    /**
     * Search statuses paginated
     *
     * Returns a [paginated](https://developer.atlassian.com/cloud/jira/platform/rest/v3/intro/#pagination) list of statuses that match a search on name or project.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer projects* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *  *  *Administer Jira* [project permission.](https://confluence.atlassian.com/x/yodKLg)
     *
     * @param params.projectId - The project the status is part of or null for global statuses.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.searchString - Term to match status names against or null to search for all statuses in the search scope.
     * @param params.statusCategory - Category of the status to filter by. The supported values are: `TODO`, `IN_PROGRESS`, and `DONE`.
     * @param params.includeGlobalStatuses - Whether to include global statuses (scope = null, not tied to any project) in the response. Defaults to false. Only relevant for project scoped queries.
     * @returns Returned if the request is successful.
     */
    search(
      params?: {
        /** The project the status is part of or null for global statuses. */
        projectId?: string;
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 200
         * @format int32
         */
        maxResults?: number;
        /** Term to match status names against or null to search for all statuses in the search scope. */
        searchString?: string;
        /** Category of the status to filter by. The supported values are: `TODO`, `IN_PROGRESS`, and `DONE`. */
        statusCategory?: string;
        /** Whether to include global statuses (scope = null, not tied to any project) in the response. Defaults to false. Only relevant for project scoped queries. */
        includeGlobalStatuses?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageOfStatuses> {
      return search(ctx, params, options);
    },

    /**
     * Get issue type usages by status and project
     *
     * Returns a page of issue types in a project using a given status.
     *
     * @param params.statusId - The statusId to fetch issue type usages for
     * @param params.projectId - The projectId to fetch issue type usages for
     * @param params.nextPageToken - The cursor for pagination
     * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
     * @returns Returned if the request is successful.
     */
    getProjectIssueTypeUsagesForStatus(
      params: {
        /** The statusId to fetch issue type usages for */
        statusId: string;
        /** The projectId to fetch issue type usages for */
        projectId: string;
        /** The cursor for pagination */
        nextPageToken?: string;
        /**
         * The maximum number of results to return. Must be an integer between 1 and 200.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StatusProjectIssueTypeUsageDto> {
      return getProjectIssueTypeUsagesForStatus(ctx, params, options);
    },

    /**
     * Get project usages by status
     *
     * Returns a page of projects using a given status.
     *
     * @param params.statusId - The statusId to fetch project usages for
     * @param params.nextPageToken - The cursor for pagination
     * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
     * @returns Returned if the request is successful.
     */
    getProjectUsagesForStatus(
      params: {
        /** The statusId to fetch project usages for */
        statusId: string;
        /** The cursor for pagination */
        nextPageToken?: string;
        /**
         * The maximum number of results to return. Must be an integer between 1 and 200.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StatusProjectUsageDto> {
      return getProjectUsagesForStatus(ctx, params, options);
    },

    /**
     * Get workflow usages by status
     *
     * Returns a page of workflows using a given status.
     *
     * @param params.statusId - The statusId to fetch workflow usages for
     * @param params.nextPageToken - The cursor for pagination
     * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
     * @returns Returned if the request is successful.
     */
    getWorkflowUsagesForStatus(
      params: {
        /** The statusId to fetch workflow usages for */
        statusId: string;
        /** The cursor for pagination */
        nextPageToken?: string;
        /**
         * The maximum number of results to return. Must be an integer between 1 and 200.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StatusWorkflowUsageDto> {
      return getWorkflowUsagesForStatus(ctx, params, options);
    },
  };
}
