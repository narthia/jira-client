// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { EntityProperty, PropertyKeys } from "../types/index.ts";

/**
 * Get comment property keys
 *
 * Returns the keys of all the properties of a comment.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *  *  If the comment has visibility restrictions, belongs to the group or has the role visibility is restricted to.
 *
 * @param params.commentId - The ID of the comment.
 * @returns Returned if the request is successful.
 */
export function getCommentPropertyKeys(
  ctx: ClientContext,
  params: {
    /** The ID of the comment. */
    commentId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PropertyKeys> {
  const { commentId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/comment/{commentId}/properties",
    pathParams: { commentId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get comment property
 *
 * Returns the value of a comment property.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *  *  If the comment has visibility restrictions, belongs to the group or has the role visibility is restricted to.
 *
 * @param params.commentId - The ID of the comment.
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function getCommentProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the comment. */
    commentId: string;
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<EntityProperty> {
  const { commentId, propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/comment/{commentId}/properties/{propertyKey}",
    pathParams: { commentId, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set comment property
 *
 * Creates or updates the value of a property for a comment. Use this resource to store custom data against a comment.
 *
 * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
 *
 * **[Permissions](#permissions) required:** either of:
 *
 *  *  *Edit All Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to create or update the value of a property on any comment.
 *  *  *Edit Own Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to create or update the value of a property on a comment created by the user.
 *
 * @param params.commentId - The ID of the comment.
 * @param params.propertyKey - The key of the property. The maximum length is 255 characters.
 * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
 * @returns Returned if the comment property is updated.
 */
export function setCommentProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the comment. */
    commentId: string;
    /** The key of the property. The maximum length is 255 characters. */
    propertyKey: string;
    body: unknown;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { commentId, propertyKey, body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/comment/{commentId}/properties/{propertyKey}",
    pathParams: { commentId, propertyKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete comment property
 *
 * Deletes a comment property.
 *
 * **[Permissions](#permissions) required:** either of:
 *
 *  *  *Edit All Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to delete a property from any comment.
 *  *  *Edit Own Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to delete a property from a comment created by the user.
 *
 * @param params.commentId - The ID of the comment.
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function deleteCommentProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the comment. */
    commentId: string;
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { commentId, propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/comment/{commentId}/properties/{propertyKey}",
    pathParams: { commentId, propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents [issue comment](#api-group-Issue-comments) properties, which provides for storing custom data against an issue comment. Use is to get, set, and delete issue comment properties as well as obtain the keys of all properties on a comment. Comment properties are a type of [entity property](https://developer.atlassian.com/cloud/jira/platform/jira-entity-properties/). */
export function createIssueCommentPropertiesService(ctx: ClientContext) {
  return {
    /**
     * Get comment property keys
     *
     * Returns the keys of all the properties of a comment.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *  *  If the comment has visibility restrictions, belongs to the group or has the role visibility is restricted to.
     *
     * @param params.commentId - The ID of the comment.
     * @returns Returned if the request is successful.
     */
    getCommentPropertyKeys(
      params: {
        /** The ID of the comment. */
        commentId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PropertyKeys> {
      return getCommentPropertyKeys(ctx, params, options);
    },

    /**
     * Get comment property
     *
     * Returns the value of a comment property.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *  *  If the comment has visibility restrictions, belongs to the group or has the role visibility is restricted to.
     *
     * @param params.commentId - The ID of the comment.
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    getCommentProperty(
      params: {
        /** The ID of the comment. */
        commentId: string;
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<EntityProperty> {
      return getCommentProperty(ctx, params, options);
    },

    /**
     * Set comment property
     *
     * Creates or updates the value of a property for a comment. Use this resource to store custom data against a comment.
     *
     * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
     *
     * **[Permissions](#permissions) required:** either of:
     *
     *  *  *Edit All Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to create or update the value of a property on any comment.
     *  *  *Edit Own Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to create or update the value of a property on a comment created by the user.
     *
     * @param params.commentId - The ID of the comment.
     * @param params.propertyKey - The key of the property. The maximum length is 255 characters.
     * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
     * @returns Returned if the comment property is updated.
     */
    setCommentProperty(
      params: {
        /** The ID of the comment. */
        commentId: string;
        /** The key of the property. The maximum length is 255 characters. */
        propertyKey: string;
        body: unknown;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setCommentProperty(ctx, params, options);
    },

    /**
     * Delete comment property
     *
     * Deletes a comment property.
     *
     * **[Permissions](#permissions) required:** either of:
     *
     *  *  *Edit All Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to delete a property from any comment.
     *  *  *Edit Own Comments* [project permission](https://confluence.atlassian.com/x/yodKLg) to delete a property from a comment created by the user.
     *
     * @param params.commentId - The ID of the comment.
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    deleteCommentProperty(
      params: {
        /** The ID of the comment. */
        commentId: string;
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteCommentProperty(ctx, params, options);
    },
  };
}
