// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { TaskProgress } from "../types/index.ts";

/**
 * Get Connect issue field migration task
 *
 * Returns the details of a Connect issue field's migration to Forge.
 *
 * When migrating a Connect app to Forge, [Issue Field](https://developer.atlassian.com/cloud/jira/software/modules/issue-field/) modules
 * must be converted to [Custom field](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/). When the
 * Forge version of the app is installed, Forge creates a
 * [background task](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-tasks/#api-group-tasks) to track the
 * migration of field data across. This endpoint returns the status and other details of that background task.
 *
 * For more details, see
 * [Jira modules > Jira Custom Fields](https://developer.atlassian.com/platform/adopting-forge-from-connect/migrate-jira-custom-fields/).
 *
 * **[Permissions](#permissions) required:** Only Connect and Forge apps can make this request.
 *
 * @param params.connectKey - The key of the Connect app that contains the Jira issue field being migrated.
 * @param params.jiraIssueFieldsKey - The module key of the Connect issue field being migrated.
 * @returns Returned if the request is successful and a migration task is found.
 */
export function connectToForgeMigrationFetchTaskResourceFetchMigrationTaskGet(
  ctx: ClientContext,
  params: {
    /** The key of the Connect app that contains the Jira issue field being migrated. */
    connectKey: string;
    /** The module key of the Connect issue field being migrated. */
    jiraIssueFieldsKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<TaskProgress> {
  const { connectKey, jiraIssueFieldsKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/atlassian-connect/1/migration/{connectKey}/{jiraIssueFieldsKey}/task",
    pathParams: { connectKey, jiraIssueFieldsKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Submit Connect issue field migration task
 *
 * Submits a request to trigger migration of connect issue field to its Forge custom field counterpart.
 *
 * When migrating a Connect app to Forge, [Issue Field](https://developer.atlassian.com/cloud/jira/software/modules/issue-field/) modules
 * must be converted to [Custom field](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/) modules.
 * This endpoint triggers the background migration of field data. Use the GET endpoint to retrieve
 * the status and progress of the task.
 *
 * For more details, see
 * [Jira modules > Jira Custom Fields](https://developer.atlassian.com/platform/adopting-forge-from-connect/migrate-jira-custom-fields/).
 *
 * **[Permissions](#permissions) required:** Only Connect and Forge apps can make this request.
 *
 * @param params.connectKey - The key of the Connect app that contains the Jira issue field being migrated.
 * @param params.jiraIssueFieldsKey - The module key of the Connect issue field being migrated.
 * @param params.retriggerCompletedMigration - Whether to retrigger the migration if it has already completed.
 * @returns Returned if the migration task was submitted successfully.
 */
export function connectToForgeMigrationTaskSubmissionResourceSubmitTaskPost(
  ctx: ClientContext,
  params: {
    /** The key of the Connect app that contains the Jira issue field being migrated. */
    connectKey: string;
    /** The module key of the Connect issue field being migrated. */
    jiraIssueFieldsKey: string;
    /**
     * Whether to retrigger the migration if it has already completed.
     *
     * @default false
     */
    retriggerCompletedMigration?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { connectKey, jiraIssueFieldsKey, retriggerCompletedMigration } = params;
  return ctx.request({
    method: "post",
    path: "/rest/atlassian-connect/1/migration/{connectKey}/{jiraIssueFieldsKey}/task",
    pathParams: { connectKey, jiraIssueFieldsKey },
    query: { retriggerCompletedMigration },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource supports the migration of some Connect modules to their equivalent Forge modules. */
export function createMigrationOfConnectModulesToForgeService(ctx: ClientContext) {
  return {
    /**
     * Get Connect issue field migration task
     *
     * Returns the details of a Connect issue field's migration to Forge.
     *
     * When migrating a Connect app to Forge, [Issue Field](https://developer.atlassian.com/cloud/jira/software/modules/issue-field/) modules
     * must be converted to [Custom field](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/). When the
     * Forge version of the app is installed, Forge creates a
     * [background task](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-tasks/#api-group-tasks) to track the
     * migration of field data across. This endpoint returns the status and other details of that background task.
     *
     * For more details, see
     * [Jira modules > Jira Custom Fields](https://developer.atlassian.com/platform/adopting-forge-from-connect/migrate-jira-custom-fields/).
     *
     * **[Permissions](#permissions) required:** Only Connect and Forge apps can make this request.
     *
     * @param params.connectKey - The key of the Connect app that contains the Jira issue field being migrated.
     * @param params.jiraIssueFieldsKey - The module key of the Connect issue field being migrated.
     * @returns Returned if the request is successful and a migration task is found.
     */
    connectToForgeMigrationFetchTaskResourceFetchMigrationTaskGet(
      params: {
        /** The key of the Connect app that contains the Jira issue field being migrated. */
        connectKey: string;
        /** The module key of the Connect issue field being migrated. */
        jiraIssueFieldsKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<TaskProgress> {
      return connectToForgeMigrationFetchTaskResourceFetchMigrationTaskGet(ctx, params, options);
    },

    /**
     * Submit Connect issue field migration task
     *
     * Submits a request to trigger migration of connect issue field to its Forge custom field counterpart.
     *
     * When migrating a Connect app to Forge, [Issue Field](https://developer.atlassian.com/cloud/jira/software/modules/issue-field/) modules
     * must be converted to [Custom field](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/) modules.
     * This endpoint triggers the background migration of field data. Use the GET endpoint to retrieve
     * the status and progress of the task.
     *
     * For more details, see
     * [Jira modules > Jira Custom Fields](https://developer.atlassian.com/platform/adopting-forge-from-connect/migrate-jira-custom-fields/).
     *
     * **[Permissions](#permissions) required:** Only Connect and Forge apps can make this request.
     *
     * @param params.connectKey - The key of the Connect app that contains the Jira issue field being migrated.
     * @param params.jiraIssueFieldsKey - The module key of the Connect issue field being migrated.
     * @param params.retriggerCompletedMigration - Whether to retrigger the migration if it has already completed.
     * @returns Returned if the migration task was submitted successfully.
     */
    connectToForgeMigrationTaskSubmissionResourceSubmitTaskPost(
      params: {
        /** The key of the Connect app that contains the Jira issue field being migrated. */
        connectKey: string;
        /** The module key of the Connect issue field being migrated. */
        jiraIssueFieldsKey: string;
        /**
         * Whether to retrigger the migration if it has already completed.
         *
         * @default false
         */
        retriggerCompletedMigration?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return connectToForgeMigrationTaskSubmissionResourceSubmitTaskPost(ctx, params, options);
    },
  };
}
