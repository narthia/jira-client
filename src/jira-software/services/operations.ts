// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";

/**
 * Submit Operations Workspace Ids
 *
 * Insert Operations Workspace IDs to establish a relationship between them and the Jira site the app is installed in. If a relationship between the Workspace ID and Jira already exists then the workspace ID will be ignored and Jira will process the rest of the entries.
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'WRITE' scope for Connect apps.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Submission accepted. Each submitted Operations Workspace ID will be linked to Jira.
 */
export function submitOperationsWorkspaces(
  ctx: ClientContext,
  params: {
    /**
     * The IDs of Operations Workspaces that are available to this Jira site.
     *
     * @example
     * ```json
     * [
     *   "111-222-333",
     *   "444-555-666"
     * ]
     * ```
     */
    workspaceIds: string[];
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Operations Workspaces that have been linked to the Jira site in this request.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedWorkspaceIds?: string[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/operations/1.0/linkedWorkspaces/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete Operations Workpaces by Id
 *
 * Bulk delete all Operations Workspaces that match the given request.
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * e.g. DELETE /bulk?workspaceIds=111-222-333,444-555-666
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete accepted. Workspaces and relate data will eventually be removed from Jira.
 */
export function deleteWorkspaces(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "delete",
    path: "/rest/operations/1.0/linkedWorkspaces/bulk",
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all Operations Workspace IDs or a specific Operations Workspace by ID
 *
 * Retrieve the either all Operations Workspace IDs associated with the Jira site or a specific Operations Workspace ID for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * e.g. GET /workspace?workspaceId=111-222-333
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'READ' scope for Connect apps.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Either the ID provided if stored or a list of available IDs.
 */
export function getWorkspaces(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Operations Workspaces that are available to this Jira site.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  workspaceIds: string[];
}> {
  return ctx.request({
    method: "get",
    path: "/rest/operations/1.0/linkedWorkspaces",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Submit Incident or Review data
 *
 * Update / insert Incident or Review data.
 *
 * Incidents and reviews are identified by their ID, and existing Incident and Review data for the same ID will be replaced if it exists and the updateSequenceNumber of existing data is less than the incoming data.
 *
 * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getIncidentById or getReviewById operation can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple Incidents and Reviews being submitted in one request, each is validated individually prior to submission. Details of which entities failed submission (if any) are available in the response object.
 *
 * A maximum of 1000 incidents can be submitted in one request.
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'WRITE' scope for Connect apps.
 *
 * @param params.body - Incident data to submit.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Submission accepted. Each submitted Incident that is of a valid format will be eventually available in Jira. Details of which Incidents were submitted and which failed submission (due to data format problems etc.) are available in the response object.
 */
export function submitEntity(
  ctx: ClientContext,
  params: {
    body:
      | {
          incidents?: {
            /**
             * The IncidentData schema version used for this incident data.
             *
             * Placeholder to support potential schema changes in the future.
             *
             * @default "1.0"
             * @example
             * ```json
             * "1.0"
             * ```
             */
            schemaVersion: "1.0";
            /**
             * The identifier for the Incident. Must be unique for a given Provider.
             *
             * @example
             * ```json
             * "111-222-333"
             * ```
             */
            id: string;
            /**
             * An ID used to apply an ordering to updates for this Incident in the case of out-of-order receipt of update requests.
             *
             * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Incident and increment that on each update to Jira).
             *
             * Updates for a Incident that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
             *
             * @format int64
             * @example
             * ```json
             * 1523494301448
             * ```
             */
            updateSequenceNumber: number;
            /**
             * The IDs of the Components impacted by this Incident. Must be unique for a given Provider.
             *
             * @example
             * ```json
             * [
             *   "111-222-333",
             *   "444-555-666"
             * ]
             * ```
             */
            affectedComponents: string[];
            /**
             * The human-readable summary for the Incident. Will be shown in the UI.
             *
             * If not provided, will use the ID for display.
             *
             * @example
             * ```json
             * "Unable to log into VPN"
             * ```
             */
            summary: string;
            /**
             * A description of the issue in Markdown format. Will be shown in the UI and used when creating Jira Issues.
             *
             * @example
             * ```json
             * null
             * ```
             */
            description: string;
            /**
             * A URL users can use to link to a summary view of this incident, if appropriate.
             *
             * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the incident in that project).
             *
             * @format uri
             * @example
             * ```json
             * "https://example.com/project/ITHELPDESK-9/summary"
             * ```
             */
            url: string;
            /**
             * The timestamp to present to the user that shows when the Incident was raised.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            createdDate: string;
            /**
             * The last-updated timestamp to present to the user the last time the Incident was updated.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            lastUpdated: string;
            /**
             * Severity information for a single Incident.
             *
             * This is the severity information that will be presented to the user on e.g. the Jira Incidents screen.
             */
            severity?: {
              /**
               * The severity level of the Incident with P1 being the highest and P5 being the lowest
               *
               * @example
               * ```json
               * "P1"
               * ```
               */
              level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown";
            };
            /**
             * The current status of the Incident.
             *
             * @example
             * ```json
             * "open"
             * ```
             */
            status: "open" | "resolved" | "unknown";
            /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
            associations?: {
              /**
               * the type of the association being made
               *
               * @example
               * ```json
               * "issueIdOrKeys"
               * ```
               */
              associationType?:
                | "issueIdOrKeys"
                | "serviceIdOrKeys"
                | "ati:cloud:compass:event-source";
              values?: string[];
            }[];
          }[];
        }
      | {
          reviews?: {
            /**
             * The PostIncidentReviewData schema version used for this post-incident review data.
             *
             * Placeholder to support potential schema changes in the future.
             *
             * @default "1.0"
             * @example
             * ```json
             * "1.0"
             * ```
             */
            schemaVersion: "1.0";
            /**
             * The identifier for the Review. Must be unique for a given Provider.
             *
             * @example
             * ```json
             * "111-222-333"
             * ```
             */
            id: string;
            /**
             * An ID used to apply an ordering to updates for this Review in the case of out-of-order receipt of update requests.
             *
             * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Review and increment that on each update to Jira).
             *
             * Updates for a Review that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
             *
             * @format int64
             * @example
             * ```json
             * 1523494301448
             * ```
             */
            updateSequenceNumber: number;
            /**
             * The IDs of the Incidents covered by this Review. Must be unique for a given Provider.
             *
             * @example
             * ```json
             * [
             *   "111-222-333",
             *   "444-555-666"
             * ]
             * ```
             */
            reviews: string[];
            /**
             * The human-readable summary for the Post-Incident Review. Will be shown in the UI.
             *
             * If not provided, will use the ID for display.
             *
             * @example
             * ```json
             * "PIR - Unable to log into VPN"
             * ```
             */
            summary: string;
            /**
             * A description of the review in Markdown format. Will be shown in the UI and used when creating Jira Issues.
             *
             * @example
             * ```json
             * null
             * ```
             */
            description: string;
            /**
             * A URL users can use to link to a summary view of this review, if appropriate.
             *
             * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the review in that project).
             *
             * @format uri
             * @example
             * ```json
             * "https://example.com/project/ITHELPDESK-9/summary"
             * ```
             */
            url: string;
            /**
             * The timestamp to present to the user that shows when the Review was raised.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            createdDate: string;
            /**
             * The last-updated timestamp to present to the user the last time the Review was updated.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            lastUpdated: string;
            /**
             * The current status of the Post-Incident Review.
             *
             * @example
             * ```json
             * "open"
             * ```
             */
            status: "in progress" | "outstanding actions" | "completed" | "unknown";
            /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
            associations?: {
              /**
               * the type of the association being made
               *
               * @example
               * ```json
               * "issueIdOrKeys"
               * ```
               */
              associationType?:
                | "issueIdOrKeys"
                | "serviceIdOrKeys"
                | "ati:cloud:compass:event-source";
              values?: string[];
            }[];
          }[];
        };
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Incidents that have been accepted for submission.
   *
   * A Incident may be rejected if it was only associated with unknown project keys.
   *
   * Note that a Incident that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedIncidents?: string[];
  /**
   * Details of Incidents that have not been accepted for submission, usually due to a problem with the request data.
   *
   * The object (if present) will be keyed by Incident ID and include any errors associated with that Incident that have prevented it being submitted.
   */
  failedIncidents?: Record<
    string,
    {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[]
  >;
  /**
   * Project keys that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF` is sometimes incorrectly identified as a Jira project key), or they may be for projects that no longer exist.
   *
   * If a Incident has been associated with project keys other than those in this array it will still be stored against those valid keys.
   * If a Incident was only associated with project keys deemed to be invalid it won't be persisted.
   */
  unknownProjectKeys?: string[];
}> {
  const { body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/operations/1.0/bulk",
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete Incidents or Review by Property
 *
 * Bulk delete all Entties that match the given request.
 *
 * One or more query params must be supplied to specify Properties to delete by.
 * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
 * See the documentation for the submitEntity operation for more details.
 *
 * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
 *
 * Deletion is performed asynchronously. The getIncidentById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteEntityByProperty(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "delete",
    path: "/rest/operations/1.0/bulkByProperties",
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Incident by ID
 *
 * Retrieve the currently stored Incident data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'READ' scope for Connect apps.
 *
 * @param params.incidentId - The ID of the Incident to fetch.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns The Incident data currently stored for the given ID.
 */
export function getIncidentById(
  ctx: ClientContext,
  params: {
    /** The ID of the Incident to fetch. */
    incidentId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IncidentData schema version used for this incident data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion: "1.0";
  /**
   * The identifier for the Incident. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * An ID used to apply an ordering to updates for this Incident in the case of out-of-order receipt of update requests.
   *
   * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Incident and increment that on each update to Jira).
   *
   * Updates for a Incident that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceNumber: number;
  /**
   * The IDs of the Components impacted by this Incident. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  affectedComponents: string[];
  /**
   * The human-readable summary for the Incident. Will be shown in the UI.
   *
   * If not provided, will use the ID for display.
   *
   * @example
   * ```json
   * "Unable to log into VPN"
   * ```
   */
  summary: string;
  /**
   * A description of the issue in Markdown format. Will be shown in the UI and used when creating Jira Issues.
   *
   * @example
   * ```json
   * null
   * ```
   */
  description: string;
  /**
   * A URL users can use to link to a summary view of this incident, if appropriate.
   *
   * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the incident in that project).
   *
   * @format uri
   * @example
   * ```json
   * "https://example.com/project/ITHELPDESK-9/summary"
   * ```
   */
  url: string;
  /**
   * The timestamp to present to the user that shows when the Incident was raised.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  createdDate: string;
  /**
   * The last-updated timestamp to present to the user the last time the Incident was updated.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
  /**
   * Severity information for a single Incident.
   *
   * This is the severity information that will be presented to the user on e.g. the Jira Incidents screen.
   */
  severity?: {
    /**
     * The severity level of the Incident with P1 being the highest and P5 being the lowest
     *
     * @example
     * ```json
     * "P1"
     * ```
     */
    level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown";
  };
  /**
   * The current status of the Incident.
   *
   * @example
   * ```json
   * "open"
   * ```
   */
  status: "open" | "resolved" | "unknown";
  /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
  associations?: {
    /**
     * the type of the association being made
     *
     * @example
     * ```json
     * "issueIdOrKeys"
     * ```
     */
    associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source";
    values?: string[];
  }[];
}> {
  const { incidentId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/operations/1.0/incidents/{incidentId}",
    pathParams: { incidentId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Incident by ID
 *
 * Delete the Incident data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The getIncidentById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * @param params.incidentId - The ID of the Incident to delete.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteIncidentById(
  ctx: ClientContext,
  params: {
    /** The ID of the Incident to delete. */
    incidentId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { incidentId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/operations/1.0/incidents/{incidentId}",
    pathParams: { incidentId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Review by ID
 *
 * Retrieve the currently stored Review data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'READ' scope for Connect apps.
 *
 * @param params.reviewId - The ID of the Review to fetch.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns The Review data currently stored for the given ID.
 */
export function getReviewById(
  ctx: ClientContext,
  params: {
    /** The ID of the Review to fetch. */
    reviewId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The PostIncidentReviewData schema version used for this post-incident review data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion: "1.0";
  /**
   * The identifier for the Review. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * An ID used to apply an ordering to updates for this Review in the case of out-of-order receipt of update requests.
   *
   * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Review and increment that on each update to Jira).
   *
   * Updates for a Review that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceNumber: number;
  /**
   * The IDs of the Incidents covered by this Review. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  reviews: string[];
  /**
   * The human-readable summary for the Post-Incident Review. Will be shown in the UI.
   *
   * If not provided, will use the ID for display.
   *
   * @example
   * ```json
   * "PIR - Unable to log into VPN"
   * ```
   */
  summary: string;
  /**
   * A description of the review in Markdown format. Will be shown in the UI and used when creating Jira Issues.
   *
   * @example
   * ```json
   * null
   * ```
   */
  description: string;
  /**
   * A URL users can use to link to a summary view of this review, if appropriate.
   *
   * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the review in that project).
   *
   * @format uri
   * @example
   * ```json
   * "https://example.com/project/ITHELPDESK-9/summary"
   * ```
   */
  url: string;
  /**
   * The timestamp to present to the user that shows when the Review was raised.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  createdDate: string;
  /**
   * The last-updated timestamp to present to the user the last time the Review was updated.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
  /**
   * The current status of the Post-Incident Review.
   *
   * @example
   * ```json
   * "open"
   * ```
   */
  status: "in progress" | "outstanding actions" | "completed" | "unknown";
  /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
  associations?: {
    /**
     * the type of the association being made
     *
     * @example
     * ```json
     * "issueIdOrKeys"
     * ```
     */
    associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source";
    values?: string[];
  }[];
}> {
  const { reviewId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/operations/1.0/post-incident-reviews/{reviewId}",
    pathParams: { reviewId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Review by ID
 *
 * Delete the Review data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The getReviewById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
 * This resource requires the 'DELETE' scope for Connect apps.
 *
 * @param params.reviewId - The ID of the Review to delete.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteReviewById(
  ctx: ClientContext,
  params: {
    /** The ID of the Review to delete. */
    reviewId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { reviewId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/operations/1.0/post-incident-reviews/{reviewId}",
    pathParams: { reviewId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * APIs related to integrating Incident and Post-Incident Review (PIR) data with Jira Software.
 *
 * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraOperationsInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/operations-information/.
 */
export function createOperationsService(ctx: ClientContext) {
  return {
    /**
     * Submit Operations Workspace Ids
     *
     * Insert Operations Workspace IDs to establish a relationship between them and the Jira site the app is installed in. If a relationship between the Workspace ID and Jira already exists then the workspace ID will be ignored and Jira will process the rest of the entries.
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'WRITE' scope for Connect apps.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Submission accepted. Each submitted Operations Workspace ID will be linked to Jira.
     */
    submitOperationsWorkspaces(
      params: {
        /**
         * The IDs of Operations Workspaces that are available to this Jira site.
         *
         * @example
         * ```json
         * [
         *   "111-222-333",
         *   "444-555-666"
         * ]
         * ```
         */
        workspaceIds: string[];
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Operations Workspaces that have been linked to the Jira site in this request.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedWorkspaceIds?: string[];
    }> {
      return submitOperationsWorkspaces(ctx, params, options);
    },

    /**
     * Delete Operations Workpaces by Id
     *
     * Bulk delete all Operations Workspaces that match the given request.
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * e.g. DELETE /bulk?workspaceIds=111-222-333,444-555-666
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete accepted. Workspaces and relate data will eventually be removed from Jira.
     */
    deleteWorkspaces(options?: {
      headers?: {
        /**
         * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
         *
         * If the JWT token corresponds to an app that does not define the Operations module it will be rejected with a 403.
         *
         * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<void> {
      return deleteWorkspaces(ctx, options);
    },

    /**
     * Get all Operations Workspace IDs or a specific Operations Workspace by ID
     *
     * Retrieve the either all Operations Workspace IDs associated with the Jira site or a specific Operations Workspace ID for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * e.g. GET /workspace?workspaceId=111-222-333
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'READ' scope for Connect apps.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Either the ID provided if stored or a list of available IDs.
     */
    getWorkspaces(options?: {
      headers?: {
        /**
         * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
         *
         * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
         *
         * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<{
      /**
       * The IDs of Operations Workspaces that are available to this Jira site.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      workspaceIds: string[];
    }> {
      return getWorkspaces(ctx, options);
    },

    /**
     * Submit Incident or Review data
     *
     * Update / insert Incident or Review data.
     *
     * Incidents and reviews are identified by their ID, and existing Incident and Review data for the same ID will be replaced if it exists and the updateSequenceNumber of existing data is less than the incoming data.
     *
     * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getIncidentById or getReviewById operation can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple Incidents and Reviews being submitted in one request, each is validated individually prior to submission. Details of which entities failed submission (if any) are available in the response object.
     *
     * A maximum of 1000 incidents can be submitted in one request.
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'WRITE' scope for Connect apps.
     *
     * @param params.body - Incident data to submit.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Submission accepted. Each submitted Incident that is of a valid format will be eventually available in Jira. Details of which Incidents were submitted and which failed submission (due to data format problems etc.) are available in the response object.
     */
    submitEntity(
      params: {
        body:
          | {
              incidents?: {
                /**
                 * The IncidentData schema version used for this incident data.
                 *
                 * Placeholder to support potential schema changes in the future.
                 *
                 * @default "1.0"
                 * @example
                 * ```json
                 * "1.0"
                 * ```
                 */
                schemaVersion: "1.0";
                /**
                 * The identifier for the Incident. Must be unique for a given Provider.
                 *
                 * @example
                 * ```json
                 * "111-222-333"
                 * ```
                 */
                id: string;
                /**
                 * An ID used to apply an ordering to updates for this Incident in the case of out-of-order receipt of update requests.
                 *
                 * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Incident and increment that on each update to Jira).
                 *
                 * Updates for a Incident that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
                 *
                 * @format int64
                 * @example
                 * ```json
                 * 1523494301448
                 * ```
                 */
                updateSequenceNumber: number;
                /**
                 * The IDs of the Components impacted by this Incident. Must be unique for a given Provider.
                 *
                 * @example
                 * ```json
                 * [
                 *   "111-222-333",
                 *   "444-555-666"
                 * ]
                 * ```
                 */
                affectedComponents: string[];
                /**
                 * The human-readable summary for the Incident. Will be shown in the UI.
                 *
                 * If not provided, will use the ID for display.
                 *
                 * @example
                 * ```json
                 * "Unable to log into VPN"
                 * ```
                 */
                summary: string;
                /**
                 * A description of the issue in Markdown format. Will be shown in the UI and used when creating Jira Issues.
                 *
                 * @example
                 * ```json
                 * null
                 * ```
                 */
                description: string;
                /**
                 * A URL users can use to link to a summary view of this incident, if appropriate.
                 *
                 * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the incident in that project).
                 *
                 * @format uri
                 * @example
                 * ```json
                 * "https://example.com/project/ITHELPDESK-9/summary"
                 * ```
                 */
                url: string;
                /**
                 * The timestamp to present to the user that shows when the Incident was raised.
                 *
                 * Expected format is an RFC3339 formatted string.
                 *
                 * @format date-time
                 * @example
                 * ```json
                 * "2018-01-20T23:27:25.000Z"
                 * ```
                 */
                createdDate: string;
                /**
                 * The last-updated timestamp to present to the user the last time the Incident was updated.
                 *
                 * Expected format is an RFC3339 formatted string.
                 *
                 * @format date-time
                 * @example
                 * ```json
                 * "2018-01-20T23:27:25.000Z"
                 * ```
                 */
                lastUpdated: string;
                /**
                 * Severity information for a single Incident.
                 *
                 * This is the severity information that will be presented to the user on e.g. the Jira Incidents screen.
                 */
                severity?: {
                  /**
                   * The severity level of the Incident with P1 being the highest and P5 being the lowest
                   *
                   * @example
                   * ```json
                   * "P1"
                   * ```
                   */
                  level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown";
                };
                /**
                 * The current status of the Incident.
                 *
                 * @example
                 * ```json
                 * "open"
                 * ```
                 */
                status: "open" | "resolved" | "unknown";
                /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
                associations?: {
                  /**
                   * the type of the association being made
                   *
                   * @example
                   * ```json
                   * "issueIdOrKeys"
                   * ```
                   */
                  associationType?:
                    | "issueIdOrKeys"
                    | "serviceIdOrKeys"
                    | "ati:cloud:compass:event-source";
                  values?: string[];
                }[];
              }[];
            }
          | {
              reviews?: {
                /**
                 * The PostIncidentReviewData schema version used for this post-incident review data.
                 *
                 * Placeholder to support potential schema changes in the future.
                 *
                 * @default "1.0"
                 * @example
                 * ```json
                 * "1.0"
                 * ```
                 */
                schemaVersion: "1.0";
                /**
                 * The identifier for the Review. Must be unique for a given Provider.
                 *
                 * @example
                 * ```json
                 * "111-222-333"
                 * ```
                 */
                id: string;
                /**
                 * An ID used to apply an ordering to updates for this Review in the case of out-of-order receipt of update requests.
                 *
                 * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Review and increment that on each update to Jira).
                 *
                 * Updates for a Review that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
                 *
                 * @format int64
                 * @example
                 * ```json
                 * 1523494301448
                 * ```
                 */
                updateSequenceNumber: number;
                /**
                 * The IDs of the Incidents covered by this Review. Must be unique for a given Provider.
                 *
                 * @example
                 * ```json
                 * [
                 *   "111-222-333",
                 *   "444-555-666"
                 * ]
                 * ```
                 */
                reviews: string[];
                /**
                 * The human-readable summary for the Post-Incident Review. Will be shown in the UI.
                 *
                 * If not provided, will use the ID for display.
                 *
                 * @example
                 * ```json
                 * "PIR - Unable to log into VPN"
                 * ```
                 */
                summary: string;
                /**
                 * A description of the review in Markdown format. Will be shown in the UI and used when creating Jira Issues.
                 *
                 * @example
                 * ```json
                 * null
                 * ```
                 */
                description: string;
                /**
                 * A URL users can use to link to a summary view of this review, if appropriate.
                 *
                 * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the review in that project).
                 *
                 * @format uri
                 * @example
                 * ```json
                 * "https://example.com/project/ITHELPDESK-9/summary"
                 * ```
                 */
                url: string;
                /**
                 * The timestamp to present to the user that shows when the Review was raised.
                 *
                 * Expected format is an RFC3339 formatted string.
                 *
                 * @format date-time
                 * @example
                 * ```json
                 * "2018-01-20T23:27:25.000Z"
                 * ```
                 */
                createdDate: string;
                /**
                 * The last-updated timestamp to present to the user the last time the Review was updated.
                 *
                 * Expected format is an RFC3339 formatted string.
                 *
                 * @format date-time
                 * @example
                 * ```json
                 * "2018-01-20T23:27:25.000Z"
                 * ```
                 */
                lastUpdated: string;
                /**
                 * The current status of the Post-Incident Review.
                 *
                 * @example
                 * ```json
                 * "open"
                 * ```
                 */
                status: "in progress" | "outstanding actions" | "completed" | "unknown";
                /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
                associations?: {
                  /**
                   * the type of the association being made
                   *
                   * @example
                   * ```json
                   * "issueIdOrKeys"
                   * ```
                   */
                  associationType?:
                    | "issueIdOrKeys"
                    | "serviceIdOrKeys"
                    | "ati:cloud:compass:event-source";
                  values?: string[];
                }[];
              }[];
            };
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define the Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Incidents that have been accepted for submission.
       *
       * A Incident may be rejected if it was only associated with unknown project keys.
       *
       * Note that a Incident that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedIncidents?: string[];
      /**
       * Details of Incidents that have not been accepted for submission, usually due to a problem with the request data.
       *
       * The object (if present) will be keyed by Incident ID and include any errors associated with that Incident that have prevented it being submitted.
       */
      failedIncidents?: Record<
        string,
        {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[]
      >;
      /**
       * Project keys that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF` is sometimes incorrectly identified as a Jira project key), or they may be for projects that no longer exist.
       *
       * If a Incident has been associated with project keys other than those in this array it will still be stored against those valid keys.
       * If a Incident was only associated with project keys deemed to be invalid it won't be persisted.
       */
      unknownProjectKeys?: string[];
    }> {
      return submitEntity(ctx, params, options);
    },

    /**
     * Delete Incidents or Review by Property
     *
     * Bulk delete all Entties that match the given request.
     *
     * One or more query params must be supplied to specify Properties to delete by.
     * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
     * See the documentation for the submitEntity operation for more details.
     *
     * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
     *
     * Deletion is performed asynchronously. The getIncidentById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteEntityByProperty(options?: {
      headers?: {
        /**
         * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
         *
         * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
         *
         * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<void> {
      return deleteEntityByProperty(ctx, options);
    },

    /**
     * Get a Incident by ID
     *
     * Retrieve the currently stored Incident data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'READ' scope for Connect apps.
     *
     * @param params.incidentId - The ID of the Incident to fetch.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns The Incident data currently stored for the given ID.
     */
    getIncidentById(
      params: {
        /** The ID of the Incident to fetch. */
        incidentId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IncidentData schema version used for this incident data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the Incident. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Incident in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Incident and increment that on each update to Jira).
       *
       * Updates for a Incident that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The IDs of the Components impacted by this Incident. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      affectedComponents: string[];
      /**
       * The human-readable summary for the Incident. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "Unable to log into VPN"
       * ```
       */
      summary: string;
      /**
       * A description of the issue in Markdown format. Will be shown in the UI and used when creating Jira Issues.
       *
       * @example
       * ```json
       * null
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this incident, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the incident in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/ITHELPDESK-9/summary"
       * ```
       */
      url: string;
      /**
       * The timestamp to present to the user that shows when the Incident was raised.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      createdDate: string;
      /**
       * The last-updated timestamp to present to the user the last time the Incident was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * Severity information for a single Incident.
       *
       * This is the severity information that will be presented to the user on e.g. the Jira Incidents screen.
       */
      severity?: {
        /**
         * The severity level of the Incident with P1 being the highest and P5 being the lowest
         *
         * @example
         * ```json
         * "P1"
         * ```
         */
        level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown";
      };
      /**
       * The current status of the Incident.
       *
       * @example
       * ```json
       * "open"
       * ```
       */
      status: "open" | "resolved" | "unknown";
      /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
      associations?: {
        /**
         * the type of the association being made
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source";
        values?: string[];
      }[];
    }> {
      return getIncidentById(ctx, params, options);
    },

    /**
     * Delete a Incident by ID
     *
     * Delete the Incident data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The getIncidentById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * @param params.incidentId - The ID of the Incident to delete.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteIncidentById(
      params: {
        /** The ID of the Incident to delete. */
        incidentId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteIncidentById(ctx, params, options);
    },

    /**
     * Get a Review by ID
     *
     * Retrieve the currently stored Review data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'READ' scope for Connect apps.
     *
     * @param params.reviewId - The ID of the Review to fetch.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns The Review data currently stored for the given ID.
     */
    getReviewById(
      params: {
        /** The ID of the Review to fetch. */
        reviewId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The PostIncidentReviewData schema version used for this post-incident review data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the Review. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Review in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Review and increment that on each update to Jira).
       *
       * Updates for a Review that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The IDs of the Incidents covered by this Review. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      reviews: string[];
      /**
       * The human-readable summary for the Post-Incident Review. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "PIR - Unable to log into VPN"
       * ```
       */
      summary: string;
      /**
       * A description of the review in Markdown format. Will be shown in the UI and used when creating Jira Issues.
       *
       * @example
       * ```json
       * null
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this review, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the review in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/ITHELPDESK-9/summary"
       * ```
       */
      url: string;
      /**
       * The timestamp to present to the user that shows when the Review was raised.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      createdDate: string;
      /**
       * The last-updated timestamp to present to the user the last time the Review was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * The current status of the Post-Incident Review.
       *
       * @example
       * ```json
       * "open"
       * ```
       */
      status: "in progress" | "outstanding actions" | "completed" | "unknown";
      /** The IDs of the Jira issues related to this Incident. Must be unique for a given Provider. */
      associations?: {
        /**
         * the type of the association being made
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source";
        values?: string[];
      }[];
    }> {
      return getReviewById(ctx, params, options);
    },

    /**
     * Delete a Review by ID
     *
     * Delete the Review data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The getReviewById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * Only Connect apps that define the `jiraOperationsInfoProvider` module can access this resource.
     * This resource requires the 'DELETE' scope for Connect apps.
     *
     * @param params.reviewId - The ID of the Review to delete.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteReviewById(
      params: {
        /** The ID of the Review to delete. */
        reviewId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Operations Information module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteReviewById(ctx, params, options);
    },
  };
}
