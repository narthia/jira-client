// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { AuthConfig, Transport } from "../../client/index.ts";
import { http as httpTransport } from "../../transports/_http.ts";

/** Auth accepted by `createPlatformV3Sdk`. */
export interface CreatePlatformV3SdkAuthConfig {
  email: string;
  apiToken: string;
}

function toRuntimeAuth(auth: CreatePlatformV3SdkAuthConfig): AuthConfig {
  return { type: "basic", username: auth.email, password: auth.apiToken };
}

/** Options for this SDK's {@link http} transport. */
export interface HttpOptions {
  /** Base URL requests are resolved against, e.g. `https://api.example.com/v2`. */
  baseUrl: string;
  /** Authentication applied to every request. */
  auth?: CreatePlatformV3SdkAuthConfig;
  /** Custom fetch implementation (polyfill, mock, or instrumented). */
  fetch?: typeof globalThis.fetch;
  /** Extra fetch options merged into every request (e.g. `cache`, `credentials`). */
  fetchOptions?: Omit<RequestInit, "method" | "headers" | "body" | "signal">;
}

/** HTTP transport for this SDK, with typed auth. */
export function http(options: HttpOptions): Transport {
  const { auth, ...rest } = options;
  return httpTransport({ ...rest, auth: auth ? toRuntimeAuth(auth) : undefined });
}
