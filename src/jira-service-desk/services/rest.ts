// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { CustomerCreateDto, ServiceDeskCustomerDto, UserDto } from "../types/index.ts";

/**
 * Create customer
 *
 * Creates a customer account on behalf of jsd-nutmeg.
 *
 * This endpoint is restricted to jsd-nutmeg via ASAP authentication. It provides the same capability as the public \{@code POST /servicedeskapi/customer\} endpoint, but does not require a User Context Token (UCT) or Connect app user \\u2014 authorization is enforced entirely via the ASAP token.
 *
 * No user permission checks are performed; \{@code null\} is passed as the acting user to bypass the permission check in the underlying service.
 *
 * @param params.strictConflictStatusCode - Optional boolean flag; when \{@code true\}, returns 409 Conflict for duplicate email instead of the default 400.
 * @returns 201 response
 */
export function createCustomer(
  ctx: ClientContext,
  params: CustomerCreateDto & {
    /** Optional boolean flag; when \{@code true\}, returns 409 Conflict for duplicate email instead of the default 400. */
    strictConflictStatusCode?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<UserDto> {
  const { strictConflictStatusCode, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/customer/skip-permission-check",
    query: { strictConflictStatusCode },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * View knowledge base article
 *
 * @returns 200 response
 */
export function viewArticle(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    pageId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<string> {
  const { pageId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/knowledgebase/article/view/{pageId}",
    pathParams: { pageId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add customers
 *
 * Adds one or more customers to a service desk on behalf of jsd-nutmeg.
 *
 * This endpoint is restricted to jsd-nutmeg via ASAP authentication. It provides the same capability as the public \{@code POST /servicedeskapi/servicedesk/\{serviceDeskId\}/customer\} endpoint, but does not require a User Context Token (UCT) or Connect app user \\u2014 authorization is enforced entirely via the ASAP token.
 *
 * No user permission checks are performed; \{@code null\} is passed as the acting user to bypass the permission check in the underlying service.
 *
 * If any of the passed customers are already associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
 *
 * @param params.serviceDeskId - The ID of the service desk to add customers to. This can alternatively be a project identifier.
 * @returns The request completed successfully. No additional content will be sent in the response.
 */
export function addCustomers(
  ctx: ClientContext,
  params: ServiceDeskCustomerDto & {
    /** The ID of the service desk to add customers to. This can alternatively be a project identifier. */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { serviceDeskId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/customer/skip-permission-check",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `rest` service. */
export function createRestService(ctx: ClientContext) {
  return {
    /**
     * Create customer
     *
     * Creates a customer account on behalf of jsd-nutmeg.
     *
     * This endpoint is restricted to jsd-nutmeg via ASAP authentication. It provides the same capability as the public \{@code POST /servicedeskapi/customer\} endpoint, but does not require a User Context Token (UCT) or Connect app user \\u2014 authorization is enforced entirely via the ASAP token.
     *
     * No user permission checks are performed; \{@code null\} is passed as the acting user to bypass the permission check in the underlying service.
     *
     * @param params.strictConflictStatusCode - Optional boolean flag; when \{@code true\}, returns 409 Conflict for duplicate email instead of the default 400.
     * @returns 201 response
     */
    createCustomer(
      params: CustomerCreateDto & {
        /** Optional boolean flag; when \{@code true\}, returns 409 Conflict for duplicate email instead of the default 400. */
        strictConflictStatusCode?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<UserDto> {
      return createCustomer(ctx, params, options);
    },

    /**
     * View knowledge base article
     *
     * @returns 200 response
     */
    viewArticle(
      params: {
        /** @format int64 */
        pageId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<string> {
      return viewArticle(ctx, params, options);
    },

    /**
     * Add customers
     *
     * Adds one or more customers to a service desk on behalf of jsd-nutmeg.
     *
     * This endpoint is restricted to jsd-nutmeg via ASAP authentication. It provides the same capability as the public \{@code POST /servicedeskapi/servicedesk/\{serviceDeskId\}/customer\} endpoint, but does not require a User Context Token (UCT) or Connect app user \\u2014 authorization is enforced entirely via the ASAP token.
     *
     * No user permission checks are performed; \{@code null\} is passed as the acting user to bypass the permission check in the underlying service.
     *
     * If any of the passed customers are already associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
     *
     * @param params.serviceDeskId - The ID of the service desk to add customers to. This can alternatively be a project identifier.
     * @returns The request completed successfully. No additional content will be sent in the response.
     */
    addCustomers(
      params: ServiceDeskCustomerDto & {
        /** The ID of the service desk to add customers to. This can alternatively be a project identifier. */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return addCustomers(ctx, params, options);
    },
  };
}
