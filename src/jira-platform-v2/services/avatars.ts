// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { Avatar, Avatars, StreamingResponseBody, SystemAvatars } from "../types/index.ts";

/**
 * Get system avatars by type
 *
 * Returns a list of system avatar details by owner type, where the owner types are issue type, project, user or priority.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.type - The avatar type.
 * @returns Returned if the request is successful.
 */
export function getAllSystemAvatars(
  ctx: ClientContext,
  params: {
    /**
     * The avatar type.
     *
     * @example
     * ```json
     * "project"
     * ```
     */
    type: "issuetype" | "project" | "user" | "priority";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SystemAvatars> {
  const { type } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/avatar/{type}/system",
    pathParams: { type },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get avatars
 *
 * Returns the system and custom avatars for a project, issue type or priority.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  for custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
 *  *  for custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
 *  *  for system avatars, none.
 *  *  for priority avatars, none.
 *
 * @param params.type - The avatar type.
 * @param params.entityId - The ID of the item the avatar is associated with.
 * @returns Returned if the request is successful.
 */
export function getAvatars(
  ctx: ClientContext,
  params: {
    /** The avatar type. */
    type: "project" | "issuetype" | "priority";
    /** The ID of the item the avatar is associated with. */
    entityId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Avatars> {
  const { type, entityId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/universal_avatar/type/{type}/owner/{entityId}",
    pathParams: { type, entityId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Load avatar
 *
 * Loads a custom avatar for a project, issue type or priority.
 *
 * Specify the avatar's local file location in the body of the request. Also, include the following headers:
 *
 *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
 *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
 *
 * For example:
 * `curl --request POST `
 *
 * `--user email@example.com:<api_token> `
 *
 * `--header 'X-Atlassian-Token: no-check' `
 *
 * `--header 'Content-Type: image/< image_type>' `
 *
 * `--data-binary "<@/path/to/file/with/your/avatar>" `
 *
 * `--url 'https://your-domain.atlassian.net/rest/api/2/universal_avatar/type/{type}/owner/{entityId}'`
 *
 * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
 *
 * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
 *
 * After creating the avatar use:
 *
 *  *  [Update issue type](#api-rest-api-2-issuetype-id-put) to set it as the issue type's displayed avatar.
 *  *  [Set project avatar](#api-rest-api-2-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
 *  *  [Update priority](#api-rest-api-2-priority-id-put) to set it as the priority's displayed avatar.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.type - The avatar type.
 * @param params.entityId - The ID of the item the avatar is associated with.
 * @param params.x - The X coordinate of the top-left corner of the crop region.
 * @param params.y - The Y coordinate of the top-left corner of the crop region.
 * @param params.size - The length of each side of the crop region.
 * @returns Returned if the request is successful.
 */
export function storeAvatar(
  ctx: ClientContext,
  params: {
    /** The avatar type. */
    type: "project" | "issuetype" | "priority";
    /** The ID of the item the avatar is associated with. */
    entityId: string;
    /**
     * The X coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    x?: number;
    /**
     * The Y coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    y?: number;
    /**
     * The length of each side of the crop region.
     *
     * @default 0
     * @format int32
     */
    size: number;
    body: Blob;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Avatar> {
  const { type, entityId, x, y, size, body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/universal_avatar/type/{type}/owner/{entityId}",
    pathParams: { type, entityId },
    query: { x, y, size },
    headers: options?.headers,
    body: body,
    bodyType: "binary",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete avatar
 *
 * Deletes an avatar from a project, issue type or priority.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.type - The avatar type.
 * @param params.owningObjectId - The ID of the item the avatar is associated with.
 * @param params.id - The ID of the avatar.
 * @returns Returned if the request is successful.
 */
export function deleteAvatar(
  ctx: ClientContext,
  params: {
    /** The avatar type. */
    type: "project" | "issuetype" | "priority";
    /** The ID of the item the avatar is associated with. */
    owningObjectId: string;
    /**
     * The ID of the avatar.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { type, owningObjectId, id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/universal_avatar/type/{type}/owner/{owningObjectId}/avatar/{id}",
    pathParams: { type, owningObjectId, id },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get avatar image by type
 *
 * Returns the default project, issue type or priority avatar image.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.type - The icon type of the avatar.
 * @param params.size - The size of the avatar image. If not provided the default size is returned.
 * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
 * @returns Returned if the request is successful.
 */
export function getAvatarImageByType(
  ctx: ClientContext,
  params: {
    /** The icon type of the avatar. */
    type: "issuetype" | "project" | "priority";
    /** The size of the avatar image. If not provided the default size is returned. */
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    /** The format to return the avatar image in. If not provided the original content format is returned. */
    format?: "png" | "svg";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StreamingResponseBody> {
  const { type, size, format } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/universal_avatar/view/type/{type}",
    pathParams: { type },
    query: { size, format },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get avatar image by ID
 *
 * Returns a project, issue type or priority avatar image by ID.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  For system avatars, none.
 *  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
 *  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
 *  *  For priority avatars, none.
 *
 * @param params.type - The icon type of the avatar.
 * @param params.id - The ID of the avatar.
 * @param params.size - The size of the avatar image. If not provided the default size is returned.
 * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
 * @returns Returned if the request is successful.
 */
export function getAvatarImageById(
  ctx: ClientContext,
  params: {
    /** The icon type of the avatar. */
    type: "issuetype" | "project" | "priority";
    /**
     * The ID of the avatar.
     *
     * @format int64
     */
    id: number;
    /** The size of the avatar image. If not provided the default size is returned. */
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    /** The format to return the avatar image in. If not provided the original content format is returned. */
    format?: "png" | "svg";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StreamingResponseBody> {
  const { type, id, size, format } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/universal_avatar/view/type/{type}/avatar/{id}",
    pathParams: { type, id },
    query: { size, format },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get avatar image by owner
 *
 * Returns the avatar image for a project, issue type or priority.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  For system avatars, none.
 *  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
 *  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
 *  *  For priority avatars, none.
 *
 * @param params.type - The icon type of the avatar.
 * @param params.entityId - The ID of the project or issue type the avatar belongs to.
 * @param params.size - The size of the avatar image. If not provided the default size is returned.
 * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
 * @returns Returned if the request is successful.
 */
export function getAvatarImageByOwner(
  ctx: ClientContext,
  params: {
    /** The icon type of the avatar. */
    type: "issuetype" | "project" | "priority";
    /** The ID of the project or issue type the avatar belongs to. */
    entityId: string;
    /** The size of the avatar image. If not provided the default size is returned. */
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    /** The format to return the avatar image in. If not provided the original content format is returned. */
    format?: "png" | "svg";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StreamingResponseBody> {
  const { type, entityId, size, format } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/universal_avatar/view/type/{type}/owner/{entityId}",
    pathParams: { type, entityId },
    query: { size, format },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents system and custom avatars. Use it to obtain the details of system or custom avatars, add and remove avatars from a project, issue type or priority and obtain avatar images. */
export function createAvatarsService(ctx: ClientContext) {
  return {
    /**
     * Get system avatars by type
     *
     * Returns a list of system avatar details by owner type, where the owner types are issue type, project, user or priority.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.type - The avatar type.
     * @returns Returned if the request is successful.
     */
    getAllSystemAvatars(
      params: {
        /**
         * The avatar type.
         *
         * @example
         * ```json
         * "project"
         * ```
         */
        type: "issuetype" | "project" | "user" | "priority";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SystemAvatars> {
      return getAllSystemAvatars(ctx, params, options);
    },

    /**
     * Get avatars
     *
     * Returns the system and custom avatars for a project, issue type or priority.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  for custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
     *  *  for custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
     *  *  for system avatars, none.
     *  *  for priority avatars, none.
     *
     * @param params.type - The avatar type.
     * @param params.entityId - The ID of the item the avatar is associated with.
     * @returns Returned if the request is successful.
     */
    getAvatars(
      params: {
        /** The avatar type. */
        type: "project" | "issuetype" | "priority";
        /** The ID of the item the avatar is associated with. */
        entityId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Avatars> {
      return getAvatars(ctx, params, options);
    },

    /**
     * Load avatar
     *
     * Loads a custom avatar for a project, issue type or priority.
     *
     * Specify the avatar's local file location in the body of the request. Also, include the following headers:
     *
     *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
     *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
     *
     * For example:
     * `curl --request POST `
     *
     * `--user email@example.com:<api_token> `
     *
     * `--header 'X-Atlassian-Token: no-check' `
     *
     * `--header 'Content-Type: image/< image_type>' `
     *
     * `--data-binary "<@/path/to/file/with/your/avatar>" `
     *
     * `--url 'https://your-domain.atlassian.net/rest/api/2/universal_avatar/type/{type}/owner/{entityId}'`
     *
     * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
     *
     * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
     *
     * After creating the avatar use:
     *
     *  *  [Update issue type](#api-rest-api-2-issuetype-id-put) to set it as the issue type's displayed avatar.
     *  *  [Set project avatar](#api-rest-api-2-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
     *  *  [Update priority](#api-rest-api-2-priority-id-put) to set it as the priority's displayed avatar.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.type - The avatar type.
     * @param params.entityId - The ID of the item the avatar is associated with.
     * @param params.x - The X coordinate of the top-left corner of the crop region.
     * @param params.y - The Y coordinate of the top-left corner of the crop region.
     * @param params.size - The length of each side of the crop region.
     * @returns Returned if the request is successful.
     */
    storeAvatar(
      params: {
        /** The avatar type. */
        type: "project" | "issuetype" | "priority";
        /** The ID of the item the avatar is associated with. */
        entityId: string;
        /**
         * The X coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        x?: number;
        /**
         * The Y coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        y?: number;
        /**
         * The length of each side of the crop region.
         *
         * @default 0
         * @format int32
         */
        size: number;
        body: Blob;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Avatar> {
      return storeAvatar(ctx, params, options);
    },

    /**
     * Delete avatar
     *
     * Deletes an avatar from a project, issue type or priority.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.type - The avatar type.
     * @param params.owningObjectId - The ID of the item the avatar is associated with.
     * @param params.id - The ID of the avatar.
     * @returns Returned if the request is successful.
     */
    deleteAvatar(
      params: {
        /** The avatar type. */
        type: "project" | "issuetype" | "priority";
        /** The ID of the item the avatar is associated with. */
        owningObjectId: string;
        /**
         * The ID of the avatar.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteAvatar(ctx, params, options);
    },

    /**
     * Get avatar image by type
     *
     * Returns the default project, issue type or priority avatar image.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.type - The icon type of the avatar.
     * @param params.size - The size of the avatar image. If not provided the default size is returned.
     * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
     * @returns Returned if the request is successful.
     */
    getAvatarImageByType(
      params: {
        /** The icon type of the avatar. */
        type: "issuetype" | "project" | "priority";
        /** The size of the avatar image. If not provided the default size is returned. */
        size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
        /** The format to return the avatar image in. If not provided the original content format is returned. */
        format?: "png" | "svg";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StreamingResponseBody> {
      return getAvatarImageByType(ctx, params, options);
    },

    /**
     * Get avatar image by ID
     *
     * Returns a project, issue type or priority avatar image by ID.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  For system avatars, none.
     *  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
     *  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
     *  *  For priority avatars, none.
     *
     * @param params.type - The icon type of the avatar.
     * @param params.id - The ID of the avatar.
     * @param params.size - The size of the avatar image. If not provided the default size is returned.
     * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
     * @returns Returned if the request is successful.
     */
    getAvatarImageById(
      params: {
        /** The icon type of the avatar. */
        type: "issuetype" | "project" | "priority";
        /**
         * The ID of the avatar.
         *
         * @format int64
         */
        id: number;
        /** The size of the avatar image. If not provided the default size is returned. */
        size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
        /** The format to return the avatar image in. If not provided the original content format is returned. */
        format?: "png" | "svg";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StreamingResponseBody> {
      return getAvatarImageById(ctx, params, options);
    },

    /**
     * Get avatar image by owner
     *
     * Returns the avatar image for a project, issue type or priority.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  For system avatars, none.
     *  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
     *  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
     *  *  For priority avatars, none.
     *
     * @param params.type - The icon type of the avatar.
     * @param params.entityId - The ID of the project or issue type the avatar belongs to.
     * @param params.size - The size of the avatar image. If not provided the default size is returned.
     * @param params.format - The format to return the avatar image in. If not provided the original content format is returned.
     * @returns Returned if the request is successful.
     */
    getAvatarImageByOwner(
      params: {
        /** The icon type of the avatar. */
        type: "issuetype" | "project" | "priority";
        /** The ID of the project or issue type the avatar belongs to. */
        entityId: string;
        /** The size of the avatar image. If not provided the default size is returned. */
        size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
        /** The format to return the avatar image in. If not provided the original content format is returned. */
        format?: "png" | "svg";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StreamingResponseBody> {
      return getAvatarImageByOwner(ctx, params, options);
    },
  };
}
