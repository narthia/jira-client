// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { EntityProperty, OperationMessage, PropertyKeys } from "../types/index.ts";

/**
 * Get app properties
 *
 * Gets all the properties of an app. The reserved key `connect_client_key_019cdff3-8bfb-71fe-9628-875b700aebb8` is not returned.
 *
 * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
 * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
 *
 * @param params.addonKey - The key of the app, as defined in its descriptor.
 * @returns Returned if the request is successful.
 */
export function addonPropertiesResourceGetAddonPropertiesGet(
  ctx: ClientContext,
  params: {
    /** The key of the app, as defined in its descriptor. */
    addonKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PropertyKeys> {
  const { addonKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/atlassian-connect/1/addons/{addonKey}/properties",
    pathParams: { addonKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get app property
 *
 * Returns the key and value of an app's property. The property key `connect_client_key_019cdff3-8bfb-71fe-9628-875b700aebb8`
 * is reserved. It returns a synthetic, read-only property containing the Connect `clientKey` for the requested tenant.
 * This is intended for Forge apps with `app.connect.key` to retrieve the Connect client key during migration.
 *
 * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
 * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
 *
 * @param params.addonKey - The key of the app, as defined in its descriptor.
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function addonPropertiesResourceGetAddonPropertyGet(
  ctx: ClientContext,
  params: {
    /** The key of the app, as defined in its descriptor. */
    addonKey: string;
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<EntityProperty> {
  const { addonKey, propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}",
    pathParams: { addonKey, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set app property
 *
 * Sets the value of an app's property. Use this resource to store custom data for your app.
 *
 * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
 *
 * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
 * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
 *
 * @param params.addonKey - The key of the app, as defined in its descriptor.
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the property is updated.
 */
export function addonPropertiesResourcePutAddonPropertyPut(
  ctx: ClientContext,
  params: {
    /** The key of the app, as defined in its descriptor. */
    addonKey: string;
    /** The key of the property. */
    propertyKey: string;
    body: unknown;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<OperationMessage> {
  const { addonKey, propertyKey, body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}",
    pathParams: { addonKey, propertyKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete app property
 *
 * Deletes an app's property.
 *
 * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
 * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
 *
 * @param params.addonKey - The key of the app, as defined in its descriptor.
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function addonPropertiesResourceDeleteAddonPropertyDelete(
  ctx: ClientContext,
  params: {
    /** The key of the app, as defined in its descriptor. */
    addonKey: string;
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { addonKey, propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/atlassian-connect/1/addons/{addonKey}/properties/{propertyKey}",
    pathParams: { addonKey, propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get app property keys (Forge)
 *
 * Returns all property keys for the Forge app.
 *
 * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
 *
 * @returns Returned if the request is successful.
 */
export function getForgeAppPropertyKeys(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  keys?: {
    key?: string;
    self?: string;
  }[];
}> {
  return ctx.request({
    method: "get",
    path: "/rest/forge/1/app/properties",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get app property (Forge)
 *
 * Returns the value of a Forge app's property.
 *
 * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
 *
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function getForgeAppProperty(
  ctx: ClientContext,
  params: {
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  key?: string;
  value?: unknown;
}> {
  const { propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/forge/1/app/properties/{propertyKey}",
    pathParams: { propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set app property (Forge)
 *
 * Sets the value of a Forge app's property.
 * These values can be retrieved in [Jira expressions](/cloud/jira/platform/jira-expressions/)
 * through the `app` [context variable](/cloud/jira/platform/jira-expressions/#context-variables).
 * They are also available in [entity property display conditions](/platform/forge/manifest-reference/display-conditions/entity-property-conditions/).
 *
 * For other use cases, use the [Storage API](/platform/forge/runtime-reference/storage-api/).
 *
 * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
 *
 * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
 *
 * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the property is updated.
 */
export function putForgeAppProperty(
  ctx: ClientContext,
  params: {
    /** The key of the property. */
    propertyKey: string;
    body: unknown;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<OperationMessage> {
  const { propertyKey, body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/forge/1/app/properties/{propertyKey}",
    pathParams: { propertyKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete app property (Forge)
 *
 * Deletes a Forge app's property.
 *
 * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
 *
 * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @param params.propertyKey - The key of the property.
 * @returns Returned if the request is successful.
 */
export function deleteForgeAppProperty(
  ctx: ClientContext,
  params: {
    /** The key of the property. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/forge/1/app/properties/{propertyKey}",
    pathParams: { propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents app properties. Use it to store arbitrary data for your
 * [Connect app](https://developer.atlassian.com/cloud/jira/platform/index/#connect-apps).
 */
export function createAppPropertiesService(ctx: ClientContext) {
  return {
    /**
     * Get app properties
     *
     * Gets all the properties of an app. The reserved key `connect_client_key_019cdff3-8bfb-71fe-9628-875b700aebb8` is not returned.
     *
     * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
     * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     *
     * @param params.addonKey - The key of the app, as defined in its descriptor.
     * @returns Returned if the request is successful.
     */
    addonPropertiesResourceGetAddonPropertiesGet(
      params: {
        /** The key of the app, as defined in its descriptor. */
        addonKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PropertyKeys> {
      return addonPropertiesResourceGetAddonPropertiesGet(ctx, params, options);
    },

    /**
     * Get app property
     *
     * Returns the key and value of an app's property. The property key `connect_client_key_019cdff3-8bfb-71fe-9628-875b700aebb8`
     * is reserved. It returns a synthetic, read-only property containing the Connect `clientKey` for the requested tenant.
     * This is intended for Forge apps with `app.connect.key` to retrieve the Connect client key during migration.
     *
     * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
     * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     *
     * @param params.addonKey - The key of the app, as defined in its descriptor.
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    addonPropertiesResourceGetAddonPropertyGet(
      params: {
        /** The key of the app, as defined in its descriptor. */
        addonKey: string;
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<EntityProperty> {
      return addonPropertiesResourceGetAddonPropertyGet(ctx, params, options);
    },

    /**
     * Set app property
     *
     * Sets the value of an app's property. Use this resource to store custom data for your app.
     *
     * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
     *
     * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
     * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     *
     * @param params.addonKey - The key of the app, as defined in its descriptor.
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the property is updated.
     */
    addonPropertiesResourcePutAddonPropertyPut(
      params: {
        /** The key of the app, as defined in its descriptor. */
        addonKey: string;
        /** The key of the property. */
        propertyKey: string;
        body: unknown;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<OperationMessage> {
      return addonPropertiesResourcePutAddonPropertyPut(ctx, params, options);
    },

    /**
     * Delete app property
     *
     * Deletes an app's property.
     *
     * **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.
     * Additionally, Forge apps can access Connect app properties (stored against the same `app.connect.key`).
     *
     * @param params.addonKey - The key of the app, as defined in its descriptor.
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    addonPropertiesResourceDeleteAddonPropertyDelete(
      params: {
        /** The key of the app, as defined in its descriptor. */
        addonKey: string;
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return addonPropertiesResourceDeleteAddonPropertyDelete(ctx, params, options);
    },

    /**
     * Get app property keys (Forge)
     *
     * Returns all property keys for the Forge app.
     *
     * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
     *
     * @returns Returned if the request is successful.
     */
    getForgeAppPropertyKeys(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<{
      keys?: {
        key?: string;
        self?: string;
      }[];
    }> {
      return getForgeAppPropertyKeys(ctx, options);
    },

    /**
     * Get app property (Forge)
     *
     * Returns the value of a Forge app's property.
     *
     * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
     *
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    getForgeAppProperty(
      params: {
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      key?: string;
      value?: unknown;
    }> {
      return getForgeAppProperty(ctx, params, options);
    },

    /**
     * Set app property (Forge)
     *
     * Sets the value of a Forge app's property.
     * These values can be retrieved in [Jira expressions](/cloud/jira/platform/jira-expressions/)
     * through the `app` [context variable](/cloud/jira/platform/jira-expressions/#context-variables).
     * They are also available in [entity property display conditions](/platform/forge/manifest-reference/display-conditions/entity-property-conditions/).
     *
     * For other use cases, use the [Storage API](/platform/forge/runtime-reference/storage-api/).
     *
     * The value of the request body must be a [valid](http://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters.
     *
     * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
     *
     * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the property is updated.
     */
    putForgeAppProperty(
      params: {
        /** The key of the property. */
        propertyKey: string;
        body: unknown;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<OperationMessage> {
      return putForgeAppProperty(ctx, params, options);
    },

    /**
     * Delete app property (Forge)
     *
     * Deletes a Forge app's property.
     *
     * **[Permissions](#permissions) required:** Only Forge apps can make this request. This API can only be accessed using **[asApp()](https://developer.atlassian.com/platform/forge/apis-reference/fetch-api-product.requestjira/#method-signature)** requests from Forge.
     *
     * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @param params.propertyKey - The key of the property.
     * @returns Returned if the request is successful.
     */
    deleteForgeAppProperty(
      params: {
        /** The key of the property. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteForgeAppProperty(ctx, params, options);
    },
  };
}
