// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { AddFieldBean, MoveFieldBean, ScreenableField } from "../types/index.ts";

/**
 * Get all screen tab fields
 *
 * Returns all fields for a screen tab.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg) when the project key is specified, providing that the screen is associated with the project through a Screen Scheme and Issue Type Screen Scheme.
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @param params.projectKey - The key of the project.
 * @returns Returned if the request is successful.
 */
export function getAllScreenTabFields(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
    /** The key of the project. */
    projectKey?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenableField[]> {
  const { screenId, tabId, projectKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields",
    pathParams: { screenId, tabId },
    query: { projectKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add screen tab field
 *
 * Adds a field to a screen tab.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @param params.skipFieldAssociation - Whether to skip associating the added field with projects linked to this screen. By default, adding a field to a screen tab also updates the field configuration of associated projects, making the field visible in issue experiences. When set to true, the field is added to the screen tab only and will not be visible in experiences such as issue view for associated projects unless the field association is updated separately.
 * @returns Returned if the request is successful.
 */
export function addScreenTabField(
  ctx: ClientContext,
  params: AddFieldBean & {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
    /**
     * Whether to skip associating the added field with projects linked to this screen. By default, adding a field to a screen tab also updates the field configuration of associated projects, making the field visible in issue experiences. When set to true, the field is added to the screen tab only and will not be visible in experiences such as issue view for associated projects unless the field association is updated separately.
     *
     * @default false
     */
    skipFieldAssociation?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenableField> {
  const { screenId, tabId, skipFieldAssociation, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields",
    pathParams: { screenId, tabId },
    query: { skipFieldAssociation },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove screen tab field
 *
 * Removes a field from a screen tab.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @param params.id - The ID of the field.
 * @returns Returned if the request is successful.
 */
export function removeScreenTabField(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
    /** The ID of the field. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { screenId, tabId, id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}",
    pathParams: { screenId, tabId, id },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move screen tab field
 *
 * Moves a screen tab field.
 *
 * If `after` and `position` are provided in the request, `position` is ignored.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @param params.id - The ID of the field.
 * @returns Returned if the request is successful.
 */
export function moveScreenTabField(
  ctx: ClientContext,
  params: MoveFieldBean & {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
    /** The ID of the field. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { screenId, tabId, id, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/screens/{screenId}/tabs/{tabId}/fields/{id}/move",
    pathParams: { screenId, tabId, id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents the screen tab fields used to record issue details. Use it to get, add, move, and remove fields from screen tabs. */
export function createScreenTabFieldsService(ctx: ClientContext) {
  return {
    /**
     * Get all screen tab fields
     *
     * Returns all fields for a screen tab.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg) when the project key is specified, providing that the screen is associated with the project through a Screen Scheme and Issue Type Screen Scheme.
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @param params.projectKey - The key of the project.
     * @returns Returned if the request is successful.
     */
    getAllScreenTabFields(
      params: {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
        /** The key of the project. */
        projectKey?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenableField[]> {
      return getAllScreenTabFields(ctx, params, options);
    },

    /**
     * Add screen tab field
     *
     * Adds a field to a screen tab.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @param params.skipFieldAssociation - Whether to skip associating the added field with projects linked to this screen. By default, adding a field to a screen tab also updates the field configuration of associated projects, making the field visible in issue experiences. When set to true, the field is added to the screen tab only and will not be visible in experiences such as issue view for associated projects unless the field association is updated separately.
     * @returns Returned if the request is successful.
     */
    addScreenTabField(
      params: AddFieldBean & {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
        /**
         * Whether to skip associating the added field with projects linked to this screen. By default, adding a field to a screen tab also updates the field configuration of associated projects, making the field visible in issue experiences. When set to true, the field is added to the screen tab only and will not be visible in experiences such as issue view for associated projects unless the field association is updated separately.
         *
         * @default false
         */
        skipFieldAssociation?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenableField> {
      return addScreenTabField(ctx, params, options);
    },

    /**
     * Remove screen tab field
     *
     * Removes a field from a screen tab.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @param params.id - The ID of the field.
     * @returns Returned if the request is successful.
     */
    removeScreenTabField(
      params: {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
        /** The ID of the field. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return removeScreenTabField(ctx, params, options);
    },

    /**
     * Move screen tab field
     *
     * Moves a screen tab field.
     *
     * If `after` and `position` are provided in the request, `position` is ignored.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @param params.id - The ID of the field.
     * @returns Returned if the request is successful.
     */
    moveScreenTabField(
      params: MoveFieldBean & {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
        /** The ID of the field. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return moveScreenTabField(ctx, params, options);
    },
  };
}
