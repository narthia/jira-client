// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  Avatar,
  IssueTypeCreateBean,
  IssueTypeDetails,
  IssueTypeUpdateBean,
} from "../types/index.ts";

/**
 * Get all issue types for user
 *
 * Returns all issue types.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** Issue types are only returned as follows:
 *
 *  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg), all issue types are returned.
 *  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, the issue types associated with the projects the user has permission to browse are returned.
 *  *  if the user is anonymous then they will be able to access projects with the *Browse projects* for anonymous users
 *  *  if the user authentication is incorrect they will fall back to anonymous
 *
 * @returns Returned if the request is successful.
 */
export function getIssueAllTypes(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails[]> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuetype",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create issue type
 *
 * Creates an issue type.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createIssueType(
  ctx: ClientContext,
  params: IssueTypeCreateBean,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/issuetype",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue types for project
 *
 * Returns issue types for a project.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) in the relevant project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.projectId - The ID of the project.
 * @param params.level - The level of the issue type to filter by. Use: *  `-1` for Subtask. *  `0` for Base. *  `1` for Epic.
 * @returns Returned if the request is successful.
 */
export function getIssueTypesForProject(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the project.
     *
     * @format int64
     */
    projectId: number;
    /**
     * The level of the issue type to filter by. Use:
     *
     *  *  `-1` for Subtask.
     *  *  `0` for Base.
     *  *  `1` for Epic.
     *
     * @format int32
     */
    level?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails[]> {
  const { projectId, level } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuetype/project",
    query: { projectId, level },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue type
 *
 * Returns an issue type.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) in a project the issue type is associated with or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function getIssueType(
  ctx: ClientContext,
  params: {
    /** The ID of the issue type. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuetype/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue type
 *
 * Updates the issue type.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function updateIssueType(
  ctx: ClientContext,
  params: IssueTypeUpdateBean & {
    /** The ID of the issue type. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuetype/{id}",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue type
 *
 * Deletes the issue type. If the issue type is in use, all uses are updated with the alternative issue type (`alternativeIssueTypeId`). A list of alternative issue types are obtained from the [Get alternative issue types](#api-rest-api-2-issuetype-id-alternatives-get) resource.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue type.
 * @param params.alternativeIssueTypeId - The ID of the replacement issue type.
 * @returns Returned if the request is successful.
 */
export function deleteIssueType(
  ctx: ClientContext,
  params: {
    /** The ID of the issue type. */
    id: string;
    /** The ID of the replacement issue type. */
    alternativeIssueTypeId?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, alternativeIssueTypeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issuetype/{id}",
    pathParams: { id },
    query: { alternativeIssueTypeId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get alternative issue types
 *
 * Returns a list of issue types that can be used to replace the issue type. The alternative issue types are those assigned to the same workflow scheme, field configuration scheme, and screen scheme.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.id - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function getAlternativeIssueTypes(
  ctx: ClientContext,
  params: {
    /** The ID of the issue type. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeDetails[]> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuetype/{id}/alternatives",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Load issue type avatar
 *
 * Loads an avatar for the issue type.
 *
 * Specify the avatar's local file location in the body of the request. Also, include the following headers:
 *
 *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
 *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
 *
 * For example:
 * `curl --request POST \ --user email@example.com:<api_token> \ --header 'X-Atlassian-Token: no-check' \ --header 'Content-Type: image/< image_type>' \ --data-binary "<@/path/to/file/with/your/avatar>" \ --url 'https://your-domain.atlassian.net/rest/api/2/issuetype/{issueTypeId}'This`
 *
 * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
 *
 * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
 *
 * After creating the avatar, use [ Update issue type](#api-rest-api-2-issuetype-id-put) to set it as the issue type's displayed avatar.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue type.
 * @param params.x - The X coordinate of the top-left corner of the crop region.
 * @param params.y - The Y coordinate of the top-left corner of the crop region.
 * @param params.size - The length of each side of the crop region.
 * @returns Returned if the request is successful.
 */
export function createIssueTypeAvatar(
  ctx: ClientContext,
  params: {
    /** The ID of the issue type. */
    id: string;
    /**
     * The X coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    x?: number;
    /**
     * The Y coordinate of the top-left corner of the crop region.
     *
     * @default 0
     * @format int32
     */
    y?: number;
    /**
     * The length of each side of the crop region.
     *
     * @format int32
     */
    size: number;
    body: Blob;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Avatar> {
  const { id, x, y, size, body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/issuetype/{id}/avatar2",
    pathParams: { id },
    query: { x, y, size },
    headers: options?.headers,
    body: body,
    bodyType: "binary",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents issues types. Use it to:
 *
 *  *  get, create, update, and delete issue types.
 *  *  get all issue types for a user.
 *  *  get alternative issue types.
 *  *  set an avatar for an issue type.
 */
export function createIssueTypesService(ctx: ClientContext) {
  return {
    /**
     * Get all issue types for user
     *
     * Returns all issue types.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** Issue types are only returned as follows:
     *
     *  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg), all issue types are returned.
     *  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, the issue types associated with the projects the user has permission to browse are returned.
     *  *  if the user is anonymous then they will be able to access projects with the *Browse projects* for anonymous users
     *  *  if the user authentication is incorrect they will fall back to anonymous
     *
     * @returns Returned if the request is successful.
     */
    getIssueAllTypes(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<IssueTypeDetails[]> {
      return getIssueAllTypes(ctx, options);
    },

    /**
     * Create issue type
     *
     * Creates an issue type.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createIssueType(
      params: IssueTypeCreateBean,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeDetails> {
      return createIssueType(ctx, params, options);
    },

    /**
     * Get issue types for project
     *
     * Returns issue types for a project.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) in the relevant project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.projectId - The ID of the project.
     * @param params.level - The level of the issue type to filter by. Use: *  `-1` for Subtask. *  `0` for Base. *  `1` for Epic.
     * @returns Returned if the request is successful.
     */
    getIssueTypesForProject(
      params: {
        /**
         * The ID of the project.
         *
         * @format int64
         */
        projectId: number;
        /**
         * The level of the issue type to filter by. Use:
         *
         *  *  `-1` for Subtask.
         *  *  `0` for Base.
         *  *  `1` for Epic.
         *
         * @format int32
         */
        level?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeDetails[]> {
      return getIssueTypesForProject(ctx, params, options);
    },

    /**
     * Get issue type
     *
     * Returns an issue type.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) in a project the issue type is associated with or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    getIssueType(
      params: {
        /** The ID of the issue type. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeDetails> {
      return getIssueType(ctx, params, options);
    },

    /**
     * Update issue type
     *
     * Updates the issue type.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    updateIssueType(
      params: IssueTypeUpdateBean & {
        /** The ID of the issue type. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeDetails> {
      return updateIssueType(ctx, params, options);
    },

    /**
     * Delete issue type
     *
     * Deletes the issue type. If the issue type is in use, all uses are updated with the alternative issue type (`alternativeIssueTypeId`). A list of alternative issue types are obtained from the [Get alternative issue types](#api-rest-api-2-issuetype-id-alternatives-get) resource.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue type.
     * @param params.alternativeIssueTypeId - The ID of the replacement issue type.
     * @returns Returned if the request is successful.
     */
    deleteIssueType(
      params: {
        /** The ID of the issue type. */
        id: string;
        /** The ID of the replacement issue type. */
        alternativeIssueTypeId?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteIssueType(ctx, params, options);
    },

    /**
     * Get alternative issue types
     *
     * Returns a list of issue types that can be used to replace the issue type. The alternative issue types are those assigned to the same workflow scheme, field configuration scheme, and screen scheme.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.id - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    getAlternativeIssueTypes(
      params: {
        /** The ID of the issue type. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeDetails[]> {
      return getAlternativeIssueTypes(ctx, params, options);
    },

    /**
     * Load issue type avatar
     *
     * Loads an avatar for the issue type.
     *
     * Specify the avatar's local file location in the body of the request. Also, include the following headers:
     *
     *  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
     *  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
     *
     * For example:
     * `curl --request POST \ --user email@example.com:<api_token> \ --header 'X-Atlassian-Token: no-check' \ --header 'Content-Type: image/< image_type>' \ --data-binary "<@/path/to/file/with/your/avatar>" \ --url 'https://your-domain.atlassian.net/rest/api/2/issuetype/{issueTypeId}'This`
     *
     * The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
     *
     * The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
     *
     * After creating the avatar, use [ Update issue type](#api-rest-api-2-issuetype-id-put) to set it as the issue type's displayed avatar.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue type.
     * @param params.x - The X coordinate of the top-left corner of the crop region.
     * @param params.y - The Y coordinate of the top-left corner of the crop region.
     * @param params.size - The length of each side of the crop region.
     * @returns Returned if the request is successful.
     */
    createIssueTypeAvatar(
      params: {
        /** The ID of the issue type. */
        id: string;
        /**
         * The X coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        x?: number;
        /**
         * The Y coordinate of the top-left corner of the crop region.
         *
         * @default 0
         * @format int32
         */
        y?: number;
        /**
         * The length of each side of the crop region.
         *
         * @format int32
         */
        size: number;
        body: Blob;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Avatar> {
      return createIssueTypeAvatar(ctx, params, options);
    },
  };
}
