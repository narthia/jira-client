// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreatePrioritySchemeDetails,
  PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects,
  PageBeanPriorityWithSequence,
  PageBeanProject,
  PrioritySchemeId,
  SuggestedMappingsRequestBean,
  UpdatePrioritySchemeRequestBean,
  UpdatePrioritySchemeResponseBean,
} from "../types/index.ts";

/**
 * Get priority schemes
 *
 * Returns a [paginated](#pagination) list of priority schemes.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.priorityId - A set of priority IDs to filter by. To include multiple IDs, provide an ampersand-separated list. For example, `priorityId=10000&priorityId=10001`.
 * @param params.schemeId - A set of priority scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `schemeId=10000&schemeId=10001`.
 * @param params.schemeName - The name of scheme to search for.
 * @param params.onlyDefault - Whether only the default priority is returned.
 * @param params.orderBy - The ordering to return the priority schemes by.
 * @param params.expand - A comma separated list of additional information to return. "priorities" will return priorities associated with the priority scheme. "projects" will return projects associated with the priority scheme. `expand=priorities,projects`.
 * @returns Returned if the request is successful.
 */
export function getPrioritySchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** A set of priority IDs to filter by. To include multiple IDs, provide an ampersand-separated list. For example, `priorityId=10000&priorityId=10001`. */
    priorityId?: number[];
    /** A set of priority scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `schemeId=10000&schemeId=10001`. */
    schemeId?: number[];
    /**
     * The name of scheme to search for.
     *
     * @default ""
     */
    schemeName?: string;
    /**
     * Whether only the default priority is returned.
     *
     * @default false
     */
    onlyDefault?: boolean;
    /**
     * The ordering to return the priority schemes by.
     *
     * @default "+name"
     */
    orderBy?: "name" | "+name" | "-name";
    /** A comma separated list of additional information to return. "priorities" will return priorities associated with the priority scheme. "projects" will return projects associated with the priority scheme. `expand=priorities,projects`. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects> {
  const { startAt, maxResults, priorityId, schemeId, schemeName, onlyDefault, orderBy, expand } =
    params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priorityscheme",
    query: { startAt, maxResults, priorityId, schemeId, schemeName, onlyDefault, orderBy, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create priority scheme
 *
 * Creates a new priority scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is completed.
 */
export function createPriorityScheme(
  ctx: ClientContext,
  params: CreatePrioritySchemeDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PrioritySchemeId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/priorityscheme",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Suggested priorities for mappings
 *
 * Returns a [paginated](#pagination) list of priorities that would require mapping, given a change in priorities or projects associated with a priority scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function suggestedPrioritiesForMappings(
  ctx: ClientContext,
  params: SuggestedMappingsRequestBean,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanPriorityWithSequence> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/priorityscheme/mappings",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get available priorities by priority scheme
 *
 * Returns a [paginated](#pagination) list of priorities available for adding to a priority scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.query - The string to query priorities on by name.
 * @param params.schemeId - The priority scheme ID.
 * @param params.exclude - A list of priority IDs to exclude from the results.
 * @returns Returned if the request is successful.
 */
export function getAvailablePrioritiesByPriorityScheme(
  ctx: ClientContext,
  params: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /**
     * The string to query priorities on by name.
     *
     * @default ""
     */
    query?: string;
    /** The priority scheme ID. */
    schemeId: string;
    /** A list of priority IDs to exclude from the results. */
    exclude?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanPriorityWithSequence> {
  const { startAt, maxResults, query, schemeId, exclude } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priorityscheme/priorities/available",
    query: { startAt, maxResults, query, schemeId, exclude },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update priority scheme
 *
 * Updates a priority scheme. This includes its details, the lists of priorities and projects in it
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the priority scheme.
 * @returns Returned if the request is accepted.
 */
export function updatePriorityScheme(
  ctx: ClientContext,
  params: UpdatePrioritySchemeRequestBean & {
    /**
     * The ID of the priority scheme.
     *
     * @format int64
     */
    schemeId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<UpdatePrioritySchemeResponseBean> {
  const { schemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/priorityscheme/{schemeId}",
    pathParams: { schemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete priority scheme
 *
 * Deletes a priority scheme.
 *
 * This operation is only available for priority schemes without any associated projects. Any associated projects must be removed from the priority scheme before this operation can be performed.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The priority scheme ID.
 * @returns Returned if the request is successful.
 */
export function deletePriorityScheme(
  ctx: ClientContext,
  params: {
    /**
     * The priority scheme ID.
     *
     * @format int64
     */
    schemeId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/priorityscheme/{schemeId}",
    pathParams: { schemeId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get priorities by priority scheme
 *
 * Returns a [paginated](#pagination) list of priorities by scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The priority scheme ID.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @returns Returned if the request is successful.
 */
export function getPrioritiesByPriorityScheme(
  ctx: ClientContext,
  params: {
    /** The priority scheme ID. */
    schemeId: string;
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanPriorityWithSequence> {
  const { schemeId, startAt, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priorityscheme/{schemeId}/priorities",
    pathParams: { schemeId },
    query: { startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get projects by priority scheme
 *
 * Returns a [paginated](#pagination) list of projects by scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The priority scheme ID.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.projectId - The project IDs to filter by. For example, `projectId=10000&projectId=10001`.
 * @param params.query - The string to query projects on by name.
 * @returns Returned if the request is successful.
 */
export function getProjectsByPriorityScheme(
  ctx: ClientContext,
  params: {
    /** The priority scheme ID. */
    schemeId: string;
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The project IDs to filter by. For example, `projectId=10000&projectId=10001`. */
    projectId?: number[];
    /**
     * The string to query projects on by name.
     *
     * @default ""
     */
    query?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanProject> {
  const { schemeId, startAt, maxResults, projectId, query } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priorityscheme/{schemeId}/projects",
    pathParams: { schemeId },
    query: { startAt, maxResults, projectId, query },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents issue priority schemes. Use it to get priority schemes and related information, and to create, update and delete priority schemes. */
export function createPrioritySchemesService(ctx: ClientContext) {
  return {
    /**
     * Get priority schemes
     *
     * Returns a [paginated](#pagination) list of priority schemes.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.priorityId - A set of priority IDs to filter by. To include multiple IDs, provide an ampersand-separated list. For example, `priorityId=10000&priorityId=10001`.
     * @param params.schemeId - A set of priority scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `schemeId=10000&schemeId=10001`.
     * @param params.schemeName - The name of scheme to search for.
     * @param params.onlyDefault - Whether only the default priority is returned.
     * @param params.orderBy - The ordering to return the priority schemes by.
     * @param params.expand - A comma separated list of additional information to return. "priorities" will return priorities associated with the priority scheme. "projects" will return projects associated with the priority scheme. `expand=priorities,projects`.
     * @returns Returned if the request is successful.
     */
    getPrioritySchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** A set of priority IDs to filter by. To include multiple IDs, provide an ampersand-separated list. For example, `priorityId=10000&priorityId=10001`. */
        priorityId?: number[];
        /** A set of priority scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `schemeId=10000&schemeId=10001`. */
        schemeId?: number[];
        /**
         * The name of scheme to search for.
         *
         * @default ""
         */
        schemeName?: string;
        /**
         * Whether only the default priority is returned.
         *
         * @default false
         */
        onlyDefault?: boolean;
        /**
         * The ordering to return the priority schemes by.
         *
         * @default "+name"
         */
        orderBy?: "name" | "+name" | "-name";
        /** A comma separated list of additional information to return. "priorities" will return priorities associated with the priority scheme. "projects" will return projects associated with the priority scheme. `expand=priorities,projects`. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects> {
      return getPrioritySchemes(ctx, params, options);
    },

    /**
     * Create priority scheme
     *
     * Creates a new priority scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is completed.
     */
    createPriorityScheme(
      params: CreatePrioritySchemeDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PrioritySchemeId> {
      return createPriorityScheme(ctx, params, options);
    },

    /**
     * Suggested priorities for mappings
     *
     * Returns a [paginated](#pagination) list of priorities that would require mapping, given a change in priorities or projects associated with a priority scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    suggestedPrioritiesForMappings(
      params: SuggestedMappingsRequestBean,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanPriorityWithSequence> {
      return suggestedPrioritiesForMappings(ctx, params, options);
    },

    /**
     * Get available priorities by priority scheme
     *
     * Returns a [paginated](#pagination) list of priorities available for adding to a priority scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.query - The string to query priorities on by name.
     * @param params.schemeId - The priority scheme ID.
     * @param params.exclude - A list of priority IDs to exclude from the results.
     * @returns Returned if the request is successful.
     */
    getAvailablePrioritiesByPriorityScheme(
      params: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /**
         * The string to query priorities on by name.
         *
         * @default ""
         */
        query?: string;
        /** The priority scheme ID. */
        schemeId: string;
        /** A list of priority IDs to exclude from the results. */
        exclude?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanPriorityWithSequence> {
      return getAvailablePrioritiesByPriorityScheme(ctx, params, options);
    },

    /**
     * Update priority scheme
     *
     * Updates a priority scheme. This includes its details, the lists of priorities and projects in it
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the priority scheme.
     * @returns Returned if the request is accepted.
     */
    updatePriorityScheme(
      params: UpdatePrioritySchemeRequestBean & {
        /**
         * The ID of the priority scheme.
         *
         * @format int64
         */
        schemeId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<UpdatePrioritySchemeResponseBean> {
      return updatePriorityScheme(ctx, params, options);
    },

    /**
     * Delete priority scheme
     *
     * Deletes a priority scheme.
     *
     * This operation is only available for priority schemes without any associated projects. Any associated projects must be removed from the priority scheme before this operation can be performed.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The priority scheme ID.
     * @returns Returned if the request is successful.
     */
    deletePriorityScheme(
      params: {
        /**
         * The priority scheme ID.
         *
         * @format int64
         */
        schemeId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deletePriorityScheme(ctx, params, options);
    },

    /**
     * Get priorities by priority scheme
     *
     * Returns a [paginated](#pagination) list of priorities by scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The priority scheme ID.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @returns Returned if the request is successful.
     */
    getPrioritiesByPriorityScheme(
      params: {
        /** The priority scheme ID. */
        schemeId: string;
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanPriorityWithSequence> {
      return getPrioritiesByPriorityScheme(ctx, params, options);
    },

    /**
     * Get projects by priority scheme
     *
     * Returns a [paginated](#pagination) list of projects by scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The priority scheme ID.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.projectId - The project IDs to filter by. For example, `projectId=10000&projectId=10001`.
     * @param params.query - The string to query projects on by name.
     * @returns Returned if the request is successful.
     */
    getProjectsByPriorityScheme(
      params: {
        /** The priority scheme ID. */
        schemeId: string;
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The project IDs to filter by. For example, `projectId=10000&projectId=10001`. */
        projectId?: number[];
        /**
         * The string to query projects on by name.
         *
         * @default ""
         */
        query?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanProject> {
      return getProjectsByPriorityScheme(ctx, params, options);
    },
  };
}
