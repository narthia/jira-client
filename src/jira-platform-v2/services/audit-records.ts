// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { AuditRecords } from "../types/index.ts";

/**
 * Get audit records
 *
 * Returns a list of audit records. The list can be filtered to include items:
 *
 *  *  where each item in `filter` has at least one match in any of these fields:
 *
 *      *  `summary`
 *      *  `category`
 *      *  `eventSource`
 *      *  `objectItem.name` If the object is a user, account ID is available to filter.
 *      *  `objectItem.parentName`
 *      *  `objectItem.typeName`
 *      *  `changedValues.changedFrom`
 *      *  `changedValues.changedTo`
 *      *  `remoteAddress`
 *
 *     For example, if `filter` contains *man ed*, an audit record containing `summary": "User added to group"` and `"category": "group management"` is returned.
 *  *  created on or after a date and time.
 *  *  created or or before a date and time.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.offset - The number of records to skip before returning the first result.
 * @param params.limit - The maximum number of results to return.
 * @param params.filter - The strings to match with audit field content, space separated.
 * @param params.from - The date and time on or after which returned audit records must have been created. If `to` is provided `from` must be before `to` or no audit records are returned.
 * @param params.to - The date and time on or before which returned audit results must have been created. If `from` is provided `to` must be after `from` or no audit records are returned.
 * @returns Returned if the request is successful.
 */
export function getAuditRecords(
  ctx: ClientContext,
  params?: {
    /**
     * The number of records to skip before returning the first result.
     *
     * @default 0
     * @format int32
     */
    offset?: number;
    /**
     * The maximum number of results to return.
     *
     * @default 1000
     * @format int32
     */
    limit?: number;
    /** The strings to match with audit field content, space separated. */
    filter?: string;
    /** The date and time on or after which returned audit records must have been created. If `to` is provided `from` must be before `to` or no audit records are returned. */
    from?: string;
    /** The date and time on or before which returned audit results must have been created. If `from` is provided `to` must be after `from` or no audit records are returned. */
    to?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<AuditRecords> {
  const { offset, limit, filter, from, to } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/auditing/record",
    query: { offset, limit, filter, from, to },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents audits that record activities undertaken in Jira. Use it to get a list of audit records. */
export function createAuditRecordsService(ctx: ClientContext) {
  return {
    /**
     * Get audit records
     *
     * Returns a list of audit records. The list can be filtered to include items:
     *
     *  *  where each item in `filter` has at least one match in any of these fields:
     *
     *      *  `summary`
     *      *  `category`
     *      *  `eventSource`
     *      *  `objectItem.name` If the object is a user, account ID is available to filter.
     *      *  `objectItem.parentName`
     *      *  `objectItem.typeName`
     *      *  `changedValues.changedFrom`
     *      *  `changedValues.changedTo`
     *      *  `remoteAddress`
     *
     *     For example, if `filter` contains *man ed*, an audit record containing `summary": "User added to group"` and `"category": "group management"` is returned.
     *  *  created on or after a date and time.
     *  *  created or or before a date and time.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.offset - The number of records to skip before returning the first result.
     * @param params.limit - The maximum number of results to return.
     * @param params.filter - The strings to match with audit field content, space separated.
     * @param params.from - The date and time on or after which returned audit records must have been created. If `to` is provided `from` must be before `to` or no audit records are returned.
     * @param params.to - The date and time on or before which returned audit results must have been created. If `from` is provided `to` must be after `from` or no audit records are returned.
     * @returns Returned if the request is successful.
     */
    getAuditRecords(
      params?: {
        /**
         * The number of records to skip before returning the first result.
         *
         * @default 0
         * @format int32
         */
        offset?: number;
        /**
         * The maximum number of results to return.
         *
         * @default 1000
         * @format int32
         */
        limit?: number;
        /** The strings to match with audit field content, space separated. */
        filter?: string;
        /** The date and time on or after which returned audit records must have been created. If `to` is provided `from` must be before `to` or no audit records are returned. */
        from?: string;
        /** The date and time on or before which returned audit results must have been created. If `from` is provided `to` must be after `from` or no audit records are returned. */
        to?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<AuditRecords> {
      return getAuditRecords(ctx, params, options);
    },
  };
}
