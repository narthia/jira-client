// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { MoveIssuesToBacklogForBoardBody } from "../types/index.ts";

/**
 * Rank issues
 *
 * Moves (ranks) issues before or after a given issue. At most 50 issues may be ranked at once.
 *
 * This operation may fail for some issues, although this will be rare. In that case the 207 status code is returned for the whole response and detailed information regarding each issue is available in the response body.
 *
 * If rankCustomFieldId is not defined, the default rank field will be used.
 *
 * @returns Empty response is returned if operation was successful.
 */
export function rankIssues(
  ctx: ClientContext,
  params: MoveIssuesToBacklogForBoardBody,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/issue/rank",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue
 *
 * Returns a single issue, for a given issue ID or issue key. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic.
 *
 * @param params.issueIdOrKey - The ID or key of the requested issue.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @param params.updateHistory - A boolean indicating whether the issue retrieved by this method should be added to the current user's issue history
 * @returns Returns the requested issue.
 */
export function getIssue(
  ctx: ClientContext,
  params: {
    /** The ID or key of the requested issue. */
    issueIdOrKey: string;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
    /** A boolean indicating whether the issue retrieved by this method should be added to the current user's issue history */
    updateHistory?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, fields, expand, updateHistory } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/issue/{issueIdOrKey}",
    pathParams: { issueIdOrKey },
    query: { fields, expand, updateHistory },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue estimation for board
 *
 * Returns the estimation of the issue and a fieldId of the field that is used for it. `boardId` param is required. This param determines which field will be updated on a issue.
 *
 * Original time internally stores and returns the estimation as a number of seconds.
 *
 * The field used for estimation on the given board can be obtained from [board configuration resource](#agile/1.0/board-getConfiguration). More information about the field are returned by [edit meta resource](#api-rest-api-3-issue-getEditIssueMeta) or [field resource](#api-rest-api-3-field-get).
 *
 * @param params.issueIdOrKey - The ID or key of the requested issue.
 * @param params.boardId - The ID of the board required to determine which field is used for estimation.
 * @returns Returns the estimation of the issue and a fieldId of the field that is used for it.
 */
export function getIssueEstimationForBoard(
  ctx: ClientContext,
  params: {
    /** The ID or key of the requested issue. */
    issueIdOrKey: string;
    /**
     * The ID of the board required to determine which field is used for estimation.
     *
     * @format int64
     */
    boardId?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/issue/{issueIdOrKey}/estimation",
    pathParams: { issueIdOrKey },
    query: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Estimate issue for board
 *
 * Updates the estimation of the issue. boardId param is required. This param determines which field will be updated on a issue.
 *
 * Note that this resource changes the estimation field of the issue regardless of appearance the field on the screen.
 *
 * Original time tracking estimation field accepts estimation in formats like "1w", "2d", "3h", "20m" or number which represent number of minutes. However, internally the field stores and returns the estimation as a number of seconds.
 *
 * The field used for estimation on the given board can be obtained from [board configuration resource](#agile/1.0/board-getConfiguration). More information about the field are returned by [edit meta resource](#api-rest-api-3-issue-issueIdOrKey-editmeta-get) or [field resource](#api-rest-api-3-field-get).
 *
 * @param params.issueIdOrKey - The ID or key of the requested issue.
 * @param params.boardId - The ID of the board required to determine which field is used for estimation.
 * @returns Returns the estimation of the issue and a fieldId of the field that is used for it.
 */
export function estimateIssueForBoard(
  ctx: ClientContext,
  params: {
    value?: string;
  } & {
    /** The ID or key of the requested issue. */
    issueIdOrKey: string;
    /**
     * The ID of the board required to determine which field is used for estimation.
     *
     * @format int64
     */
    boardId?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, boardId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/issue/{issueIdOrKey}/estimation",
    pathParams: { issueIdOrKey },
    query: { boardId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Apis related to issues */
export function createIssueService(ctx: ClientContext) {
  return {
    /**
     * Rank issues
     *
     * Moves (ranks) issues before or after a given issue. At most 50 issues may be ranked at once.
     *
     * This operation may fail for some issues, although this will be rare. In that case the 207 status code is returned for the whole response and detailed information regarding each issue is available in the response body.
     *
     * If rankCustomFieldId is not defined, the default rank field will be used.
     *
     * @returns Empty response is returned if operation was successful.
     */
    rankIssues(
      params: MoveIssuesToBacklogForBoardBody,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return rankIssues(ctx, params, options);
    },

    /**
     * Get issue
     *
     * Returns a single issue, for a given issue ID or issue key. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic.
     *
     * @param params.issueIdOrKey - The ID or key of the requested issue.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @param params.updateHistory - A boolean indicating whether the issue retrieved by this method should be added to the current user's issue history
     * @returns Returns the requested issue.
     */
    getIssue(
      params: {
        /** The ID or key of the requested issue. */
        issueIdOrKey: string;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
        /** A boolean indicating whether the issue retrieved by this method should be added to the current user's issue history */
        updateHistory?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getIssue(ctx, params, options);
    },

    /**
     * Get issue estimation for board
     *
     * Returns the estimation of the issue and a fieldId of the field that is used for it. `boardId` param is required. This param determines which field will be updated on a issue.
     *
     * Original time internally stores and returns the estimation as a number of seconds.
     *
     * The field used for estimation on the given board can be obtained from [board configuration resource](#agile/1.0/board-getConfiguration). More information about the field are returned by [edit meta resource](#api-rest-api-3-issue-getEditIssueMeta) or [field resource](#api-rest-api-3-field-get).
     *
     * @param params.issueIdOrKey - The ID or key of the requested issue.
     * @param params.boardId - The ID of the board required to determine which field is used for estimation.
     * @returns Returns the estimation of the issue and a fieldId of the field that is used for it.
     */
    getIssueEstimationForBoard(
      params: {
        /** The ID or key of the requested issue. */
        issueIdOrKey: string;
        /**
         * The ID of the board required to determine which field is used for estimation.
         *
         * @format int64
         */
        boardId?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getIssueEstimationForBoard(ctx, params, options);
    },

    /**
     * Estimate issue for board
     *
     * Updates the estimation of the issue. boardId param is required. This param determines which field will be updated on a issue.
     *
     * Note that this resource changes the estimation field of the issue regardless of appearance the field on the screen.
     *
     * Original time tracking estimation field accepts estimation in formats like "1w", "2d", "3h", "20m" or number which represent number of minutes. However, internally the field stores and returns the estimation as a number of seconds.
     *
     * The field used for estimation on the given board can be obtained from [board configuration resource](#agile/1.0/board-getConfiguration). More information about the field are returned by [edit meta resource](#api-rest-api-3-issue-issueIdOrKey-editmeta-get) or [field resource](#api-rest-api-3-field-get).
     *
     * @param params.issueIdOrKey - The ID or key of the requested issue.
     * @param params.boardId - The ID of the board required to determine which field is used for estimation.
     * @returns Returns the estimation of the issue and a fieldId of the field that is used for it.
     */
    estimateIssueForBoard(
      params: {
        value?: string;
      } & {
        /** The ID or key of the requested issue. */
        issueIdOrKey: string;
        /**
         * The ID of the board required to determine which field is used for estimation.
         *
         * @format int64
         */
        boardId?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return estimateIssueForBoard(ctx, params, options);
    },
  };
}
