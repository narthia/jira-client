// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreateResolutionDetails,
  PageBeanResolutionJsonBean,
  ReorderIssueResolutionsRequest,
  Resolution,
  ResolutionId,
  SetDefaultResolutionRequest,
  UpdateResolutionDetails,
} from "../types/index.ts";

/**
 * Get resolutions
 *
 * Returns a list of all issue resolution values.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @returns Returned if the request is successful.
 * @deprecated
 */
export function getResolutions(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Resolution[]> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/resolution",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create resolution
 *
 * Creates an issue resolution.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createResolution(
  ctx: ClientContext,
  params: CreateResolutionDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ResolutionId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/resolution",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set default resolution
 *
 * Sets default issue resolution.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function setDefaultResolution(
  ctx: ClientContext,
  params: SetDefaultResolutionRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/resolution/default",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move resolutions
 *
 * Changes the order of issue resolutions.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function moveResolutions(
  ctx: ClientContext,
  params: ReorderIssueResolutionsRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/resolution/move",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Search resolutions
 *
 * Returns a [paginated](#pagination) list of resolutions. The list can contain all resolutions or a subset determined by any combination of these criteria:
 *
 *  *  a list of resolutions IDs.
 *  *  whether the field configuration is a default. This returns resolutions from company-managed (classic) projects only, as there is no concept of default resolutions in team-managed projects.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of resolutions IDs to be filtered out
 * @param params.onlyDefault - When set to true, return default only, when IDs provided, if none of them is default, return empty page. Default value is false
 * @returns Returned if the request is successful.
 */
export function searchResolutions(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of resolutions IDs to be filtered out */
    id?: string[];
    /**
     * When set to true, return default only, when IDs provided, if none of them is default, return empty page. Default value is false
     *
     * @default false
     */
    onlyDefault?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanResolutionJsonBean> {
  const { startAt, maxResults, id, onlyDefault } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/resolution/search",
    query: { startAt, maxResults, id, onlyDefault },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get resolution
 *
 * Returns an issue resolution value.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @param params.id - The ID of the issue resolution value.
 * @returns Returned if the request is successful.
 */
export function getResolution(
  ctx: ClientContext,
  params: {
    /** The ID of the issue resolution value. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Resolution> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/resolution/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update resolution
 *
 * Updates an issue resolution.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue resolution.
 * @returns Returned if the request is successful.
 */
export function updateResolution(
  ctx: ClientContext,
  params: UpdateResolutionDetails & {
    /** The ID of the issue resolution. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/resolution/{id}",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete resolution
 *
 * Deletes an issue resolution.
 *
 * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue resolution.
 * @param params.replaceWith - The ID of the issue resolution that will replace the currently selected resolution.
 */
export function deleteResolution(
  ctx: ClientContext,
  params: {
    /** The ID of the issue resolution. */
    id: string;
    /**
     * The ID of the issue resolution that will replace the currently selected resolution.
     *
     * @default ""
     */
    replaceWith: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, replaceWith } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/resolution/{id}",
    pathParams: { id },
    query: { replaceWith },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents issue resolution values. Use it to obtain a list of all issue resolution values and the details of individual resolution values. */
export function createIssueResolutionsService(ctx: ClientContext) {
  return {
    /**
     * Get resolutions
     *
     * Returns a list of all issue resolution values.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @returns Returned if the request is successful.
     * @deprecated
     */
    getResolutions(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<Resolution[]> {
      return getResolutions(ctx, options);
    },

    /**
     * Create resolution
     *
     * Creates an issue resolution.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createResolution(
      params: CreateResolutionDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ResolutionId> {
      return createResolution(ctx, params, options);
    },

    /**
     * Set default resolution
     *
     * Sets default issue resolution.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    setDefaultResolution(
      params: SetDefaultResolutionRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setDefaultResolution(ctx, params, options);
    },

    /**
     * Move resolutions
     *
     * Changes the order of issue resolutions.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    moveResolutions(
      params: ReorderIssueResolutionsRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return moveResolutions(ctx, params, options);
    },

    /**
     * Search resolutions
     *
     * Returns a [paginated](#pagination) list of resolutions. The list can contain all resolutions or a subset determined by any combination of these criteria:
     *
     *  *  a list of resolutions IDs.
     *  *  whether the field configuration is a default. This returns resolutions from company-managed (classic) projects only, as there is no concept of default resolutions in team-managed projects.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of resolutions IDs to be filtered out
     * @param params.onlyDefault - When set to true, return default only, when IDs provided, if none of them is default, return empty page. Default value is false
     * @returns Returned if the request is successful.
     */
    searchResolutions(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of resolutions IDs to be filtered out */
        id?: string[];
        /**
         * When set to true, return default only, when IDs provided, if none of them is default, return empty page. Default value is false
         *
         * @default false
         */
        onlyDefault?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanResolutionJsonBean> {
      return searchResolutions(ctx, params, options);
    },

    /**
     * Get resolution
     *
     * Returns an issue resolution value.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @param params.id - The ID of the issue resolution value.
     * @returns Returned if the request is successful.
     */
    getResolution(
      params: {
        /** The ID of the issue resolution value. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Resolution> {
      return getResolution(ctx, params, options);
    },

    /**
     * Update resolution
     *
     * Updates an issue resolution.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue resolution.
     * @returns Returned if the request is successful.
     */
    updateResolution(
      params: UpdateResolutionDetails & {
        /** The ID of the issue resolution. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateResolution(ctx, params, options);
    },

    /**
     * Delete resolution
     *
     * Deletes an issue resolution.
     *
     * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue resolution.
     * @param params.replaceWith - The ID of the issue resolution that will replace the currently selected resolution.
     */
    deleteResolution(
      params: {
        /** The ID of the issue resolution. */
        id: string;
        /**
         * The ID of the issue resolution that will replace the currently selected resolution.
         *
         * @default ""
         */
        replaceWith: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteResolution(ctx, params, options);
    },
  };
}
