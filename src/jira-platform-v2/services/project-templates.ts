// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  EditTemplateRequest,
  ProjectCustomTemplateCreateRequestDto,
  ProjectTemplateModel,
  SaveTemplateRequest,
  SaveTemplateResponse,
} from "../types/index.ts";

/**
 * Create custom project
 *
 * Creates a project based on a custom template provided in the request.
 *
 * The request body should contain the project details and the capabilities that comprise the project:
 *
 *  *  `details` \- represents the project details settings
 *  *  `template` \- represents a list of capabilities responsible for creating specific parts of a project
 *
 * A capability is defined as a unit of configuration for the project you want to create.
 *
 * This operation is:
 *
 *  *  [asynchronous](#async). Follow the `Location` link in the response header to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
 *
 * ***Note: This API is only supported for Jira Enterprise edition.***
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 */
export function createProjectWithCustomTemplate(
  ctx: ClientContext,
  params: ProjectCustomTemplateCreateRequestDto,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/project-template",
    headers: options?.headers,
    body: params,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Edit a custom project template
 *
 * Edit custom template
 *
 * This API endpoint allows you to edit an existing customised template.
 *
 * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
 *
 * @returns 200 response
 */
export function editTemplate(
  ctx: ClientContext,
  params: EditTemplateRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/project-template/edit-template",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Gets a custom project template
 *
 * Get custom template
 *
 * This API endpoint allows you to get a live custom project template details by either templateKey or projectId
 *
 * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
 *
 * @param params.projectId - optional - The \{@link String\} containing the project key linked to the custom template to retrieve
 * @param params.templateKey - optional - The \{@link String\} containing the key of the custom template to retrieve
 * @returns 200 response
 */
export function liveTemplate(
  ctx: ClientContext,
  params?: {
    /** optional - The \{@link String\} containing the project key linked to the custom template to retrieve */
    projectId?: string;
    /** optional - The \{@link String\} containing the key of the custom template to retrieve */
    templateKey?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectTemplateModel> {
  const { projectId, templateKey } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/project-template/live-template",
    query: { projectId, templateKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Deletes a custom project template
 *
 * Remove custom template
 *
 * This API endpoint allows you to remove a specified customised template
 *
 * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
 *
 * @param params.templateKey - The \{@link String\} containing the key of the custom template to remove
 * @returns 200 response
 */
export function removeTemplate(
  ctx: ClientContext,
  params: {
    /** The \{@link String\} containing the key of the custom template to remove */
    templateKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { templateKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/project-template/remove-template",
    query: { templateKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Save a custom project template
 *
 * Save custom template
 *
 * This API endpoint allows you to save a customised template
 *
 * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
 *
 * @returns 200 response
 */
export function saveTemplate(
  ctx: ClientContext,
  params: SaveTemplateRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SaveTemplateResponse> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/project-template/save-template",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents project templates. Use it to create a new project from a custom template. */
export function createProjectTemplatesService(ctx: ClientContext) {
  return {
    /**
     * Create custom project
     *
     * Creates a project based on a custom template provided in the request.
     *
     * The request body should contain the project details and the capabilities that comprise the project:
     *
     *  *  `details` \- represents the project details settings
     *  *  `template` \- represents a list of capabilities responsible for creating specific parts of a project
     *
     * A capability is defined as a unit of configuration for the project you want to create.
     *
     * This operation is:
     *
     *  *  [asynchronous](#async). Follow the `Location` link in the response header to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
     *
     * ***Note: This API is only supported for Jira Enterprise edition.***
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     */
    createProjectWithCustomTemplate(
      params: ProjectCustomTemplateCreateRequestDto,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return createProjectWithCustomTemplate(ctx, params, options);
    },

    /**
     * Edit a custom project template
     *
     * Edit custom template
     *
     * This API endpoint allows you to edit an existing customised template.
     *
     * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
     *
     * @returns 200 response
     */
    editTemplate(
      params: EditTemplateRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return editTemplate(ctx, params, options);
    },

    /**
     * Gets a custom project template
     *
     * Get custom template
     *
     * This API endpoint allows you to get a live custom project template details by either templateKey or projectId
     *
     * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
     *
     * @param params.projectId - optional - The \{@link String\} containing the project key linked to the custom template to retrieve
     * @param params.templateKey - optional - The \{@link String\} containing the key of the custom template to retrieve
     * @returns 200 response
     */
    liveTemplate(
      params?: {
        /** optional - The \{@link String\} containing the project key linked to the custom template to retrieve */
        projectId?: string;
        /** optional - The \{@link String\} containing the key of the custom template to retrieve */
        templateKey?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectTemplateModel> {
      return liveTemplate(ctx, params, options);
    },

    /**
     * Deletes a custom project template
     *
     * Remove custom template
     *
     * This API endpoint allows you to remove a specified customised template
     *
     * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
     *
     * @param params.templateKey - The \{@link String\} containing the key of the custom template to remove
     * @returns 200 response
     */
    removeTemplate(
      params: {
        /** The \{@link String\} containing the key of the custom template to remove */
        templateKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return removeTemplate(ctx, params, options);
    },

    /**
     * Save a custom project template
     *
     * Save custom template
     *
     * This API endpoint allows you to save a customised template
     *
     * ***Note: Custom Templates are only supported for Jira Enterprise edition.***
     *
     * @returns 200 response
     */
    saveTemplate(
      params: SaveTemplateRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SaveTemplateResponse> {
      return saveTemplate(ctx, params, options);
    },
  };
}
