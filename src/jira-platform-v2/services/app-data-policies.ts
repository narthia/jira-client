// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { ProjectDataPolicies, WorkspaceDataPolicy } from "../types/index.ts";

/**
 * Get data policy for the workspace
 *
 * Returns data policy for the workspace.
 *
 * @returns Returned if the request is successful
 */
export function getPolicy(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkspaceDataPolicy> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/data-policy",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get data policy for projects
 *
 * Returns data policies for the projects specified in the request.
 *
 * @param params.ids - A list of project identifiers. This parameter accepts a comma-separated list.
 * @returns Returned if the request is successful.
 */
export function getPolicies(
  ctx: ClientContext,
  params?: {
    /** A list of project identifiers. This parameter accepts a comma-separated list. */
    ids?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectDataPolicies> {
  const { ids } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/data-policy/project",
    query: { ids },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents app access rule data policies. */
export function createAppDataPoliciesService(ctx: ClientContext) {
  return {
    /**
     * Get data policy for the workspace
     *
     * Returns data policy for the workspace.
     *
     * @returns Returned if the request is successful
     */
    getPolicy(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<WorkspaceDataPolicy> {
      return getPolicy(ctx, options);
    },

    /**
     * Get data policy for projects
     *
     * Returns data policies for the projects specified in the request.
     *
     * @param params.ids - A list of project identifiers. This parameter accepts a comma-separated list.
     * @returns Returned if the request is successful.
     */
    getPolicies(
      params?: {
        /** A list of project identifiers. This parameter accepts a comma-separated list. */
        ids?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectDataPolicies> {
      return getPolicies(ctx, params, options);
    },
  };
}
