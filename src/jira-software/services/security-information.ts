// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";

/**
 * Submit Security Workspaces to link
 *
 * Insert Security Workspace IDs to establish a relationship between them and the Jira site the app is installed on. If a relationship between the workspace ID and Jira already exists then the workspace ID will be ignored and Jira will process the rest of the entries.
 *
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns Submission accepted. Each submitted Security Workspace ID will be linked to Jira.
 */
export function submitWorkspaces(
  ctx: ClientContext,
  params: {
    /**
     * The IDs of Security Workspaces to link to this Jira site. These must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(\/[a-zA-Z0-9\\-_.~@:{}=]+)*`
     *
     * @example
     * ```json
     * [
     *   "111-222-333",
     *   "444-555-666"
     * ]
     * ```
     */
    workspaceIds: string[];
  },
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "post",
    path: "/rest/security/1.0/linkedWorkspaces/bulk",
    headers: options?.headers,
    body: params,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete linked Security Workspaces
 *
 * Bulk delete all linked Security Workspaces that match the given request.
 *
 * e.g. DELETE /bulk?workspaceIds=111-222-333,444-555-666
 *
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns Delete accepted. Workspaces and related data will eventually be removed from Jira.
 */
export function deleteLinkedWorkspaces(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "delete",
    path: "/rest/security/1.0/linkedWorkspaces/bulk",
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get linked Security Workspaces
 *
 * Retrieve all Security Workspaces linked with the Jira site.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns A list of all stored workspace IDs.
 */
export function getLinkedWorkspaces(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Security Workspaces that are linked to this Jira site.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  workspaceIds: string[];
}> {
  return ctx.request({
    method: "get",
    path: "/rest/security/1.0/linkedWorkspaces",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a linked Security Workspace by ID
 *
 * Retrieve a specific Security Workspace linked to the Jira site for the given workspace ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param params.workspaceId - The ID of the workspace to fetch.
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns The Security Workspace information stored for the given ID.
 */
export function getLinkedWorkspaceById(
  ctx: ClientContext,
  params: {
    /** The ID of the workspace to fetch. */
    workspaceId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The Security Workspace ID
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  workspaceId: string;
  /**
   * Latest date and time that the Security Workspace was updated in Jira.
   *
   * @format date-time
   * @example
   * ```json
   * "2020-01-17T09:30:00.000Z"
   * ```
   */
  updatedAt: string;
}> {
  const { workspaceId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/security/1.0/linkedWorkspaces/{workspaceId}",
    pathParams: { workspaceId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Submit Vulnerability data
 *
 * Update / Insert Vulnerability data.
 *
 * Vulnerabilities are identified by their ID, any existing Vulnerability data with the same ID will be replaced if it exists and the updateSequenceNumber of the existing data is less than the incoming data.
 *
 * Submissions are performed asynchronously. Most updates are available within a short period of time but may take some time during peak load and/or maintenance times. The GET vulnerability endpoint can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple Vulnerabilities being submitted in one request, each is validated individually prior to submission. Details of Vulnerabilities that failed submission (if any) are available in the response object.
 *
 * A maximum of 1000 vulnerabilities can be submitted in one request.
 *
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns Submission accepted. Each Vulnerability submitted in a valid format will eventually be available in Jira. Details of any Vulnerabilities that were submitted but failed submission (due to data format problems, etc.) are available in the response object.
 */
export function submitVulnerabilities(
  ctx: ClientContext,
  params: {
    /**
     * Indicates the operation being performed by the provider system when sending this data. "NORMAL" - Data received during real-time, user-triggered actions (e.g. user closed or updated a vulnerability). "SCAN" - Data sent through some automated process (e.g. some periodically scheduled repository scan). "BACKFILL" - Data received while backfilling existing data (e.g. pushing historical vulnerabilities when re-connect a workspace). Default is "NORMAL". "NORMAL" traffic has higher priority but tighter rate limits, "SCAN" traffic has medium priority and looser limits, "BACKFILL" has lower priority and much looser limits
     *
     * @example
     * ```json
     * "SCAN"
     * ```
     */
    operationType?: "NORMAL" | "SCAN" | "BACKFILL";
    /**
     * Properties assigned to vulnerability data that can then be used for delete / query operations.
     *
     * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
     *
     * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
     *
     * @example
     * ```json
     * {
     *   "accountId": "account-234",
     *   "projectId": "project-123"
     * }
     * ```
     */
    properties?: Record<string, string>;
    vulnerabilities: {
      /**
       * The VulnerabilityData schema version used for this vulnerability data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the Vulnerability. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Vulnerability in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Vulnerability and increment that on each update to Jira).
       *
       * Updates for a Vulnerability that are received with an updateSequenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The identifier of the Container where this Vulnerability was found. Must be unique for a given Provider. This must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(/[a-zA-Z0-9\\-_.~@:{}=]+)*`
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      containerId: string;
      /**
       * The human-readable name for the Vulnerability. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "curl/libcurl3 - Buffer Override"
       * ```
       */
      displayName: string;
      /**
       * A description of the issue in markdown format that will be shown in the UI and used when creating Jira Issues. HTML tags are not supported in the markdown format. For creating a new line `\n` can be used. Read more about the accepted markdown transformations [here](https://atlaskit.atlassian.com/packages/editor/editor-markdown-transformer).
       *
       * @example
       * ```json
       * "## Overview\n\n\nAffected versions of this package are vulnerable to MeltLeak"
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this vulnerability, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the vulnerability in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/CWE-123/summary"
       * ```
       */
      url: string;
      /**
       * The type of Vulnerability detected.
       *
       * @example
       * ```json
       * "sca"
       * ```
       */
      type: "sca" | "sast" | "dast" | "unknown";
      /**
       * The timestamp to present to the user that shows when the Vulnerability was introduced.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      introducedDate: string;
      /**
       * The last-updated timestamp to present to the user the last time the Vulnerability was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * Severity information for a single Vulnerability.
       *
       * This is the severity information that will be presented to the user on e.g. the Jira Security screen.
       */
      severity: {
        /**
         * The severity level of the Vulnerability.
         *
         * @example
         * ```json
         * "critical"
         * ```
         */
        level: "critical" | "high" | "medium" | "low" | "unknown";
      };
      /** The identifying information for the Vulnerability. */
      identifiers?: {
        /**
         * The display name of the Vulnerability identified.
         *
         * @example
         * ```json
         * "CWE-123"
         * ```
         */
        displayName: string;
        /**
         * A URL users can use to link to the definition of the Vulnerability identified.
         *
         * @format uri
         * @example
         * ```json
         * "https://cwe.mitre.org/data/definitions/123.html"
         * ```
         */
        url: string;
      }[];
      /**
       * The current status of the Vulnerability.
       *
       * @example
       * ```json
       * "open"
       * ```
       */
      status: "open" | "closed" | "ignored" | "unknown";
      /** Extra information (optional). This data will be shown in the security feature under the vulnerability displayName. */
      additionalInfo?: {
        /**
         * The content of the additionalInfo.
         *
         * @example
         * ```json
         * "More information on the vulnerability, as a string"
         * ```
         */
        content: string;
        /**
         * Optional URL linking to the information
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/CWE-123/additionalInfo"
         * ```
         */
        url?: string;
      };
      /** The associations (e.g. Jira issue) to add in addition to the currently stored associations of the Security Vulnerability. */
      addAssociations?: {
        /**
         * Defines the association type.
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType: "issueIdOrKeys";
        /**
         * The Jira issue id or keys to associate the Security information with.
         *
         * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
         */
        values: string[];
      }[];
      /** The associations (e.g. Jira issue) to remove from currently stored associations of the Security Vulnerability. */
      removeAssociations?: {
        /**
         * Defines the association type.
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType: "issueIdOrKeys";
        /**
         * The Jira issue id or keys to associate the Security information with.
         *
         * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
         */
        values: string[];
      }[];
      /**
       * An ISO-8601 Date-time string representing the last time the provider updated associations on this entity.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      associationsLastUpdated?: string;
      /**
       * A sequence number to compare when writing entity associations to the database.
       *
       * This can be any monotonically increasing number. A highly recommended implementation is to use epoch millis.
       *
       * This is an optional field. If it is not provided it will default to being equal to the corresponding entity's `updateSequenceNumber`.
       *
       * Associations are written following a LastWriteWins strategy, association that are received with an associationsUpdateSequenceNumber lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      associationsUpdateSequenceNumber?: number;
    }[];
    /**
     * Information about the provider. This is useful for auditing, logging, debugging,
     * and other internal uses. Information in this property is not considered private, so it should not contain personally identifiable information
     */
    providerMetadata?: {
      /**
       * An optional name of the source of the vulnerabilities.
       *
       * @example
       * ```json
       * "Atlassian Security Platform 2.1.0"
       * ```
       */
      product?: string;
    };
  },
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Vulnerabilities that have been accepted for submission.
   *
   * A Vulnerability may be rejected if it was only associated with unknown project keys.
   *
   * Note that a Vulnerability that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedVulnerabilities?: string[];
  /**
   * Details of Vulnerabilities that have not been accepted for submission, usually due to a problem with the request data.
   *
   * The object (if present) will be keyed by Vulnerability ID and include any errors associated with that Vulnerability that have prevented it being submitted.
   */
  failedVulnerabilities?: Record<
    string,
    {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[]
  >;
  /**
   * Associations (e.g. Service IDs) that are not known on this Jira instance (if any).
   *
   * If a Vulnerability has been associated with any other association other than those in this array it will still be stored against those valid associations.
   * If a Vulnerability was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
   */
  unknownAssociations?: {
    /**
     * Defines the association type.
     *
     * @example
     * ```json
     * "issueIdOrKeys"
     * ```
     */
    associationType: "issueIdOrKeys";
    /**
     * The Jira issue id or keys to associate the Security information with.
     *
     * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
     */
    values: string[];
  }[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/security/1.0/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete Vulnerabilities by Property
 *
 * Bulk delete all Vulnerabilities that match the given request.
 *
 * One or more query params must be supplied to specify Properties to delete by.
 * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
 * Read the POST bulk endpoint documentation for more details.
 *
 * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
 *
 * Deletion is performed asynchronously. The GET vulnerability endpoint can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteVulnerabilitiesByProperty(
  ctx: ClientContext,
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "delete",
    path: "/rest/security/1.0/bulkByProperties",
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Vulnerability by ID
 *
 * Retrieve the currently stored Vulnerability data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param params.vulnerabilityId - The ID of the Vulnerability to fetch.
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns The Vulnerability data currently stored for the given ID.
 */
export function getVulnerabilityById(
  ctx: ClientContext,
  params: {
    /** The ID of the Vulnerability to fetch. */
    vulnerabilityId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The VulnerabilityData schema version used for this vulnerability data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion: "1.0";
  /**
   * The identifier for the Vulnerability. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * An ID used to apply an ordering to updates for this Vulnerability in the case of out-of-order receipt of update requests.
   *
   * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Vulnerability and increment that on each update to Jira).
   *
   * Updates for a Vulnerability that are received with an updateSequenceId lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceNumber: number;
  /**
   * The identifier of the Container where this Vulnerability was found. Must be unique for a given Provider. This must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(/[a-zA-Z0-9\\-_.~@:{}=]+)*`
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  containerId: string;
  /**
   * The human-readable name for the Vulnerability. Will be shown in the UI.
   *
   * If not provided, will use the ID for display.
   *
   * @example
   * ```json
   * "curl/libcurl3 - Buffer Override"
   * ```
   */
  displayName: string;
  /**
   * A description of the issue in markdown format that will be shown in the UI and used when creating Jira Issues. HTML tags are not supported in the markdown format. For creating a new line `\n` can be used. Read more about the accepted markdown transformations [here](https://atlaskit.atlassian.com/packages/editor/editor-markdown-transformer).
   *
   * @example
   * ```json
   * "## Overview\n\n\nAffected versions of this package are vulnerable to MeltLeak"
   * ```
   */
  description: string;
  /**
   * A URL users can use to link to a summary view of this vulnerability, if appropriate.
   *
   * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the vulnerability in that project).
   *
   * @format uri
   * @example
   * ```json
   * "https://example.com/project/CWE-123/summary"
   * ```
   */
  url: string;
  /**
   * The type of Vulnerability detected.
   *
   * @example
   * ```json
   * "sca"
   * ```
   */
  type: "sca" | "sast" | "dast" | "unknown";
  /**
   * The timestamp to present to the user that shows when the Vulnerability was introduced.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  introducedDate: string;
  /**
   * The last-updated timestamp to present to the user the last time the Vulnerability was updated.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
  /**
   * Severity information for a single Vulnerability.
   *
   * This is the severity information that will be presented to the user on e.g. the Jira Security screen.
   */
  severity: {
    /**
     * The severity level of the Vulnerability.
     *
     * @example
     * ```json
     * "critical"
     * ```
     */
    level: "critical" | "high" | "medium" | "low" | "unknown";
  };
  /** The identifying information for the Vulnerability. */
  identifiers?: {
    /**
     * The display name of the Vulnerability identified.
     *
     * @example
     * ```json
     * "CWE-123"
     * ```
     */
    displayName: string;
    /**
     * A URL users can use to link to the definition of the Vulnerability identified.
     *
     * @format uri
     * @example
     * ```json
     * "https://cwe.mitre.org/data/definitions/123.html"
     * ```
     */
    url: string;
  }[];
  /**
   * The current status of the Vulnerability.
   *
   * @example
   * ```json
   * "open"
   * ```
   */
  status: "open" | "closed" | "ignored" | "unknown";
  /** Extra information (optional). This data will be shown in the security feature under the vulnerability displayName. */
  additionalInfo?: {
    /**
     * The content of the additionalInfo.
     *
     * @example
     * ```json
     * "More information on the vulnerability, as a string"
     * ```
     */
    content: string;
    /**
     * Optional URL linking to the information
     *
     * @format uri
     * @example
     * ```json
     * "https://example.com/project/CWE-123/additionalInfo"
     * ```
     */
    url?: string;
  };
  /** The associations (e.g. Jira issue) to add in addition to the currently stored associations of the Security Vulnerability. */
  addAssociations?: {
    /**
     * Defines the association type.
     *
     * @example
     * ```json
     * "issueIdOrKeys"
     * ```
     */
    associationType: "issueIdOrKeys";
    /**
     * The Jira issue id or keys to associate the Security information with.
     *
     * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
     */
    values: string[];
  }[];
  /** The associations (e.g. Jira issue) to remove from currently stored associations of the Security Vulnerability. */
  removeAssociations?: {
    /**
     * Defines the association type.
     *
     * @example
     * ```json
     * "issueIdOrKeys"
     * ```
     */
    associationType: "issueIdOrKeys";
    /**
     * The Jira issue id or keys to associate the Security information with.
     *
     * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
     */
    values: string[];
  }[];
  /**
   * An ISO-8601 Date-time string representing the last time the provider updated associations on this entity.
   *
   * Expected format is an RFC3339 formatted string.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  associationsLastUpdated?: string;
  /**
   * A sequence number to compare when writing entity associations to the database.
   *
   * This can be any monotonically increasing number. A highly recommended implementation is to use epoch millis.
   *
   * This is an optional field. If it is not provided it will default to being equal to the corresponding entity's `updateSequenceNumber`.
   *
   * Associations are written following a LastWriteWins strategy, association that are received with an associationsUpdateSequenceNumber lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  associationsUpdateSequenceNumber?: number;
}> {
  const { vulnerabilityId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/security/1.0/vulnerability/{vulnerabilityId}",
    pathParams: { vulnerabilityId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Vulnerability by ID
 *
 * Delete the Vulnerability data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The GET vulnerability endpoint can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param params.vulnerabilityId - The ID of the Vulnerability to delete.
 * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
 * @returns Delete has been accepted. If the data exists, it will eventually be removed from Jira.
 */
export function deleteVulnerabilityById(
  ctx: ClientContext,
  params: {
    /** The ID of the Vulnerability to delete. */
    vulnerabilityId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
       *
       * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
       *
       * Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { vulnerabilityId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/security/1.0/vulnerability/{vulnerabilityId}",
    pathParams: { vulnerabilityId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Send security information to Jira Software and enable your teams to turn unplanned vulnerabilities into planned and tracked work.
 *
 * Security is everyone's responsibility, and the security feature in Jira lets you triage and track relevant vulnerabilities as a team. Discuss and prioritise issues, minimise errors and duplication, and plan security work to complete in your sprints.
 *
 * APIs related to integrating Security information with Jira Software are available to Atlassian Connect apps. To use these APIs you must have a security info provider module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/security-information/.
 *
 * These APIs are available to Forge apps with the `devops:securityInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-security-info/.
 */
export function createSecurityInformationService(ctx: ClientContext) {
  return {
    /**
     * Submit Security Workspaces to link
     *
     * Insert Security Workspace IDs to establish a relationship between them and the Jira site the app is installed on. If a relationship between the workspace ID and Jira already exists then the workspace ID will be ignored and Jira will process the rest of the entries.
     *
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns Submission accepted. Each submitted Security Workspace ID will be linked to Jira.
     */
    submitWorkspaces(
      params: {
        /**
         * The IDs of Security Workspaces to link to this Jira site. These must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(\/[a-zA-Z0-9\\-_.~@:{}=]+)*`
         *
         * @example
         * ```json
         * [
         *   "111-222-333",
         *   "444-555-666"
         * ]
         * ```
         */
        workspaceIds: string[];
      },
      options?: {
        headers?: {
          /**
           * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
           *
           * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
           *
           * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return submitWorkspaces(ctx, params, options);
    },

    /**
     * Delete linked Security Workspaces
     *
     * Bulk delete all linked Security Workspaces that match the given request.
     *
     * e.g. DELETE /bulk?workspaceIds=111-222-333,444-555-666
     *
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns Delete accepted. Workspaces and related data will eventually be removed from Jira.
     */
    deleteLinkedWorkspaces(options?: {
      headers?: {
        /**
         * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
         *
         * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
         *
         * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<void> {
      return deleteLinkedWorkspaces(ctx, options);
    },

    /**
     * Get linked Security Workspaces
     *
     * Retrieve all Security Workspaces linked with the Jira site.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns A list of all stored workspace IDs.
     */
    getLinkedWorkspaces(options?: {
      headers?: {
        /**
         * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
         *
         * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
         *
         * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<{
      /**
       * The IDs of Security Workspaces that are linked to this Jira site.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      workspaceIds: string[];
    }> {
      return getLinkedWorkspaces(ctx, options);
    },

    /**
     * Get a linked Security Workspace by ID
     *
     * Retrieve a specific Security Workspace linked to the Jira site for the given workspace ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param params.workspaceId - The ID of the workspace to fetch.
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns The Security Workspace information stored for the given ID.
     */
    getLinkedWorkspaceById(
      params: {
        /** The ID of the workspace to fetch. */
        workspaceId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
           *
           * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
           *
           * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The Security Workspace ID
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      workspaceId: string;
      /**
       * Latest date and time that the Security Workspace was updated in Jira.
       *
       * @format date-time
       * @example
       * ```json
       * "2020-01-17T09:30:00.000Z"
       * ```
       */
      updatedAt: string;
    }> {
      return getLinkedWorkspaceById(ctx, params, options);
    },

    /**
     * Submit Vulnerability data
     *
     * Update / Insert Vulnerability data.
     *
     * Vulnerabilities are identified by their ID, any existing Vulnerability data with the same ID will be replaced if it exists and the updateSequenceNumber of the existing data is less than the incoming data.
     *
     * Submissions are performed asynchronously. Most updates are available within a short period of time but may take some time during peak load and/or maintenance times. The GET vulnerability endpoint can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple Vulnerabilities being submitted in one request, each is validated individually prior to submission. Details of Vulnerabilities that failed submission (if any) are available in the response object.
     *
     * A maximum of 1000 vulnerabilities can be submitted in one request.
     *
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns Submission accepted. Each Vulnerability submitted in a valid format will eventually be available in Jira. Details of any Vulnerabilities that were submitted but failed submission (due to data format problems, etc.) are available in the response object.
     */
    submitVulnerabilities(
      params: {
        /**
         * Indicates the operation being performed by the provider system when sending this data. "NORMAL" - Data received during real-time, user-triggered actions (e.g. user closed or updated a vulnerability). "SCAN" - Data sent through some automated process (e.g. some periodically scheduled repository scan). "BACKFILL" - Data received while backfilling existing data (e.g. pushing historical vulnerabilities when re-connect a workspace). Default is "NORMAL". "NORMAL" traffic has higher priority but tighter rate limits, "SCAN" traffic has medium priority and looser limits, "BACKFILL" has lower priority and much looser limits
         *
         * @example
         * ```json
         * "SCAN"
         * ```
         */
        operationType?: "NORMAL" | "SCAN" | "BACKFILL";
        /**
         * Properties assigned to vulnerability data that can then be used for delete / query operations.
         *
         * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
         *
         * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
         *
         * @example
         * ```json
         * {
         *   "accountId": "account-234",
         *   "projectId": "project-123"
         * }
         * ```
         */
        properties?: Record<string, string>;
        vulnerabilities: {
          /**
           * The VulnerabilityData schema version used for this vulnerability data.
           *
           * Placeholder to support potential schema changes in the future.
           *
           * @default "1.0"
           * @example
           * ```json
           * "1.0"
           * ```
           */
          schemaVersion: "1.0";
          /**
           * The identifier for the Vulnerability. Must be unique for a given Provider.
           *
           * @example
           * ```json
           * "111-222-333"
           * ```
           */
          id: string;
          /**
           * An ID used to apply an ordering to updates for this Vulnerability in the case of out-of-order receipt of update requests.
           *
           * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Vulnerability and increment that on each update to Jira).
           *
           * Updates for a Vulnerability that are received with an updateSequenceId lower than what is currently stored will be ignored.
           *
           * @format int64
           * @example
           * ```json
           * 1523494301448
           * ```
           */
          updateSequenceNumber: number;
          /**
           * The identifier of the Container where this Vulnerability was found. Must be unique for a given Provider. This must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(/[a-zA-Z0-9\\-_.~@:{}=]+)*`
           *
           * @example
           * ```json
           * "111-222-333"
           * ```
           */
          containerId: string;
          /**
           * The human-readable name for the Vulnerability. Will be shown in the UI.
           *
           * If not provided, will use the ID for display.
           *
           * @example
           * ```json
           * "curl/libcurl3 - Buffer Override"
           * ```
           */
          displayName: string;
          /**
           * A description of the issue in markdown format that will be shown in the UI and used when creating Jira Issues. HTML tags are not supported in the markdown format. For creating a new line `\n` can be used. Read more about the accepted markdown transformations [here](https://atlaskit.atlassian.com/packages/editor/editor-markdown-transformer).
           *
           * @example
           * ```json
           * "## Overview\n\n\nAffected versions of this package are vulnerable to MeltLeak"
           * ```
           */
          description: string;
          /**
           * A URL users can use to link to a summary view of this vulnerability, if appropriate.
           *
           * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the vulnerability in that project).
           *
           * @format uri
           * @example
           * ```json
           * "https://example.com/project/CWE-123/summary"
           * ```
           */
          url: string;
          /**
           * The type of Vulnerability detected.
           *
           * @example
           * ```json
           * "sca"
           * ```
           */
          type: "sca" | "sast" | "dast" | "unknown";
          /**
           * The timestamp to present to the user that shows when the Vulnerability was introduced.
           *
           * Expected format is an RFC3339 formatted string.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          introducedDate: string;
          /**
           * The last-updated timestamp to present to the user the last time the Vulnerability was updated.
           *
           * Expected format is an RFC3339 formatted string.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          lastUpdated: string;
          /**
           * Severity information for a single Vulnerability.
           *
           * This is the severity information that will be presented to the user on e.g. the Jira Security screen.
           */
          severity: {
            /**
             * The severity level of the Vulnerability.
             *
             * @example
             * ```json
             * "critical"
             * ```
             */
            level: "critical" | "high" | "medium" | "low" | "unknown";
          };
          /** The identifying information for the Vulnerability. */
          identifiers?: {
            /**
             * The display name of the Vulnerability identified.
             *
             * @example
             * ```json
             * "CWE-123"
             * ```
             */
            displayName: string;
            /**
             * A URL users can use to link to the definition of the Vulnerability identified.
             *
             * @format uri
             * @example
             * ```json
             * "https://cwe.mitre.org/data/definitions/123.html"
             * ```
             */
            url: string;
          }[];
          /**
           * The current status of the Vulnerability.
           *
           * @example
           * ```json
           * "open"
           * ```
           */
          status: "open" | "closed" | "ignored" | "unknown";
          /** Extra information (optional). This data will be shown in the security feature under the vulnerability displayName. */
          additionalInfo?: {
            /**
             * The content of the additionalInfo.
             *
             * @example
             * ```json
             * "More information on the vulnerability, as a string"
             * ```
             */
            content: string;
            /**
             * Optional URL linking to the information
             *
             * @format uri
             * @example
             * ```json
             * "https://example.com/project/CWE-123/additionalInfo"
             * ```
             */
            url?: string;
          };
          /** The associations (e.g. Jira issue) to add in addition to the currently stored associations of the Security Vulnerability. */
          addAssociations?: {
            /**
             * Defines the association type.
             *
             * @example
             * ```json
             * "issueIdOrKeys"
             * ```
             */
            associationType: "issueIdOrKeys";
            /**
             * The Jira issue id or keys to associate the Security information with.
             *
             * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
             */
            values: string[];
          }[];
          /** The associations (e.g. Jira issue) to remove from currently stored associations of the Security Vulnerability. */
          removeAssociations?: {
            /**
             * Defines the association type.
             *
             * @example
             * ```json
             * "issueIdOrKeys"
             * ```
             */
            associationType: "issueIdOrKeys";
            /**
             * The Jira issue id or keys to associate the Security information with.
             *
             * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
             */
            values: string[];
          }[];
          /**
           * An ISO-8601 Date-time string representing the last time the provider updated associations on this entity.
           *
           * Expected format is an RFC3339 formatted string.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          associationsLastUpdated?: string;
          /**
           * A sequence number to compare when writing entity associations to the database.
           *
           * This can be any monotonically increasing number. A highly recommended implementation is to use epoch millis.
           *
           * This is an optional field. If it is not provided it will default to being equal to the corresponding entity's `updateSequenceNumber`.
           *
           * Associations are written following a LastWriteWins strategy, association that are received with an associationsUpdateSequenceNumber lower than what is currently stored will be ignored.
           *
           * @format int64
           * @example
           * ```json
           * 1523494301448
           * ```
           */
          associationsUpdateSequenceNumber?: number;
        }[];
        /**
         * Information about the provider. This is useful for auditing, logging, debugging,
         * and other internal uses. Information in this property is not considered private, so it should not contain personally identifiable information
         */
        providerMetadata?: {
          /**
           * An optional name of the source of the vulnerabilities.
           *
           * @example
           * ```json
           * "Atlassian Security Platform 2.1.0"
           * ```
           */
          product?: string;
        };
      },
      options?: {
        headers?: {
          /**
           * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
           *
           * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
           *
           * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Vulnerabilities that have been accepted for submission.
       *
       * A Vulnerability may be rejected if it was only associated with unknown project keys.
       *
       * Note that a Vulnerability that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedVulnerabilities?: string[];
      /**
       * Details of Vulnerabilities that have not been accepted for submission, usually due to a problem with the request data.
       *
       * The object (if present) will be keyed by Vulnerability ID and include any errors associated with that Vulnerability that have prevented it being submitted.
       */
      failedVulnerabilities?: Record<
        string,
        {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[]
      >;
      /**
       * Associations (e.g. Service IDs) that are not known on this Jira instance (if any).
       *
       * If a Vulnerability has been associated with any other association other than those in this array it will still be stored against those valid associations.
       * If a Vulnerability was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
       */
      unknownAssociations?: {
        /**
         * Defines the association type.
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType: "issueIdOrKeys";
        /**
         * The Jira issue id or keys to associate the Security information with.
         *
         * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
         */
        values: string[];
      }[];
    }> {
      return submitVulnerabilities(ctx, params, options);
    },

    /**
     * Delete Vulnerabilities by Property
     *
     * Bulk delete all Vulnerabilities that match the given request.
     *
     * One or more query params must be supplied to specify Properties to delete by.
     * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
     * Read the POST bulk endpoint documentation for more details.
     *
     * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
     *
     * Deletion is performed asynchronously. The GET vulnerability endpoint can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteVulnerabilitiesByProperty(options?: {
      headers?: {
        /**
         * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
         *
         * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
         *
         * Read [understanding jwt](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
         */
        Authorization: string;
      } & Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<void> {
      return deleteVulnerabilitiesByProperty(ctx, options);
    },

    /**
     * Get a Vulnerability by ID
     *
     * Retrieve the currently stored Vulnerability data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param params.vulnerabilityId - The ID of the Vulnerability to fetch.
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns The Vulnerability data currently stored for the given ID.
     */
    getVulnerabilityById(
      params: {
        /** The ID of the Vulnerability to fetch. */
        vulnerabilityId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
           *
           * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
           *
           * Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The VulnerabilityData schema version used for this vulnerability data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion: "1.0";
      /**
       * The identifier for the Vulnerability. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Vulnerability in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Vulnerability and increment that on each update to Jira).
       *
       * Updates for a Vulnerability that are received with an updateSequenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The identifier of the Container where this Vulnerability was found. Must be unique for a given Provider. This must follow this regex pattern: `[a-zA-Z0-9\\-_.~@:{}=]+(/[a-zA-Z0-9\\-_.~@:{}=]+)*`
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      containerId: string;
      /**
       * The human-readable name for the Vulnerability. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "curl/libcurl3 - Buffer Override"
       * ```
       */
      displayName: string;
      /**
       * A description of the issue in markdown format that will be shown in the UI and used when creating Jira Issues. HTML tags are not supported in the markdown format. For creating a new line `\n` can be used. Read more about the accepted markdown transformations [here](https://atlaskit.atlassian.com/packages/editor/editor-markdown-transformer).
       *
       * @example
       * ```json
       * "## Overview\n\n\nAffected versions of this package are vulnerable to MeltLeak"
       * ```
       */
      description: string;
      /**
       * A URL users can use to link to a summary view of this vulnerability, if appropriate.
       *
       * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific project, it might make sense to link the user to the vulnerability in that project).
       *
       * @format uri
       * @example
       * ```json
       * "https://example.com/project/CWE-123/summary"
       * ```
       */
      url: string;
      /**
       * The type of Vulnerability detected.
       *
       * @example
       * ```json
       * "sca"
       * ```
       */
      type: "sca" | "sast" | "dast" | "unknown";
      /**
       * The timestamp to present to the user that shows when the Vulnerability was introduced.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      introducedDate: string;
      /**
       * The last-updated timestamp to present to the user the last time the Vulnerability was updated.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * Severity information for a single Vulnerability.
       *
       * This is the severity information that will be presented to the user on e.g. the Jira Security screen.
       */
      severity: {
        /**
         * The severity level of the Vulnerability.
         *
         * @example
         * ```json
         * "critical"
         * ```
         */
        level: "critical" | "high" | "medium" | "low" | "unknown";
      };
      /** The identifying information for the Vulnerability. */
      identifiers?: {
        /**
         * The display name of the Vulnerability identified.
         *
         * @example
         * ```json
         * "CWE-123"
         * ```
         */
        displayName: string;
        /**
         * A URL users can use to link to the definition of the Vulnerability identified.
         *
         * @format uri
         * @example
         * ```json
         * "https://cwe.mitre.org/data/definitions/123.html"
         * ```
         */
        url: string;
      }[];
      /**
       * The current status of the Vulnerability.
       *
       * @example
       * ```json
       * "open"
       * ```
       */
      status: "open" | "closed" | "ignored" | "unknown";
      /** Extra information (optional). This data will be shown in the security feature under the vulnerability displayName. */
      additionalInfo?: {
        /**
         * The content of the additionalInfo.
         *
         * @example
         * ```json
         * "More information on the vulnerability, as a string"
         * ```
         */
        content: string;
        /**
         * Optional URL linking to the information
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/CWE-123/additionalInfo"
         * ```
         */
        url?: string;
      };
      /** The associations (e.g. Jira issue) to add in addition to the currently stored associations of the Security Vulnerability. */
      addAssociations?: {
        /**
         * Defines the association type.
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType: "issueIdOrKeys";
        /**
         * The Jira issue id or keys to associate the Security information with.
         *
         * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
         */
        values: string[];
      }[];
      /** The associations (e.g. Jira issue) to remove from currently stored associations of the Security Vulnerability. */
      removeAssociations?: {
        /**
         * Defines the association type.
         *
         * @example
         * ```json
         * "issueIdOrKeys"
         * ```
         */
        associationType: "issueIdOrKeys";
        /**
         * The Jira issue id or keys to associate the Security information with.
         *
         * The number of values counted across all associationTypes (issueIdOrKeys) must not exceed a limit of 500.
         */
        values: string[];
      }[];
      /**
       * An ISO-8601 Date-time string representing the last time the provider updated associations on this entity.
       *
       * Expected format is an RFC3339 formatted string.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      associationsLastUpdated?: string;
      /**
       * A sequence number to compare when writing entity associations to the database.
       *
       * This can be any monotonically increasing number. A highly recommended implementation is to use epoch millis.
       *
       * This is an optional field. If it is not provided it will default to being equal to the corresponding entity's `updateSequenceNumber`.
       *
       * Associations are written following a LastWriteWins strategy, association that are received with an associationsUpdateSequenceNumber lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      associationsUpdateSequenceNumber?: number;
    }> {
      return getVulnerabilityById(ctx, params, options);
    },

    /**
     * Delete a Vulnerability by ID
     *
     * Delete the Vulnerability data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The GET vulnerability endpoint can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param params.vulnerabilityId - The ID of the Vulnerability to delete.
     * @param options.headers.Authorization - All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira. If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403. Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
     * @returns Delete has been accepted. If the data exists, it will eventually be removed from Jira.
     */
    deleteVulnerabilityById(
      params: {
        /** The ID of the Vulnerability to delete. */
        vulnerabilityId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be authenticated as an app — either with a Connect JWT token for Connect apps, or with an OAuth 2.0 access token for Forge and OAuth 2.0 apps — that corresponds to the Provider app installed in Jira.
           *
           * If the app does not define a security information provider module, or does not have the required scope, the request will be rejected with a 403.
           *
           * Read [Understanding JWT](https://developer.atlassian.com/blog/2015/01/understanding-jwt/) for more details about Connect JWT tokens.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteVulnerabilityById(ctx, params, options);
    },
  };
}
