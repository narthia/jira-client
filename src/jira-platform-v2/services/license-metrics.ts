// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { License, LicenseMetric } from "../types/index.ts";

/**
 * Get license
 *
 * Returns licensing information about the Jira instance.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @returns Returned if the request is successful.
 */
export function getLicense(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<License> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/instance/license",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approximate license count
 *
 * Returns the approximate number of user accounts across all Jira licenses. Note that this information is cached with a 7-day lifecycle and could be stale at the time of call.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function getApproximateLicenseCount(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<LicenseMetric> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/license/approximateLicenseCount",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approximate application license count
 *
 * Returns the total approximate number of user accounts for a single Jira license. Note that this information is cached with a 7-day lifecycle and could be stale at the time of call.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.applicationKey - The ID of the application, represents a specific version of Jira.
 * @returns Returned if the request is successful.
 */
export function getApproximateApplicationLicenseCount(
  ctx: ClientContext,
  params: {
    /** The ID of the application, represents a specific version of Jira. */
    applicationKey: "jira-core" | "jira-product-discovery" | "jira-software" | "jira-servicedesk";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<LicenseMetric> {
  const { applicationKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/license/approximateLicenseCount/product/{applicationKey}",
    pathParams: { applicationKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents license metrics. Use it to get available metrics for Jira licences. */
export function createLicenseMetricsService(ctx: ClientContext) {
  return {
    /**
     * Get license
     *
     * Returns licensing information about the Jira instance.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @returns Returned if the request is successful.
     */
    getLicense(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<License> {
      return getLicense(ctx, options);
    },

    /**
     * Get approximate license count
     *
     * Returns the approximate number of user accounts across all Jira licenses. Note that this information is cached with a 7-day lifecycle and could be stale at the time of call.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    getApproximateLicenseCount(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<LicenseMetric> {
      return getApproximateLicenseCount(ctx, options);
    },

    /**
     * Get approximate application license count
     *
     * Returns the total approximate number of user accounts for a single Jira license. Note that this information is cached with a 7-day lifecycle and could be stale at the time of call.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.applicationKey - The ID of the application, represents a specific version of Jira.
     * @returns Returned if the request is successful.
     */
    getApproximateApplicationLicenseCount(
      params: {
        /** The ID of the application, represents a specific version of Jira. */
        applicationKey:
          | "jira-core"
          | "jira-product-discovery"
          | "jira-software"
          | "jira-servicedesk";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<LicenseMetric> {
      return getApproximateApplicationLicenseCount(ctx, params, options);
    },
  };
}
