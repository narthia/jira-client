// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  PageBeanScreenScheme,
  ScreenSchemeDetails,
  ScreenSchemeId,
  UpdateScreenSchemeDetails,
} from "../types/index.ts";

/**
 * Get screen schemes
 *
 * Returns a [paginated](#pagination) list of screen schemes.
 *
 * Only screen schemes used in classic projects are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`.
 * @param params.expand - Use [expand](#expansion) include additional information in the response. This parameter accepts `issueTypeScreenSchemes` that, for each screen schemes, returns information about the issue type screen scheme the screen scheme is assigned to.
 * @param params.queryString - String used to perform a case-insensitive partial match with screen scheme name.
 * @param params.orderBy - [Order](#ordering) the results by a field: *  `id` Sorts by screen scheme ID. *  `name` Sorts by screen scheme name.
 * @returns Returned if the request is successful.
 */
export function getScreenSchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 25
     * @format int32
     */
    maxResults?: number;
    /** The list of screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`. */
    id?: number[];
    /**
     * Use [expand](#expansion) include additional information in the response. This parameter accepts `issueTypeScreenSchemes` that, for each screen schemes, returns information about the issue type screen scheme the screen scheme is assigned to.
     *
     * @default ""
     */
    expand?: string;
    /**
     * String used to perform a case-insensitive partial match with screen scheme name.
     *
     * @default ""
     */
    queryString?: string;
    /**
     * [Order](#ordering) the results by a field:
     *
     *  *  `id` Sorts by screen scheme ID.
     *  *  `name` Sorts by screen scheme name.
     */
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanScreenScheme> {
  const { startAt, maxResults, id, expand, queryString, orderBy } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/screenscheme",
    query: { startAt, maxResults, id, expand, queryString, orderBy },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create screen scheme
 *
 * Creates a screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createScreenScheme(
  ctx: ClientContext,
  params: ScreenSchemeDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenSchemeId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/screenscheme",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update screen scheme
 *
 * Updates a screen scheme. Only screen schemes used in classic projects can be updated.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenSchemeId - The ID of the screen scheme.
 * @returns Returned if the request is successful.
 */
export function updateScreenScheme(
  ctx: ClientContext,
  params: UpdateScreenSchemeDetails & {
    /** The ID of the screen scheme. */
    screenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { screenSchemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/screenscheme/{screenSchemeId}",
    pathParams: { screenSchemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete screen scheme
 *
 * Deletes a screen scheme. A screen scheme cannot be deleted if it is used in an issue type screen scheme.
 *
 * Only screens schemes used in classic projects can be deleted.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenSchemeId - The ID of the screen scheme.
 * @returns Returned if the screen scheme is deleted.
 */
export function deleteScreenScheme(
  ctx: ClientContext,
  params: {
    /** The ID of the screen scheme. */
    screenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { screenSchemeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/screenscheme/{screenSchemeId}",
    pathParams: { screenSchemeId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents screen schemes in classic projects. Use it to get, create, update, and delete screen schemes. */
export function createScreenSchemesService(ctx: ClientContext) {
  return {
    /**
     * Get screen schemes
     *
     * Returns a [paginated](#pagination) list of screen schemes.
     *
     * Only screen schemes used in classic projects are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`.
     * @param params.expand - Use [expand](#expansion) include additional information in the response. This parameter accepts `issueTypeScreenSchemes` that, for each screen schemes, returns information about the issue type screen scheme the screen scheme is assigned to.
     * @param params.queryString - String used to perform a case-insensitive partial match with screen scheme name.
     * @param params.orderBy - [Order](#ordering) the results by a field: *  `id` Sorts by screen scheme ID. *  `name` Sorts by screen scheme name.
     * @returns Returned if the request is successful.
     */
    getScreenSchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 25
         * @format int32
         */
        maxResults?: number;
        /** The list of screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`. */
        id?: number[];
        /**
         * Use [expand](#expansion) include additional information in the response. This parameter accepts `issueTypeScreenSchemes` that, for each screen schemes, returns information about the issue type screen scheme the screen scheme is assigned to.
         *
         * @default ""
         */
        expand?: string;
        /**
         * String used to perform a case-insensitive partial match with screen scheme name.
         *
         * @default ""
         */
        queryString?: string;
        /**
         * [Order](#ordering) the results by a field:
         *
         *  *  `id` Sorts by screen scheme ID.
         *  *  `name` Sorts by screen scheme name.
         */
        orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanScreenScheme> {
      return getScreenSchemes(ctx, params, options);
    },

    /**
     * Create screen scheme
     *
     * Creates a screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createScreenScheme(
      params: ScreenSchemeDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenSchemeId> {
      return createScreenScheme(ctx, params, options);
    },

    /**
     * Update screen scheme
     *
     * Updates a screen scheme. Only screen schemes used in classic projects can be updated.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenSchemeId - The ID of the screen scheme.
     * @returns Returned if the request is successful.
     */
    updateScreenScheme(
      params: UpdateScreenSchemeDetails & {
        /** The ID of the screen scheme. */
        screenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateScreenScheme(ctx, params, options);
    },

    /**
     * Delete screen scheme
     *
     * Deletes a screen scheme. A screen scheme cannot be deleted if it is used in an issue type screen scheme.
     *
     * Only screens schemes used in classic projects can be deleted.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenSchemeId - The ID of the screen scheme.
     * @returns Returned if the screen scheme is deleted.
     */
    deleteScreenScheme(
      params: {
        /** The ID of the screen scheme. */
        screenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteScreenScheme(ctx, params, options);
    },
  };
}
