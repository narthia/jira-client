// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { AuthConfig, ClientConfig, ClientContext, Transport } from "../client/index.ts";
import { ApiError, createClient as createRuntimeClient } from "../client/index.ts";

export { ApiError };
export type { ClientConfig, ClientContext, Transport };

/** Auth accepted by `createPlatformV3Sdk`. */
export interface CreatePlatformV3SdkAuthConfig {
  email: string;
  apiToken: string;
}

function toRuntimeAuth(auth: CreatePlatformV3SdkAuthConfig): AuthConfig {
  return { type: "basic", username: auth.email, password: auth.apiToken };
}

/** Configuration for `createSdk` and `createClient`. */
export type SdkConfig = Omit<ClientConfig, "auth"> & { auth?: CreatePlatformV3SdkAuthConfig };

/** Create a client context that applies the configured auth. */
export function createClient(config: SdkConfig = {}) {
  const { auth, ...rest } = config;
  return createRuntimeClient(auth ? { ...rest, auth: toRuntimeAuth(auth) } : rest);
}
