// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  AddSecuritySchemeLevelsRequestBean,
  AssociateSecuritySchemeWithProjectDetails,
  CreateIssueSecuritySchemeDetails,
  PageBeanIssueSecuritySchemeToProjectMapping,
  PageBeanSecurityLevel,
  PageBeanSecurityLevelMember,
  PageBeanSecuritySchemeWithProjects,
  SecurityScheme,
  SecuritySchemeId,
  SecuritySchemeMembersRequest,
  SecuritySchemes,
  SetDefaultLevelsRequest,
  UpdateIssueSecurityLevelDetails,
  UpdateIssueSecuritySchemeRequestBean,
} from "../types/index.ts";

/**
 * Get issue security schemes
 *
 * Returns all [issue security schemes](https://confluence.atlassian.com/x/J4lKLg).
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function getIssueSecuritySchemes(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SecuritySchemes> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create issue security scheme
 *
 * Creates a security scheme with security scheme levels and levels' members. You can create up to 100 security scheme levels and security scheme levels' members per request.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createIssueSecurityScheme(
  ctx: ClientContext,
  params: CreateIssueSecuritySchemeDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SecuritySchemeId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/issuesecurityschemes",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue security levels
 *
 * Returns a [paginated](#pagination) list of issue security levels.
 *
 * Only issue security levels in the context of classic projects are returned.
 *
 * Filtering using IDs is inclusive: if you specify both security scheme IDs and level IDs, the result will include both specified issue security levels and all issue security levels from the specified schemes.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of issue security scheme level IDs. To include multiple issue security levels, separate IDs with an ampersand: `id=10000&id=10001`.
 * @param params.schemeId - The list of issue security scheme IDs. To include multiple issue security schemes, separate IDs with an ampersand: `schemeId=10000&schemeId=10001`.
 * @param params.onlyDefault - When set to true, returns multiple default levels for each security scheme containing a default. If you provide scheme and level IDs not associated with the default, returns an empty page. The default value is false.
 * @returns Returned if the request is successful.
 */
export function getSecurityLevels(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of issue security scheme level IDs. To include multiple issue security levels, separate IDs with an ampersand: `id=10000&id=10001`. */
    id?: string[];
    /** The list of issue security scheme IDs. To include multiple issue security schemes, separate IDs with an ampersand: `schemeId=10000&schemeId=10001`. */
    schemeId?: string[];
    /**
     * When set to true, returns multiple default levels for each security scheme containing a default. If you provide scheme and level IDs not associated with the default, returns an empty page. The default value is false.
     *
     * @default false
     */
    onlyDefault?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanSecurityLevel> {
  const { startAt, maxResults, id, schemeId, onlyDefault } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes/level",
    query: { startAt, maxResults, id, schemeId, onlyDefault },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set default issue security levels
 *
 * Sets default issue security levels for schemes.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function setDefaultLevels(
  ctx: ClientContext,
  params: SetDefaultLevelsRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/level/default",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue security level members
 *
 * Returns a [paginated](#pagination) list of issue security level members.
 *
 * Only issue security level members in the context of classic projects are returned.
 *
 * Filtering using parameters is inclusive: if you specify both security scheme IDs and level IDs, the result will include all issue security level members from the specified schemes and levels.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of issue security level member IDs. To include multiple issue security level members separate IDs with an ampersand: `id=10000&id=10001`.
 * @param params.schemeId - The list of issue security scheme IDs. To include multiple issue security schemes separate IDs with an ampersand: `schemeId=10000&schemeId=10001`.
 * @param params.levelId - The list of issue security level IDs. To include multiple issue security levels separate IDs with an ampersand: `levelId=10000&levelId=10001`.
 * @param params.expand - Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include: *  `all` Returns all expandable information *  `field` Returns information about the custom field granted the permission *  `group` Returns information about the group that is granted the permission *  `projectRole` Returns information about the project role granted the permission *  `user` Returns information about the user who is granted the permission
 * @returns Returned if the request is successful.
 */
export function getSecurityLevelMembers(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of issue security level member IDs. To include multiple issue security level members separate IDs with an ampersand: `id=10000&id=10001`. */
    id?: string[];
    /** The list of issue security scheme IDs. To include multiple issue security schemes separate IDs with an ampersand: `schemeId=10000&schemeId=10001`. */
    schemeId?: string[];
    /** The list of issue security level IDs. To include multiple issue security levels separate IDs with an ampersand: `levelId=10000&levelId=10001`. */
    levelId?: string[];
    /**
     * Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `all` Returns all expandable information
     *  *  `field` Returns information about the custom field granted the permission
     *  *  `group` Returns information about the group that is granted the permission
     *  *  `projectRole` Returns information about the project role granted the permission
     *  *  `user` Returns information about the user who is granted the permission
     */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanSecurityLevelMember> {
  const { startAt, maxResults, id, schemeId, levelId, expand } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes/level/member",
    query: { startAt, maxResults, id, schemeId, levelId, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get projects using issue security schemes
 *
 * Returns a [paginated](#pagination) mapping of projects that are using security schemes. You can provide either one or multiple security scheme IDs or project IDs to filter by. If you don't provide any, this will return a list of all mappings. Only issue security schemes in the context of classic projects are supported. **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.issueSecuritySchemeId - The list of security scheme IDs to be filtered out.
 * @param params.projectId - The list of project IDs to be filtered out.
 * @returns Returned if the request is successful.
 */
export function searchProjectsUsingSecuritySchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of security scheme IDs to be filtered out. */
    issueSecuritySchemeId?: string[];
    /** The list of project IDs to be filtered out. */
    projectId?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanIssueSecuritySchemeToProjectMapping> {
  const { startAt, maxResults, issueSecuritySchemeId, projectId } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes/project",
    query: { startAt, maxResults, issueSecuritySchemeId, projectId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Associate security scheme to project
 *
 * Associates an issue security scheme with a project and remaps security levels of issues to the new levels, if provided.
 *
 * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 */
export function associateSchemesToProjects(
  ctx: ClientContext,
  params: AssociateSecuritySchemeWithProjectDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/project",
    headers: options?.headers,
    body: params,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Search issue security schemes
 *
 * Returns a [paginated](#pagination) list of issue security schemes.
 * If you specify the project ID parameter, the result will contain issue security schemes and related project IDs you filter by. Use \{@link IssueSecuritySchemeResource\#searchProjectsUsingSecuritySchemes(String, String, Set, Set)\} to obtain all projects related to scheme.
 *
 * Only issue security schemes in the context of classic projects are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of issue security scheme IDs. To include multiple issue security scheme IDs, separate IDs with an ampersand: `id=10000&id=10001`.
 * @param params.projectId - The list of project IDs. To include multiple project IDs, separate IDs with an ampersand: `projectId=10000&projectId=10001`.
 * @returns Returned if the request is successful.
 */
export function searchSecuritySchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of issue security scheme IDs. To include multiple issue security scheme IDs, separate IDs with an ampersand: `id=10000&id=10001`. */
    id?: string[];
    /** The list of project IDs. To include multiple project IDs, separate IDs with an ampersand: `projectId=10000&projectId=10001`. */
    projectId?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanSecuritySchemeWithProjects> {
  const { startAt, maxResults, id, projectId } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes/search",
    query: { startAt, maxResults, id, projectId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue security scheme
 *
 * Returns an issue security scheme along with its security levels.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project that uses the requested issue security scheme.
 *
 * @param params.id - The ID of the issue security scheme. Use the [Get issue security schemes](#api-rest-api-2-issuesecurityschemes-get) operation to get a list of issue security scheme IDs.
 * @returns Returned if the request is successful.
 */
export function getIssueSecurityScheme(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the issue security scheme. Use the [Get issue security schemes](#api-rest-api-2-issuesecurityschemes-get) operation to get a list of issue security scheme IDs.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SecurityScheme> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issuesecurityschemes/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue security scheme
 *
 * Updates the issue security scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue security scheme.
 * @returns Returned if the request is successful.
 */
export function updateIssueSecurityScheme(
  ctx: ClientContext,
  params: UpdateIssueSecuritySchemeRequestBean & {
    /** The ID of the issue security scheme. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/{id}",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue security scheme
 *
 * Deletes an issue security scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme.
 * @returns Returned if the request is successful.
 */
export function deleteSecurityScheme(
  ctx: ClientContext,
  params: {
    /** The ID of the issue security scheme. */
    schemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}",
    pathParams: { schemeId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add issue security levels
 *
 * Adds levels and levels' members to the issue security scheme. You can add up to 100 levels per request.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme.
 * @returns Returned if the request is successful.
 */
export function addSecurityLevel(
  ctx: ClientContext,
  params: AddSecuritySchemeLevelsRequestBean & {
    /** The ID of the issue security scheme. */
    schemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}/level",
    pathParams: { schemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue security level
 *
 * Updates the issue security level.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme level belongs to.
 * @param params.levelId - The ID of the issue security level to update.
 * @returns Returned if the request is successful.
 */
export function updateSecurityLevel(
  ctx: ClientContext,
  params: UpdateIssueSecurityLevelDetails & {
    /** The ID of the issue security scheme level belongs to. */
    schemeId: string;
    /** The ID of the issue security level to update. */
    levelId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId, levelId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}/level/{levelId}",
    pathParams: { schemeId, levelId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove issue security level
 *
 * Deletes an issue security level.
 *
 * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme.
 * @param params.levelId - The ID of the issue security level to remove.
 * @param params.replaceWith - The ID of the issue security level that will replace the currently selected level.
 */
export function removeLevel(
  ctx: ClientContext,
  params: {
    /** The ID of the issue security scheme. */
    schemeId: string;
    /** The ID of the issue security level to remove. */
    levelId: string;
    /** The ID of the issue security level that will replace the currently selected level. */
    replaceWith?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { schemeId, levelId, replaceWith } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}/level/{levelId}",
    pathParams: { schemeId, levelId },
    query: { replaceWith },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add issue security level members
 *
 * Adds members to the issue security level. You can add up to 100 members per request.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme.
 * @param params.levelId - The ID of the issue security level.
 * @returns Returned if the request is successful.
 */
export function addSecurityLevelMembers(
  ctx: ClientContext,
  params: SecuritySchemeMembersRequest & {
    /** The ID of the issue security scheme. */
    schemeId: string;
    /** The ID of the issue security level. */
    levelId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId, levelId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}/level/{levelId}/member",
    pathParams: { schemeId, levelId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove member from issue security level
 *
 * Removes an issue security level member from an issue security scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.schemeId - The ID of the issue security scheme.
 * @param params.levelId - The ID of the issue security level.
 * @param params.memberId - The ID of the issue security level member to be removed.
 * @returns Returned if the request is successful.
 */
export function removeMemberFromSecurityLevel(
  ctx: ClientContext,
  params: {
    /** The ID of the issue security scheme. */
    schemeId: string;
    /** The ID of the issue security level. */
    levelId: string;
    /** The ID of the issue security level member to be removed. */
    memberId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { schemeId, levelId, memberId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issuesecurityschemes/{schemeId}/level/{levelId}/member/{memberId}",
    pathParams: { schemeId, levelId, memberId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents issue security schemes. Use it to get an issue security scheme or a list of issue security schemes.
 *
 * Issue security schemes control which users or groups of users can view an issue. When an issue security scheme is associated with a project, its security levels can be applied to issues in that project. Sub-tasks also inherit the security level of their parent issue.
 */
export function createIssueSecuritySchemesService(ctx: ClientContext) {
  return {
    /**
     * Get issue security schemes
     *
     * Returns all [issue security schemes](https://confluence.atlassian.com/x/J4lKLg).
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    getIssueSecuritySchemes(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<SecuritySchemes> {
      return getIssueSecuritySchemes(ctx, options);
    },

    /**
     * Create issue security scheme
     *
     * Creates a security scheme with security scheme levels and levels' members. You can create up to 100 security scheme levels and security scheme levels' members per request.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createIssueSecurityScheme(
      params: CreateIssueSecuritySchemeDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SecuritySchemeId> {
      return createIssueSecurityScheme(ctx, params, options);
    },

    /**
     * Get issue security levels
     *
     * Returns a [paginated](#pagination) list of issue security levels.
     *
     * Only issue security levels in the context of classic projects are returned.
     *
     * Filtering using IDs is inclusive: if you specify both security scheme IDs and level IDs, the result will include both specified issue security levels and all issue security levels from the specified schemes.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of issue security scheme level IDs. To include multiple issue security levels, separate IDs with an ampersand: `id=10000&id=10001`.
     * @param params.schemeId - The list of issue security scheme IDs. To include multiple issue security schemes, separate IDs with an ampersand: `schemeId=10000&schemeId=10001`.
     * @param params.onlyDefault - When set to true, returns multiple default levels for each security scheme containing a default. If you provide scheme and level IDs not associated with the default, returns an empty page. The default value is false.
     * @returns Returned if the request is successful.
     */
    getSecurityLevels(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of issue security scheme level IDs. To include multiple issue security levels, separate IDs with an ampersand: `id=10000&id=10001`. */
        id?: string[];
        /** The list of issue security scheme IDs. To include multiple issue security schemes, separate IDs with an ampersand: `schemeId=10000&schemeId=10001`. */
        schemeId?: string[];
        /**
         * When set to true, returns multiple default levels for each security scheme containing a default. If you provide scheme and level IDs not associated with the default, returns an empty page. The default value is false.
         *
         * @default false
         */
        onlyDefault?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanSecurityLevel> {
      return getSecurityLevels(ctx, params, options);
    },

    /**
     * Set default issue security levels
     *
     * Sets default issue security levels for schemes.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    setDefaultLevels(
      params: SetDefaultLevelsRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setDefaultLevels(ctx, params, options);
    },

    /**
     * Get issue security level members
     *
     * Returns a [paginated](#pagination) list of issue security level members.
     *
     * Only issue security level members in the context of classic projects are returned.
     *
     * Filtering using parameters is inclusive: if you specify both security scheme IDs and level IDs, the result will include all issue security level members from the specified schemes and levels.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of issue security level member IDs. To include multiple issue security level members separate IDs with an ampersand: `id=10000&id=10001`.
     * @param params.schemeId - The list of issue security scheme IDs. To include multiple issue security schemes separate IDs with an ampersand: `schemeId=10000&schemeId=10001`.
     * @param params.levelId - The list of issue security level IDs. To include multiple issue security levels separate IDs with an ampersand: `levelId=10000&levelId=10001`.
     * @param params.expand - Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include: *  `all` Returns all expandable information *  `field` Returns information about the custom field granted the permission *  `group` Returns information about the group that is granted the permission *  `projectRole` Returns information about the project role granted the permission *  `user` Returns information about the user who is granted the permission
     * @returns Returned if the request is successful.
     */
    getSecurityLevelMembers(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of issue security level member IDs. To include multiple issue security level members separate IDs with an ampersand: `id=10000&id=10001`. */
        id?: string[];
        /** The list of issue security scheme IDs. To include multiple issue security schemes separate IDs with an ampersand: `schemeId=10000&schemeId=10001`. */
        schemeId?: string[];
        /** The list of issue security level IDs. To include multiple issue security levels separate IDs with an ampersand: `levelId=10000&levelId=10001`. */
        levelId?: string[];
        /**
         * Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
         *
         *  *  `all` Returns all expandable information
         *  *  `field` Returns information about the custom field granted the permission
         *  *  `group` Returns information about the group that is granted the permission
         *  *  `projectRole` Returns information about the project role granted the permission
         *  *  `user` Returns information about the user who is granted the permission
         */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanSecurityLevelMember> {
      return getSecurityLevelMembers(ctx, params, options);
    },

    /**
     * Get projects using issue security schemes
     *
     * Returns a [paginated](#pagination) mapping of projects that are using security schemes. You can provide either one or multiple security scheme IDs or project IDs to filter by. If you don't provide any, this will return a list of all mappings. Only issue security schemes in the context of classic projects are supported. **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.issueSecuritySchemeId - The list of security scheme IDs to be filtered out.
     * @param params.projectId - The list of project IDs to be filtered out.
     * @returns Returned if the request is successful.
     */
    searchProjectsUsingSecuritySchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of security scheme IDs to be filtered out. */
        issueSecuritySchemeId?: string[];
        /** The list of project IDs to be filtered out. */
        projectId?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanIssueSecuritySchemeToProjectMapping> {
      return searchProjectsUsingSecuritySchemes(ctx, params, options);
    },

    /**
     * Associate security scheme to project
     *
     * Associates an issue security scheme with a project and remaps security levels of issues to the new levels, if provided.
     *
     * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     */
    associateSchemesToProjects(
      params: AssociateSecuritySchemeWithProjectDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return associateSchemesToProjects(ctx, params, options);
    },

    /**
     * Search issue security schemes
     *
     * Returns a [paginated](#pagination) list of issue security schemes.
     * If you specify the project ID parameter, the result will contain issue security schemes and related project IDs you filter by. Use \{@link IssueSecuritySchemeResource\#searchProjectsUsingSecuritySchemes(String, String, Set, Set)\} to obtain all projects related to scheme.
     *
     * Only issue security schemes in the context of classic projects are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of issue security scheme IDs. To include multiple issue security scheme IDs, separate IDs with an ampersand: `id=10000&id=10001`.
     * @param params.projectId - The list of project IDs. To include multiple project IDs, separate IDs with an ampersand: `projectId=10000&projectId=10001`.
     * @returns Returned if the request is successful.
     */
    searchSecuritySchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of issue security scheme IDs. To include multiple issue security scheme IDs, separate IDs with an ampersand: `id=10000&id=10001`. */
        id?: string[];
        /** The list of project IDs. To include multiple project IDs, separate IDs with an ampersand: `projectId=10000&projectId=10001`. */
        projectId?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanSecuritySchemeWithProjects> {
      return searchSecuritySchemes(ctx, params, options);
    },

    /**
     * Get issue security scheme
     *
     * Returns an issue security scheme along with its security levels.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project that uses the requested issue security scheme.
     *
     * @param params.id - The ID of the issue security scheme. Use the [Get issue security schemes](#api-rest-api-2-issuesecurityschemes-get) operation to get a list of issue security scheme IDs.
     * @returns Returned if the request is successful.
     */
    getIssueSecurityScheme(
      params: {
        /**
         * The ID of the issue security scheme. Use the [Get issue security schemes](#api-rest-api-2-issuesecurityschemes-get) operation to get a list of issue security scheme IDs.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SecurityScheme> {
      return getIssueSecurityScheme(ctx, params, options);
    },

    /**
     * Update issue security scheme
     *
     * Updates the issue security scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue security scheme.
     * @returns Returned if the request is successful.
     */
    updateIssueSecurityScheme(
      params: UpdateIssueSecuritySchemeRequestBean & {
        /** The ID of the issue security scheme. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateIssueSecurityScheme(ctx, params, options);
    },

    /**
     * Delete issue security scheme
     *
     * Deletes an issue security scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme.
     * @returns Returned if the request is successful.
     */
    deleteSecurityScheme(
      params: {
        /** The ID of the issue security scheme. */
        schemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteSecurityScheme(ctx, params, options);
    },

    /**
     * Add issue security levels
     *
     * Adds levels and levels' members to the issue security scheme. You can add up to 100 levels per request.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme.
     * @returns Returned if the request is successful.
     */
    addSecurityLevel(
      params: AddSecuritySchemeLevelsRequestBean & {
        /** The ID of the issue security scheme. */
        schemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return addSecurityLevel(ctx, params, options);
    },

    /**
     * Update issue security level
     *
     * Updates the issue security level.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme level belongs to.
     * @param params.levelId - The ID of the issue security level to update.
     * @returns Returned if the request is successful.
     */
    updateSecurityLevel(
      params: UpdateIssueSecurityLevelDetails & {
        /** The ID of the issue security scheme level belongs to. */
        schemeId: string;
        /** The ID of the issue security level to update. */
        levelId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateSecurityLevel(ctx, params, options);
    },

    /**
     * Remove issue security level
     *
     * Deletes an issue security level.
     *
     * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-2-task-taskId-get) to obtain subsequent updates.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme.
     * @param params.levelId - The ID of the issue security level to remove.
     * @param params.replaceWith - The ID of the issue security level that will replace the currently selected level.
     */
    removeLevel(
      params: {
        /** The ID of the issue security scheme. */
        schemeId: string;
        /** The ID of the issue security level to remove. */
        levelId: string;
        /** The ID of the issue security level that will replace the currently selected level. */
        replaceWith?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return removeLevel(ctx, params, options);
    },

    /**
     * Add issue security level members
     *
     * Adds members to the issue security level. You can add up to 100 members per request.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme.
     * @param params.levelId - The ID of the issue security level.
     * @returns Returned if the request is successful.
     */
    addSecurityLevelMembers(
      params: SecuritySchemeMembersRequest & {
        /** The ID of the issue security scheme. */
        schemeId: string;
        /** The ID of the issue security level. */
        levelId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return addSecurityLevelMembers(ctx, params, options);
    },

    /**
     * Remove member from issue security level
     *
     * Removes an issue security level member from an issue security scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.schemeId - The ID of the issue security scheme.
     * @param params.levelId - The ID of the issue security level.
     * @param params.memberId - The ID of the issue security level member to be removed.
     * @returns Returned if the request is successful.
     */
    removeMemberFromSecurityLevel(
      params: {
        /** The ID of the issue security scheme. */
        schemeId: string;
        /** The ID of the issue security level. */
        levelId: string;
        /** The ID of the issue security level member to be removed. */
        memberId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return removeMemberFromSecurityLevel(ctx, params, options);
    },
  };
}
