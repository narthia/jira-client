// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  ApprovalDecisionRequestDto,
  ApprovalDto,
  AttachmentCreateDto,
  AttachmentCreateResultDto,
  CommentCreateDto,
  CommentDto,
  CsatFeedbackFullDto,
  CustomerRequestDto,
  CustomerTransitionExecutionDto,
  PagedDtoApprovalDto,
  PagedDtoAttachmentDto,
  PagedDtoCommentDto,
  PagedDtoCustomerRequestDto,
  PagedDtoCustomerRequestStatusDto,
  PagedDtoCustomerTransitionDto,
  PagedDtoSlaInformationDto,
  PagedDtoUserDto,
  RequestCreateDto,
  RequestNotificationSubscriptionDto,
  RequestParticipantUpdateDto,
  SlaInformationDto,
} from "../types/index.ts";

/**
 * Get customer requests
 *
 * This method returns all customer requests for the user executing the query.
 *
 * The returned customer requests are ordered chronologically by the latest activity on each request. For example, the latest status transition or comment.
 *
 * **[Permissions](#permissions) required**: Permission to access the specified service desk.
 *
 * **Response limitations**: For customers, the list returned will include request they created (or were created on their behalf) or are participating in only.
 *
 * @param params.searchTerm - Filters customer requests where the request summary matches the `searchTerm`. [Wildcards](https://confluence.atlassian.com/display/JIRACORECLOUD/Search+syntax+for+text+fields) can be used in the `searchTerm` parameter.
 * @param params.requestOwnership - Filters customer requests using the following values: *  `OWNED_REQUESTS` returns customer requests where the user is the creator. *  `PARTICIPATED_REQUESTS` returns customer requests where the user is a participant. *  `ORGANIZATION` returns customer requests for an organization of which the user is a member when used in conjunction with `organizationId`. *  `ALL_ORGANIZATIONS` returns customer requests that belong to all organizations of which the user is a member. *  `APPROVER` returns customer requests where the user is an approver. Can be used in conjunction with `approvalStatus` to filter pending or complete approvals. *  `ALL_REQUESTS` returns all customer requests. **Deprecated and will be removed, as the returned requests may change if more values are added in the future. Instead, explicitly list the desired filtering strategies.** Multiple values of the query parameter are supported. For example, `requestOwnership=OWNED_REQUESTS&requestOwnership=PARTICIPATED_REQUESTS` will only return customer requests where the user is the creator or a participant. If not specified, filtering defaults to `OWNED_REQUESTS`, `PARTICIPATED_REQUESTS`, and `ALL_ORGANIZATIONS`.
 * @param params.requestStatus - Filters customer requests where the request is closed, open, or either of the two where: *  `CLOSED_REQUESTS` returns customer requests that are closed. *  `OPEN_REQUESTS` returns customer requests that are open. *  `ALL_REQUESTS` returns all customer requests.
 * @param params.approvalStatus - Filters results to customer requests based on their approval status: *  `MY_PENDING_APPROVAL` returns customer requests pending the user's approval. *  `MY_HISTORY_APPROVAL` returns customer requests where the user was an approver. **Note**: Valid only when used with requestOwnership=APPROVER.
 * @param params.organizationId - Filters customer requests that belong to a specific organization (note that the user must be a member of that organization). **Note**: Valid only when used with requestOwnership=ORGANIZATION.
 * @param params.serviceDeskId - Filters customer requests by service desk.
 * @param params.requestTypeId - Filters customer requests by request type. Note that the `serviceDeskId` must be specified for the service desk in which the request type belongs.
 * @param params.expand - A multi-value parameter indicating which properties of the customer request to expand, where: *  `serviceDesk` returns additional details for each service desk. *  `requestType` returns additional details for each request type. *  `participant` returns the participant details, if any, for each customer request. *  `sla` returns the SLA information on each customer request. *  `status` returns the status transitions, in chronological order, for each customer request. *  `attachment` returns the attachments for the customer request. *  `action` returns the actions that the user can or cannot perform on this customer request. *  `comment` returns the comments, if any, for each customer request. *  `comment.attachment` returns the attachment details, if any, for each comment. *  `comment.renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the customer requests, on the specified page of the results.
 */
export function getCustomerRequests(
  ctx: ClientContext,
  params?: {
    /** Filters customer requests where the request summary matches the `searchTerm`. [Wildcards](https://confluence.atlassian.com/display/JIRACORECLOUD/Search+syntax+for+text+fields) can be used in the `searchTerm` parameter. */
    searchTerm?: string;
    /**
     * Filters customer requests using the following values:
     *
     *  *  `OWNED_REQUESTS` returns customer requests where the user is the creator.
     *  *  `PARTICIPATED_REQUESTS` returns customer requests where the user is a participant.
     *  *  `ORGANIZATION` returns customer requests for an organization of which the user is a member when used in conjunction with `organizationId`.
     *  *  `ALL_ORGANIZATIONS` returns customer requests that belong to all organizations of which the user is a member.
     *  *  `APPROVER` returns customer requests where the user is an approver. Can be used in conjunction with `approvalStatus` to filter pending or complete approvals.
     *  *  `ALL_REQUESTS` returns all customer requests. **Deprecated and will be removed, as the returned requests may change if more values are added in the future. Instead, explicitly list the desired filtering strategies.**
     *
     * Multiple values of the query parameter are supported. For example, `requestOwnership=OWNED_REQUESTS&requestOwnership=PARTICIPATED_REQUESTS` will only return customer requests where the user is the creator or a participant. If not specified, filtering defaults to `OWNED_REQUESTS`, `PARTICIPATED_REQUESTS`, and `ALL_ORGANIZATIONS`.
     */
    requestOwnership?: string[];
    /**
     * Filters customer requests where the request is closed, open, or either of the two where:
     *
     *  *  `CLOSED_REQUESTS` returns customer requests that are closed.
     *  *  `OPEN_REQUESTS` returns customer requests that are open.
     *  *  `ALL_REQUESTS` returns all customer requests.
     */
    requestStatus?: string;
    /**
     * Filters results to customer requests based on their approval status:
     *
     *  *  `MY_PENDING_APPROVAL` returns customer requests pending the user's approval.
     *  *  `MY_HISTORY_APPROVAL` returns customer requests where the user was an approver.
     *
     * **Note**: Valid only when used with requestOwnership=APPROVER.
     */
    approvalStatus?: string;
    /**
     * Filters customer requests that belong to a specific organization (note that the user must be a member of that organization). **Note**: Valid only when used with requestOwnership=ORGANIZATION.
     *
     * @format int32
     */
    organizationId?: number;
    /**
     * Filters customer requests by service desk.
     *
     * @format int32
     */
    serviceDeskId?: number;
    /**
     * Filters customer requests by request type. Note that the `serviceDeskId` must be specified for the service desk in which the request type belongs.
     *
     * @format int32
     */
    requestTypeId?: number;
    /**
     * A multi-value parameter indicating which properties of the customer request to expand, where:
     *
     *  *  `serviceDesk` returns additional details for each service desk.
     *  *  `requestType` returns additional details for each request type.
     *  *  `participant` returns the participant details, if any, for each customer request.
     *  *  `sla` returns the SLA information on each customer request.
     *  *  `status` returns the status transitions, in chronological order, for each customer request.
     *  *  `attachment` returns the attachments for the customer request.
     *  *  `action` returns the actions that the user can or cannot perform on this customer request.
     *  *  `comment` returns the comments, if any, for each customer request.
     *  *  `comment.attachment` returns the attachment details, if any, for each comment.
     *  *  `comment.renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
     */
    expand?: string[];
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoCustomerRequestDto> {
  const {
    searchTerm,
    requestOwnership,
    requestStatus,
    approvalStatus,
    organizationId,
    serviceDeskId,
    requestTypeId,
    expand,
    start,
    limit,
  } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request",
    query: {
      searchTerm,
      requestOwnership,
      requestStatus,
      approvalStatus,
      organizationId,
      serviceDeskId,
      requestTypeId,
      expand,
      start,
      limit,
    },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create customer request
 *
 * This method creates a customer request in a service desk.
 *
 * The JSON request must include the service desk and customer request type, as well as any fields that are required for the request type. A list of the fields required by a customer request type can be obtained using [servicedesk/\{serviceDeskId\}/requesttype/\{requestTypeId\}/field](#api-servicedesk-serviceDeskId-requesttype-requestTypeId-field-get).
 *
 * The fields required for a customer request type depend on the user's permissions:
 *
 *  *  `raiseOnBehalfOf` is not available to Users who have the customer permission only.
 *  *  `requestParticipants` is not available to Users who have the customer permission only or if the feature is turned off for customers.
 *
 * `requestFieldValues` is a map of Jira field IDs and their values. See [Field input formats](#fieldformats), for details of each field's JSON semantics and the values they can take.
 *
 * **[Permissions](#permissions) required**: Permission to create requests in the specified service desk.
 *
 * @returns Returned if the customer request was created.
 */
export function createCustomerRequest(
  ctx: ClientContext,
  params: RequestCreateDto,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomerRequestDto> {
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get customer request by id or key
 *
 * This method returns a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to access the specified service desk.
 *
 * **Response limitations**: For customers, only a request they created, was created on their behalf, or they are participating in will be returned.
 *
 * **Note:** `requestFieldValues` does not include hidden fields. To get a list of request type fields that includes hidden fields, see [/rest/servicedeskapi/servicedesk/\{serviceDeskId\}/requesttype/\{requestTypeId\}/field](https://developer.atlassian.com/cloud/jira/service-desk/rest/api-group-servicedesk/#api-rest-servicedeskapi-servicedesk-servicedeskid-requesttype-requesttypeid-field-get)
 *
 * @param params.issueIdOrKey - The ID or Key of the customer request to be returned
 * @param params.expand - A multi-value parameter indicating which properties of the customer request to expand, where: *  `serviceDesk` returns additional service desk details. *  `requestType` returns additional customer request type details. *  `participant` returns the participant details. *  `sla` returns the SLA information. *  `status` returns the status transitions, in chronological order. *  `attachment` returns the attachments. *  `action` returns the actions that the user can or cannot perform. *  `comment` returns the comments. *  `comment.attachment` returns the attachment details for each comment. *  `comment.renderedBody` (Experimental) return the rendered body in HTML format (in addition to the raw body) for each comment.
 * @returns Returns the customer request.
 */
export function getCustomerRequestByIdOrKey(
  ctx: ClientContext,
  params: {
    /** The ID or Key of the customer request to be returned */
    issueIdOrKey: string;
    /**
     * A multi-value parameter indicating which properties of the customer request to expand, where:
     *
     *  *  `serviceDesk` returns additional service desk details.
     *  *  `requestType` returns additional customer request type details.
     *  *  `participant` returns the participant details.
     *  *  `sla` returns the SLA information.
     *  *  `status` returns the status transitions, in chronological order.
     *  *  `attachment` returns the attachments.
     *  *  `action` returns the actions that the user can or cannot perform.
     *  *  `comment` returns the comments.
     *  *  `comment.attachment` returns the attachment details for each comment.
     *  *  `comment.renderedBody` (Experimental) return the rendered body in HTML format (in addition to the raw body) for each comment.
     */
    expand?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomerRequestDto> {
  const { issueIdOrKey, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}",
    pathParams: { issueIdOrKey },
    query: { expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approvals
 *
 * This method returns all approvals on a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be queried for its approvals.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of approvals to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the customer request's approvals.
 */
export function getApprovals(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be queried for its approvals. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of approvals to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoApprovalDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/approval",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approval by id
 *
 * This method returns an approval. Use this method to determine the status of an approval and the list of approvers.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request the approval is on.
 * @param params.approvalId - The ID of the approval to be returned.
 * @returns Returns the requested approval.
 */
export function getApprovalById(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request the approval is on. */
    issueIdOrKey: string;
    /**
     * The ID of the approval to be returned.
     *
     * @format int32
     */
    approvalId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ApprovalDto> {
  const { issueIdOrKey, approvalId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/approval/{approvalId}",
    pathParams: { issueIdOrKey, approvalId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Answer approval
 *
 * This method enables a user to **Approve** or **Decline** an approval on a customer request. The approval is assumed to be owned by the user making the call.
 *
 * **[Permissions](#permissions) required**: User is assigned to the approval request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be updated.
 * @param params.approvalId - The ID of the approval to be updated.
 * @returns Returns the updated approval.
 */
export function answerApproval(
  ctx: ClientContext,
  params: ApprovalDecisionRequestDto & {
    /** The ID or key of the customer request to be updated. */
    issueIdOrKey: string;
    /**
     * The ID of the approval to be updated.
     *
     * @format int32
     */
    approvalId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ApprovalDto> {
  const { issueIdOrKey, approvalId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/approval/{approvalId}",
    pathParams: { issueIdOrKey, approvalId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get attachments for request
 *
 * This method returns all the attachments for a customer requests.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * **Response limitations**: Customers will only get a list of public attachments.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request from which the attachments will be listed.
 * @param params.start - The starting index of the returned attachment. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the visible attachments from the customer request.
 */
export function getAttachmentsForRequest(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request from which the attachments will be listed. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned attachment. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @default 0
     * @format int32
     */
    start: number;
    /**
     * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @default 50
     * @format int32
     */
    limit: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoAttachmentDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/attachment",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create comment with attachment
 *
 * This method creates a comment on a customer request using one or more attachment files (uploaded using [servicedeskapi/servicedesk/\{serviceDeskId\}/attachTemporaryFile](https://developer.atlassian.com/cloud/jira/service-desk/rest/api-group-servicedesk/#api-rest-servicedeskapi-servicedesk-servicedeskid-attachtemporaryfile-post)), with the visibility set by `public`. See
 *
 *  *  GET [servicedeskapi/request/\{issueIdOrKey\}/attachment](./#api-rest-servicedeskapi-request-issueidorkey-attachment-get)
 *  *  GET [servicedeskapi/request/\{issueIdOrKey\}/comment/\{commentId\}/attachment](./#api-rest-servicedeskapi-request-issueidorkey-comment-commentid-attachment-get)
 *
 * **[Permissions](#permissions) required**: Permission to add an attachment.
 *
 * **Request limitations**: Customers can set public visibility only.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to which the attachment will be added.
 * @returns Returns the attachments and comment.
 */
export function createCommentWithAttachment(
  ctx: ClientContext,
  params: AttachmentCreateDto & {
    /** The ID or key of the customer request to which the attachment will be added. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<AttachmentCreateResultDto> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/attachment",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get attachment content
 *
 * Returns the contents of an attachment.
 *
 * To return a thumbnail of the attachment, use [servicedeskapi/request/\{issueIdOrKey\}/attachment/\{attachmentId\}/thumbnail](./#api-rest-servicedeskapi-request-issueidorkey-attachment-attachmentid-thumbnail-get).
 *
 * **[Permissions](#permissions) required:** For the issue containing the attachment:
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key for the customer request the attachment is associated with
 * @param params.attachmentId - The ID for the attachment
 * @returns 200 response
 */
export function getAttachmentContent(
  ctx: ClientContext,
  params: {
    /** The ID or key for the customer request the attachment is associated with */
    issueIdOrKey: string;
    /**
     * The ID for the attachment
     *
     * @format int64
     */
    attachmentId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, attachmentId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/attachment/{attachmentId}",
    pathParams: { issueIdOrKey, attachmentId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get attachment thumbnail
 *
 * Returns the thumbnail of an attachment.
 *
 * To return the attachment contents, use [servicedeskapi/request/\{issueIdOrKey\}/attachment/\{attachmentId\}](#api-rest-servicedeskapi-request-issueidorkey-attachment-attachmentid-get).
 *
 * **[Permissions](#permissions) required:** For the issue containing the attachment:
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key for the customer request the attachment is associated with
 * @param params.attachmentId - The ID of the attachment.
 * @returns 200 response
 */
export function getAttachmentThumbnail(
  ctx: ClientContext,
  params: {
    /** The ID or key for the customer request the attachment is associated with */
    issueIdOrKey: string;
    /**
     * The ID of the attachment.
     *
     * @format int64
     */
    attachmentId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, attachmentId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/attachment/{attachmentId}/thumbnail",
    pathParams: { issueIdOrKey, attachmentId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request comments
 *
 * This method returns all comments on a customer request. No permissions error is provided if, for example, the user doesn't have access to the service desk or request, the method simply returns an empty response.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * **Response limitations**: Customers are returned public comments only.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request whose comments will be retrieved.
 * @param params.public_ - Specifies whether to return public comments or not. Default: true.
 * @param params.internal - Specifies whether to return internal comments or not. Default: true.
 * @param params.expand - A multi-value parameter indicating which properties of the comment to expand: *  `attachment` returns the attachment details, if any, for each comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).) *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
 * @param params.start - The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the comments, on the specified page of the results.
 */
export function getRequestComments(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request whose comments will be retrieved. */
    issueIdOrKey: string;
    /** Specifies whether to return public comments or not. Default: true. */
    public_?: boolean;
    /** Specifies whether to return internal comments or not. Default: true. */
    internal?: boolean;
    /**
     * A multi-value parameter indicating which properties of the comment to expand:
     *
     *  *  `attachment` returns the attachment details, if any, for each comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).)
     *  *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
     */
    expand?: string[];
    /**
     * The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoCommentDto> {
  const { issueIdOrKey, public_, internal, expand, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/comment",
    pathParams: { issueIdOrKey },
    query: { public: public_, internal, expand, start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create request comment
 *
 * This method creates a public or private (internal) comment on a customer request, with the comment visibility set by `public`. The user recorded as the author of the comment.
 *
 * **[Permissions](#permissions) required**: User has Add Comments permission.
 *
 * **Request limitations**: Customers can set comments to public visibility only.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to which the comment will be added.
 * @returns Returns the comment.
 */
export function createRequestComment(
  ctx: ClientContext,
  params: CommentCreateDto & {
    /** The ID or key of the customer request to which the comment will be added. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CommentDto> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/comment",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request comment by id
 *
 * This method returns details of a customer request's comment.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * **Response limitations**: Customers can only view public comments on requests where they are the reporter or a participant whereas agents can see both internal and public comments.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request that contains the comment.
 * @param params.commentId - The ID of the comment to retrieve.
 * @param params.expand - A multi-value parameter indicating which properties of the comment to expand: *  `attachment` returns the attachment details, if any, for the comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).) *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) of the comment.
 * @returns Returns the comment.
 */
export function getRequestCommentById(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request that contains the comment. */
    issueIdOrKey: string;
    /**
     * The ID of the comment to retrieve.
     *
     * @format int64
     */
    commentId: number;
    /**
     * A multi-value parameter indicating which properties of the comment to expand:
     *
     *  *  `attachment` returns the attachment details, if any, for the comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).)
     *  *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) of the comment.
     */
    expand?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CommentDto> {
  const { issueIdOrKey, commentId, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/comment/{commentId}",
    pathParams: { issueIdOrKey, commentId },
    query: { expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get comment attachments
 *
 * This method returns the attachments referenced in a comment.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * **Response limitations**: Customers can only view public comments, and retrieve their attachments, on requests where they are the reporter or a participant whereas agents can see both internal and public comments.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request that contains the comment.
 * @param params.commentId - The ID of the comment.
 * @param params.start - The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the attachments, on the specified page of the results.
 */
export function getCommentAttachments(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request that contains the comment. */
    issueIdOrKey: string;
    /**
     * The ID of the comment.
     *
     * @format int64
     */
    commentId: number;
    /**
     * The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoAttachmentDto> {
  const { issueIdOrKey, commentId, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/comment/{commentId}/attachment",
    pathParams: { issueIdOrKey, commentId },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get subscription status
 *
 * This method returns the notification subscription status of the user making the request. Use this method to determine if the user is subscribed to a customer request's notifications.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be queried for subscription status.
 * @returns Returns the status of the notification subscription.
 */
export function getSubscriptionStatus(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be queried for subscription status. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RequestNotificationSubscriptionDto> {
  const { issueIdOrKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/notification",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Subscribe
 *
 * This method subscribes the user to receiving notifications from a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be subscribed to.
 * @returns Returns if the user was subscribed.
 */
export function subscribe(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be subscribed to. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueIdOrKey } = params;
  return ctx.request({
    method: "put",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/notification",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Unsubscribe
 *
 * This method unsubscribes the user from notifications from a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be unsubscribed from.
 * @returns Returns if the user was unsubscribed.
 */
export function unsubscribe(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be unsubscribed from. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueIdOrKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/notification",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request participants
 *
 * This method returns a list of all the participants on a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be queried for its participants.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the customer request's participants, on the specified page of the results.
 */
export function getRequestParticipants(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be queried for its participants. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoUserDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/participant",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add request participants
 *
 * This method adds participants to a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to manage participants on the customer request.
 *
 * Note, participants can be added when creating a customer request using the [request](#api-request-post) resource, by defining the participants in the `requestParticipants` field.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to have participants added.
 * @returns Returns the participants added to the customer request.
 */
export function addRequestParticipants(
  ctx: ClientContext,
  params: RequestParticipantUpdateDto & {
    /** The ID or key of the customer request to have participants added. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoUserDto> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/participant",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove request participants
 *
 * This method removes participants from a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to manage participants on the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to have participants removed.
 * @returns Returns the first page of the customer request's participants (after removal of the users).
 */
export function removeRequestParticipants(
  ctx: ClientContext,
  params: RequestParticipantUpdateDto & {
    /** The ID or key of the customer request to have participants removed. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoUserDto> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/participant",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get sla information
 *
 * This method returns all the SLA records on a customer request. A customer request can have zero or more SLAs. Each SLA can have recordings for zero or more "completed cycles" and zero or 1 "ongoing cycle". Each cycle includes information on when it started and stopped, and whether it breached the SLA goal.
 *
 * **[Permissions](#permissions) required**:
 *
 *  *  Agent for the Service Desk containing the queried customer request, AND
 *  *  Browse Projects permission on the project containing the customer request, including any restrictions imposed by issue security schemes or custom permission schemes on the specific issue.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request whose SLAs will be retrieved.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the SLA records on the customer request, on the specified page of the results.
 */
export function getSlaInformation(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request whose SLAs will be retrieved. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoSlaInformationDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/sla",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get sla information by id
 *
 * This method returns the details for an SLA on a customer request.
 *
 * **[Permissions](#permissions) required**:
 *
 *  *  Agent for the Service Desk containing the queried customer request, AND
 *  *  Browse Projects permission on the project containing the customer request, including any restrictions imposed by issue security schemes or custom permission schemes on the specific issue.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request whose SLAs will be retrieved.
 * @param params.slaMetricId - The ID or key of the SLAs metric to be retrieved.
 * @returns Returns the SLA record, on the specified page of the results.
 */
export function getSlaInformationById(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request whose SLAs will be retrieved. */
    issueIdOrKey: string;
    /**
     * The ID or key of the SLAs metric to be retrieved.
     *
     * @format int64
     */
    slaMetricId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SlaInformationDto> {
  const { issueIdOrKey, slaMetricId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/sla/{slaMetricId}",
    pathParams: { issueIdOrKey, slaMetricId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get customer request status
 *
 * This method returns a list of all the statuses a customer Request has achieved. A status represents the state of an issue in its workflow. An issue can have one active status only. The list returns the status history in chronological order, most recent (current) status first.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request to be retrieved.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the customer request's status history, on the specified page of the results.
 */
export function getCustomerRequestStatus(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request to be retrieved. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoCustomerRequestStatusDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/status",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get customer transitions
 *
 * This method returns a list of transitions, the workflow processes that moves a customer request from one status to another, that the user can perform on a request. Use this method to provide a user with a list if the actions they can take on a customer request.
 *
 * **[Permissions](#permissions) required**: Permission to view the customer request.
 *
 * @param params.issueIdOrKey - The ID or key of the customer request whose transitions will be retrieved.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the transitions available to the user on the customer request.
 */
export function getCustomerTransitions(
  ctx: ClientContext,
  params: {
    /** The ID or key of the customer request whose transitions will be retrieved. */
    issueIdOrKey: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoCustomerTransitionDto> {
  const { issueIdOrKey, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/transition",
    pathParams: { issueIdOrKey },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Perform customer transition
 *
 * This method performs a customer transition for a given request and transition. An optional comment can be included to provide a reason for the transition.
 *
 * **[Permissions](#permissions) required**: The user must be able to view the request and have the Transition Issues permission. If a comment is passed the user must have the Add Comments permission.
 *
 * @param params.issueIdOrKey - ID or key of the issue to transition
 * @returns Returned if the request is transitioned.
 */
export function performCustomerTransition(
  ctx: ClientContext,
  params: CustomerTransitionExecutionDto & {
    /** ID or key of the issue to transition */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{issueIdOrKey}/transition",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get feedback
 *
 * This method retrieves a feedback of a request using it's `requestKey` or `requestId`
 *
 * **[Permissions](#permissions) required**: User has view request permissions.
 *
 * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
 * @returns Returns the comment.
 */
export function getFeedback(
  ctx: ClientContext,
  params: {
    /** The id or the key of the request to post the feedback on */
    requestIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CsatFeedbackFullDto> {
  const { requestIdOrKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/request/{requestIdOrKey}/feedback",
    pathParams: { requestIdOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Post feedback
 *
 * This method adds a feedback on an request using it's `requestKey` or `requestId`
 *
 * **[Permissions](#permissions) required**: User must be the reporter or an Atlassian Connect app.
 *
 * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
 * @returns Returns the comment.
 */
export function postFeedback(
  ctx: ClientContext,
  params: CsatFeedbackFullDto & {
    /** The id or the key of the request to post the feedback on */
    requestIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CsatFeedbackFullDto> {
  const { requestIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/request/{requestIdOrKey}/feedback",
    pathParams: { requestIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete feedback
 *
 * This method deletes the feedback of request using it's `requestKey` or `requestId`
 *
 * **[Permissions](#permissions) required**: User must be the reporter or an Atlassian Connect app.
 *
 * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
 * @returns No content.
 */
export function deleteFeedback(
  ctx: ClientContext,
  params: {
    /** The id or the key of the request to post the feedback on */
    requestIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { requestIdOrKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/request/{requestIdOrKey}/feedback",
    pathParams: { requestIdOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `request` service. */
export function createRequestService(ctx: ClientContext) {
  return {
    /**
     * Get customer requests
     *
     * This method returns all customer requests for the user executing the query.
     *
     * The returned customer requests are ordered chronologically by the latest activity on each request. For example, the latest status transition or comment.
     *
     * **[Permissions](#permissions) required**: Permission to access the specified service desk.
     *
     * **Response limitations**: For customers, the list returned will include request they created (or were created on their behalf) or are participating in only.
     *
     * @param params.searchTerm - Filters customer requests where the request summary matches the `searchTerm`. [Wildcards](https://confluence.atlassian.com/display/JIRACORECLOUD/Search+syntax+for+text+fields) can be used in the `searchTerm` parameter.
     * @param params.requestOwnership - Filters customer requests using the following values: *  `OWNED_REQUESTS` returns customer requests where the user is the creator. *  `PARTICIPATED_REQUESTS` returns customer requests where the user is a participant. *  `ORGANIZATION` returns customer requests for an organization of which the user is a member when used in conjunction with `organizationId`. *  `ALL_ORGANIZATIONS` returns customer requests that belong to all organizations of which the user is a member. *  `APPROVER` returns customer requests where the user is an approver. Can be used in conjunction with `approvalStatus` to filter pending or complete approvals. *  `ALL_REQUESTS` returns all customer requests. **Deprecated and will be removed, as the returned requests may change if more values are added in the future. Instead, explicitly list the desired filtering strategies.** Multiple values of the query parameter are supported. For example, `requestOwnership=OWNED_REQUESTS&requestOwnership=PARTICIPATED_REQUESTS` will only return customer requests where the user is the creator or a participant. If not specified, filtering defaults to `OWNED_REQUESTS`, `PARTICIPATED_REQUESTS`, and `ALL_ORGANIZATIONS`.
     * @param params.requestStatus - Filters customer requests where the request is closed, open, or either of the two where: *  `CLOSED_REQUESTS` returns customer requests that are closed. *  `OPEN_REQUESTS` returns customer requests that are open. *  `ALL_REQUESTS` returns all customer requests.
     * @param params.approvalStatus - Filters results to customer requests based on their approval status: *  `MY_PENDING_APPROVAL` returns customer requests pending the user's approval. *  `MY_HISTORY_APPROVAL` returns customer requests where the user was an approver. **Note**: Valid only when used with requestOwnership=APPROVER.
     * @param params.organizationId - Filters customer requests that belong to a specific organization (note that the user must be a member of that organization). **Note**: Valid only when used with requestOwnership=ORGANIZATION.
     * @param params.serviceDeskId - Filters customer requests by service desk.
     * @param params.requestTypeId - Filters customer requests by request type. Note that the `serviceDeskId` must be specified for the service desk in which the request type belongs.
     * @param params.expand - A multi-value parameter indicating which properties of the customer request to expand, where: *  `serviceDesk` returns additional details for each service desk. *  `requestType` returns additional details for each request type. *  `participant` returns the participant details, if any, for each customer request. *  `sla` returns the SLA information on each customer request. *  `status` returns the status transitions, in chronological order, for each customer request. *  `attachment` returns the attachments for the customer request. *  `action` returns the actions that the user can or cannot perform on this customer request. *  `comment` returns the comments, if any, for each customer request. *  `comment.attachment` returns the attachment details, if any, for each comment. *  `comment.renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the customer requests, on the specified page of the results.
     */
    getCustomerRequests(
      params?: {
        /** Filters customer requests where the request summary matches the `searchTerm`. [Wildcards](https://confluence.atlassian.com/display/JIRACORECLOUD/Search+syntax+for+text+fields) can be used in the `searchTerm` parameter. */
        searchTerm?: string;
        /**
         * Filters customer requests using the following values:
         *
         *  *  `OWNED_REQUESTS` returns customer requests where the user is the creator.
         *  *  `PARTICIPATED_REQUESTS` returns customer requests where the user is a participant.
         *  *  `ORGANIZATION` returns customer requests for an organization of which the user is a member when used in conjunction with `organizationId`.
         *  *  `ALL_ORGANIZATIONS` returns customer requests that belong to all organizations of which the user is a member.
         *  *  `APPROVER` returns customer requests where the user is an approver. Can be used in conjunction with `approvalStatus` to filter pending or complete approvals.
         *  *  `ALL_REQUESTS` returns all customer requests. **Deprecated and will be removed, as the returned requests may change if more values are added in the future. Instead, explicitly list the desired filtering strategies.**
         *
         * Multiple values of the query parameter are supported. For example, `requestOwnership=OWNED_REQUESTS&requestOwnership=PARTICIPATED_REQUESTS` will only return customer requests where the user is the creator or a participant. If not specified, filtering defaults to `OWNED_REQUESTS`, `PARTICIPATED_REQUESTS`, and `ALL_ORGANIZATIONS`.
         */
        requestOwnership?: string[];
        /**
         * Filters customer requests where the request is closed, open, or either of the two where:
         *
         *  *  `CLOSED_REQUESTS` returns customer requests that are closed.
         *  *  `OPEN_REQUESTS` returns customer requests that are open.
         *  *  `ALL_REQUESTS` returns all customer requests.
         */
        requestStatus?: string;
        /**
         * Filters results to customer requests based on their approval status:
         *
         *  *  `MY_PENDING_APPROVAL` returns customer requests pending the user's approval.
         *  *  `MY_HISTORY_APPROVAL` returns customer requests where the user was an approver.
         *
         * **Note**: Valid only when used with requestOwnership=APPROVER.
         */
        approvalStatus?: string;
        /**
         * Filters customer requests that belong to a specific organization (note that the user must be a member of that organization). **Note**: Valid only when used with requestOwnership=ORGANIZATION.
         *
         * @format int32
         */
        organizationId?: number;
        /**
         * Filters customer requests by service desk.
         *
         * @format int32
         */
        serviceDeskId?: number;
        /**
         * Filters customer requests by request type. Note that the `serviceDeskId` must be specified for the service desk in which the request type belongs.
         *
         * @format int32
         */
        requestTypeId?: number;
        /**
         * A multi-value parameter indicating which properties of the customer request to expand, where:
         *
         *  *  `serviceDesk` returns additional details for each service desk.
         *  *  `requestType` returns additional details for each request type.
         *  *  `participant` returns the participant details, if any, for each customer request.
         *  *  `sla` returns the SLA information on each customer request.
         *  *  `status` returns the status transitions, in chronological order, for each customer request.
         *  *  `attachment` returns the attachments for the customer request.
         *  *  `action` returns the actions that the user can or cannot perform on this customer request.
         *  *  `comment` returns the comments, if any, for each customer request.
         *  *  `comment.attachment` returns the attachment details, if any, for each comment.
         *  *  `comment.renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
         */
        expand?: string[];
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoCustomerRequestDto> {
      return getCustomerRequests(ctx, params, options);
    },

    /**
     * Create customer request
     *
     * This method creates a customer request in a service desk.
     *
     * The JSON request must include the service desk and customer request type, as well as any fields that are required for the request type. A list of the fields required by a customer request type can be obtained using [servicedesk/\{serviceDeskId\}/requesttype/\{requestTypeId\}/field](#api-servicedesk-serviceDeskId-requesttype-requestTypeId-field-get).
     *
     * The fields required for a customer request type depend on the user's permissions:
     *
     *  *  `raiseOnBehalfOf` is not available to Users who have the customer permission only.
     *  *  `requestParticipants` is not available to Users who have the customer permission only or if the feature is turned off for customers.
     *
     * `requestFieldValues` is a map of Jira field IDs and their values. See [Field input formats](#fieldformats), for details of each field's JSON semantics and the values they can take.
     *
     * **[Permissions](#permissions) required**: Permission to create requests in the specified service desk.
     *
     * @returns Returned if the customer request was created.
     */
    createCustomerRequest(
      params: RequestCreateDto,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomerRequestDto> {
      return createCustomerRequest(ctx, params, options);
    },

    /**
     * Get customer request by id or key
     *
     * This method returns a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to access the specified service desk.
     *
     * **Response limitations**: For customers, only a request they created, was created on their behalf, or they are participating in will be returned.
     *
     * **Note:** `requestFieldValues` does not include hidden fields. To get a list of request type fields that includes hidden fields, see [/rest/servicedeskapi/servicedesk/\{serviceDeskId\}/requesttype/\{requestTypeId\}/field](https://developer.atlassian.com/cloud/jira/service-desk/rest/api-group-servicedesk/#api-rest-servicedeskapi-servicedesk-servicedeskid-requesttype-requesttypeid-field-get)
     *
     * @param params.issueIdOrKey - The ID or Key of the customer request to be returned
     * @param params.expand - A multi-value parameter indicating which properties of the customer request to expand, where: *  `serviceDesk` returns additional service desk details. *  `requestType` returns additional customer request type details. *  `participant` returns the participant details. *  `sla` returns the SLA information. *  `status` returns the status transitions, in chronological order. *  `attachment` returns the attachments. *  `action` returns the actions that the user can or cannot perform. *  `comment` returns the comments. *  `comment.attachment` returns the attachment details for each comment. *  `comment.renderedBody` (Experimental) return the rendered body in HTML format (in addition to the raw body) for each comment.
     * @returns Returns the customer request.
     */
    getCustomerRequestByIdOrKey(
      params: {
        /** The ID or Key of the customer request to be returned */
        issueIdOrKey: string;
        /**
         * A multi-value parameter indicating which properties of the customer request to expand, where:
         *
         *  *  `serviceDesk` returns additional service desk details.
         *  *  `requestType` returns additional customer request type details.
         *  *  `participant` returns the participant details.
         *  *  `sla` returns the SLA information.
         *  *  `status` returns the status transitions, in chronological order.
         *  *  `attachment` returns the attachments.
         *  *  `action` returns the actions that the user can or cannot perform.
         *  *  `comment` returns the comments.
         *  *  `comment.attachment` returns the attachment details for each comment.
         *  *  `comment.renderedBody` (Experimental) return the rendered body in HTML format (in addition to the raw body) for each comment.
         */
        expand?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomerRequestDto> {
      return getCustomerRequestByIdOrKey(ctx, params, options);
    },

    /**
     * Get approvals
     *
     * This method returns all approvals on a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be queried for its approvals.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of approvals to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the customer request's approvals.
     */
    getApprovals(
      params: {
        /** The ID or key of the customer request to be queried for its approvals. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of approvals to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoApprovalDto> {
      return getApprovals(ctx, params, options);
    },

    /**
     * Get approval by id
     *
     * This method returns an approval. Use this method to determine the status of an approval and the list of approvers.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request the approval is on.
     * @param params.approvalId - The ID of the approval to be returned.
     * @returns Returns the requested approval.
     */
    getApprovalById(
      params: {
        /** The ID or key of the customer request the approval is on. */
        issueIdOrKey: string;
        /**
         * The ID of the approval to be returned.
         *
         * @format int32
         */
        approvalId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ApprovalDto> {
      return getApprovalById(ctx, params, options);
    },

    /**
     * Answer approval
     *
     * This method enables a user to **Approve** or **Decline** an approval on a customer request. The approval is assumed to be owned by the user making the call.
     *
     * **[Permissions](#permissions) required**: User is assigned to the approval request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be updated.
     * @param params.approvalId - The ID of the approval to be updated.
     * @returns Returns the updated approval.
     */
    answerApproval(
      params: ApprovalDecisionRequestDto & {
        /** The ID or key of the customer request to be updated. */
        issueIdOrKey: string;
        /**
         * The ID of the approval to be updated.
         *
         * @format int32
         */
        approvalId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ApprovalDto> {
      return answerApproval(ctx, params, options);
    },

    /**
     * Get attachments for request
     *
     * This method returns all the attachments for a customer requests.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * **Response limitations**: Customers will only get a list of public attachments.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request from which the attachments will be listed.
     * @param params.start - The starting index of the returned attachment. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the visible attachments from the customer request.
     */
    getAttachmentsForRequest(
      params: {
        /** The ID or key of the customer request from which the attachments will be listed. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned attachment. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @default 0
         * @format int32
         */
        start: number;
        /**
         * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @default 50
         * @format int32
         */
        limit: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoAttachmentDto> {
      return getAttachmentsForRequest(ctx, params, options);
    },

    /**
     * Create comment with attachment
     *
     * This method creates a comment on a customer request using one or more attachment files (uploaded using [servicedeskapi/servicedesk/\{serviceDeskId\}/attachTemporaryFile](https://developer.atlassian.com/cloud/jira/service-desk/rest/api-group-servicedesk/#api-rest-servicedeskapi-servicedesk-servicedeskid-attachtemporaryfile-post)), with the visibility set by `public`. See
     *
     *  *  GET [servicedeskapi/request/\{issueIdOrKey\}/attachment](./#api-rest-servicedeskapi-request-issueidorkey-attachment-get)
     *  *  GET [servicedeskapi/request/\{issueIdOrKey\}/comment/\{commentId\}/attachment](./#api-rest-servicedeskapi-request-issueidorkey-comment-commentid-attachment-get)
     *
     * **[Permissions](#permissions) required**: Permission to add an attachment.
     *
     * **Request limitations**: Customers can set public visibility only.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to which the attachment will be added.
     * @returns Returns the attachments and comment.
     */
    createCommentWithAttachment(
      params: AttachmentCreateDto & {
        /** The ID or key of the customer request to which the attachment will be added. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<AttachmentCreateResultDto> {
      return createCommentWithAttachment(ctx, params, options);
    },

    /**
     * Get attachment content
     *
     * Returns the contents of an attachment.
     *
     * To return a thumbnail of the attachment, use [servicedeskapi/request/\{issueIdOrKey\}/attachment/\{attachmentId\}/thumbnail](./#api-rest-servicedeskapi-request-issueidorkey-attachment-attachmentid-thumbnail-get).
     *
     * **[Permissions](#permissions) required:** For the issue containing the attachment:
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key for the customer request the attachment is associated with
     * @param params.attachmentId - The ID for the attachment
     * @returns 200 response
     */
    getAttachmentContent(
      params: {
        /** The ID or key for the customer request the attachment is associated with */
        issueIdOrKey: string;
        /**
         * The ID for the attachment
         *
         * @format int64
         */
        attachmentId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getAttachmentContent(ctx, params, options);
    },

    /**
     * Get attachment thumbnail
     *
     * Returns the thumbnail of an attachment.
     *
     * To return the attachment contents, use [servicedeskapi/request/\{issueIdOrKey\}/attachment/\{attachmentId\}](#api-rest-servicedeskapi-request-issueidorkey-attachment-attachmentid-get).
     *
     * **[Permissions](#permissions) required:** For the issue containing the attachment:
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key for the customer request the attachment is associated with
     * @param params.attachmentId - The ID of the attachment.
     * @returns 200 response
     */
    getAttachmentThumbnail(
      params: {
        /** The ID or key for the customer request the attachment is associated with */
        issueIdOrKey: string;
        /**
         * The ID of the attachment.
         *
         * @format int64
         */
        attachmentId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getAttachmentThumbnail(ctx, params, options);
    },

    /**
     * Get request comments
     *
     * This method returns all comments on a customer request. No permissions error is provided if, for example, the user doesn't have access to the service desk or request, the method simply returns an empty response.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * **Response limitations**: Customers are returned public comments only.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request whose comments will be retrieved.
     * @param params.public_ - Specifies whether to return public comments or not. Default: true.
     * @param params.internal - Specifies whether to return internal comments or not. Default: true.
     * @param params.expand - A multi-value parameter indicating which properties of the comment to expand: *  `attachment` returns the attachment details, if any, for each comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).) *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
     * @param params.start - The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the comments, on the specified page of the results.
     */
    getRequestComments(
      params: {
        /** The ID or key of the customer request whose comments will be retrieved. */
        issueIdOrKey: string;
        /** Specifies whether to return public comments or not. Default: true. */
        public_?: boolean;
        /** Specifies whether to return internal comments or not. Default: true. */
        internal?: boolean;
        /**
         * A multi-value parameter indicating which properties of the comment to expand:
         *
         *  *  `attachment` returns the attachment details, if any, for each comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).)
         *  *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) for each comment.
         */
        expand?: string[];
        /**
         * The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoCommentDto> {
      return getRequestComments(ctx, params, options);
    },

    /**
     * Create request comment
     *
     * This method creates a public or private (internal) comment on a customer request, with the comment visibility set by `public`. The user recorded as the author of the comment.
     *
     * **[Permissions](#permissions) required**: User has Add Comments permission.
     *
     * **Request limitations**: Customers can set comments to public visibility only.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to which the comment will be added.
     * @returns Returns the comment.
     */
    createRequestComment(
      params: CommentCreateDto & {
        /** The ID or key of the customer request to which the comment will be added. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CommentDto> {
      return createRequestComment(ctx, params, options);
    },

    /**
     * Get request comment by id
     *
     * This method returns details of a customer request's comment.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * **Response limitations**: Customers can only view public comments on requests where they are the reporter or a participant whereas agents can see both internal and public comments.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request that contains the comment.
     * @param params.commentId - The ID of the comment to retrieve.
     * @param params.expand - A multi-value parameter indicating which properties of the comment to expand: *  `attachment` returns the attachment details, if any, for the comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).) *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) of the comment.
     * @returns Returns the comment.
     */
    getRequestCommentById(
      params: {
        /** The ID or key of the customer request that contains the comment. */
        issueIdOrKey: string;
        /**
         * The ID of the comment to retrieve.
         *
         * @format int64
         */
        commentId: number;
        /**
         * A multi-value parameter indicating which properties of the comment to expand:
         *
         *  *  `attachment` returns the attachment details, if any, for the comment. (If you want to get all attachments for a request, use [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-get).)
         *  *  `renderedBody` (Experimental) returns the rendered body in HTML format (in addition to the raw body) of the comment.
         */
        expand?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CommentDto> {
      return getRequestCommentById(ctx, params, options);
    },

    /**
     * Get comment attachments
     *
     * This method returns the attachments referenced in a comment.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * **Response limitations**: Customers can only view public comments, and retrieve their attachments, on requests where they are the reporter or a participant whereas agents can see both internal and public comments.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request that contains the comment.
     * @param params.commentId - The ID of the comment.
     * @param params.start - The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the attachments, on the specified page of the results.
     */
    getCommentAttachments(
      params: {
        /** The ID or key of the customer request that contains the comment. */
        issueIdOrKey: string;
        /**
         * The ID of the comment.
         *
         * @format int64
         */
        commentId: number;
        /**
         * The starting index of the returned comments. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of comments to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoAttachmentDto> {
      return getCommentAttachments(ctx, params, options);
    },

    /**
     * Get subscription status
     *
     * This method returns the notification subscription status of the user making the request. Use this method to determine if the user is subscribed to a customer request's notifications.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be queried for subscription status.
     * @returns Returns the status of the notification subscription.
     */
    getSubscriptionStatus(
      params: {
        /** The ID or key of the customer request to be queried for subscription status. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RequestNotificationSubscriptionDto> {
      return getSubscriptionStatus(ctx, params, options);
    },

    /**
     * Subscribe
     *
     * This method subscribes the user to receiving notifications from a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be subscribed to.
     * @returns Returns if the user was subscribed.
     */
    subscribe(
      params: {
        /** The ID or key of the customer request to be subscribed to. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return subscribe(ctx, params, options);
    },

    /**
     * Unsubscribe
     *
     * This method unsubscribes the user from notifications from a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be unsubscribed from.
     * @returns Returns if the user was unsubscribed.
     */
    unsubscribe(
      params: {
        /** The ID or key of the customer request to be unsubscribed from. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return unsubscribe(ctx, params, options);
    },

    /**
     * Get request participants
     *
     * This method returns a list of all the participants on a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be queried for its participants.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the customer request's participants, on the specified page of the results.
     */
    getRequestParticipants(
      params: {
        /** The ID or key of the customer request to be queried for its participants. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoUserDto> {
      return getRequestParticipants(ctx, params, options);
    },

    /**
     * Add request participants
     *
     * This method adds participants to a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to manage participants on the customer request.
     *
     * Note, participants can be added when creating a customer request using the [request](#api-request-post) resource, by defining the participants in the `requestParticipants` field.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to have participants added.
     * @returns Returns the participants added to the customer request.
     */
    addRequestParticipants(
      params: RequestParticipantUpdateDto & {
        /** The ID or key of the customer request to have participants added. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoUserDto> {
      return addRequestParticipants(ctx, params, options);
    },

    /**
     * Remove request participants
     *
     * This method removes participants from a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to manage participants on the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to have participants removed.
     * @returns Returns the first page of the customer request's participants (after removal of the users).
     */
    removeRequestParticipants(
      params: RequestParticipantUpdateDto & {
        /** The ID or key of the customer request to have participants removed. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoUserDto> {
      return removeRequestParticipants(ctx, params, options);
    },

    /**
     * Get sla information
     *
     * This method returns all the SLA records on a customer request. A customer request can have zero or more SLAs. Each SLA can have recordings for zero or more "completed cycles" and zero or 1 "ongoing cycle". Each cycle includes information on when it started and stopped, and whether it breached the SLA goal.
     *
     * **[Permissions](#permissions) required**:
     *
     *  *  Agent for the Service Desk containing the queried customer request, AND
     *  *  Browse Projects permission on the project containing the customer request, including any restrictions imposed by issue security schemes or custom permission schemes on the specific issue.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request whose SLAs will be retrieved.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the SLA records on the customer request, on the specified page of the results.
     */
    getSlaInformation(
      params: {
        /** The ID or key of the customer request whose SLAs will be retrieved. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of request types to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoSlaInformationDto> {
      return getSlaInformation(ctx, params, options);
    },

    /**
     * Get sla information by id
     *
     * This method returns the details for an SLA on a customer request.
     *
     * **[Permissions](#permissions) required**:
     *
     *  *  Agent for the Service Desk containing the queried customer request, AND
     *  *  Browse Projects permission on the project containing the customer request, including any restrictions imposed by issue security schemes or custom permission schemes on the specific issue.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request whose SLAs will be retrieved.
     * @param params.slaMetricId - The ID or key of the SLAs metric to be retrieved.
     * @returns Returns the SLA record, on the specified page of the results.
     */
    getSlaInformationById(
      params: {
        /** The ID or key of the customer request whose SLAs will be retrieved. */
        issueIdOrKey: string;
        /**
         * The ID or key of the SLAs metric to be retrieved.
         *
         * @format int64
         */
        slaMetricId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SlaInformationDto> {
      return getSlaInformationById(ctx, params, options);
    },

    /**
     * Get customer request status
     *
     * This method returns a list of all the statuses a customer Request has achieved. A status represents the state of an issue in its workflow. An issue can have one active status only. The list returns the status history in chronological order, most recent (current) status first.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request to be retrieved.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the customer request's status history, on the specified page of the results.
     */
    getCustomerRequestStatus(
      params: {
        /** The ID or key of the customer request to be retrieved. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoCustomerRequestStatusDto> {
      return getCustomerRequestStatus(ctx, params, options);
    },

    /**
     * Get customer transitions
     *
     * This method returns a list of transitions, the workflow processes that moves a customer request from one status to another, that the user can perform on a request. Use this method to provide a user with a list if the actions they can take on a customer request.
     *
     * **[Permissions](#permissions) required**: Permission to view the customer request.
     *
     * @param params.issueIdOrKey - The ID or key of the customer request whose transitions will be retrieved.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the transitions available to the user on the customer request.
     */
    getCustomerTransitions(
      params: {
        /** The ID or key of the customer request whose transitions will be retrieved. */
        issueIdOrKey: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoCustomerTransitionDto> {
      return getCustomerTransitions(ctx, params, options);
    },

    /**
     * Perform customer transition
     *
     * This method performs a customer transition for a given request and transition. An optional comment can be included to provide a reason for the transition.
     *
     * **[Permissions](#permissions) required**: The user must be able to view the request and have the Transition Issues permission. If a comment is passed the user must have the Add Comments permission.
     *
     * @param params.issueIdOrKey - ID or key of the issue to transition
     * @returns Returned if the request is transitioned.
     */
    performCustomerTransition(
      params: CustomerTransitionExecutionDto & {
        /** ID or key of the issue to transition */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return performCustomerTransition(ctx, params, options);
    },

    /**
     * Get feedback
     *
     * This method retrieves a feedback of a request using it's `requestKey` or `requestId`
     *
     * **[Permissions](#permissions) required**: User has view request permissions.
     *
     * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
     * @returns Returns the comment.
     */
    getFeedback(
      params: {
        /** The id or the key of the request to post the feedback on */
        requestIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CsatFeedbackFullDto> {
      return getFeedback(ctx, params, options);
    },

    /**
     * Post feedback
     *
     * This method adds a feedback on an request using it's `requestKey` or `requestId`
     *
     * **[Permissions](#permissions) required**: User must be the reporter or an Atlassian Connect app.
     *
     * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
     * @returns Returns the comment.
     */
    postFeedback(
      params: CsatFeedbackFullDto & {
        /** The id or the key of the request to post the feedback on */
        requestIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CsatFeedbackFullDto> {
      return postFeedback(ctx, params, options);
    },

    /**
     * Delete feedback
     *
     * This method deletes the feedback of request using it's `requestKey` or `requestId`
     *
     * **[Permissions](#permissions) required**: User must be the reporter or an Atlassian Connect app.
     *
     * @param params.requestIdOrKey - The id or the key of the request to post the feedback on
     * @returns No content.
     */
    deleteFeedback(
      params: {
        /** The id or the key of the request to post the feedback on */
        requestIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteFeedback(ctx, params, options);
    },
  };
}
