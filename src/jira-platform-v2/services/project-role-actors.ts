// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  ActorInputBean,
  ActorsMap,
  ProjectRole,
  ProjectRoleActorsUpdateBean,
} from "../types/index.ts";

/**
 * Set actors for project role
 *
 * Sets the actors for a project role for a project, replacing all existing actors.
 *
 * To add actors to the project without overwriting the existing list, use [Add actors to project role](#api-rest-api-2-project-projectIdOrKey-role-id-post).
 *
 * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.projectIdOrKey - The project ID or project key (case sensitive).
 * @param params.id_path - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @returns Returned if the request is successful. The complete list of actors for the project is returned.
 */
export function setActors(
  ctx: ClientContext,
  params: ProjectRoleActorsUpdateBean & {
    /** The project ID or project key (case sensitive). */
    projectIdOrKey: string;
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id_path: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectRole> {
  const { projectIdOrKey, id_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/project/{projectIdOrKey}/role/{id}",
    pathParams: { projectIdOrKey, id: id_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add actors to project role
 *
 * Adds actors to a project role for the project.
 *
 * To replace all actors for the project, use [Set actors for project role](#api-rest-api-2-project-projectIdOrKey-role-id-put).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.projectIdOrKey - The project ID or project key (case sensitive).
 * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @returns Returned if the request is successful. The complete list of actors for the project is returned. For example, the cURL request above adds a group, *jira-developers*. For the response below to be returned as a result of that request, the user *Mia Krystof* would have previously been added as a `user` actor for this project.
 */
export function addActorUsers(
  ctx: ClientContext,
  params: ActorsMap & {
    /** The project ID or project key (case sensitive). */
    projectIdOrKey: string;
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectRole> {
  const { projectIdOrKey, id, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/project/{projectIdOrKey}/role/{id}",
    pathParams: { projectIdOrKey, id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete actors from project role
 *
 * Deletes actors from a project role for the project.
 *
 * To remove default actors from the project role, use [Delete default actors from project role](#api-rest-api-2-role-id-actors-delete).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.projectIdOrKey - The project ID or project key (case sensitive).
 * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @param params.user - The user account ID of the user to remove from the project role.
 * @param params.group - The name of the group to remove from the project role. This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended.
 * @param params.groupId - The ID of the group to remove from the project role. This parameter cannot be used with the `group` parameter.
 * @returns Returned if the request is successful.
 */
export function deleteActor(
  ctx: ClientContext,
  params: {
    /** The project ID or project key (case sensitive). */
    projectIdOrKey: string;
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id: number;
    /**
     * The user account ID of the user to remove from the project role.
     *
     * @example
     * ```json
     * "5b10ac8d82e05b22cc7d4ef5"
     * ```
     */
    user?: string;
    /** The name of the group to remove from the project role. This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended. */
    group?: string;
    /** The ID of the group to remove from the project role. This parameter cannot be used with the `group` parameter. */
    groupId?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { projectIdOrKey, id, user, group, groupId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/project/{projectIdOrKey}/role/{id}",
    pathParams: { projectIdOrKey, id },
    query: { user, group, groupId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get default actors for project role
 *
 * Returns the [default actors](#api-rest-api-2-resolution-get) for the project role.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @returns Returned if the request is successful.
 */
export function getProjectRoleActorsForRole(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectRole> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/role/{id}/actors",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add default actors to project role
 *
 * Adds [default actors](#api-rest-api-2-resolution-get) to a role. You may add groups or users, but you cannot add groups and users in the same request.
 *
 * Changing a project role's default actors does not affect project role members for projects already created.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @returns Returned if the request is successful.
 */
export function addProjectRoleActorsToRole(
  ctx: ClientContext,
  params: ActorInputBean & {
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectRole> {
  const { id, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/role/{id}/actors",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete default actors from project role
 *
 * Deletes the [default actors](#api-rest-api-2-resolution-get) from a project role. You may delete a group or user, but you cannot delete a group and a user in the same request.
 *
 * Changing a project role's default actors does not affect project role members for projects already created.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
 * @param params.user - The user account ID of the user to remove as a default actor.
 * @param params.groupId - The group ID of the group to be removed as a default actor. This parameter cannot be used with the `group` parameter.
 * @param params.group - The group name of the group to be removed as a default actor.This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended.
 * @returns Returned if the request is successful.
 */
export function deleteProjectRoleActorsFromRole(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     *
     * @format int64
     */
    id: number;
    /**
     * The user account ID of the user to remove as a default actor.
     *
     * @example
     * ```json
     * "5b10ac8d82e05b22cc7d4ef5"
     * ```
     */
    user?: string;
    /** The group ID of the group to be removed as a default actor. This parameter cannot be used with the `group` parameter. */
    groupId?: string;
    /** The group name of the group to be removed as a default actor.This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended. */
    group?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ProjectRole> {
  const { id, user, groupId, group } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/role/{id}/actors",
    pathParams: { id },
    query: { user, groupId, group },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents the users assigned to [project roles](#api-group-Issue-comments). Use it to get, add, and remove default users from project roles. Also use it to add and remove users from a project role associated with a project. */
export function createProjectRoleActorsService(ctx: ClientContext) {
  return {
    /**
     * Set actors for project role
     *
     * Sets the actors for a project role for a project, replacing all existing actors.
     *
     * To add actors to the project without overwriting the existing list, use [Add actors to project role](#api-rest-api-2-project-projectIdOrKey-role-id-post).
     *
     * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.projectIdOrKey - The project ID or project key (case sensitive).
     * @param params.id_path - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @returns Returned if the request is successful. The complete list of actors for the project is returned.
     */
    setActors(
      params: ProjectRoleActorsUpdateBean & {
        /** The project ID or project key (case sensitive). */
        projectIdOrKey: string;
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id_path: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectRole> {
      return setActors(ctx, params, options);
    },

    /**
     * Add actors to project role
     *
     * Adds actors to a project role for the project.
     *
     * To replace all actors for the project, use [Set actors for project role](#api-rest-api-2-project-projectIdOrKey-role-id-put).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.projectIdOrKey - The project ID or project key (case sensitive).
     * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @returns Returned if the request is successful. The complete list of actors for the project is returned. For example, the cURL request above adds a group, *jira-developers*. For the response below to be returned as a result of that request, the user *Mia Krystof* would have previously been added as a `user` actor for this project.
     */
    addActorUsers(
      params: ActorsMap & {
        /** The project ID or project key (case sensitive). */
        projectIdOrKey: string;
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectRole> {
      return addActorUsers(ctx, params, options);
    },

    /**
     * Delete actors from project role
     *
     * Deletes actors from a project role for the project.
     *
     * To remove default actors from the project role, use [Delete default actors from project role](#api-rest-api-2-role-id-actors-delete).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Administer Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project or *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.projectIdOrKey - The project ID or project key (case sensitive).
     * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @param params.user - The user account ID of the user to remove from the project role.
     * @param params.group - The name of the group to remove from the project role. This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended.
     * @param params.groupId - The ID of the group to remove from the project role. This parameter cannot be used with the `group` parameter.
     * @returns Returned if the request is successful.
     */
    deleteActor(
      params: {
        /** The project ID or project key (case sensitive). */
        projectIdOrKey: string;
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id: number;
        /**
         * The user account ID of the user to remove from the project role.
         *
         * @example
         * ```json
         * "5b10ac8d82e05b22cc7d4ef5"
         * ```
         */
        user?: string;
        /** The name of the group to remove from the project role. This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended. */
        group?: string;
        /** The ID of the group to remove from the project role. This parameter cannot be used with the `group` parameter. */
        groupId?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteActor(ctx, params, options);
    },

    /**
     * Get default actors for project role
     *
     * Returns the [default actors](#api-rest-api-2-resolution-get) for the project role.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @returns Returned if the request is successful.
     */
    getProjectRoleActorsForRole(
      params: {
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectRole> {
      return getProjectRoleActorsForRole(ctx, params, options);
    },

    /**
     * Add default actors to project role
     *
     * Adds [default actors](#api-rest-api-2-resolution-get) to a role. You may add groups or users, but you cannot add groups and users in the same request.
     *
     * Changing a project role's default actors does not affect project role members for projects already created.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @returns Returned if the request is successful.
     */
    addProjectRoleActorsToRole(
      params: ActorInputBean & {
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectRole> {
      return addProjectRoleActorsToRole(ctx, params, options);
    },

    /**
     * Delete default actors from project role
     *
     * Deletes the [default actors](#api-rest-api-2-resolution-get) from a project role. You may delete a group or user, but you cannot delete a group and a user in the same request.
     *
     * Changing a project role's default actors does not affect project role members for projects already created.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
     * @param params.user - The user account ID of the user to remove as a default actor.
     * @param params.groupId - The group ID of the group to be removed as a default actor. This parameter cannot be used with the `group` parameter.
     * @param params.group - The group name of the group to be removed as a default actor.This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended.
     * @returns Returned if the request is successful.
     */
    deleteProjectRoleActorsFromRole(
      params: {
        /**
         * The ID of the project role. Use [Get all project roles](#api-rest-api-2-role-get) to get a list of project role IDs.
         *
         * @format int64
         */
        id: number;
        /**
         * The user account ID of the user to remove as a default actor.
         *
         * @example
         * ```json
         * "5b10ac8d82e05b22cc7d4ef5"
         * ```
         */
        user?: string;
        /** The group ID of the group to be removed as a default actor. This parameter cannot be used with the `group` parameter. */
        groupId?: string;
        /** The group name of the group to be removed as a default actor.This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended. */
        group?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ProjectRole> {
      return deleteProjectRoleActorsFromRole(ctx, params, options);
    },
  };
}
