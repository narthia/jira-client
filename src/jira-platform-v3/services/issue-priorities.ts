// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreatePriorityDetails,
  PageBeanPriority,
  Priority,
  PriorityId,
  ReorderIssuePriorities,
  SetDefaultPriorityRequest,
  UpdatePriorityDetails,
} from "../types/index.ts";

/**
 * Get priorities
 *
 * Returns the list of all issue priorities.
 *
 * **Deprecated:** Use [Search priorities](#api-rest-api-3-priority-search-get) instead. **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @returns Returned if the request is successful.
 * @deprecated
 */
export function getPriorities(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Priority[]> {
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priority",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create priority
 *
 * Creates an issue priority.
 *
 * **Deprecation notice:** The `iconUrl` parameter was sunset on 16th Mar 2025, and replaced with `avatarId`. See [CHANGE-1525](https://developer.atlassian.com/changelog/#CHANGE-1525).
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createPriority(
  ctx: ClientContext,
  params: CreatePriorityDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PriorityId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/priority",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set default priority
 *
 * Sets default issue priority.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function setDefaultPriority(
  ctx: ClientContext,
  params: SetDefaultPriorityRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/3/priority/default",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move priorities
 *
 * Changes the order of issue priorities.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function movePriorities(
  ctx: ClientContext,
  params: ReorderIssuePriorities,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/3/priority/move",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Search priorities
 *
 * Returns a [paginated](#pagination) list of priorities. The list can contain all priorities or a subset determined by any combination of these criteria:
 *
 *  *  a list of priority IDs. Any invalid priority IDs are ignored.
 *  *  a list of project IDs. Only priorities that are available in these projects will be returned. Any invalid project IDs are ignored.
 *  *  whether the field configuration is a default. This returns priorities from company-managed (classic) projects only, as there is no concept of default priorities in team-managed projects.
 *
 * **Deprecation notice:** The `onlyDefault` parameter is deprecated and will be removed at a later date. See [CHANGE-1655](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-1655).
 *
 * **Deprecation notice:** The `isDefault` property of priorities is deprecated and will be removed at a later date. See [CHANGE-1655](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-1655).
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of priority IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=2&id=3`.
 * @param params.projectId - The list of projects IDs. To include multiple IDs, provide an ampersand-separated list. For example, `projectId=10010&projectId=10111`.
 * @param params.priorityName - The name of priority to search for.
 * @param params.onlyDefault - Whether only the default priority is returned.
 * @param params.expand - Use `schemes` to return the associated priority schemes for each priority. Limited to returning first 15 priority schemes per priority.
 * @returns Returned if the request is successful.
 */
export function searchPriorities(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default "0"
     */
    startAt?: string;
    /**
     * The maximum number of items to return per page.
     *
     * @default "50"
     */
    maxResults?: string;
    /** The list of priority IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=2&id=3`. */
    id?: string[];
    /** The list of projects IDs. To include multiple IDs, provide an ampersand-separated list. For example, `projectId=10010&projectId=10111`. */
    projectId?: string[];
    /**
     * The name of priority to search for.
     *
     * @default ""
     */
    priorityName?: string;
    /**
     * Whether only the default priority is returned.
     *
     * @default false
     */
    onlyDefault?: boolean;
    /**
     * Use `schemes` to return the associated priority schemes for each priority. Limited to returning first 15 priority schemes per priority.
     *
     * @default ""
     */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanPriority> {
  const { startAt, maxResults, id, projectId, priorityName, onlyDefault, expand } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priority/search",
    query: { startAt, maxResults, id, projectId, priorityName, onlyDefault, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get priority
 *
 * Returns an issue priority. To fetch multiple priorities at once, use [Search priorities](#api-rest-api-3-priority-search-get) instead.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @param params.id - The ID of the issue priority.
 * @returns Returned if the request is successful.
 */
export function getPriority(
  ctx: ClientContext,
  params: {
    /** The ID of the issue priority. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<Priority> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/priority/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update priority
 *
 * Updates an issue priority.
 *
 * At least one request body parameter must be defined.
 *
 * **Deprecation notice:** The `iconUrl` parameter was sunset on 16th Mar 2025, and replaced with `avatarId`. See [CHANGE-1525](https://developer.atlassian.com/changelog/#CHANGE-1525).
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue priority.
 * @returns Returned if the request is successful.
 */
export function updatePriority(
  ctx: ClientContext,
  params: UpdatePriorityDetails & {
    /** The ID of the issue priority. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/priority/{id}",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete priority
 *
 * Deletes an issue priority.
 *
 * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain subsequent updates.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the issue priority.
 */
export function deletePriority(
  ctx: ClientContext,
  params: {
    /** The ID of the issue priority. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/priority/{id}",
    pathParams: { id },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents issue priorities. Use it to get, create and update issue priorities and details for individual issue priorities. */
export function createIssuePrioritiesService(ctx: ClientContext) {
  return {
    /**
     * Get priorities
     *
     * Returns the list of all issue priorities.
     *
     * **Deprecated:** Use [Search priorities](#api-rest-api-3-priority-search-get) instead. **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @returns Returned if the request is successful.
     * @deprecated
     */
    getPriorities(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<Priority[]> {
      return getPriorities(ctx, options);
    },

    /**
     * Create priority
     *
     * Creates an issue priority.
     *
     * **Deprecation notice:** The `iconUrl` parameter was sunset on 16th Mar 2025, and replaced with `avatarId`. See [CHANGE-1525](https://developer.atlassian.com/changelog/#CHANGE-1525).
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createPriority(
      params: CreatePriorityDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PriorityId> {
      return createPriority(ctx, params, options);
    },

    /**
     * Set default priority
     *
     * Sets default issue priority.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    setDefaultPriority(
      params: SetDefaultPriorityRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setDefaultPriority(ctx, params, options);
    },

    /**
     * Move priorities
     *
     * Changes the order of issue priorities.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    movePriorities(
      params: ReorderIssuePriorities,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return movePriorities(ctx, params, options);
    },

    /**
     * Search priorities
     *
     * Returns a [paginated](#pagination) list of priorities. The list can contain all priorities or a subset determined by any combination of these criteria:
     *
     *  *  a list of priority IDs. Any invalid priority IDs are ignored.
     *  *  a list of project IDs. Only priorities that are available in these projects will be returned. Any invalid project IDs are ignored.
     *  *  whether the field configuration is a default. This returns priorities from company-managed (classic) projects only, as there is no concept of default priorities in team-managed projects.
     *
     * **Deprecation notice:** The `onlyDefault` parameter is deprecated and will be removed at a later date. See [CHANGE-1655](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-1655).
     *
     * **Deprecation notice:** The `isDefault` property of priorities is deprecated and will be removed at a later date. See [CHANGE-1655](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-1655).
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of priority IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=2&id=3`.
     * @param params.projectId - The list of projects IDs. To include multiple IDs, provide an ampersand-separated list. For example, `projectId=10010&projectId=10111`.
     * @param params.priorityName - The name of priority to search for.
     * @param params.onlyDefault - Whether only the default priority is returned.
     * @param params.expand - Use `schemes` to return the associated priority schemes for each priority. Limited to returning first 15 priority schemes per priority.
     * @returns Returned if the request is successful.
     */
    searchPriorities(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default "0"
         */
        startAt?: string;
        /**
         * The maximum number of items to return per page.
         *
         * @default "50"
         */
        maxResults?: string;
        /** The list of priority IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=2&id=3`. */
        id?: string[];
        /** The list of projects IDs. To include multiple IDs, provide an ampersand-separated list. For example, `projectId=10010&projectId=10111`. */
        projectId?: string[];
        /**
         * The name of priority to search for.
         *
         * @default ""
         */
        priorityName?: string;
        /**
         * Whether only the default priority is returned.
         *
         * @default false
         */
        onlyDefault?: boolean;
        /**
         * Use `schemes` to return the associated priority schemes for each priority. Limited to returning first 15 priority schemes per priority.
         *
         * @default ""
         */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanPriority> {
      return searchPriorities(ctx, params, options);
    },

    /**
     * Get priority
     *
     * Returns an issue priority. To fetch multiple priorities at once, use [Search priorities](#api-rest-api-3-priority-search-get) instead.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @param params.id - The ID of the issue priority.
     * @returns Returned if the request is successful.
     */
    getPriority(
      params: {
        /** The ID of the issue priority. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<Priority> {
      return getPriority(ctx, params, options);
    },

    /**
     * Update priority
     *
     * Updates an issue priority.
     *
     * At least one request body parameter must be defined.
     *
     * **Deprecation notice:** The `iconUrl` parameter was sunset on 16th Mar 2025, and replaced with `avatarId`. See [CHANGE-1525](https://developer.atlassian.com/changelog/#CHANGE-1525).
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue priority.
     * @returns Returned if the request is successful.
     */
    updatePriority(
      params: UpdatePriorityDetails & {
        /** The ID of the issue priority. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updatePriority(ctx, params, options);
    },

    /**
     * Delete priority
     *
     * Deletes an issue priority.
     *
     * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain subsequent updates.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the issue priority.
     */
    deletePriority(
      params: {
        /** The ID of the issue priority. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deletePriority(ctx, params, options);
    },
  };
}
