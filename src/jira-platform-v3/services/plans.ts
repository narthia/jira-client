// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreatePlanRequest,
  DuplicatePlanRequest,
  GetPlanResponse,
  PageWithCursorGetPlanResponseForPage,
} from "../types/index.ts";

/**
 * Get plans paginated
 *
 * Returns a [paginated](#pagination) list of plans.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.includeTrashed - Whether to include trashed plans in the results.
 * @param params.includeArchived - Whether to include archived plans in the results.
 * @param params.cursor - The cursor to start from. If not provided, the first page will be returned.
 * @param params.maxResults - The maximum number of plans to return per page. The maximum value is 50. The default value is 50.
 * @returns Returned if the request is successful.
 */
export function getPlans(
  ctx: ClientContext,
  params?: {
    /**
     * Whether to include trashed plans in the results.
     *
     * @default false
     */
    includeTrashed?: boolean;
    /**
     * Whether to include archived plans in the results.
     *
     * @default false
     */
    includeArchived?: boolean;
    /**
     * The cursor to start from. If not provided, the first page will be returned.
     *
     * @default ""
     */
    cursor?: string;
    /**
     * The maximum number of plans to return per page. The maximum value is 50. The default value is 50.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageWithCursorGetPlanResponseForPage> {
  const { includeTrashed, includeArchived, cursor, maxResults } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/plans/plan",
    query: { includeTrashed, includeArchived, cursor, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create plan
 *
 * Creates a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.useGroupId - Whether to accept group IDs instead of group names. Group names are deprecated.
 * @returns Returned if the request is successful.
 */
export function createPlan(
  ctx: ClientContext,
  params: CreatePlanRequest & {
    /**
     * Whether to accept group IDs instead of group names. Group names are deprecated.
     *
     * @default false
     */
    useGroupId?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<number> {
  const { useGroupId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/plans/plan",
    query: { useGroupId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get plan
 *
 * Returns a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.useGroupId - Whether to return group IDs instead of group names. Group names are deprecated.
 * @returns Returned if the request is successful.
 */
export function getPlan(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * Whether to return group IDs instead of group names. Group names are deprecated.
     *
     * @default false
     */
    useGroupId?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<GetPlanResponse> {
  const { planId, useGroupId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/plans/plan/{planId}",
    pathParams: { planId },
    query: { useGroupId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update plan
 *
 * Updates any of the following details of a plan using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
 *
 *  *  name
 *  *  leadAccountId
 *  *  scheduling
 *
 *      *  estimation with StoryPoints, Days or Hours as possible values
 *      *  startDate
 *
 *          *  type with DueDate, TargetStartDate, TargetEndDate or DateCustomField as possible values
 *          *  dateCustomFieldId
 *      *  endDate
 *
 *          *  type with DueDate, TargetStartDate, TargetEndDate or DateCustomField as possible values
 *          *  dateCustomFieldId
 *      *  inferredDates with None, SprintDates or ReleaseDates as possible values
 *      *  dependencies with Sequential or Concurrent as possible values
 *  *  issueSources
 *
 *      *  type with Board, Project or Filter as possible values
 *      *  value
 *  *  exclusionRules
 *
 *      *  numberOfDaysToShowCompletedIssues
 *      *  issueIds
 *      *  workStatusIds
 *      *  workStatusCategoryIds
 *      *  issueTypeIds
 *      *  releaseIds
 *  *  crossProjectReleases
 *
 *      *  name
 *      *  releaseIds
 *  *  customFields
 *
 *      *  customFieldId
 *      *  filter
 *  *  permissions
 *
 *      *  type with View or Edit as possible values
 *      *  holder
 *
 *          *  type with Group or AccountId as possible values
 *          *  value
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * *Note that "add" operations do not respect array indexes in target locations. Call the "Get plan" endpoint to find out the order of array elements.*
 *
 * @param params.planId - The ID of the plan.
 * @param params.useGroupId - Whether to accept group IDs instead of group names. Group names are deprecated.
 * @returns Returned if the request is successful.
 */
export function updatePlan(
  ctx: ClientContext,
  params: Record<string, unknown> & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * Whether to accept group IDs instead of group names. Group names are deprecated.
     *
     * @default false
     */
    useGroupId?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, useGroupId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/plans/plan/{planId}",
    pathParams: { planId },
    query: { useGroupId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Archive plan
 *
 * Archives a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @returns Returned if the request is successful.
 */
export function archivePlan(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/plans/plan/{planId}/archive",
    pathParams: { planId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Duplicate plan
 *
 * Duplicates a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @returns Returned if the request is successful.
 */
export function duplicatePlan(
  ctx: ClientContext,
  params: DuplicatePlanRequest & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<number> {
  const { planId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/plans/plan/{planId}/duplicate",
    pathParams: { planId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Trash plan
 *
 * Moves a plan to trash.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @returns Returned if the request is successful.
 */
export function trashPlan(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/plans/plan/{planId}/trash",
    pathParams: { planId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents plans. Use it to get, create, duplicate, update, trash and archive plans. */
export function createPlansService(ctx: ClientContext) {
  return {
    /**
     * Get plans paginated
     *
     * Returns a [paginated](#pagination) list of plans.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.includeTrashed - Whether to include trashed plans in the results.
     * @param params.includeArchived - Whether to include archived plans in the results.
     * @param params.cursor - The cursor to start from. If not provided, the first page will be returned.
     * @param params.maxResults - The maximum number of plans to return per page. The maximum value is 50. The default value is 50.
     * @returns Returned if the request is successful.
     */
    getPlans(
      params?: {
        /**
         * Whether to include trashed plans in the results.
         *
         * @default false
         */
        includeTrashed?: boolean;
        /**
         * Whether to include archived plans in the results.
         *
         * @default false
         */
        includeArchived?: boolean;
        /**
         * The cursor to start from. If not provided, the first page will be returned.
         *
         * @default ""
         */
        cursor?: string;
        /**
         * The maximum number of plans to return per page. The maximum value is 50. The default value is 50.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageWithCursorGetPlanResponseForPage> {
      return getPlans(ctx, params, options);
    },

    /**
     * Create plan
     *
     * Creates a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.useGroupId - Whether to accept group IDs instead of group names. Group names are deprecated.
     * @returns Returned if the request is successful.
     */
    createPlan(
      params: CreatePlanRequest & {
        /**
         * Whether to accept group IDs instead of group names. Group names are deprecated.
         *
         * @default false
         */
        useGroupId?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<number> {
      return createPlan(ctx, params, options);
    },

    /**
     * Get plan
     *
     * Returns a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.useGroupId - Whether to return group IDs instead of group names. Group names are deprecated.
     * @returns Returned if the request is successful.
     */
    getPlan(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * Whether to return group IDs instead of group names. Group names are deprecated.
         *
         * @default false
         */
        useGroupId?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<GetPlanResponse> {
      return getPlan(ctx, params, options);
    },

    /**
     * Update plan
     *
     * Updates any of the following details of a plan using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
     *
     *  *  name
     *  *  leadAccountId
     *  *  scheduling
     *
     *      *  estimation with StoryPoints, Days or Hours as possible values
     *      *  startDate
     *
     *          *  type with DueDate, TargetStartDate, TargetEndDate or DateCustomField as possible values
     *          *  dateCustomFieldId
     *      *  endDate
     *
     *          *  type with DueDate, TargetStartDate, TargetEndDate or DateCustomField as possible values
     *          *  dateCustomFieldId
     *      *  inferredDates with None, SprintDates or ReleaseDates as possible values
     *      *  dependencies with Sequential or Concurrent as possible values
     *  *  issueSources
     *
     *      *  type with Board, Project or Filter as possible values
     *      *  value
     *  *  exclusionRules
     *
     *      *  numberOfDaysToShowCompletedIssues
     *      *  issueIds
     *      *  workStatusIds
     *      *  workStatusCategoryIds
     *      *  issueTypeIds
     *      *  releaseIds
     *  *  crossProjectReleases
     *
     *      *  name
     *      *  releaseIds
     *  *  customFields
     *
     *      *  customFieldId
     *      *  filter
     *  *  permissions
     *
     *      *  type with View or Edit as possible values
     *      *  holder
     *
     *          *  type with Group or AccountId as possible values
     *          *  value
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * *Note that "add" operations do not respect array indexes in target locations. Call the "Get plan" endpoint to find out the order of array elements.*
     *
     * @param params.planId - The ID of the plan.
     * @param params.useGroupId - Whether to accept group IDs instead of group names. Group names are deprecated.
     * @returns Returned if the request is successful.
     */
    updatePlan(
      params: Record<string, unknown> & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * Whether to accept group IDs instead of group names. Group names are deprecated.
         *
         * @default false
         */
        useGroupId?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updatePlan(ctx, params, options);
    },

    /**
     * Archive plan
     *
     * Archives a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @returns Returned if the request is successful.
     */
    archivePlan(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return archivePlan(ctx, params, options);
    },

    /**
     * Duplicate plan
     *
     * Duplicates a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @returns Returned if the request is successful.
     */
    duplicatePlan(
      params: DuplicatePlanRequest & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<number> {
      return duplicatePlan(ctx, params, options);
    },

    /**
     * Trash plan
     *
     * Moves a plan to trash.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @returns Returned if the request is successful.
     */
    trashPlan(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return trashPlan(ctx, params, options);
    },
  };
}
