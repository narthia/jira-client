// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  MoveIssuesToBacklogForBoardBody,
  SoftwareIssueResults,
  UpdateSprintBody,
} from "../types/index.ts";

/**
 * Create sprint
 *
 * Creates a future sprint. Sprint name and origin board id are required. Start date, end date, and goal are optional.
 *
 * Note that the sprint name is trimmed. Also, when starting sprints from the UI, the "endDate" set through this call is ignored and instead the last sprint's duration is used to fill the form.
 *
 * @returns Created sprint
 */
export function createSprint(
  ctx: ClientContext,
  params: {
    endDate?: string;
    goal?: string;
    name?: string;
    /** @format int64 */
    originBoardId?: number;
    startDate?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/sprint",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get sprint
 *
 * Returns the sprint for a given sprint ID. The sprint will only be returned if the user can view the board that the sprint was created on, or view at least one of the issues in the sprint.
 *
 * @param params.sprintId - The ID of the requested sprint.
 * @returns Returns the requested sprint.
 */
export function getSprint(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the requested sprint.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/sprint/{sprintId}",
    pathParams: { sprintId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update sprint
 *
 * Performs a full update of a sprint. A full update means that the result will be exactly the same as the request body. Any fields not present in the request JSON will be set to null.
 *
 * Notes:
 *
 *  *  For closed sprints, only the name and goal can be updated; changes to other fields will be ignored.
 *  *  A sprint can be started by updating the state to 'active'. This requires the sprint to be in the 'future' state and have a startDate and endDate set.
 *  *  A sprint can be completed by updating the state to 'closed'. This action requires the sprint to be in the 'active' state. This sets the completeDate to the time of the request.
 *  *  Other changes to state are not allowed.
 *  *  The completeDate field cannot be updated manually.
 *
 * @param params.sprintId - the ID of the sprint to update.
 * @returns Updated sprint
 */
export function updateSprint(
  ctx: ClientContext,
  params: UpdateSprintBody & {
    /**
     * the ID of the sprint to update.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/sprint/{sprintId}",
    pathParams: { sprintId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Partially update sprint
 *
 * Performs a partial update of a sprint. A partial update means that fields not present in the request JSON will not be updated.
 *
 * Notes:
 *
 *  *  For closed sprints, only the name and goal can be updated; changes to other fields will be ignored.
 *  *  A sprint can be started by updating the state to 'active'. This requires the sprint to be in the 'future' state and have a startDate and endDate set.
 *  *  A sprint can be completed by updating the state to 'closed'. This action requires the sprint to be in the 'active' state. This sets the completeDate to the time of the request.
 *  *  Other changes to state are not allowed.
 *  *  The completeDate field cannot be updated manually.
 *
 * @param params.sprintId - The ID of the sprint to update.
 * @returns Updated sprint
 */
export function partiallyUpdateSprint(
  ctx: ClientContext,
  params: UpdateSprintBody & {
    /**
     * The ID of the sprint to update.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/sprint/{sprintId}",
    pathParams: { sprintId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete sprint
 *
 * Deletes a sprint. Once a sprint is deleted, all open issues in the sprint will be moved to the backlog.
 *
 * @param params.sprintId - The ID of the sprint to delete.
 * @returns Returned if the sprint was deleted successfully
 */
export function deleteSprint(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the sprint to delete.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { sprintId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/agile/1.0/sprint/{sprintId}",
    pathParams: { sprintId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for sprint
 *
 * Returns all issues in a sprint, for a given sprint ID. This only includes issues that the user has permission to view. By default, the returned issues are ordered by rank.
 *
 * @param params.sprintId - The ID of the sprint that contains the requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getIssuesForSprint(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the sprint that contains the requested issues.
     *
     * @format int64
     */
    sprintId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/sprint/{sprintId}/issue",
    pathParams: { sprintId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move issues to sprint and rank
 *
 * Moves issues to a sprint, for a given sprint ID. Issues can only be moved to open or active sprints. The maximum number of issues that can be moved in one operation is 50.
 *
 * @param params.sprintId - The ID of the sprint that you want to assign issues to.
 * @returns Empty response is returned if operation was successful.
 */
export function moveIssuesToSprintAndRank(
  ctx: ClientContext,
  params: MoveIssuesToBacklogForBoardBody & {
    /**
     * The ID of the sprint that you want to assign issues to.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { sprintId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/sprint/{sprintId}/issue",
    pathParams: { sprintId },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for sprint (enhanced)
 *
 * Returns all issues in a sprint, for a given sprint ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.sprintId - The ID of the sprint that contains the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getIssuesForSprintJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the sprint that contains the requested issues.
     *
     * @format int64
     */
    sprintId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    sprintId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/sprint/{sprintId}/issue",
    pathParams: { sprintId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get properties keys
 *
 * Returns the keys of all properties for the sprint identified by the id. The user who retrieves the property keys is required to have permissions to view the sprint.
 *
 * @param params.sprintId - the ID of the sprint from which property keys will be returned.
 * @returns Returned if the sprint with given id exists.
 */
export function getPropertiesKeys(
  ctx: ClientContext,
  params: {
    /** the ID of the sprint from which property keys will be returned. */
    sprintId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/sprint/{sprintId}/properties",
    pathParams: { sprintId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get property
 *
 * Returns the value of the property with a given key from the sprint identified by the provided id. The user who retrieves the property is required to have permissions to view the sprint.
 *
 * @param params.sprintId - the ID of the sprint from which the property will be returned.
 * @param params.propertyKey - the key of the property to return.
 * @returns Returned if the sprint exists and the property was found.
 */
export function getProperty(
  ctx: ClientContext,
  params: {
    /** the ID of the sprint from which the property will be returned. */
    sprintId: string;
    /** the key of the property to return. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId, propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/sprint/{sprintId}/properties/{propertyKey}",
    pathParams: { sprintId, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set property
 *
 * Sets the value of the specified sprint's property.
 *
 * You can use this resource to store a custom data against the sprint identified by the id. The user who stores the data is required to have permissions to modify the sprint.
 *
 * @param params.sprintId - the ID of the sprint on which the property will be set.
 * @param params.propertyKey - the key of the sprint's property. The maximum length of the key is 255 bytes.
 * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
 * @returns Returned if the sprint property is successfully updated.
 */
export function setProperty(
  ctx: ClientContext,
  params: {
    /** the ID of the sprint on which the property will be set. */
    sprintId: string;
    /** the key of the sprint's property. The maximum length of the key is 255 bytes. */
    propertyKey: string;
    body: unknown;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { sprintId, propertyKey, body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/sprint/{sprintId}/properties/{propertyKey}",
    pathParams: { sprintId, propertyKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete property
 *
 * Removes the property from the sprint identified by the id. Ths user removing the property is required to have permissions to modify the sprint.
 *
 * @param params.sprintId - the ID of the sprint from which the property will be removed.
 * @param params.propertyKey - the key of the property to remove.
 * @returns Returned if the sprint property was removed successfully.
 */
export function deleteProperty(
  ctx: ClientContext,
  params: {
    /** the ID of the sprint from which the property will be removed. */
    sprintId: string;
    /** the key of the property to remove. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { sprintId, propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/agile/1.0/sprint/{sprintId}/properties/{propertyKey}",
    pathParams: { sprintId, propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Swap sprint
 *
 * Swap the position of the sprint with the second sprint.
 *
 * @param params.sprintId - The ID of the sprint to swap.
 * @returns Returned if the sprint swap was performed successfully
 */
export function swapSprint(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    sprintToSwapWith?: number;
  } & {
    /**
     * The ID of the sprint to swap.
     *
     * @format int64
     */
    sprintId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { sprintId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/sprint/{sprintId}/swap",
    pathParams: { sprintId },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Apis related to sprints */
export function createSprintService(ctx: ClientContext) {
  return {
    /**
     * Create sprint
     *
     * Creates a future sprint. Sprint name and origin board id are required. Start date, end date, and goal are optional.
     *
     * Note that the sprint name is trimmed. Also, when starting sprints from the UI, the "endDate" set through this call is ignored and instead the last sprint's duration is used to fill the form.
     *
     * @returns Created sprint
     */
    createSprint(
      params: {
        endDate?: string;
        goal?: string;
        name?: string;
        /** @format int64 */
        originBoardId?: number;
        startDate?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return createSprint(ctx, params, options);
    },

    /**
     * Get sprint
     *
     * Returns the sprint for a given sprint ID. The sprint will only be returned if the user can view the board that the sprint was created on, or view at least one of the issues in the sprint.
     *
     * @param params.sprintId - The ID of the requested sprint.
     * @returns Returns the requested sprint.
     */
    getSprint(
      params: {
        /**
         * The ID of the requested sprint.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getSprint(ctx, params, options);
    },

    /**
     * Update sprint
     *
     * Performs a full update of a sprint. A full update means that the result will be exactly the same as the request body. Any fields not present in the request JSON will be set to null.
     *
     * Notes:
     *
     *  *  For closed sprints, only the name and goal can be updated; changes to other fields will be ignored.
     *  *  A sprint can be started by updating the state to 'active'. This requires the sprint to be in the 'future' state and have a startDate and endDate set.
     *  *  A sprint can be completed by updating the state to 'closed'. This action requires the sprint to be in the 'active' state. This sets the completeDate to the time of the request.
     *  *  Other changes to state are not allowed.
     *  *  The completeDate field cannot be updated manually.
     *
     * @param params.sprintId - the ID of the sprint to update.
     * @returns Updated sprint
     */
    updateSprint(
      params: UpdateSprintBody & {
        /**
         * the ID of the sprint to update.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateSprint(ctx, params, options);
    },

    /**
     * Partially update sprint
     *
     * Performs a partial update of a sprint. A partial update means that fields not present in the request JSON will not be updated.
     *
     * Notes:
     *
     *  *  For closed sprints, only the name and goal can be updated; changes to other fields will be ignored.
     *  *  A sprint can be started by updating the state to 'active'. This requires the sprint to be in the 'future' state and have a startDate and endDate set.
     *  *  A sprint can be completed by updating the state to 'closed'. This action requires the sprint to be in the 'active' state. This sets the completeDate to the time of the request.
     *  *  Other changes to state are not allowed.
     *  *  The completeDate field cannot be updated manually.
     *
     * @param params.sprintId - The ID of the sprint to update.
     * @returns Updated sprint
     */
    partiallyUpdateSprint(
      params: UpdateSprintBody & {
        /**
         * The ID of the sprint to update.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return partiallyUpdateSprint(ctx, params, options);
    },

    /**
     * Delete sprint
     *
     * Deletes a sprint. Once a sprint is deleted, all open issues in the sprint will be moved to the backlog.
     *
     * @param params.sprintId - The ID of the sprint to delete.
     * @returns Returned if the sprint was deleted successfully
     */
    deleteSprint(
      params: {
        /**
         * The ID of the sprint to delete.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteSprint(ctx, params, options);
    },

    /**
     * Get issues for sprint
     *
     * Returns all issues in a sprint, for a given sprint ID. This only includes issues that the user has permission to view. By default, the returned issues are ordered by rank.
     *
     * @param params.sprintId - The ID of the sprint that contains the requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getIssuesForSprint(
      params: {
        /**
         * The ID of the sprint that contains the requested issues.
         *
         * @format int64
         */
        sprintId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getIssuesForSprint(ctx, params, options);
    },

    /**
     * Move issues to sprint and rank
     *
     * Moves issues to a sprint, for a given sprint ID. Issues can only be moved to open or active sprints. The maximum number of issues that can be moved in one operation is 50.
     *
     * @param params.sprintId - The ID of the sprint that you want to assign issues to.
     * @returns Empty response is returned if operation was successful.
     */
    moveIssuesToSprintAndRank(
      params: MoveIssuesToBacklogForBoardBody & {
        /**
         * The ID of the sprint that you want to assign issues to.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return moveIssuesToSprintAndRank(ctx, params, options);
    },

    /**
     * Get issues for sprint (enhanced)
     *
     * Returns all issues in a sprint, for a given sprint ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.sprintId - The ID of the sprint that contains the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getIssuesForSprintJsis(
      params: {
        /**
         * The ID of the sprint that contains the requested issues.
         *
         * @format int64
         */
        sprintId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getIssuesForSprintJsis(ctx, params, options);
    },

    /**
     * Get properties keys
     *
     * Returns the keys of all properties for the sprint identified by the id. The user who retrieves the property keys is required to have permissions to view the sprint.
     *
     * @param params.sprintId - the ID of the sprint from which property keys will be returned.
     * @returns Returned if the sprint with given id exists.
     */
    getPropertiesKeys(
      params: {
        /** the ID of the sprint from which property keys will be returned. */
        sprintId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getPropertiesKeys(ctx, params, options);
    },

    /**
     * Get property
     *
     * Returns the value of the property with a given key from the sprint identified by the provided id. The user who retrieves the property is required to have permissions to view the sprint.
     *
     * @param params.sprintId - the ID of the sprint from which the property will be returned.
     * @param params.propertyKey - the key of the property to return.
     * @returns Returned if the sprint exists and the property was found.
     */
    getProperty(
      params: {
        /** the ID of the sprint from which the property will be returned. */
        sprintId: string;
        /** the key of the property to return. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getProperty(ctx, params, options);
    },

    /**
     * Set property
     *
     * Sets the value of the specified sprint's property.
     *
     * You can use this resource to store a custom data against the sprint identified by the id. The user who stores the data is required to have permissions to modify the sprint.
     *
     * @param params.sprintId - the ID of the sprint on which the property will be set.
     * @param params.propertyKey - the key of the sprint's property. The maximum length of the key is 255 bytes.
     * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
     * @returns Returned if the sprint property is successfully updated.
     */
    setProperty(
      params: {
        /** the ID of the sprint on which the property will be set. */
        sprintId: string;
        /** the key of the sprint's property. The maximum length of the key is 255 bytes. */
        propertyKey: string;
        body: unknown;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setProperty(ctx, params, options);
    },

    /**
     * Delete property
     *
     * Removes the property from the sprint identified by the id. Ths user removing the property is required to have permissions to modify the sprint.
     *
     * @param params.sprintId - the ID of the sprint from which the property will be removed.
     * @param params.propertyKey - the key of the property to remove.
     * @returns Returned if the sprint property was removed successfully.
     */
    deleteProperty(
      params: {
        /** the ID of the sprint from which the property will be removed. */
        sprintId: string;
        /** the key of the property to remove. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteProperty(ctx, params, options);
    },

    /**
     * Swap sprint
     *
     * Swap the position of the sprint with the second sprint.
     *
     * @param params.sprintId - The ID of the sprint to swap.
     * @returns Returned if the sprint swap was performed successfully
     */
    swapSprint(
      params: {
        /** @format int64 */
        sprintToSwapWith?: number;
      } & {
        /**
         * The ID of the sprint to swap.
         *
         * @format int64
         */
        sprintId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return swapSprint(ctx, params, options);
    },
  };
}
