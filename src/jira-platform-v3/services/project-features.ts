// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { ContainerForProjectFeatures, ProjectFeatureState } from "../types/index.ts";

/**
 * Get project features
 *
 * Returns the list of features for a project.
 *
 * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
 * @returns Returned if the request is successful.
 */
export function getFeaturesForProject(
  ctx: ClientContext,
  params: {
    /** The ID or (case-sensitive) key of the project. */
    projectIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ContainerForProjectFeatures> {
  const { projectIdOrKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/project/{projectIdOrKey}/features",
    pathParams: { projectIdOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set project feature state
 *
 * Sets the state of a project feature.
 *
 * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
 * @param params.featureKey - The key of the feature.
 * @returns Returned if the request is successful.
 */
export function toggleFeatureForProject(
  ctx: ClientContext,
  params: ProjectFeatureState & {
    /** The ID or (case-sensitive) key of the project. */
    projectIdOrKey: string;
    /** The key of the feature. */
    featureKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ContainerForProjectFeatures> {
  const { projectIdOrKey, featureKey, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/project/{projectIdOrKey}/features/{featureKey}",
    pathParams: { projectIdOrKey, featureKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents project features. Use it to get the list of features for a project and modify the state of a feature. The project feature endpoint is available only for Jira Software, both for team- and company-managed projects. */
export function createProjectFeaturesService(ctx: ClientContext) {
  return {
    /**
     * Get project features
     *
     * Returns the list of features for a project.
     *
     * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
     * @returns Returned if the request is successful.
     */
    getFeaturesForProject(
      params: {
        /** The ID or (case-sensitive) key of the project. */
        projectIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ContainerForProjectFeatures> {
      return getFeaturesForProject(ctx, params, options);
    },

    /**
     * Set project feature state
     *
     * Sets the state of a project feature.
     *
     * @param params.projectIdOrKey - The ID or (case-sensitive) key of the project.
     * @param params.featureKey - The key of the feature.
     * @returns Returned if the request is successful.
     */
    toggleFeatureForProject(
      params: ProjectFeatureState & {
        /** The ID or (case-sensitive) key of the project. */
        projectIdOrKey: string;
        /** The key of the feature. */
        featureKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ContainerForProjectFeatures> {
      return toggleFeatureForProject(ctx, params, options);
    },
  };
}
