// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientConfig, ClientContext, SdkConfig, Transport } from "./config.ts";
import { ApiError, createClient } from "./config.ts";
import { createAssetsService } from "./services/assets.ts";
import { createCustomerService } from "./services/customer.ts";
import { createInfoService } from "./services/info.ts";
import { createKnowledgebaseService } from "./services/knowledgebase.ts";
import { createOrganizationService } from "./services/organization.ts";
import { createRequestService } from "./services/request.ts";
import { createRequesttypeService } from "./services/requesttype.ts";
import { createRestService } from "./services/rest.ts";
import { createServicedeskService } from "./services/servicedesk.ts";

export * from "./types/index.ts";
export { ApiError };
export type { ClientConfig, ClientContext, SdkConfig, Transport };

/**
 * Create a `Service Management Public REST API` SDK client (API version 1001.0.0).
 *
 * Public REST API for Jira Service Management
 *
 * @param config - Transport plus cross-cutting options (headers, interceptors).
 */
export function createServiceDeskSdk(config: SdkConfig) {
  const ctx = createClient(config);
  return {
    assets: createAssetsService(ctx),
    customer: createCustomerService(ctx),
    rest: createRestService(ctx),
    info: createInfoService(ctx),
    knowledgebase: createKnowledgebaseService(ctx),
    organization: createOrganizationService(ctx),
    request: createRequestService(ctx),
    requesttype: createRequesttypeService(ctx),
    servicedesk: createServicedeskService(ctx),
  };
}

export type CreateServiceDeskSdkClient = ReturnType<typeof createServiceDeskSdk>;
