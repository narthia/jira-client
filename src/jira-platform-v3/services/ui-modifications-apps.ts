// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreateUiModificationDetails,
  PageBeanUiModificationDetails,
  UiModificationIdentifiers,
  UpdateUiModificationDetails,
} from "../types/index.ts";

/**
 * Get UI modifications
 *
 * Gets UI modifications. UI modifications can only be retrieved by Forge apps.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * The new `read:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.expand - Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include: *  `data` Returns UI modification data. *  `contexts` Returns UI modification contexts.
 * @returns Returned if the request is successful.
 */
export function getUiModifications(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /**
     * Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `data` Returns UI modification data.
     *  *  `contexts` Returns UI modification contexts.
     */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanUiModificationDetails> {
  const { startAt, maxResults, expand } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/uiModifications",
    query: { startAt, maxResults, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create UI modification
 *
 * Creates a UI modification. UI modification can only be created by Forge apps.
 *
 * Each app can define up to 3000 UI modifications. Each UI modification can define up to 1000 contexts. The same context can be assigned to maximum 100 UI modifications.
 *
 * **Context types:**
 *
 *  *  **Jira contexts:** For Jira view types, use `projectId` and `issueTypeId`. One field can act as a wildcard. Supported Jira views:
 *
 *      *  `GIC` \- Jira global issue create
 *      *  `IssueView` \- Jira issue view
 *      *  `IssueTransition` \- Jira issue transition
 *  *  **Jira Service Management contexts:** For Jira Service Management view types, use `portalId` and `requestTypeId`. Wildcards are not supported. Supported JSM views:
 *
 *      *  `JSMRequestCreate` \- Jira Service Management request create portal view
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *None* if the UI modification is created without contexts.
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, if the UI modification is created with contexts.
 *
 * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @returns Returned if the UI modification is created.
 */
export function createUiModification(
  ctx: ClientContext,
  params: CreateUiModificationDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<UiModificationIdentifiers> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/uiModifications",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update UI modification
 *
 * Updates a UI modification. UI modification can only be updated by Forge apps.
 *
 * Each UI modification can define up to 1000 contexts. The same context can be assigned to maximum 100 UI modifications.
 *
 * **Context types:**
 *
 *  *  **Jira contexts:** For Jira view types, use `projectId` and `issueTypeId`. One field can act as a wildcard. Supported Jira views:
 *
 *      *  `GIC` \- Jira global issue create
 *      *  `IssueView` \- Jira issue view
 *      *  `IssueTransition` \- Jira issue transition
 *  *  **Jira Service Management contexts:** For Jira Service Management view types, use `portalId` and `requestTypeId`. Wildcards are not supported. Supported JSM views:
 *
 *      *  `JSMRequestCreate` \- Jira Service Management request create portal view
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *None* if the UI modification is created without contexts.
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, if the UI modification is created with contexts.
 *
 * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @param params.uiModificationId - The ID of the UI modification.
 * @returns Returned if the UI modification is updated.
 */
export function updateUiModification(
  ctx: ClientContext,
  params: UpdateUiModificationDetails & {
    /** The ID of the UI modification. */
    uiModificationId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { uiModificationId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/uiModifications/{uiModificationId}",
    pathParams: { uiModificationId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete UI modification
 *
 * Deletes a UI modification. All the contexts that belong to the UI modification are deleted too. UI modification can only be deleted by Forge apps.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
 *
 * @param params.uiModificationId - The ID of the UI modification.
 * @returns Returned if the UI modification is deleted.
 */
export function deleteUiModification(
  ctx: ClientContext,
  params: {
    /** The ID of the UI modification. */
    uiModificationId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { uiModificationId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/uiModifications/{uiModificationId}",
    pathParams: { uiModificationId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * UI modifications is a feature available for **Forge apps only**. It enables Forge apps to control how selected Jira and Jira Service Management fields behave on the following views:
 *
 *  *  Jira global issue create
 *  *  Jira issue view
 *  *  Jira issue transition
 *  *  Jira Service Management request portal create.
 *
 * For example: hide specific fields, set them as required, etc.
 */
export function createUiModificationsAppsService(ctx: ClientContext) {
  return {
    /**
     * Get UI modifications
     *
     * Gets UI modifications. UI modifications can only be retrieved by Forge apps.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * The new `read:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.expand - Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include: *  `data` Returns UI modification data. *  `contexts` Returns UI modification contexts.
     * @returns Returned if the request is successful.
     */
    getUiModifications(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /**
         * Use expand to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
         *
         *  *  `data` Returns UI modification data.
         *  *  `contexts` Returns UI modification contexts.
         */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanUiModificationDetails> {
      return getUiModifications(ctx, params, options);
    },

    /**
     * Create UI modification
     *
     * Creates a UI modification. UI modification can only be created by Forge apps.
     *
     * Each app can define up to 3000 UI modifications. Each UI modification can define up to 1000 contexts. The same context can be assigned to maximum 100 UI modifications.
     *
     * **Context types:**
     *
     *  *  **Jira contexts:** For Jira view types, use `projectId` and `issueTypeId`. One field can act as a wildcard. Supported Jira views:
     *
     *      *  `GIC` \- Jira global issue create
     *      *  `IssueView` \- Jira issue view
     *      *  `IssueTransition` \- Jira issue transition
     *  *  **Jira Service Management contexts:** For Jira Service Management view types, use `portalId` and `requestTypeId`. Wildcards are not supported. Supported JSM views:
     *
     *      *  `JSMRequestCreate` \- Jira Service Management request create portal view
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *None* if the UI modification is created without contexts.
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, if the UI modification is created with contexts.
     *
     * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @returns Returned if the UI modification is created.
     */
    createUiModification(
      params: CreateUiModificationDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<UiModificationIdentifiers> {
      return createUiModification(ctx, params, options);
    },

    /**
     * Update UI modification
     *
     * Updates a UI modification. UI modification can only be updated by Forge apps.
     *
     * Each UI modification can define up to 1000 contexts. The same context can be assigned to maximum 100 UI modifications.
     *
     * **Context types:**
     *
     *  *  **Jira contexts:** For Jira view types, use `projectId` and `issueTypeId`. One field can act as a wildcard. Supported Jira views:
     *
     *      *  `GIC` \- Jira global issue create
     *      *  `IssueView` \- Jira issue view
     *      *  `IssueTransition` \- Jira issue transition
     *  *  **Jira Service Management contexts:** For Jira Service Management view types, use `portalId` and `requestTypeId`. Wildcards are not supported. Supported JSM views:
     *
     *      *  `JSMRequestCreate` \- Jira Service Management request create portal view
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *None* if the UI modification is created without contexts.
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for one or more projects, if the UI modification is created with contexts.
     *
     * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @param params.uiModificationId - The ID of the UI modification.
     * @returns Returned if the UI modification is updated.
     */
    updateUiModification(
      params: UpdateUiModificationDetails & {
        /** The ID of the UI modification. */
        uiModificationId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateUiModification(ctx, params, options);
    },

    /**
     * Delete UI modification
     *
     * Deletes a UI modification. All the contexts that belong to the UI modification are deleted too. UI modification can only be deleted by Forge apps.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * The new `write:app-data:jira` OAuth scope is 100% optional now, and not using it won't break your app. However, we recommend adding it to your app's scope list because we will eventually make it mandatory.
     *
     * @param params.uiModificationId - The ID of the UI modification.
     * @returns Returned if the UI modification is deleted.
     */
    deleteUiModification(
      params: {
        /** The ID of the UI modification. */
        uiModificationId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteUiModification(ctx, params, options);
    },
  };
}
