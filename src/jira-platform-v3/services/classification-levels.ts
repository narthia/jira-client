// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { DataClassificationLevelsBean } from "../types/index.ts";

/**
 * Get all classification levels
 *
 * Returns all classification levels.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.status - Optional set of statuses to filter by.
 * @param params.orderBy - Ordering of the results by a given field. If not provided, values will not be sorted.
 * @returns Returned if the request is successful.
 */
export function getAllUserDataClassificationLevels(
  ctx: ClientContext,
  params?: {
    /** Optional set of statuses to filter by. */
    status?: ("PUBLISHED" | "ARCHIVED" | "DRAFT")[];
    /** Ordering of the results by a given field. If not provided, values will not be sorted. */
    orderBy?: "rank" | "-rank" | "+rank";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<DataClassificationLevelsBean> {
  const { status, orderBy } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/classification-levels",
    query: { status, orderBy },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents classification levels. */
export function createClassificationLevelsService(ctx: ClientContext) {
  return {
    /**
     * Get all classification levels
     *
     * Returns all classification levels.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.status - Optional set of statuses to filter by.
     * @param params.orderBy - Ordering of the results by a given field. If not provided, values will not be sorted.
     * @returns Returned if the request is successful.
     */
    getAllUserDataClassificationLevels(
      params?: {
        /** Optional set of statuses to filter by. */
        status?: ("PUBLISHED" | "ARCHIVED" | "DRAFT")[];
        /** Ordering of the results by a given field. If not provided, values will not be sorted. */
        orderBy?: "rank" | "-rank" | "+rank";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<DataClassificationLevelsBean> {
      return getAllUserDataClassificationLevels(ctx, params, options);
    },
  };
}
