// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  DefaultWorkflow,
  IssueTypeWorkflowMapping,
  IssueTypesWorkflowMapping,
  PublishDraftWorkflowScheme,
  WorkflowScheme,
} from "../types/index.ts";

/**
 * Create draft workflow scheme
 *
 * Create a draft workflow scheme from an active workflow scheme, by copying the active workflow scheme. Note that an active workflow scheme can only have one draft workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the active workflow scheme that the draft is created from.
 * @returns Returned if the request is successful.
 */
export function createWorkflowSchemeDraftFromParent(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the active workflow scheme that the draft is created from.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/{id}/createdraft",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get draft workflow scheme
 *
 * Returns the draft workflow scheme for an active workflow scheme. Draft workflow schemes allow changes to be made to the active workflow schemes: When an active workflow scheme is updated, a draft copy is created. The draft is modified, then the changes in the draft are copied back to the active workflow scheme. See [Configuring workflow schemes](https://confluence.atlassian.com/x/tohKLg) for more information.
 * Note that:
 *
 *  *  Only active workflow schemes can have draft workflow schemes.
 *  *  An active workflow scheme can only have one draft workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the active workflow scheme that the draft was created from.
 * @returns Returned if the request is successful.
 */
export function getWorkflowSchemeDraft(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the active workflow scheme that the draft was created from.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/draft",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update draft workflow scheme
 *
 * Updates a draft workflow scheme. If a draft workflow scheme does not exist for the active workflow scheme, then a draft is created. Note that an active workflow scheme can only have one draft workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id_path - The ID of the active workflow scheme that the draft was created from.
 * @returns Returned if the request is successful.
 */
export function updateWorkflowSchemeDraft(
  ctx: ClientContext,
  params: WorkflowScheme & {
    /**
     * The ID of the active workflow scheme that the draft was created from.
     *
     * @format int64
     */
    id_path: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/draft",
    pathParams: { id: id_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete draft workflow scheme
 *
 * Deletes a draft workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the active workflow scheme that the draft was created from.
 * @returns Returned if the request is successful.
 */
export function deleteWorkflowSchemeDraft(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the active workflow scheme that the draft was created from.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/draft",
    pathParams: { id },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get draft default workflow
 *
 * Returns the default workflow for a workflow scheme's draft. The default workflow is the workflow that is assigned any issue types that have not been mapped to any other workflow. The default workflow has *All Unassigned Issue Types* listed in its issue types for the workflow scheme in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @returns Returned if the request is successful.
 */
export function getDraftDefaultWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<DefaultWorkflow> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/draft/default",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update draft default workflow
 *
 * Sets the default workflow for a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @returns Returned if the request is successful.
 */
export function updateDraftDefaultWorkflow(
  ctx: ClientContext,
  params: DefaultWorkflow & {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/draft/default",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete draft default workflow
 *
 * Resets the default workflow for a workflow scheme's draft. That is, the default workflow is set to Jira's system workflow (the *jira* workflow).
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @returns Returned if the request is successful.
 */
export function deleteDraftDefaultWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/draft/default",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get workflow for issue type in draft workflow scheme
 *
 * Returns the issue type-workflow mapping for an issue type in a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.issueType - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function getWorkflowSchemeDraftIssueType(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issueType: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeWorkflowMapping> {
  const { id, issueType } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}",
    pathParams: { id, issueType },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set workflow for issue type in draft workflow scheme
 *
 * Sets the workflow for an issue type in a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.issue_type_path - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function setWorkflowSchemeDraftIssueType(
  ctx: ClientContext,
  params: IssueTypeWorkflowMapping & {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issue_type_path: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, issue_type_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}",
    pathParams: { id, issueType: issue_type_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete workflow for issue type in draft workflow scheme
 *
 * Deletes the issue type-workflow mapping for an issue type in a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.issueType - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function deleteWorkflowSchemeDraftIssueType(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issueType: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, issueType } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/draft/issuetype/{issueType}",
    pathParams: { id, issueType },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Publish draft workflow scheme
 *
 * Publishes a draft workflow scheme.
 *
 * Where the draft workflow includes new workflow statuses for an issue type, mappings are provided to update issues with the original workflow status to the new workflow status.
 *
 * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain updates.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.validateOnly - Whether the request only performs a validation.
 * @returns Returned if the request is only for validation and is successful.
 */
export function publishDraftWorkflowScheme(
  ctx: ClientContext,
  params: PublishDraftWorkflowScheme & {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /**
     * Whether the request only performs a validation.
     *
     * @default false
     */
    validateOnly?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, validateOnly, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/{id}/draft/publish",
    pathParams: { id },
    query: { validateOnly },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue types for workflows in draft workflow scheme
 *
 * Returns the workflow-issue type mappings for a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.workflowName - The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow.
 * @returns Returned if the request is successful.
 */
export function getDraftWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow. */
    workflowName?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypesWorkflowMapping> {
  const { id, workflowName } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/draft/workflow",
    pathParams: { id },
    query: { workflowName },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set issue types for workflow in workflow scheme
 *
 * Sets the issue types for a workflow in a workflow scheme's draft. The workflow can also be set as the default workflow for the draft workflow scheme. Unmapped issues types are mapped to the default workflow.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.workflowName - The name of the workflow.
 * @returns Returned if the request is successful.
 */
export function updateDraftWorkflowMapping(
  ctx: ClientContext,
  params: IssueTypesWorkflowMapping & {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The name of the workflow. */
    workflowName: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, workflowName, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/draft/workflow",
    pathParams: { id },
    query: { workflowName },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue types for workflow in draft workflow scheme
 *
 * Deletes the workflow-issue type mapping for a workflow in a workflow scheme's draft.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme that the draft belongs to.
 * @param params.workflowName - The name of the workflow.
 * @returns Returned if the request is successful.
 */
export function deleteDraftWorkflowMapping(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme that the draft belongs to.
     *
     * @format int64
     */
    id: number;
    /** The name of the workflow. */
    workflowName: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, workflowName } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/draft/workflow",
    pathParams: { id },
    query: { workflowName },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents draft workflow schemes. Use it to manage drafts of workflow schemes.
 *
 * A workflow scheme maps issue types to workflows. A workflow scheme can be associated with one or more projects, which enables the projects to use the workflow-issue type mappings.
 *
 * Active workflow schemes (workflow schemes that are used by projects) cannot be edited. Editing an active workflow scheme creates a draft copy of the scheme. The draft workflow scheme can then be edited and published (replacing the active scheme).
 *
 * See [Configuring workflow schemes](https://confluence.atlassian.com/x/tohKLg) for more information.
 */
export function createWorkflowSchemeDraftsService(ctx: ClientContext) {
  return {
    /**
     * Create draft workflow scheme
     *
     * Create a draft workflow scheme from an active workflow scheme, by copying the active workflow scheme. Note that an active workflow scheme can only have one draft workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the active workflow scheme that the draft is created from.
     * @returns Returned if the request is successful.
     */
    createWorkflowSchemeDraftFromParent(
      params: {
        /**
         * The ID of the active workflow scheme that the draft is created from.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return createWorkflowSchemeDraftFromParent(ctx, params, options);
    },

    /**
     * Get draft workflow scheme
     *
     * Returns the draft workflow scheme for an active workflow scheme. Draft workflow schemes allow changes to be made to the active workflow schemes: When an active workflow scheme is updated, a draft copy is created. The draft is modified, then the changes in the draft are copied back to the active workflow scheme. See [Configuring workflow schemes](https://confluence.atlassian.com/x/tohKLg) for more information.
     * Note that:
     *
     *  *  Only active workflow schemes can have draft workflow schemes.
     *  *  An active workflow scheme can only have one draft workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the active workflow scheme that the draft was created from.
     * @returns Returned if the request is successful.
     */
    getWorkflowSchemeDraft(
      params: {
        /**
         * The ID of the active workflow scheme that the draft was created from.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return getWorkflowSchemeDraft(ctx, params, options);
    },

    /**
     * Update draft workflow scheme
     *
     * Updates a draft workflow scheme. If a draft workflow scheme does not exist for the active workflow scheme, then a draft is created. Note that an active workflow scheme can only have one draft workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id_path - The ID of the active workflow scheme that the draft was created from.
     * @returns Returned if the request is successful.
     */
    updateWorkflowSchemeDraft(
      params: WorkflowScheme & {
        /**
         * The ID of the active workflow scheme that the draft was created from.
         *
         * @format int64
         */
        id_path: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateWorkflowSchemeDraft(ctx, params, options);
    },

    /**
     * Delete draft workflow scheme
     *
     * Deletes a draft workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the active workflow scheme that the draft was created from.
     * @returns Returned if the request is successful.
     */
    deleteWorkflowSchemeDraft(
      params: {
        /**
         * The ID of the active workflow scheme that the draft was created from.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteWorkflowSchemeDraft(ctx, params, options);
    },

    /**
     * Get draft default workflow
     *
     * Returns the default workflow for a workflow scheme's draft. The default workflow is the workflow that is assigned any issue types that have not been mapped to any other workflow. The default workflow has *All Unassigned Issue Types* listed in its issue types for the workflow scheme in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @returns Returned if the request is successful.
     */
    getDraftDefaultWorkflow(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<DefaultWorkflow> {
      return getDraftDefaultWorkflow(ctx, params, options);
    },

    /**
     * Update draft default workflow
     *
     * Sets the default workflow for a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @returns Returned if the request is successful.
     */
    updateDraftDefaultWorkflow(
      params: DefaultWorkflow & {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateDraftDefaultWorkflow(ctx, params, options);
    },

    /**
     * Delete draft default workflow
     *
     * Resets the default workflow for a workflow scheme's draft. That is, the default workflow is set to Jira's system workflow (the *jira* workflow).
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @returns Returned if the request is successful.
     */
    deleteDraftDefaultWorkflow(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return deleteDraftDefaultWorkflow(ctx, params, options);
    },

    /**
     * Get workflow for issue type in draft workflow scheme
     *
     * Returns the issue type-workflow mapping for an issue type in a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.issueType - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    getWorkflowSchemeDraftIssueType(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issueType: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeWorkflowMapping> {
      return getWorkflowSchemeDraftIssueType(ctx, params, options);
    },

    /**
     * Set workflow for issue type in draft workflow scheme
     *
     * Sets the workflow for an issue type in a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.issue_type_path - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    setWorkflowSchemeDraftIssueType(
      params: IssueTypeWorkflowMapping & {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issue_type_path: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return setWorkflowSchemeDraftIssueType(ctx, params, options);
    },

    /**
     * Delete workflow for issue type in draft workflow scheme
     *
     * Deletes the issue type-workflow mapping for an issue type in a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.issueType - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    deleteWorkflowSchemeDraftIssueType(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issueType: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return deleteWorkflowSchemeDraftIssueType(ctx, params, options);
    },

    /**
     * Publish draft workflow scheme
     *
     * Publishes a draft workflow scheme.
     *
     * Where the draft workflow includes new workflow statuses for an issue type, mappings are provided to update issues with the original workflow status to the new workflow status.
     *
     * This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain updates.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.validateOnly - Whether the request only performs a validation.
     * @returns Returned if the request is only for validation and is successful.
     */
    publishDraftWorkflowScheme(
      params: PublishDraftWorkflowScheme & {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /**
         * Whether the request only performs a validation.
         *
         * @default false
         */
        validateOnly?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return publishDraftWorkflowScheme(ctx, params, options);
    },

    /**
     * Get issue types for workflows in draft workflow scheme
     *
     * Returns the workflow-issue type mappings for a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.workflowName - The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow.
     * @returns Returned if the request is successful.
     */
    getDraftWorkflow(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow. */
        workflowName?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypesWorkflowMapping> {
      return getDraftWorkflow(ctx, params, options);
    },

    /**
     * Set issue types for workflow in workflow scheme
     *
     * Sets the issue types for a workflow in a workflow scheme's draft. The workflow can also be set as the default workflow for the draft workflow scheme. Unmapped issues types are mapped to the default workflow.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.workflowName - The name of the workflow.
     * @returns Returned if the request is successful.
     */
    updateDraftWorkflowMapping(
      params: IssueTypesWorkflowMapping & {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The name of the workflow. */
        workflowName: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateDraftWorkflowMapping(ctx, params, options);
    },

    /**
     * Delete issue types for workflow in draft workflow scheme
     *
     * Deletes the workflow-issue type mapping for a workflow in a workflow scheme's draft.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme that the draft belongs to.
     * @param params.workflowName - The name of the workflow.
     * @returns Returned if the request is successful.
     */
    deleteDraftWorkflowMapping(
      params: {
        /**
         * The ID of the workflow scheme that the draft belongs to.
         *
         * @format int64
         */
        id: number;
        /** The name of the workflow. */
        workflowName: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteDraftWorkflowMapping(ctx, params, options);
    },
  };
}
