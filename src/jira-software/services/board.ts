// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CreateBoardResponse,
  GetFeaturesForBoardResponse,
  IssueCountBean,
  MoveIssuesToBacklogForBoardBody,
  SearchResults,
  SoftwareIssueResults,
} from "../types/index.ts";

/**
 * Get all boards
 *
 * Returns all boards. This only includes boards that the user has permission to view.
 *
 * **Deprecation notice:** The required OAuth 2.0 scopes will be updated on February 15, 2024.
 *
 *  *  `read:board-scope:jira-software`, `read:project:jira`
 *
 * @param params.startAt - The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of boards to return per page. See the 'Pagination' section at the top of this page for more details.
 * @param params.type - Filters results to boards of the specified types. Valid values: scrum, kanban, simple.
 * @param params.name - Filters results to boards that match or partially match the specified name.
 * @param params.projectKeyOrId - Filters results to boards that are relevant to a project. Relevance means that the jql filter defined in board contains a reference to a project.
 * @param params.includePrivate - Appends private boards to the end of the list. The name and type fields are excluded for security reasons.
 * @param params.negateLocationFiltering - If set to true, negate filters used for querying by location. By default false.
 * @param params.orderBy - Ordering of the results by a given field. If not provided, values will not be sorted. Valid values: name.
 * @param params.expand - List of fields to expand for each board. Valid values: admins, permissions.
 * @param params.projectTypeLocation - Filters results to boards that are relevant to a project types. Support Jira Software, Jira Service Management. Valid values: software, service\_desk. By default software.
 * @param params.filterId - Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
 * @returns Returns the requested boards, at the specified page of the results.
 */
export function getAllBoards(
  ctx: ClientContext,
  params?: {
    /**
     * The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of boards to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /** Filters results to boards of the specified types. Valid values: scrum, kanban, simple. */
    type?: Record<string, unknown>;
    /** Filters results to boards that match or partially match the specified name. */
    name?: string;
    /** Filters results to boards that are relevant to a project. Relevance means that the jql filter defined in board contains a reference to a project. */
    projectKeyOrId?: string;
    accountIdLocation?: string;
    projectLocation?: string;
    /** Appends private boards to the end of the list. The name and type fields are excluded for security reasons. */
    includePrivate?: boolean;
    /** If set to true, negate filters used for querying by location. By default false. */
    negateLocationFiltering?: boolean;
    /** Ordering of the results by a given field. If not provided, values will not be sorted. Valid values: name. */
    orderBy?: "name" | "-name" | "+name";
    /** List of fields to expand for each board. Valid values: admins, permissions. */
    expand?: string;
    /** Filters results to boards that are relevant to a project types. Support Jira Software, Jira Service Management. Valid values: software, service\_desk. By default software. */
    projectTypeLocation?: string[];
    /**
     * Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
     *
     * @format int64
     */
    filterId?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  isLast?: boolean;
  /** @format int32 */
  maxResults?: number;
  /** @format int64 */
  startAt?: number;
  /** @format int64 */
  total?: number;
  values?: {
    admins?: {
      groups?: {
        name?: string;
        /** @format uri */
        self?: string;
      }[];
      users?: {
        /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
        accountId?: string;
        /** Whether the user is active. */
        active?: boolean;
        /** The avatars of the user. */
        avatarUrls?: {
          /**
           * The URL of the user's 16x16 pixel avatar.
           *
           * @format uri
           */
          "16x16"?: string;
          /**
           * The URL of the user's 24x24 pixel avatar.
           *
           * @format uri
           */
          "24x24"?: string;
          /**
           * The URL of the user's 32x32 pixel avatar.
           *
           * @format uri
           */
          "32x32"?: string;
          /**
           * The URL of the user's 48x48 pixel avatar.
           *
           * @format uri
           */
          "48x48"?: string;
        };
        /** The display name of the user. Depending on the user’s privacy setting, this may return an alternative value. */
        displayName?: string;
        /**
         * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.
         * The key of the user.
         */
        key?: string;
        /**
         * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.
         * The username of the user.
         */
        name?: string;
        /**
         * The URL of the user.
         *
         * @format uri
         */
        self?: string;
      }[];
    };
    /** Whether the board can be edited. */
    canEdit?: boolean;
    /** Whether the board is selected as a favorite. */
    favourite?: boolean;
    /**
     * The ID of the board.
     *
     * @format int64
     */
    id?: number;
    /** Whether the board is private. */
    isPrivate?: boolean;
    /** The container that the board is located in. */
    location?: {
      /** @format uri */
      avatarURI?: string;
      displayName?: string;
      name?: string;
      /** @format int64 */
      projectId?: number;
      projectKey?: string;
      projectName?: string;
      projectTypeKey?: string;
      userAccountId?: string;
      /** @format int64 */
      userId?: number;
    };
    /** The name of the board. */
    name?: string;
    /**
     * The URL of the board.
     *
     * @format uri
     */
    self?: string;
    /** The type the board. */
    type?: string;
  }[];
}> {
  const {
    startAt,
    maxResults,
    type,
    name,
    projectKeyOrId,
    accountIdLocation,
    projectLocation,
    includePrivate,
    negateLocationFiltering,
    orderBy,
    expand,
    projectTypeLocation,
    filterId,
  } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board",
    query: {
      startAt,
      maxResults,
      type,
      name,
      projectKeyOrId,
      accountIdLocation,
      projectLocation,
      includePrivate,
      negateLocationFiltering,
      orderBy,
      expand,
      projectTypeLocation,
      filterId,
    },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create board
 *
 * Creates a new board. Board name, type and filter ID is required.
 *
 *  *  `name` \- Must be less than 255 characters.
 *  *  `type` \- Valid values: scrum, kanban
 *  *  `filterId` \- ID of a filter that the user has permissions to view. Note, if the user does not have the 'Create shared objects' permission and tries to create a shared board, a private board will be created instead (remember that board sharing depends on the filter sharing).
 *  *  `location` \- The container that the board will be located in. `location` must include the `type` property (Valid values: project, user). If choosing 'project', then a project must be specified by a `projectKeyOrId` property in `location`. If choosing 'user', the current user is chosen by default. The `projectKeyOrId` property should not be provided.
 *
 * Note:
 *
 *  *  If you want to create a new project with an associated board, use the [Jira platform REST API](https://docs.atlassian.com/jira/REST/latest). For more information, see the [Create project](#api-rest-api-3-project-post) method. The `projectTypeKey` for software boards must be 'software' and the `projectTemplateKey` must be either `com.pyxis.greenhopper.jira:gh-kanban-template` or `com.pyxis.greenhopper.jira:gh-scrum-template`.
 *  *  You can create a filter using the [Jira REST API](https://docs.atlassian.com/jira/REST/latest). For more information, see the [Create filter](#api-rest-api-3-filter-post) method.
 *  *  If you do not ORDER BY the Rank field for the filter of your board, you will not be able to reorder issues on the board.
 *
 * @returns Returns the created board.
 */
export function createBoard(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    filterId?: number;
    location?: {
      projectKeyOrId?: string;
      type?: "project" | "user";
    };
    name?: string;
    type?: "kanban" | "scrum" | "agility";
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CreateBoardResponse> {
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/board",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board by filter id
 *
 * Returns any boards which use the provided filter id. This method can be executed by users without a valid software license in order to find which boards are using a particular filter.
 *
 * @param params.filterId - Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
 * @param params.startAt - The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of boards to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details.
 * @returns Returns the requested boards, at the specified page of the results.
 */
export function getBoardByFilterId(
  ctx: ClientContext,
  params: {
    /**
     * Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
     *
     * @format int64
     */
    filterId: number;
    /**
     * The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of boards to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  isLast?: boolean;
  /** @format int32 */
  maxResults?: number;
  /** @format int64 */
  startAt?: number;
  /** @format int64 */
  total?: number;
  values?: {
    /** @format int64 */
    id?: number;
    name?: string;
    /** @format uri */
    self?: string;
  }[];
}> {
  const { filterId, startAt, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/filter/{filterId}",
    pathParams: { filterId },
    query: { startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board
 *
 * Returns the board for the given board ID. This board will only be returned if the user has permission to view it. Admins without the view permission will see the board as a private one, so will see only a subset of the board's data (board location for instance).
 *
 * @param params.boardId - The ID of the requested board.
 * @returns Returns the requested board.
 */
export function getBoard(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the requested board.
     *
     * @format int64
     */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CreateBoardResponse> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}",
    pathParams: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete board
 *
 * Deletes the board. Admin without the view permission can still remove the board.
 *
 * @param params.boardId - ID of the board to be deleted
 * @returns Returned if the board has been successfully removed.
 */
export function deleteBoard(
  ctx: ClientContext,
  params: {
    /**
     * ID of the board to be deleted
     *
     * @format int64
     */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { boardId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/agile/1.0/board/{boardId}",
    pathParams: { boardId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for backlog
 *
 * Returns all issues from the board's backlog, for the given board ID. This only includes issues that the user has permission to view. The backlog contains incomplete issues that are not assigned to any future or active sprint. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - This parameter is currently not used.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getIssuesForBacklog(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that has the backlog containing the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** This parameter is currently not used. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SearchResults> {
  const { boardId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/backlog",
    pathParams: { boardId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for backlog (enhanced)
 *
 * Returns all issues from the board's backlog, for the given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. The backlog contains incomplete issues that are not assigned to any future or active sprint. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getIssuesForBacklogJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that has the backlog containing the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    boardId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/backlog",
    pathParams: { boardId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approximate issue count for backlog
 *
 * Returns the approximate count of all issues from the board's backlog, for the given board ID. This is equivalent to counting the issues on all pages returned by [Get issues for backlog enhanced](https://developer.atlassian.com/cloud/jira/software/rest/api-group-board/#api-rest-software-1-0-board-boardid-backlog-get). Recent updates might not be immediately visible in the returned output. This only includes issues that the user has permission to view.
 *
 * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
 * @param params.jql - Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @returns Returns the requested issue count.
 */
export function getApproximateIssueCountForBacklog(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that has the backlog containing the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /** Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead. */
    jql?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueCountBean> {
  const { boardId, jql } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/backlog/approximate-count",
    pathParams: { boardId },
    query: { jql },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get configuration
 *
 * Get the board configuration. The response contains the following fields:
 *
 *  *  `id` \- ID of the board.
 *  *  `name` \- Name of the board.
 *  *  `filter` \- Reference to the filter used by the given board.
 *  *  `location` \- Reference to the container that the board is located in. Includes the container type (Valid values: project, user).
 *  *  `subQuery` (Kanban only) - JQL subquery used by the given board.
 *  *  `columnConfig` \- The column configuration lists the columns for the board, in the order defined in the column configuration. For each column, it shows the issue status mapping as well as the constraint type (Valid values: none, issueCount, issueCountExclSubs) for the min/max number of issues. Note, the last column with statuses mapped to it is treated as the "Done" column, which means that issues in that column will be marked as already completed.
 *  *  `estimation` (Scrum only) - Contains information about type of estimation used for the board. Valid values: none, issueCount, field. If the estimation type is "field", the ID and display name of the field used for estimation is also returned. Note, estimates for an issue can be updated by a PUT /rest/api/3/issue/\{issueIdOrKey\} request, however the fields must be on the screen. "timeoriginalestimate" field will never be on the screen, so in order to update it "originalEstimate" in "timetracking" field should be updated.
 *  *  `ranking` \- Contains information about custom field used for ranking in the given board.
 *
 * @param params.boardId - The ID of the board for which configuration is requested.
 * @returns Returns the configuration of the board for given boardId.
 */
export function getConfiguration(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board for which configuration is requested.
     *
     * @format int64
     */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  columnConfig?: {
    columns?: {
      /** @format int32 */
      max?: number;
      /** @format int32 */
      min?: number;
      name?: string;
      statuses?: {
        id?: string;
        /** @format uri */
        self?: string;
      }[];
    }[];
    constraintType?: string;
  };
  estimation?: {
    field?: {
      displayName?: string;
      fieldId?: string;
    };
    type?: string;
  };
  filter?: {
    id?: string;
    /** @format uri */
    self?: string;
  };
  /** @format int64 */
  id?: number;
  location?: {
    projectKeyOrId?: string;
    type?: "project" | "user";
  };
  name?: string;
  ranking?: {
    /** @format int64 */
    rankCustomFieldId?: number;
  };
  /** @format uri */
  self?: string;
  subQuery?: {
    query?: string;
  };
  type?: string;
}> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/configuration",
    pathParams: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get epics
 *
 * Returns all epics from the board, for the given board ID. This only includes epics that the user has permission to view. Note, if the user does not have permission to view the board, no epics will be returned at all.
 *
 * @param params.boardId - The ID of the board that contains the requested epics.
 * @param params.startAt - The starting index of the returned epics. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of epics to return per page. See the 'Pagination' section at the top of this page for more details.
 * @param params.done - Filters results to epics that are either done or not done. Valid values: true, false.
 * @returns Returns the requested epics, at the specified page of the results.
 */
export function getEpics(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested epics.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned epics. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of epics to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
    /** Filters results to epics that are either done or not done. Valid values: true, false. */
    done?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, startAt, maxResults, done } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/epic",
    pathParams: { boardId },
    query: { startAt, maxResults, done },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues without epic for board
 *
 * Returns all issues that do not belong to any epic on a board, for a given board ID. This only includes issues that the user has permission to view. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getIssuesWithoutEpicForBoard(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/epic/none/issue",
    pathParams: { boardId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues without epic for board (enhanced)
 *
 * Returns all issues that do not belong to any epic on a board, for a given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getIssuesWithoutEpicForBoardJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    boardId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/epic/none/issue",
    pathParams: { boardId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board issues for epic
 *
 * Returns all issues that belong to an epic on the board, for the given epic ID and the board ID. This only includes issues that the user has permission to view. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.epicId - The ID of the epic that contains the requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getBoardIssuesForEpic(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The ID of the epic that contains the requested issues.
     *
     * @format int64
     */
    epicId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /** Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, epicId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/epic/{epicId}/issue",
    pathParams: { boardId, epicId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board issues for epic (enhanced)
 *
 * Returns all issues that belong to an epic on the board, for the given epic ID and the board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.epicId - The ID of the epic that contains the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getBoardIssuesForEpicJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The ID of the epic that contains the requested issues.
     *
     * @format int64
     */
    epicId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    boardId,
    epicId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/epic/{epicId}/issue",
    pathParams: { boardId, epicId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get features for board
 *
 * @returns 200 response
 */
export function getFeaturesForBoard(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<GetFeaturesForBoardResponse> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/features",
    pathParams: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Toggle features
 *
 * @returns 200 response
 */
export function toggleFeatures(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    boardId?: number;
    enabling?: boolean;
    feature?: string;
  } & {
    /** @format int64 */
    board_id_path: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<GetFeaturesForBoardResponse> {
  const { board_id_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/board/{boardId}/features",
    pathParams: { boardId: board_id_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for board
 *
 * Returns all issues from a board, for a given board ID. This only includes issues that the user has permission to view. An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belongs to the scrum boards. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - This parameter is currently not used.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getIssuesForBoard(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** This parameter is currently not used. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SearchResults> {
  const { boardId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/issue",
    pathParams: { boardId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move issues to board
 *
 * Move issues from the backog to the board (if they are already in the backlog of that board).
 * This operation either moves an issue(s) onto a board from the backlog (by adding it to the issueList for the board) Or transitions the issue(s) to the first column for a kanban board with backlog. At most 50 issues may be moved at once.
 *
 * @returns Empty response is returned if operation was successful.
 */
export function moveIssuesToBoard(
  ctx: ClientContext,
  params: MoveIssuesToBacklogForBoardBody & {
    /** @format int64 */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  entries?: {
    errors?: string[];
    /** @format int64 */
    issueId?: number;
    issueKey?: string;
    /** @format int32 */
    status?: number;
  }[];
}> {
  const { boardId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/agile/1.0/board/{boardId}/issue",
    pathParams: { boardId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues for board (enhanced)
 *
 * Returns all issues from a board, for a given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belong to scrum boards. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getIssuesForBoardJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    boardId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/issue",
    pathParams: { boardId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get approximate issue count for board
 *
 * Returns the approximate count of all issues from a board, for a given board ID. This is equivalent to counting the issues on all pages returned by [Get issues for board enhanced](https://developer.atlassian.com/cloud/jira/software/rest/api-group-board/#api-rest-software-1-0-board-boardid-issue-get). Recent updates might not be immediately visible in the returned output. This only includes issues that the user has permission to view.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.jql - Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @returns Returns the requested issue count.
 */
export function getApproximateIssueCountForBoard(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /** Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead. */
    jql?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueCountBean> {
  const { boardId, jql } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/issue/approximate-count",
    pathParams: { boardId },
    query: { jql },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get projects
 *
 * Returns all projects that are associated with the board, for the given board ID. If the user does not have permission to view the board, no projects will be returned at all. Returned projects are ordered by the name.
 *
 * A project is associated with a board if the board filter contains reference the project or there is an issue from the project that belongs to the board.
 *
 * The board filter contains reference the project only if JQL query guarantees that returned issues will be returned from the project set defined in JQL. For instance the query `project in (ABC, BCD) AND reporter = admin` have reference to ABC and BCD projects but query `project in (ABC, BCD) OR reporter = admin` doesn't have reference to any project.
 *
 * An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belongs to the scrum boards.
 *
 * @param params.boardId - The ID of the board that contains returned projects.
 * @param params.startAt - The starting index of the returned projects. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of projects to return per page. See the 'Pagination' section at the top of this page for more details.
 * @returns Returns the board's projects, at the specified page of the results.
 */
export function getProjects(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains returned projects.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned projects. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of projects to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, startAt, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/project",
    pathParams: { boardId },
    query: { startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get projects full
 *
 * Returns all projects that are statically associated with the board, for the given board ID. Returned projects are ordered by the name.
 *
 * A project is associated with a board if the board filter contains reference the project.
 *
 * The board filter contains reference the project only if JQL query guarantees that returned issues will be returned from the project set defined in JQL. For instance the query `project in (ABC, BCD) AND reporter = admin` have reference to ABC and BCD projects but query `project in (ABC, BCD) OR reporter = admin` doesn't have reference to any project.
 *
 * @param params.boardId - The ID of the board that contains returned projects.
 * @returns Returns the board's projects, at the specified page of the results.
 */
export function getProjectsFull(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains returned projects.
     *
     * @format int64
     */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/project/full",
    pathParams: { boardId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board property keys
 *
 * Returns the keys of all properties for the board identified by the id. The user who retrieves the property keys is required to have permissions to view the board.
 *
 * @param params.boardId - the ID of the board from which property keys will be returned.
 * @returns Returned if the board with given id exists.
 */
export function getBoardPropertyKeys(
  ctx: ClientContext,
  params: {
    /** the ID of the board from which property keys will be returned. */
    boardId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/properties",
    pathParams: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board property
 *
 * Returns the value of the property with a given key from the board identified by the provided id. The user who retrieves the property is required to have permissions to view the board.
 *
 * @param params.boardId - the ID of the board from which the property will be returned.
 * @param params.propertyKey - the key of the property to return.
 * @returns Returned if the board exists and the property was found.
 */
export function getBoardProperty(
  ctx: ClientContext,
  params: {
    /** the ID of the board from which the property will be returned. */
    boardId: string;
    /** the key of the property to return. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/properties/{propertyKey}",
    pathParams: { boardId, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set board property
 *
 * Sets the value of the specified board's property.
 *
 * You can use this resource to store a custom data against the board identified by the id. The user who stores the data is required to have permissions to modify the board.
 *
 * @param params.boardId - the ID of the board on which the property will be set.
 * @param params.propertyKey - the key of the board's property. The maximum length of the key is 255 bytes.
 * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
 * @returns Returned if the board property is successfully updated.
 */
export function setBoardProperty(
  ctx: ClientContext,
  params: {
    /** the ID of the board on which the property will be set. */
    boardId: string;
    /** the key of the board's property. The maximum length of the key is 255 bytes. */
    propertyKey: string;
    body: unknown;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, propertyKey, body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/agile/1.0/board/{boardId}/properties/{propertyKey}",
    pathParams: { boardId, propertyKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete board property
 *
 * Removes the property from the board identified by the id. Ths user removing the property is required to have permissions to modify the board.
 *
 * @param params.boardId - the id of the board from which the property will be removed.
 * @param params.propertyKey - the key of the property to remove.
 * @returns Returned if the board property was removed successfully.
 */
export function deleteBoardProperty(
  ctx: ClientContext,
  params: {
    /** the id of the board from which the property will be removed. */
    boardId: string;
    /** the key of the property to remove. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { boardId, propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/agile/1.0/board/{boardId}/properties/{propertyKey}",
    pathParams: { boardId, propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all quick filters
 *
 * Returns all quick filters from a board, for a given board ID.
 *
 * @param params.boardId - The ID of the board that contains the requested quick filters.
 * @param params.startAt - The starting index of the returned quick filters. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
 * @returns Returns the requested quick filters, at the specified page of the results. Quick filters will be ordered first by position.
 */
export function getAllQuickFilters(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested quick filters.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned quick filters. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  isLast?: boolean;
  /** @format int32 */
  maxResults?: number;
  /** @format int64 */
  startAt?: number;
  /** @format int64 */
  total?: number;
  values?: {
    /** @format int64 */
    boardId?: number;
    description?: string;
    /** @format int64 */
    id?: number;
    jql?: string;
    name?: string;
    /** @format int32 */
    position?: number;
  }[];
}> {
  const { boardId, startAt, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/quickfilter",
    pathParams: { boardId },
    query: { startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get quick filter
 *
 * Returns the quick filter for a given quick filter ID. The quick filter will only be returned if the user can view the board that the quick filter belongs to.
 *
 * @param params.quickFilterId - The ID of the requested quick filter.
 * @returns Returns the requested quick filter.
 */
export function getQuickFilter(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    boardId: number;
    /**
     * The ID of the requested quick filter.
     *
     * @format int64
     */
    quickFilterId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /** @format int64 */
  boardId?: number;
  description?: string;
  /** @format int64 */
  id?: number;
  jql?: string;
  name?: string;
  /** @format int32 */
  position?: number;
}> {
  const { boardId, quickFilterId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/quickfilter/{quickFilterId}",
    pathParams: { boardId, quickFilterId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get reports for board
 *
 * @returns 200 response
 */
export function getReportsForBoard(
  ctx: ClientContext,
  params: {
    /** @format int64 */
    boardId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  reports?: Record<string, unknown>[];
}> {
  const { boardId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/reports",
    pathParams: { boardId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all sprints
 *
 * Returns all sprints from a board, for a given board ID. This only includes sprints that the user has permission to view.
 *
 * @param params.boardId - The ID of the board that contains the requested sprints.
 * @param params.startAt - The starting index of the returned sprints. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
 * @param params.state - Filters results to sprints in specified states. Valid values: future, active, closed. You can define multiple states separated by commas, e.g. state=active,closed
 * @returns Returns the requested sprints, at the specified page of the results. Sprints will be ordered first by state (i.e. closed, active, future) then by their position in the backlog.
 */
export function getAllSprints(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested sprints.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned sprints. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
    /** Filters results to sprints in specified states. Valid values: future, active, closed. You can define multiple states separated by commas, e.g. state=active,closed */
    state?: Record<string, unknown>;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, startAt, maxResults, state } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/sprint",
    pathParams: { boardId },
    query: { startAt, maxResults, state },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board issues for sprint
 *
 * Get all issues you have access to that belong to the sprint from the board. Issue returned from this resource contains additional fields like: sprint, closedSprints, flagged and epic. Issues are returned ordered by rank. JQL order has higher priority than default rank.
 *
 * @param params.boardId - The ID of the board that contains requested issues.
 * @param params.sprintId - The ID of the sprint that contains requested issues.
 * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 * @deprecated
 */
export function getBoardIssuesForSprint(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The ID of the sprint that contains requested issues.
     *
     * @format int64
     */
    sprintId: number;
    /**
     * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     *
     * @format int32
     */
    maxResults?: number;
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, sprintId, startAt, maxResults, jql, validateQuery, fields, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/sprint/{sprintId}/issue",
    pathParams: { boardId, sprintId },
    query: { startAt, maxResults, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get board issues for sprint (enhanced)
 *
 * Get all issues you have access to that belong to the sprint from the board. Result pagination is token-based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource contains additional fields like: sprint, closedSprints, flagged, and epic. Issues are returned ordered by rank. JQL order has higher priority than default rank.
 *
 * @param params.boardId - The ID of the board that contains the requested issues.
 * @param params.sprintId - The ID of the sprint that contains the requested issues.
 * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
 * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
 * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
 * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
 * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
 * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
 * @param params.expand - A comma-separated list of the parameters to expand.
 * @returns Returns the requested issues, at the specified page of the results.
 */
export function getBoardIssuesForSprintJsis(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested issues.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The ID of the sprint that contains the requested issues.
     *
     * @format int64
     */
    sprintId: number;
    /**
     * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
     *
     * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     */
    nextPageToken?: string;
    /**
     * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     *
     * @format int32
     */
    maxResults?: number;
    /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
    reconcileIssues?: number[];
    /**
     * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     */
    jql?: string;
    /** Specifies whether to validate the JQL query or not. Default: true. */
    validateQuery?: boolean;
    /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
    fields?: Record<string, unknown>[];
    /** A comma-separated list of the parameters to expand. */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SoftwareIssueResults> {
  const {
    boardId,
    sprintId,
    nextPageToken,
    maxResults,
    reconcileIssues,
    jql,
    validateQuery,
    fields,
    expand,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/software/1.0/board/{boardId}/sprint/{sprintId}/issue",
    pathParams: { boardId, sprintId },
    query: { nextPageToken, maxResults, reconcileIssues, jql, validateQuery, fields, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all versions
 *
 * Returns all versions from a board, for a given board ID. This only includes versions that the user has permission to view. Note, if the user does not have permission to view the board, no versions will be returned at all. Returned versions are ordered by the name of the project from which they belong and then by sequence defined by user.
 *
 * @param params.boardId - The ID of the board that contains the requested versions.
 * @param params.startAt - The starting index of the returned versions. Base index: 0. See the 'Pagination' section at the top of this page for more details.
 * @param params.maxResults - The maximum number of versions to return per page. See the 'Pagination' section at the top of this page for more details.
 * @param params.released - Filters results to versions that are either released or unreleased. Valid values: true, false.
 * @returns Returns the requested versions, at the specified page of the results.
 */
export function getAllVersions(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the board that contains the requested versions.
     *
     * @format int64
     */
    boardId: number;
    /**
     * The starting index of the returned versions. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of versions to return per page. See the 'Pagination' section at the top of this page for more details.
     *
     * @format int32
     */
    maxResults?: number;
    /** Filters results to versions that are either released or unreleased. Valid values: true, false. */
    released?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { boardId, startAt, maxResults, released } = params;
  return ctx.request({
    method: "get",
    path: "/rest/agile/1.0/board/{boardId}/version",
    pathParams: { boardId },
    query: { startAt, maxResults, released },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Apis related to boards */
export function createBoardService(ctx: ClientContext) {
  return {
    /**
     * Get all boards
     *
     * Returns all boards. This only includes boards that the user has permission to view.
     *
     * **Deprecation notice:** The required OAuth 2.0 scopes will be updated on February 15, 2024.
     *
     *  *  `read:board-scope:jira-software`, `read:project:jira`
     *
     * @param params.startAt - The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of boards to return per page. See the 'Pagination' section at the top of this page for more details.
     * @param params.type - Filters results to boards of the specified types. Valid values: scrum, kanban, simple.
     * @param params.name - Filters results to boards that match or partially match the specified name.
     * @param params.projectKeyOrId - Filters results to boards that are relevant to a project. Relevance means that the jql filter defined in board contains a reference to a project.
     * @param params.includePrivate - Appends private boards to the end of the list. The name and type fields are excluded for security reasons.
     * @param params.negateLocationFiltering - If set to true, negate filters used for querying by location. By default false.
     * @param params.orderBy - Ordering of the results by a given field. If not provided, values will not be sorted. Valid values: name.
     * @param params.expand - List of fields to expand for each board. Valid values: admins, permissions.
     * @param params.projectTypeLocation - Filters results to boards that are relevant to a project types. Support Jira Software, Jira Service Management. Valid values: software, service\_desk. By default software.
     * @param params.filterId - Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
     * @returns Returns the requested boards, at the specified page of the results.
     */
    getAllBoards(
      params?: {
        /**
         * The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of boards to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /** Filters results to boards of the specified types. Valid values: scrum, kanban, simple. */
        type?: Record<string, unknown>;
        /** Filters results to boards that match or partially match the specified name. */
        name?: string;
        /** Filters results to boards that are relevant to a project. Relevance means that the jql filter defined in board contains a reference to a project. */
        projectKeyOrId?: string;
        accountIdLocation?: string;
        projectLocation?: string;
        /** Appends private boards to the end of the list. The name and type fields are excluded for security reasons. */
        includePrivate?: boolean;
        /** If set to true, negate filters used for querying by location. By default false. */
        negateLocationFiltering?: boolean;
        /** Ordering of the results by a given field. If not provided, values will not be sorted. Valid values: name. */
        orderBy?: "name" | "-name" | "+name";
        /** List of fields to expand for each board. Valid values: admins, permissions. */
        expand?: string;
        /** Filters results to boards that are relevant to a project types. Support Jira Software, Jira Service Management. Valid values: software, service\_desk. By default software. */
        projectTypeLocation?: string[];
        /**
         * Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
         *
         * @format int64
         */
        filterId?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      isLast?: boolean;
      /** @format int32 */
      maxResults?: number;
      /** @format int64 */
      startAt?: number;
      /** @format int64 */
      total?: number;
      values?: {
        admins?: {
          groups?: {
            name?: string;
            /** @format uri */
            self?: string;
          }[];
          users?: {
            /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
            accountId?: string;
            /** Whether the user is active. */
            active?: boolean;
            /** The avatars of the user. */
            avatarUrls?: {
              /**
               * The URL of the user's 16x16 pixel avatar.
               *
               * @format uri
               */
              "16x16"?: string;
              /**
               * The URL of the user's 24x24 pixel avatar.
               *
               * @format uri
               */
              "24x24"?: string;
              /**
               * The URL of the user's 32x32 pixel avatar.
               *
               * @format uri
               */
              "32x32"?: string;
              /**
               * The URL of the user's 48x48 pixel avatar.
               *
               * @format uri
               */
              "48x48"?: string;
            };
            /** The display name of the user. Depending on the user’s privacy setting, this may return an alternative value. */
            displayName?: string;
            /**
             * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.
             * The key of the user.
             */
            key?: string;
            /**
             * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.
             * The username of the user.
             */
            name?: string;
            /**
             * The URL of the user.
             *
             * @format uri
             */
            self?: string;
          }[];
        };
        /** Whether the board can be edited. */
        canEdit?: boolean;
        /** Whether the board is selected as a favorite. */
        favourite?: boolean;
        /**
         * The ID of the board.
         *
         * @format int64
         */
        id?: number;
        /** Whether the board is private. */
        isPrivate?: boolean;
        /** The container that the board is located in. */
        location?: {
          /** @format uri */
          avatarURI?: string;
          displayName?: string;
          name?: string;
          /** @format int64 */
          projectId?: number;
          projectKey?: string;
          projectName?: string;
          projectTypeKey?: string;
          userAccountId?: string;
          /** @format int64 */
          userId?: number;
        };
        /** The name of the board. */
        name?: string;
        /**
         * The URL of the board.
         *
         * @format uri
         */
        self?: string;
        /** The type the board. */
        type?: string;
      }[];
    }> {
      return getAllBoards(ctx, params, options);
    },

    /**
     * Create board
     *
     * Creates a new board. Board name, type and filter ID is required.
     *
     *  *  `name` \- Must be less than 255 characters.
     *  *  `type` \- Valid values: scrum, kanban
     *  *  `filterId` \- ID of a filter that the user has permissions to view. Note, if the user does not have the 'Create shared objects' permission and tries to create a shared board, a private board will be created instead (remember that board sharing depends on the filter sharing).
     *  *  `location` \- The container that the board will be located in. `location` must include the `type` property (Valid values: project, user). If choosing 'project', then a project must be specified by a `projectKeyOrId` property in `location`. If choosing 'user', the current user is chosen by default. The `projectKeyOrId` property should not be provided.
     *
     * Note:
     *
     *  *  If you want to create a new project with an associated board, use the [Jira platform REST API](https://docs.atlassian.com/jira/REST/latest). For more information, see the [Create project](#api-rest-api-3-project-post) method. The `projectTypeKey` for software boards must be 'software' and the `projectTemplateKey` must be either `com.pyxis.greenhopper.jira:gh-kanban-template` or `com.pyxis.greenhopper.jira:gh-scrum-template`.
     *  *  You can create a filter using the [Jira REST API](https://docs.atlassian.com/jira/REST/latest). For more information, see the [Create filter](#api-rest-api-3-filter-post) method.
     *  *  If you do not ORDER BY the Rank field for the filter of your board, you will not be able to reorder issues on the board.
     *
     * @returns Returns the created board.
     */
    createBoard(
      params: {
        /** @format int64 */
        filterId?: number;
        location?: {
          projectKeyOrId?: string;
          type?: "project" | "user";
        };
        name?: string;
        type?: "kanban" | "scrum" | "agility";
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CreateBoardResponse> {
      return createBoard(ctx, params, options);
    },

    /**
     * Get board by filter id
     *
     * Returns any boards which use the provided filter id. This method can be executed by users without a valid software license in order to find which boards are using a particular filter.
     *
     * @param params.filterId - Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
     * @param params.startAt - The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of boards to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details.
     * @returns Returns the requested boards, at the specified page of the results.
     */
    getBoardByFilterId(
      params: {
        /**
         * Filters results to boards that are relevant to a filter. Not supported for next-gen boards.
         *
         * @format int64
         */
        filterId: number;
        /**
         * The starting index of the returned boards. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of boards to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      isLast?: boolean;
      /** @format int32 */
      maxResults?: number;
      /** @format int64 */
      startAt?: number;
      /** @format int64 */
      total?: number;
      values?: {
        /** @format int64 */
        id?: number;
        name?: string;
        /** @format uri */
        self?: string;
      }[];
    }> {
      return getBoardByFilterId(ctx, params, options);
    },

    /**
     * Get board
     *
     * Returns the board for the given board ID. This board will only be returned if the user has permission to view it. Admins without the view permission will see the board as a private one, so will see only a subset of the board's data (board location for instance).
     *
     * @param params.boardId - The ID of the requested board.
     * @returns Returns the requested board.
     */
    getBoard(
      params: {
        /**
         * The ID of the requested board.
         *
         * @format int64
         */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CreateBoardResponse> {
      return getBoard(ctx, params, options);
    },

    /**
     * Delete board
     *
     * Deletes the board. Admin without the view permission can still remove the board.
     *
     * @param params.boardId - ID of the board to be deleted
     * @returns Returned if the board has been successfully removed.
     */
    deleteBoard(
      params: {
        /**
         * ID of the board to be deleted
         *
         * @format int64
         */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteBoard(ctx, params, options);
    },

    /**
     * Get issues for backlog
     *
     * Returns all issues from the board's backlog, for the given board ID. This only includes issues that the user has permission to view. The backlog contains incomplete issues that are not assigned to any future or active sprint. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - This parameter is currently not used.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getIssuesForBacklog(
      params: {
        /**
         * The ID of the board that has the backlog containing the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** This parameter is currently not used. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SearchResults> {
      return getIssuesForBacklog(ctx, params, options);
    },

    /**
     * Get issues for backlog (enhanced)
     *
     * Returns all issues from the board's backlog, for the given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. The backlog contains incomplete issues that are not assigned to any future or active sprint. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getIssuesForBacklogJsis(
      params: {
        /**
         * The ID of the board that has the backlog containing the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getIssuesForBacklogJsis(ctx, params, options);
    },

    /**
     * Get approximate issue count for backlog
     *
     * Returns the approximate count of all issues from the board's backlog, for the given board ID. This is equivalent to counting the issues on all pages returned by [Get issues for backlog enhanced](https://developer.atlassian.com/cloud/jira/software/rest/api-group-board/#api-rest-software-1-0-board-boardid-backlog-get). Recent updates might not be immediately visible in the returned output. This only includes issues that the user has permission to view.
     *
     * @param params.boardId - The ID of the board that has the backlog containing the requested issues.
     * @param params.jql - Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @returns Returns the requested issue count.
     */
    getApproximateIssueCountForBacklog(
      params: {
        /**
         * The ID of the board that has the backlog containing the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /** Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead. */
        jql?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueCountBean> {
      return getApproximateIssueCountForBacklog(ctx, params, options);
    },

    /**
     * Get configuration
     *
     * Get the board configuration. The response contains the following fields:
     *
     *  *  `id` \- ID of the board.
     *  *  `name` \- Name of the board.
     *  *  `filter` \- Reference to the filter used by the given board.
     *  *  `location` \- Reference to the container that the board is located in. Includes the container type (Valid values: project, user).
     *  *  `subQuery` (Kanban only) - JQL subquery used by the given board.
     *  *  `columnConfig` \- The column configuration lists the columns for the board, in the order defined in the column configuration. For each column, it shows the issue status mapping as well as the constraint type (Valid values: none, issueCount, issueCountExclSubs) for the min/max number of issues. Note, the last column with statuses mapped to it is treated as the "Done" column, which means that issues in that column will be marked as already completed.
     *  *  `estimation` (Scrum only) - Contains information about type of estimation used for the board. Valid values: none, issueCount, field. If the estimation type is "field", the ID and display name of the field used for estimation is also returned. Note, estimates for an issue can be updated by a PUT /rest/api/3/issue/\{issueIdOrKey\} request, however the fields must be on the screen. "timeoriginalestimate" field will never be on the screen, so in order to update it "originalEstimate" in "timetracking" field should be updated.
     *  *  `ranking` \- Contains information about custom field used for ranking in the given board.
     *
     * @param params.boardId - The ID of the board for which configuration is requested.
     * @returns Returns the configuration of the board for given boardId.
     */
    getConfiguration(
      params: {
        /**
         * The ID of the board for which configuration is requested.
         *
         * @format int64
         */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      columnConfig?: {
        columns?: {
          /** @format int32 */
          max?: number;
          /** @format int32 */
          min?: number;
          name?: string;
          statuses?: {
            id?: string;
            /** @format uri */
            self?: string;
          }[];
        }[];
        constraintType?: string;
      };
      estimation?: {
        field?: {
          displayName?: string;
          fieldId?: string;
        };
        type?: string;
      };
      filter?: {
        id?: string;
        /** @format uri */
        self?: string;
      };
      /** @format int64 */
      id?: number;
      location?: {
        projectKeyOrId?: string;
        type?: "project" | "user";
      };
      name?: string;
      ranking?: {
        /** @format int64 */
        rankCustomFieldId?: number;
      };
      /** @format uri */
      self?: string;
      subQuery?: {
        query?: string;
      };
      type?: string;
    }> {
      return getConfiguration(ctx, params, options);
    },

    /**
     * Get epics
     *
     * Returns all epics from the board, for the given board ID. This only includes epics that the user has permission to view. Note, if the user does not have permission to view the board, no epics will be returned at all.
     *
     * @param params.boardId - The ID of the board that contains the requested epics.
     * @param params.startAt - The starting index of the returned epics. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of epics to return per page. See the 'Pagination' section at the top of this page for more details.
     * @param params.done - Filters results to epics that are either done or not done. Valid values: true, false.
     * @returns Returns the requested epics, at the specified page of the results.
     */
    getEpics(
      params: {
        /**
         * The ID of the board that contains the requested epics.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned epics. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of epics to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
        /** Filters results to epics that are either done or not done. Valid values: true, false. */
        done?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getEpics(ctx, params, options);
    },

    /**
     * Get issues without epic for board
     *
     * Returns all issues that do not belong to any epic on a board, for a given board ID. This only includes issues that the user has permission to view. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getIssuesWithoutEpicForBoard(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getIssuesWithoutEpicForBoard(ctx, params, options);
    },

    /**
     * Get issues without epic for board (enhanced)
     *
     * Returns all issues that do not belong to any epic on a board, for a given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getIssuesWithoutEpicForBoardJsis(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getIssuesWithoutEpicForBoardJsis(ctx, params, options);
    },

    /**
     * Get board issues for epic
     *
     * Returns all issues that belong to an epic on the board, for the given epic ID and the board ID. This only includes issues that the user has permission to view. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.epicId - The ID of the epic that contains the requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getBoardIssuesForEpic(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The ID of the epic that contains the requested issues.
         *
         * @format int64
         */
        epicId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. Default: 50. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /** Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getBoardIssuesForEpic(ctx, params, options);
    },

    /**
     * Get board issues for epic (enhanced)
     *
     * Returns all issues that belong to an epic on the board, for the given epic ID and the board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.epicId - The ID of the epic that contains the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getBoardIssuesForEpicJsis(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The ID of the epic that contains the requested issues.
         *
         * @format int64
         */
        epicId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getBoardIssuesForEpicJsis(ctx, params, options);
    },

    /**
     * Get features for board
     *
     * @returns 200 response
     */
    getFeaturesForBoard(
      params: {
        /** @format int64 */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<GetFeaturesForBoardResponse> {
      return getFeaturesForBoard(ctx, params, options);
    },

    /**
     * Toggle features
     *
     * @returns 200 response
     */
    toggleFeatures(
      params: {
        /** @format int64 */
        boardId?: number;
        enabling?: boolean;
        feature?: string;
      } & {
        /** @format int64 */
        board_id_path: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<GetFeaturesForBoardResponse> {
      return toggleFeatures(ctx, params, options);
    },

    /**
     * Get issues for board
     *
     * Returns all issues from a board, for a given board ID. This only includes issues that the user has permission to view. An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belongs to the scrum boards. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource include Agile fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - This parameter is currently not used.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getIssuesForBoard(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** This parameter is currently not used. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SearchResults> {
      return getIssuesForBoard(ctx, params, options);
    },

    /**
     * Move issues to board
     *
     * Move issues from the backog to the board (if they are already in the backlog of that board).
     * This operation either moves an issue(s) onto a board from the backlog (by adding it to the issueList for the board) Or transitions the issue(s) to the first column for a kanban board with backlog. At most 50 issues may be moved at once.
     *
     * @returns Empty response is returned if operation was successful.
     */
    moveIssuesToBoard(
      params: MoveIssuesToBacklogForBoardBody & {
        /** @format int64 */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      entries?: {
        errors?: string[];
        /** @format int64 */
        issueId?: number;
        issueKey?: string;
        /** @format int32 */
        status?: number;
      }[];
    }> {
      return moveIssuesToBoard(ctx, params, options);
    },

    /**
     * Get issues for board (enhanced)
     *
     * Returns all issues from a board, for a given board ID. Result pagination is token based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belong to scrum boards. Issues returned from this resource include Software project fields, like sprint, closedSprints, flagged, and epic. By default, the returned issues are ordered by rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getIssuesForBoardJsis(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getIssuesForBoardJsis(ctx, params, options);
    },

    /**
     * Get approximate issue count for board
     *
     * Returns the approximate count of all issues from a board, for a given board ID. This is equivalent to counting the issues on all pages returned by [Get issues for board enhanced](https://developer.atlassian.com/cloud/jira/software/rest/api-group-board/#api-rest-software-1-0-board-boardid-issue-get). Recent updates might not be immediately visible in the returned output. This only includes issues that the user has permission to view.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.jql - Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @returns Returns the requested issue count.
     */
    getApproximateIssueCountForBoard(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /** Filters results using a JQL query. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead. */
        jql?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueCountBean> {
      return getApproximateIssueCountForBoard(ctx, params, options);
    },

    /**
     * Get projects
     *
     * Returns all projects that are associated with the board, for the given board ID. If the user does not have permission to view the board, no projects will be returned at all. Returned projects are ordered by the name.
     *
     * A project is associated with a board if the board filter contains reference the project or there is an issue from the project that belongs to the board.
     *
     * The board filter contains reference the project only if JQL query guarantees that returned issues will be returned from the project set defined in JQL. For instance the query `project in (ABC, BCD) AND reporter = admin` have reference to ABC and BCD projects but query `project in (ABC, BCD) OR reporter = admin` doesn't have reference to any project.
     *
     * An issue belongs to the board if its status is mapped to the board's column. Epic issues do not belongs to the scrum boards.
     *
     * @param params.boardId - The ID of the board that contains returned projects.
     * @param params.startAt - The starting index of the returned projects. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of projects to return per page. See the 'Pagination' section at the top of this page for more details.
     * @returns Returns the board's projects, at the specified page of the results.
     */
    getProjects(
      params: {
        /**
         * The ID of the board that contains returned projects.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned projects. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of projects to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getProjects(ctx, params, options);
    },

    /**
     * Get projects full
     *
     * Returns all projects that are statically associated with the board, for the given board ID. Returned projects are ordered by the name.
     *
     * A project is associated with a board if the board filter contains reference the project.
     *
     * The board filter contains reference the project only if JQL query guarantees that returned issues will be returned from the project set defined in JQL. For instance the query `project in (ABC, BCD) AND reporter = admin` have reference to ABC and BCD projects but query `project in (ABC, BCD) OR reporter = admin` doesn't have reference to any project.
     *
     * @param params.boardId - The ID of the board that contains returned projects.
     * @returns Returns the board's projects, at the specified page of the results.
     */
    getProjectsFull(
      params: {
        /**
         * The ID of the board that contains returned projects.
         *
         * @format int64
         */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return getProjectsFull(ctx, params, options);
    },

    /**
     * Get board property keys
     *
     * Returns the keys of all properties for the board identified by the id. The user who retrieves the property keys is required to have permissions to view the board.
     *
     * @param params.boardId - the ID of the board from which property keys will be returned.
     * @returns Returned if the board with given id exists.
     */
    getBoardPropertyKeys(
      params: {
        /** the ID of the board from which property keys will be returned. */
        boardId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getBoardPropertyKeys(ctx, params, options);
    },

    /**
     * Get board property
     *
     * Returns the value of the property with a given key from the board identified by the provided id. The user who retrieves the property is required to have permissions to view the board.
     *
     * @param params.boardId - the ID of the board from which the property will be returned.
     * @param params.propertyKey - the key of the property to return.
     * @returns Returned if the board exists and the property was found.
     */
    getBoardProperty(
      params: {
        /** the ID of the board from which the property will be returned. */
        boardId: string;
        /** the key of the property to return. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getBoardProperty(ctx, params, options);
    },

    /**
     * Set board property
     *
     * Sets the value of the specified board's property.
     *
     * You can use this resource to store a custom data against the board identified by the id. The user who stores the data is required to have permissions to modify the board.
     *
     * @param params.boardId - the ID of the board on which the property will be set.
     * @param params.propertyKey - the key of the board's property. The maximum length of the key is 255 bytes.
     * @param params.body - The value of the property. The value has to be a valid, non-empty [JSON](https://tools.ietf.org/html/rfc4627) value. The maximum length of the property value is 32768 bytes.
     * @returns Returned if the board property is successfully updated.
     */
    setBoardProperty(
      params: {
        /** the ID of the board on which the property will be set. */
        boardId: string;
        /** the key of the board's property. The maximum length of the key is 255 bytes. */
        propertyKey: string;
        body: unknown;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setBoardProperty(ctx, params, options);
    },

    /**
     * Delete board property
     *
     * Removes the property from the board identified by the id. Ths user removing the property is required to have permissions to modify the board.
     *
     * @param params.boardId - the id of the board from which the property will be removed.
     * @param params.propertyKey - the key of the property to remove.
     * @returns Returned if the board property was removed successfully.
     */
    deleteBoardProperty(
      params: {
        /** the id of the board from which the property will be removed. */
        boardId: string;
        /** the key of the property to remove. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteBoardProperty(ctx, params, options);
    },

    /**
     * Get all quick filters
     *
     * Returns all quick filters from a board, for a given board ID.
     *
     * @param params.boardId - The ID of the board that contains the requested quick filters.
     * @param params.startAt - The starting index of the returned quick filters. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
     * @returns Returns the requested quick filters, at the specified page of the results. Quick filters will be ordered first by position.
     */
    getAllQuickFilters(
      params: {
        /**
         * The ID of the board that contains the requested quick filters.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned quick filters. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      isLast?: boolean;
      /** @format int32 */
      maxResults?: number;
      /** @format int64 */
      startAt?: number;
      /** @format int64 */
      total?: number;
      values?: {
        /** @format int64 */
        boardId?: number;
        description?: string;
        /** @format int64 */
        id?: number;
        jql?: string;
        name?: string;
        /** @format int32 */
        position?: number;
      }[];
    }> {
      return getAllQuickFilters(ctx, params, options);
    },

    /**
     * Get quick filter
     *
     * Returns the quick filter for a given quick filter ID. The quick filter will only be returned if the user can view the board that the quick filter belongs to.
     *
     * @param params.quickFilterId - The ID of the requested quick filter.
     * @returns Returns the requested quick filter.
     */
    getQuickFilter(
      params: {
        /** @format int64 */
        boardId: number;
        /**
         * The ID of the requested quick filter.
         *
         * @format int64
         */
        quickFilterId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /** @format int64 */
      boardId?: number;
      description?: string;
      /** @format int64 */
      id?: number;
      jql?: string;
      name?: string;
      /** @format int32 */
      position?: number;
    }> {
      return getQuickFilter(ctx, params, options);
    },

    /**
     * Get reports for board
     *
     * @returns 200 response
     */
    getReportsForBoard(
      params: {
        /** @format int64 */
        boardId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      reports?: Record<string, unknown>[];
    }> {
      return getReportsForBoard(ctx, params, options);
    },

    /**
     * Get all sprints
     *
     * Returns all sprints from a board, for a given board ID. This only includes sprints that the user has permission to view.
     *
     * @param params.boardId - The ID of the board that contains the requested sprints.
     * @param params.startAt - The starting index of the returned sprints. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
     * @param params.state - Filters results to sprints in specified states. Valid values: future, active, closed. You can define multiple states separated by commas, e.g. state=active,closed
     * @returns Returns the requested sprints, at the specified page of the results. Sprints will be ordered first by state (i.e. closed, active, future) then by their position in the backlog.
     */
    getAllSprints(
      params: {
        /**
         * The ID of the board that contains the requested sprints.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned sprints. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of sprints to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
        /** Filters results to sprints in specified states. Valid values: future, active, closed. You can define multiple states separated by commas, e.g. state=active,closed */
        state?: Record<string, unknown>;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getAllSprints(ctx, params, options);
    },

    /**
     * Get board issues for sprint
     *
     * Get all issues you have access to that belong to the sprint from the board. Issue returned from this resource contains additional fields like: sprint, closedSprints, flagged and epic. Issues are returned ordered by rank. JQL order has higher priority than default rank.
     *
     * @param params.boardId - The ID of the board that contains requested issues.
     * @param params.sprintId - The ID of the sprint that contains requested issues.
     * @param params.startAt - The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Agile fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     * @deprecated
     */
    getBoardIssuesForSprint(
      params: {
        /**
         * The ID of the board that contains requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The ID of the sprint that contains requested issues.
         *
         * @format int64
         */
        sprintId: number;
        /**
         * The starting index of the returned issues. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of issues to return per page. See the 'Pagination' section at the top of this page for more details. Note, the total number of issues returned is limited by the property 'jira.search.views.default.max' in your Jira instance. If you exceed this limit, your results will be truncated.
         *
         * @format int32
         */
        maxResults?: number;
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Agile fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getBoardIssuesForSprint(ctx, params, options);
    },

    /**
     * Get board issues for sprint (enhanced)
     *
     * Get all issues you have access to that belong to the sprint from the board. Result pagination is token-based, using `nextPageToken` and `maxResults`. This only includes issues that the user has permission to view. Note, if the user does not have permission to view the board, no issues will be returned at all. Issues returned from this resource contains additional fields like: sprint, closedSprints, flagged, and epic. Issues are returned ordered by rank. JQL order has higher priority than default rank.
     *
     * @param params.boardId - The ID of the board that contains the requested issues.
     * @param params.sprintId - The ID of the sprint that contains the requested issues.
     * @param params.nextPageToken - The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
     * @param params.maxResults - The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
     * @param params.reconcileIssues - Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages.
     * @param params.jql - Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues. Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
     * @param params.validateQuery - Specifies whether to validate the JQL query or not. Default: true.
     * @param params.fields - The list of fields to return for each issue. By default, all navigable and Software project fields are returned.
     * @param params.expand - A comma-separated list of the parameters to expand.
     * @returns Returns the requested issues, at the specified page of the results.
     */
    getBoardIssuesForSprintJsis(
      params: {
        /**
         * The ID of the board that contains the requested issues.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The ID of the sprint that contains the requested issues.
         *
         * @format int64
         */
        sprintId: number;
        /**
         * The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues.
         *
         * Note: The `nextPageToken` field is **not included** in the response for the last page, indicating there is no next page.
         */
        nextPageToken?: string;
        /**
         * The maximum number of items to return per page. To manage page size, the API may return fewer items per page where there is a large number of fields or properties returned. It returns max 5000 issues.
         *
         * @format int32
         */
        maxResults?: number;
        /** Strong consistency issue IDs to be reconciled with search results. Accepts max 50 IDs. This list of IDs should be consistent with each paginated request across different pages. */
        reconcileIssues?: number[];
        /**
         * Filters results using a JQL query. If you define an order in your JQL query, it will override the default order of the returned issues.
         * Note that `username` and `userkey` can't be used as search terms for this parameter due to privacy reasons. Use `accountId` instead.
         */
        jql?: string;
        /** Specifies whether to validate the JQL query or not. Default: true. */
        validateQuery?: boolean;
        /** The list of fields to return for each issue. By default, all navigable and Software project fields are returned. */
        fields?: Record<string, unknown>[];
        /** A comma-separated list of the parameters to expand. */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SoftwareIssueResults> {
      return getBoardIssuesForSprintJsis(ctx, params, options);
    },

    /**
     * Get all versions
     *
     * Returns all versions from a board, for a given board ID. This only includes versions that the user has permission to view. Note, if the user does not have permission to view the board, no versions will be returned at all. Returned versions are ordered by the name of the project from which they belong and then by sequence defined by user.
     *
     * @param params.boardId - The ID of the board that contains the requested versions.
     * @param params.startAt - The starting index of the returned versions. Base index: 0. See the 'Pagination' section at the top of this page for more details.
     * @param params.maxResults - The maximum number of versions to return per page. See the 'Pagination' section at the top of this page for more details.
     * @param params.released - Filters results to versions that are either released or unreleased. Valid values: true, false.
     * @returns Returns the requested versions, at the specified page of the results.
     */
    getAllVersions(
      params: {
        /**
         * The ID of the board that contains the requested versions.
         *
         * @format int64
         */
        boardId: number;
        /**
         * The starting index of the returned versions. Base index: 0. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of versions to return per page. See the 'Pagination' section at the top of this page for more details.
         *
         * @format int32
         */
        maxResults?: number;
        /** Filters results to versions that are either released or unreleased. Valid values: true, false. */
        released?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getAllVersions(ctx, params, options);
    },
  };
}
