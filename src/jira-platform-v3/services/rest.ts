// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { BulkWorklogKeyRequestBean, BulkWorklogKeyResponseBean } from "../types/index.ts";

/**
 * Get worklogs by issue id and worklog id
 *
 * Returns worklog details for a list of issue ID and worklog ID pairs.
 *
 * This is an internal API for bulk fetching worklogs by their issue and worklog IDs. Worklogs that don't exist will be filtered out from the response.
 *
 * The returned list of worklogs is limited to 1000 items.
 *
 * **[Permissions](#permissions) required:** This is an internal service-to-service API that requires ASAP authentication. No user permission checks are performed as this bypasses normal user context.
 *
 * @returns Returned if the request is successful.
 */
export function getWorklogsByIssueIdAndWorklogId(
  ctx: ClientContext,
  params: BulkWorklogKeyRequestBean,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<BulkWorklogKeyResponseBean> {
  return ctx.request({
    method: "post",
    path: "/rest/internal/api/latest/worklog/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `rest` service. */
export function createRestService(ctx: ClientContext) {
  return {
    /**
     * Get worklogs by issue id and worklog id
     *
     * Returns worklog details for a list of issue ID and worklog ID pairs.
     *
     * This is an internal API for bulk fetching worklogs by their issue and worklog IDs. Worklogs that don't exist will be filtered out from the response.
     *
     * The returned list of worklogs is limited to 1000 items.
     *
     * **[Permissions](#permissions) required:** This is an internal service-to-service API that requires ASAP authentication. No user permission checks are performed as this bypasses normal user context.
     *
     * @returns Returned if the request is successful.
     */
    getWorklogsByIssueIdAndWorklogId(
      params: BulkWorklogKeyRequestBean,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<BulkWorklogKeyResponseBean> {
      return getWorklogsByIssueIdAndWorklogId(ctx, params, options);
    },
  };
}
