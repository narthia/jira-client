// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { IssueLinkType, IssueLinkTypes } from "../types/index.ts";

/**
 * Get issue link types
 *
 * Returns a list of all issue link types.
 *
 * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project in the site.
 *
 * @returns Returned if the request is successful.
 */
export function getIssueLinkTypes(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueLinkTypes> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issueLinkType",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create issue link type
 *
 * Creates an issue link type. Use this operation to create descriptions of the reasons why issues are linked. The issue link type consists of a name and descriptions for a link's inward and outward relationships.
 *
 * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createIssueLinkType(
  ctx: ClientContext,
  params: IssueLinkType,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueLinkType> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/issueLinkType",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue link type
 *
 * Returns an issue link type.
 *
 * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project in the site.
 *
 * @param params.issueLinkTypeId - The ID of the issue link type.
 * @returns Returned if the request is successful.
 */
export function getIssueLinkType(
  ctx: ClientContext,
  params: {
    /** The ID of the issue link type. */
    issueLinkTypeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueLinkType> {
  const { issueLinkTypeId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issueLinkType/{issueLinkTypeId}",
    pathParams: { issueLinkTypeId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue link type
 *
 * Updates an issue link type.
 *
 * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueLinkTypeId - The ID of the issue link type.
 * @returns Returned if the request is successful.
 */
export function updateIssueLinkType(
  ctx: ClientContext,
  params: IssueLinkType & {
    /** The ID of the issue link type. */
    issueLinkTypeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueLinkType> {
  const { issueLinkTypeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issueLinkType/{issueLinkTypeId}",
    pathParams: { issueLinkTypeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue link type
 *
 * Deletes an issue link type.
 *
 * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueLinkTypeId - The ID of the issue link type.
 * @returns Returned if the request is successful.
 */
export function deleteIssueLinkType(
  ctx: ClientContext,
  params: {
    /** The ID of the issue link type. */
    issueLinkTypeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueLinkTypeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issueLinkType/{issueLinkTypeId}",
    pathParams: { issueLinkTypeId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents [issue link](#api-group-Issue-links) types. Use it to get, create, update, and delete link issue types as well as get lists of all link issue types.
 *
 * To use it, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
 */
export function createIssueLinkTypesService(ctx: ClientContext) {
  return {
    /**
     * Get issue link types
     *
     * Returns a list of all issue link types.
     *
     * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project in the site.
     *
     * @returns Returned if the request is successful.
     */
    getIssueLinkTypes(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<IssueLinkTypes> {
      return getIssueLinkTypes(ctx, options);
    },

    /**
     * Create issue link type
     *
     * Creates an issue link type. Use this operation to create descriptions of the reasons why issues are linked. The issue link type consists of a name and descriptions for a link's inward and outward relationships.
     *
     * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createIssueLinkType(
      params: IssueLinkType,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueLinkType> {
      return createIssueLinkType(ctx, params, options);
    },

    /**
     * Get issue link type
     *
     * Returns an issue link type.
     *
     * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for a project in the site.
     *
     * @param params.issueLinkTypeId - The ID of the issue link type.
     * @returns Returned if the request is successful.
     */
    getIssueLinkType(
      params: {
        /** The ID of the issue link type. */
        issueLinkTypeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueLinkType> {
      return getIssueLinkType(ctx, params, options);
    },

    /**
     * Update issue link type
     *
     * Updates an issue link type.
     *
     * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueLinkTypeId - The ID of the issue link type.
     * @returns Returned if the request is successful.
     */
    updateIssueLinkType(
      params: IssueLinkType & {
        /** The ID of the issue link type. */
        issueLinkTypeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueLinkType> {
      return updateIssueLinkType(ctx, params, options);
    },

    /**
     * Delete issue link type
     *
     * Deletes an issue link type.
     *
     * To use this operation, the site must have [issue linking](https://confluence.atlassian.com/x/yoXKM) enabled.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueLinkTypeId - The ID of the issue link type.
     * @returns Returned if the request is successful.
     */
    deleteIssueLinkType(
      params: {
        /** The ID of the issue link type. */
        issueLinkTypeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteIssueLinkType(ctx, params, options);
    },
  };
}
