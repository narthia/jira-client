// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { PagedDtoRequestTypeDto } from "../types/index.ts";

/**
 * Get all request types
 *
 * This method returns all customer request types used in the Jira Service Management instance, optionally filtered by a query string.
 *
 * Use [servicedeskapi/servicedesk/\{serviceDeskId\}/requesttype](#api-servicedesk-serviceDeskId-requesttype-get) to find the customer request types supported by a specific service desk.
 *
 * The returned list of customer request types can be filtered using the `searchQuery` parameter. The parameter is matched against the customer request types' `name` or `description`. For example, searching for "Install", "Inst", "Equi", or "Equipment" will match a customer request type with the *name* "Equipment Installation Request".
 *
 * **Note:** This API by default will filter out request types hidden in the portal (i.e. request types without groups and request types where a user doesn't have permission) when `searchQuery` is provided, unless `includeHiddenRequestTypesInSearch` is set to true. Restricted request types will not be returned for those who aren't admins.
 *
 * **[Permissions](#permissions) required**: Any
 *
 * @param params.searchQuery - String to be used to filter the results.
 * @param params.serviceDeskId - Filter the request types by service desk Ids provided. Multiple values of the query parameter are supported. For example, `serviceDeskId=1&serviceDeskId=2` will return request types only for service desks 1 and 2.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @param params.includeHiddenRequestTypesInSearch - Whether to include hidden request types when searching with `searchQuery`.
 * @param params.restrictionStatus - Request type restriction status (`open` or `restricted`) used to filter the results.
 * @returns Returns the request types, on the specified page of the results.
 */
export function getAllRequestTypes(
  ctx: ClientContext,
  params?: {
    /** String to be used to filter the results. */
    searchQuery?: string;
    /** Filter the request types by service desk Ids provided. Multiple values of the query parameter are supported. For example, `serviceDeskId=1&serviceDeskId=2` will return request types only for service desks 1 and 2. */
    serviceDeskId?: number[];
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
    expand?: string[];
    /**
     * Whether to include hidden request types when searching with `searchQuery`.
     *
     * @default false
     */
    includeHiddenRequestTypesInSearch?: boolean;
    /** Request type restriction status (`open` or `restricted`) used to filter the results. */
    restrictionStatus?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoRequestTypeDto> {
  const {
    searchQuery,
    serviceDeskId,
    start,
    limit,
    expand,
    includeHiddenRequestTypesInSearch,
    restrictionStatus,
  } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/requesttype",
    query: {
      searchQuery,
      serviceDeskId,
      start,
      limit,
      expand,
      includeHiddenRequestTypesInSearch,
      restrictionStatus,
    },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `requesttype` service. */
export function createRequesttypeService(ctx: ClientContext) {
  return {
    /**
     * Get all request types
     *
     * This method returns all customer request types used in the Jira Service Management instance, optionally filtered by a query string.
     *
     * Use [servicedeskapi/servicedesk/\{serviceDeskId\}/requesttype](#api-servicedesk-serviceDeskId-requesttype-get) to find the customer request types supported by a specific service desk.
     *
     * The returned list of customer request types can be filtered using the `searchQuery` parameter. The parameter is matched against the customer request types' `name` or `description`. For example, searching for "Install", "Inst", "Equi", or "Equipment" will match a customer request type with the *name* "Equipment Installation Request".
     *
     * **Note:** This API by default will filter out request types hidden in the portal (i.e. request types without groups and request types where a user doesn't have permission) when `searchQuery` is provided, unless `includeHiddenRequestTypesInSearch` is set to true. Restricted request types will not be returned for those who aren't admins.
     *
     * **[Permissions](#permissions) required**: Any
     *
     * @param params.searchQuery - String to be used to filter the results.
     * @param params.serviceDeskId - Filter the request types by service desk Ids provided. Multiple values of the query parameter are supported. For example, `serviceDeskId=1&serviceDeskId=2` will return request types only for service desks 1 and 2.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @param params.includeHiddenRequestTypesInSearch - Whether to include hidden request types when searching with `searchQuery`.
     * @param params.restrictionStatus - Request type restriction status (`open` or `restricted`) used to filter the results.
     * @returns Returns the request types, on the specified page of the results.
     */
    getAllRequestTypes(
      params?: {
        /** String to be used to filter the results. */
        searchQuery?: string;
        /** Filter the request types by service desk Ids provided. Multiple values of the query parameter are supported. For example, `serviceDeskId=1&serviceDeskId=2` will return request types only for service desks 1 and 2. */
        serviceDeskId?: number[];
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
        expand?: string[];
        /**
         * Whether to include hidden request types when searching with `searchQuery`.
         *
         * @default false
         */
        includeHiddenRequestTypesInSearch?: boolean;
        /** Request type restriction status (`open` or `restricted`) used to filter the results. */
        restrictionStatus?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoRequestTypeDto> {
      return getAllRequestTypes(ctx, params, options);
    },
  };
}
