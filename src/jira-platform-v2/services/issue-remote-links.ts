// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  RemoteIssueLink,
  RemoteIssueLinkIdentifies,
  RemoteIssueLinkRequest,
} from "../types/index.ts";

/**
 * Get remote issue links
 *
 * Returns the remote issue links for an issue. When a remote issue link global ID is provided the record with that global ID is returned, otherwise all remote issue links are returned. Where a global ID includes reserved URL characters these must be escaped in the request. For example, pass `system=http://www.mycompany.com/support&id=1` as `system%3Dhttp%3A%2F%2Fwww.mycompany.com%2Fsupport%26id%3D1`.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @param params.globalId - The global ID of the remote issue link.
 * @returns Returned if the request is successful. A single RemoteIssueLink will be returned when specifying `globalId`, otherwise an array of RemoteIssueLink is returned.
 */
export function getRemoteIssueLinks(
  ctx: ClientContext,
  params: {
    /**
     * The ID or key of the issue.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    issueIdOrKey: string;
    /** The global ID of the remote issue link. */
    globalId?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RemoteIssueLink[] | RemoteIssueLink> {
  const { issueIdOrKey, globalId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink",
    pathParams: { issueIdOrKey },
    query: { globalId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create or update remote issue link
 *
 * Creates or updates a remote issue link for an issue.
 *
 * If a `globalId` is provided and a remote issue link with that global ID is found it is updated. Any fields without values in the request are set to null. Otherwise, the remote issue link is created.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @returns Returned if the remote issue link is updated.
 */
export function createOrUpdateRemoteIssueLink(
  ctx: ClientContext,
  params: RemoteIssueLinkRequest & {
    /** The ID or key of the issue. */
    issueIdOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RemoteIssueLinkIdentifies> {
  const { issueIdOrKey, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink",
    pathParams: { issueIdOrKey },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete remote issue link by global ID
 *
 * Deletes the remote issue link from the issue using the link's global ID. Where the global ID includes reserved URL characters these must be escaped in the request. For example, pass `system=http://www.mycompany.com/support&id=1` as `system%3Dhttp%3A%2F%2Fwww.mycompany.com%2Fsupport%26id%3D1`.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is implemented, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @param params.globalId - The global ID of a remote issue link.
 * @returns Returned if the request is successful.
 */
export function deleteRemoteIssueLinkByGlobalId(
  ctx: ClientContext,
  params: {
    /**
     * The ID or key of the issue.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    issueIdOrKey: string;
    /**
     * The global ID of a remote issue link.
     *
     * @example
     * ```json
     * "system=http://www.mycompany.com/support&id=1"
     * ```
     */
    globalId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueIdOrKey, globalId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink",
    pathParams: { issueIdOrKey },
    query: { globalId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get remote issue link by ID
 *
 * Returns a remote issue link for an issue.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @param params.linkId - The ID of the remote issue link.
 * @returns Returned if the request is successful.
 */
export function getRemoteIssueLinkById(
  ctx: ClientContext,
  params: {
    /** The ID or key of the issue. */
    issueIdOrKey: string;
    /** The ID of the remote issue link. */
    linkId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RemoteIssueLink> {
  const { issueIdOrKey, linkId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink/{linkId}",
    pathParams: { issueIdOrKey, linkId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update remote issue link by ID
 *
 * Updates a remote issue link for an issue.
 *
 * Note: Fields without values in the request are set to null.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @param params.linkId - The ID of the remote issue link.
 * @returns Returned if the request is successful.
 */
export function updateRemoteIssueLink(
  ctx: ClientContext,
  params: RemoteIssueLinkRequest & {
    /**
     * The ID or key of the issue.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    issueIdOrKey: string;
    /**
     * The ID of the remote issue link.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    linkId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueIdOrKey, linkId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink/{linkId}",
    pathParams: { issueIdOrKey, linkId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete remote issue link by ID
 *
 * Deletes a remote issue link from an issue.
 *
 * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Browse projects*, *Edit issues*, and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
 *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
 *
 * @param params.issueIdOrKey - The ID or key of the issue.
 * @param params.linkId - The ID of a remote issue link.
 * @returns Returned if the request is successful.
 */
export function deleteRemoteIssueLinkById(
  ctx: ClientContext,
  params: {
    /**
     * The ID or key of the issue.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    issueIdOrKey: string;
    /**
     * The ID of a remote issue link.
     *
     * @example
     * ```json
     * "10000"
     * ```
     */
    linkId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { issueIdOrKey, linkId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/issue/{issueIdOrKey}/remotelink/{linkId}",
    pathParams: { issueIdOrKey, linkId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents remote issue links, a way of linking Jira to information in other systems. Use it to get, create, update, and delete remote issue links either by ID or global ID. The global ID provides a way of accessing remote issue links using information about the item's remote system host and remote system identifier. */
export function createIssueRemoteLinksService(ctx: ClientContext) {
  return {
    /**
     * Get remote issue links
     *
     * Returns the remote issue links for an issue. When a remote issue link global ID is provided the record with that global ID is returned, otherwise all remote issue links are returned. Where a global ID includes reserved URL characters these must be escaped in the request. For example, pass `system=http://www.mycompany.com/support&id=1` as `system%3Dhttp%3A%2F%2Fwww.mycompany.com%2Fsupport%26id%3D1`.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @param params.globalId - The global ID of the remote issue link.
     * @returns Returned if the request is successful. A single RemoteIssueLink will be returned when specifying `globalId`, otherwise an array of RemoteIssueLink is returned.
     */
    getRemoteIssueLinks(
      params: {
        /**
         * The ID or key of the issue.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        issueIdOrKey: string;
        /** The global ID of the remote issue link. */
        globalId?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RemoteIssueLink[] | RemoteIssueLink> {
      return getRemoteIssueLinks(ctx, params, options);
    },

    /**
     * Create or update remote issue link
     *
     * Creates or updates a remote issue link for an issue.
     *
     * If a `globalId` is provided and a remote issue link with that global ID is found it is updated. Any fields without values in the request are set to null. Otherwise, the remote issue link is created.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @returns Returned if the remote issue link is updated.
     */
    createOrUpdateRemoteIssueLink(
      params: RemoteIssueLinkRequest & {
        /** The ID or key of the issue. */
        issueIdOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RemoteIssueLinkIdentifies> {
      return createOrUpdateRemoteIssueLink(ctx, params, options);
    },

    /**
     * Delete remote issue link by global ID
     *
     * Deletes the remote issue link from the issue using the link's global ID. Where the global ID includes reserved URL characters these must be escaped in the request. For example, pass `system=http://www.mycompany.com/support&id=1` as `system%3Dhttp%3A%2F%2Fwww.mycompany.com%2Fsupport%26id%3D1`.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is implemented, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @param params.globalId - The global ID of a remote issue link.
     * @returns Returned if the request is successful.
     */
    deleteRemoteIssueLinkByGlobalId(
      params: {
        /**
         * The ID or key of the issue.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        issueIdOrKey: string;
        /**
         * The global ID of a remote issue link.
         *
         * @example
         * ```json
         * "system=http://www.mycompany.com/support&id=1"
         * ```
         */
        globalId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteRemoteIssueLinkByGlobalId(ctx, params, options);
    },

    /**
     * Get remote issue link by ID
     *
     * Returns a remote issue link for an issue.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @param params.linkId - The ID of the remote issue link.
     * @returns Returned if the request is successful.
     */
    getRemoteIssueLinkById(
      params: {
        /** The ID or key of the issue. */
        issueIdOrKey: string;
        /** The ID of the remote issue link. */
        linkId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RemoteIssueLink> {
      return getRemoteIssueLinkById(ctx, params, options);
    },

    /**
     * Update remote issue link by ID
     *
     * Updates a remote issue link for an issue.
     *
     * Note: Fields without values in the request are set to null.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects* and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @param params.linkId - The ID of the remote issue link.
     * @returns Returned if the request is successful.
     */
    updateRemoteIssueLink(
      params: RemoteIssueLinkRequest & {
        /**
         * The ID or key of the issue.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        issueIdOrKey: string;
        /**
         * The ID of the remote issue link.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        linkId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateRemoteIssueLink(ctx, params, options);
    },

    /**
     * Delete remote issue link by ID
     *
     * Deletes a remote issue link from an issue.
     *
     * This operation requires [issue linking to be active](https://confluence.atlassian.com/x/yoXKM).
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Browse projects*, *Edit issues*, and *Link issues* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.
     *  *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue.
     *
     * @param params.issueIdOrKey - The ID or key of the issue.
     * @param params.linkId - The ID of a remote issue link.
     * @returns Returned if the request is successful.
     */
    deleteRemoteIssueLinkById(
      params: {
        /**
         * The ID or key of the issue.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        issueIdOrKey: string;
        /**
         * The ID of a remote issue link.
         *
         * @example
         * ```json
         * "10000"
         * ```
         */
        linkId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteRemoteIssueLinkById(ctx, params, options);
    },
  };
}
