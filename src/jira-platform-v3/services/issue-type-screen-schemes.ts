// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  IssueTypeIds,
  IssueTypeScreenSchemeDetails,
  IssueTypeScreenSchemeId,
  IssueTypeScreenSchemeMappingDetails,
  IssueTypeScreenSchemeProjectAssociation,
  IssueTypeScreenSchemeUpdateDetails,
  PageBeanIssueTypeScreenScheme,
  PageBeanIssueTypeScreenSchemeItem,
  PageBeanIssueTypeScreenSchemesProjects,
  PageBeanProjectDetails,
  UpdateDefaultScreenScheme,
} from "../types/index.ts";

/**
 * Get issue type screen schemes
 *
 * Returns a [paginated](#pagination) list of issue type screen schemes.
 *
 * Only issue type screen schemes used in classic projects are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.id - The list of issue type screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`.
 * @param params.queryString - String used to perform a case-insensitive partial match with issue type screen scheme name.
 * @param params.orderBy - [Order](#ordering) the results by a field: *  `name` Sorts by issue type screen scheme name. *  `id` Sorts by issue type screen scheme ID.
 * @param params.expand - Use [expand](#expansion) to include additional information in the response. This parameter accepts `projects` that, for each issue type screen schemes, returns information about the projects the issue type screen scheme is assigned to.
 * @returns Returned if the request is successful.
 */
export function getIssueTypeScreenSchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /** The list of issue type screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`. */
    id?: number[];
    /**
     * String used to perform a case-insensitive partial match with issue type screen scheme name.
     *
     * @default ""
     */
    queryString?: string;
    /**
     * [Order](#ordering) the results by a field:
     *
     *  *  `name` Sorts by issue type screen scheme name.
     *  *  `id` Sorts by issue type screen scheme ID.
     *
     * @default "id"
     */
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
    /**
     * Use [expand](#expansion) to include additional information in the response. This parameter accepts `projects` that, for each issue type screen schemes, returns information about the projects the issue type screen scheme is assigned to.
     *
     * @default ""
     */
    expand?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanIssueTypeScreenScheme> {
  const { startAt, maxResults, id, queryString, orderBy, expand } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/issuetypescreenscheme",
    query: { startAt, maxResults, id, queryString, orderBy, expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create issue type screen scheme
 *
 * Creates an issue type screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createIssueTypeScreenScheme(
  ctx: ClientContext,
  params: IssueTypeScreenSchemeDetails,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeScreenSchemeId> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/issuetypescreenscheme",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue type screen scheme items
 *
 * Returns a [paginated](#pagination) list of issue type screen scheme items.
 *
 * Only issue type screen schemes used in classic projects are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.issueTypeScreenSchemeId - The list of issue type screen scheme IDs. To include multiple issue type screen schemes, separate IDs with ampersand: `issueTypeScreenSchemeId=10000&issueTypeScreenSchemeId=10001`.
 * @returns Returned if the request is successful.
 */
export function getIssueTypeScreenSchemeMappings(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /** The list of issue type screen scheme IDs. To include multiple issue type screen schemes, separate IDs with ampersand: `issueTypeScreenSchemeId=10000&issueTypeScreenSchemeId=10001`. */
    issueTypeScreenSchemeId?: number[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanIssueTypeScreenSchemeItem> {
  const { startAt, maxResults, issueTypeScreenSchemeId } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/issuetypescreenscheme/mapping",
    query: { startAt, maxResults, issueTypeScreenSchemeId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue type screen schemes for projects
 *
 * Returns a [paginated](#pagination) list of issue type screen schemes and, for each issue type screen scheme, a list of the projects that use it.
 *
 * Only issue type screen schemes used in classic projects are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @param params.projectId - The list of project IDs. To include multiple projects, separate IDs with ampersand: `projectId=10000&projectId=10001`.
 * @returns Returned if the request is successful.
 */
export function getIssueTypeScreenSchemeProjectAssociations(
  ctx: ClientContext,
  params: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /** The list of project IDs. To include multiple projects, separate IDs with ampersand: `projectId=10000&projectId=10001`. */
    projectId: number[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanIssueTypeScreenSchemesProjects> {
  const { startAt, maxResults, projectId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/issuetypescreenscheme/project",
    query: { startAt, maxResults, projectId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Assign issue type screen scheme to project
 *
 * Assigns an issue type screen scheme to a project.
 *
 * Issue type screen schemes can only be assigned to classic projects.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function assignIssueTypeScreenSchemeToProject(
  ctx: ClientContext,
  params: IssueTypeScreenSchemeProjectAssociation,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "put",
    path: "/rest/api/3/issuetypescreenscheme/project",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue type screen scheme
 *
 * Updates an issue type screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @returns Returned if the request is successful.
 */
export function updateIssueTypeScreenScheme(
  ctx: ClientContext,
  params: IssueTypeScreenSchemeUpdateDetails & {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueTypeScreenSchemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}",
    pathParams: { issueTypeScreenSchemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue type screen scheme
 *
 * Deletes an issue type screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @returns Returned if the issue type screen scheme is deleted.
 */
export function deleteIssueTypeScreenScheme(
  ctx: ClientContext,
  params: {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueTypeScreenSchemeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}",
    pathParams: { issueTypeScreenSchemeId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Append mappings to issue type screen scheme
 *
 * Appends issue type to screen scheme mappings to an issue type screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @returns Returned if the request is successful.
 */
export function appendMappingsForIssueTypeScreenScheme(
  ctx: ClientContext,
  params: IssueTypeScreenSchemeMappingDetails & {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueTypeScreenSchemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping",
    pathParams: { issueTypeScreenSchemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update issue type screen scheme default screen scheme
 *
 * Updates the default screen scheme of an issue type screen scheme. The default screen scheme is used for all unmapped issue types.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @returns Returned if the request is successful.
 */
export function updateDefaultScreenScheme(
  ctx: ClientContext,
  params: UpdateDefaultScreenScheme & {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueTypeScreenSchemeId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/default",
    pathParams: { issueTypeScreenSchemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove mappings from issue type screen scheme
 *
 * Removes issue type to screen scheme mappings from an issue type screen scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @returns Returned if the screen scheme mappings are removed from the issue type screen scheme.
 */
export function removeMappingsFromIssueTypeScreenScheme(
  ctx: ClientContext,
  params: IssueTypeIds & {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { issueTypeScreenSchemeId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/mapping/remove",
    pathParams: { issueTypeScreenSchemeId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue type screen scheme projects
 *
 * Returns a [paginated](#pagination) list of projects associated with an issue type screen scheme.
 *
 * Only company-managed projects associated with an issue type screen scheme are returned.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @returns Returned if the request is successful.
 */
export function getProjectsForIssueTypeScreenScheme(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the issue type screen scheme.
     *
     * @format int64
     */
    issueTypeScreenSchemeId: number;
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
    /** @default "" */
    query?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanProjectDetails> {
  const { issueTypeScreenSchemeId, startAt, maxResults, query } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/issuetypescreenscheme/{issueTypeScreenSchemeId}/project",
    pathParams: { issueTypeScreenSchemeId },
    query: { startAt, maxResults, query },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents issue type screen schemes. Use it to:
 *
 *  *  get issue type screen schemes and a list of the projects that use them.
 *  *  create issue type screen schemes.
 *  *  update issue type screen schemes.
 *  *  delete issue type screen schemes.
 *  *  associate issue type screen schemes with projects.
 *  *  append issue type to screen scheme mappings to issue type screen schemes.
 *  *  remove issue type to screen scheme mappings from issue type screen schemes.
 *  *  update default screen scheme of issue type screen scheme.
 */
export function createIssueTypeScreenSchemesService(ctx: ClientContext) {
  return {
    /**
     * Get issue type screen schemes
     *
     * Returns a [paginated](#pagination) list of issue type screen schemes.
     *
     * Only issue type screen schemes used in classic projects are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.id - The list of issue type screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`.
     * @param params.queryString - String used to perform a case-insensitive partial match with issue type screen scheme name.
     * @param params.orderBy - [Order](#ordering) the results by a field: *  `name` Sorts by issue type screen scheme name. *  `id` Sorts by issue type screen scheme ID.
     * @param params.expand - Use [expand](#expansion) to include additional information in the response. This parameter accepts `projects` that, for each issue type screen schemes, returns information about the projects the issue type screen scheme is assigned to.
     * @returns Returned if the request is successful.
     */
    getIssueTypeScreenSchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /** The list of issue type screen scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`. */
        id?: number[];
        /**
         * String used to perform a case-insensitive partial match with issue type screen scheme name.
         *
         * @default ""
         */
        queryString?: string;
        /**
         * [Order](#ordering) the results by a field:
         *
         *  *  `name` Sorts by issue type screen scheme name.
         *  *  `id` Sorts by issue type screen scheme ID.
         *
         * @default "id"
         */
        orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
        /**
         * Use [expand](#expansion) to include additional information in the response. This parameter accepts `projects` that, for each issue type screen schemes, returns information about the projects the issue type screen scheme is assigned to.
         *
         * @default ""
         */
        expand?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanIssueTypeScreenScheme> {
      return getIssueTypeScreenSchemes(ctx, params, options);
    },

    /**
     * Create issue type screen scheme
     *
     * Creates an issue type screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createIssueTypeScreenScheme(
      params: IssueTypeScreenSchemeDetails,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeScreenSchemeId> {
      return createIssueTypeScreenScheme(ctx, params, options);
    },

    /**
     * Get issue type screen scheme items
     *
     * Returns a [paginated](#pagination) list of issue type screen scheme items.
     *
     * Only issue type screen schemes used in classic projects are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.issueTypeScreenSchemeId - The list of issue type screen scheme IDs. To include multiple issue type screen schemes, separate IDs with ampersand: `issueTypeScreenSchemeId=10000&issueTypeScreenSchemeId=10001`.
     * @returns Returned if the request is successful.
     */
    getIssueTypeScreenSchemeMappings(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /** The list of issue type screen scheme IDs. To include multiple issue type screen schemes, separate IDs with ampersand: `issueTypeScreenSchemeId=10000&issueTypeScreenSchemeId=10001`. */
        issueTypeScreenSchemeId?: number[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanIssueTypeScreenSchemeItem> {
      return getIssueTypeScreenSchemeMappings(ctx, params, options);
    },

    /**
     * Get issue type screen schemes for projects
     *
     * Returns a [paginated](#pagination) list of issue type screen schemes and, for each issue type screen scheme, a list of the projects that use it.
     *
     * Only issue type screen schemes used in classic projects are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @param params.projectId - The list of project IDs. To include multiple projects, separate IDs with ampersand: `projectId=10000&projectId=10001`.
     * @returns Returned if the request is successful.
     */
    getIssueTypeScreenSchemeProjectAssociations(
      params: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /** The list of project IDs. To include multiple projects, separate IDs with ampersand: `projectId=10000&projectId=10001`. */
        projectId: number[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanIssueTypeScreenSchemesProjects> {
      return getIssueTypeScreenSchemeProjectAssociations(ctx, params, options);
    },

    /**
     * Assign issue type screen scheme to project
     *
     * Assigns an issue type screen scheme to a project.
     *
     * Issue type screen schemes can only be assigned to classic projects.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    assignIssueTypeScreenSchemeToProject(
      params: IssueTypeScreenSchemeProjectAssociation,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return assignIssueTypeScreenSchemeToProject(ctx, params, options);
    },

    /**
     * Update issue type screen scheme
     *
     * Updates an issue type screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @returns Returned if the request is successful.
     */
    updateIssueTypeScreenScheme(
      params: IssueTypeScreenSchemeUpdateDetails & {
        /** The ID of the issue type screen scheme. */
        issueTypeScreenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateIssueTypeScreenScheme(ctx, params, options);
    },

    /**
     * Delete issue type screen scheme
     *
     * Deletes an issue type screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @returns Returned if the issue type screen scheme is deleted.
     */
    deleteIssueTypeScreenScheme(
      params: {
        /** The ID of the issue type screen scheme. */
        issueTypeScreenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteIssueTypeScreenScheme(ctx, params, options);
    },

    /**
     * Append mappings to issue type screen scheme
     *
     * Appends issue type to screen scheme mappings to an issue type screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @returns Returned if the request is successful.
     */
    appendMappingsForIssueTypeScreenScheme(
      params: IssueTypeScreenSchemeMappingDetails & {
        /** The ID of the issue type screen scheme. */
        issueTypeScreenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return appendMappingsForIssueTypeScreenScheme(ctx, params, options);
    },

    /**
     * Update issue type screen scheme default screen scheme
     *
     * Updates the default screen scheme of an issue type screen scheme. The default screen scheme is used for all unmapped issue types.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @returns Returned if the request is successful.
     */
    updateDefaultScreenScheme(
      params: UpdateDefaultScreenScheme & {
        /** The ID of the issue type screen scheme. */
        issueTypeScreenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateDefaultScreenScheme(ctx, params, options);
    },

    /**
     * Remove mappings from issue type screen scheme
     *
     * Removes issue type to screen scheme mappings from an issue type screen scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @returns Returned if the screen scheme mappings are removed from the issue type screen scheme.
     */
    removeMappingsFromIssueTypeScreenScheme(
      params: IssueTypeIds & {
        /** The ID of the issue type screen scheme. */
        issueTypeScreenSchemeId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return removeMappingsFromIssueTypeScreenScheme(ctx, params, options);
    },

    /**
     * Get issue type screen scheme projects
     *
     * Returns a [paginated](#pagination) list of projects associated with an issue type screen scheme.
     *
     * Only company-managed projects associated with an issue type screen scheme are returned.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.issueTypeScreenSchemeId - The ID of the issue type screen scheme.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @returns Returned if the request is successful.
     */
    getProjectsForIssueTypeScreenScheme(
      params: {
        /**
         * The ID of the issue type screen scheme.
         *
         * @format int64
         */
        issueTypeScreenSchemeId: number;
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
        /** @default "" */
        query?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanProjectDetails> {
      return getProjectsForIssueTypeScreenScheme(ctx, params, options);
    },
  };
}
