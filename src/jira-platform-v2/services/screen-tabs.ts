// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { ScreenableTab } from "../types/index.ts";

/**
 * Get bulk screen tabs
 *
 * Returns the list of tabs for a bulk of screens.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The list of screen IDs. To include multiple screen IDs, provide an ampersand-separated list. For example, `screenId=10000&screenId=10001`.
 * @param params.tabId - The list of tab IDs. To include multiple tab IDs, provide an ampersand-separated list. For example, `tabId=10000&tabId=10001`.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResult - The maximum number of items to return per page. The maximum number is 100,
 * @returns Returned if the request is successful.
 */
export function getBulkScreenTabs(
  ctx: ClientContext,
  params?: {
    /** The list of screen IDs. To include multiple screen IDs, provide an ampersand-separated list. For example, `screenId=10000&screenId=10001`. */
    screenId?: number[];
    /** The list of tab IDs. To include multiple tab IDs, provide an ampersand-separated list. For example, `tabId=10000&tabId=10001`. */
    tabId?: number[];
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page. The maximum number is 100,
     *
     * @default 100
     * @format int32
     */
    maxResult?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { screenId, tabId, startAt, maxResult } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/screens/tabs",
    query: { screenId, tabId, startAt, maxResult },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get all screen tabs
 *
 * Returns the list of tabs for a screen.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg) when the project key is specified, providing that the screen is associated with the project through a Screen Scheme and Issue Type Screen Scheme.
 *
 * @param params.screenId - The ID of the screen.
 * @param params.projectKey - The key of the project.
 * @returns Returned if the request is successful.
 */
export function getAllScreenTabs(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /** The key of the project. */
    projectKey?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenableTab[]> {
  const { screenId, projectKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/screens/{screenId}/tabs",
    pathParams: { screenId },
    query: { projectKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create screen tab
 *
 * Creates a tab for a screen.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @returns Returned if the request is successful.
 */
export function addScreenTab(
  ctx: ClientContext,
  params: ScreenableTab & {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenableTab> {
  const { screenId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/screens/{screenId}/tabs",
    pathParams: { screenId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update screen tab
 *
 * Updates the name of a screen tab.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @returns Returned if the request is successful.
 */
export function renameScreenTab(
  ctx: ClientContext,
  params: ScreenableTab & {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ScreenableTab> {
  const { screenId, tabId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/screens/{screenId}/tabs/{tabId}",
    pathParams: { screenId, tabId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete screen tab
 *
 * Deletes a screen tab.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @returns Returned if the request is successful.
 */
export function deleteScreenTab(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { screenId, tabId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/screens/{screenId}/tabs/{tabId}",
    pathParams: { screenId, tabId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Move screen tab
 *
 * Moves a screen tab.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.screenId - The ID of the screen.
 * @param params.tabId - The ID of the screen tab.
 * @param params.pos - The position of tab. The base index is 0.
 * @returns Returned if the request is successful.
 */
export function moveScreenTab(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the screen.
     *
     * @format int64
     */
    screenId: number;
    /**
     * The ID of the screen tab.
     *
     * @format int64
     */
    tabId: number;
    /**
     * The position of tab. The base index is 0.
     *
     * @format int32
     */
    pos: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { screenId, tabId, pos } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/screens/{screenId}/tabs/{tabId}/move/{pos}",
    pathParams: { screenId, tabId, pos },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents the screen tabs used to record issue details. Use it to get, create, update, move, and delete screen tabs. */
export function createScreenTabsService(ctx: ClientContext) {
  return {
    /**
     * Get bulk screen tabs
     *
     * Returns the list of tabs for a bulk of screens.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The list of screen IDs. To include multiple screen IDs, provide an ampersand-separated list. For example, `screenId=10000&screenId=10001`.
     * @param params.tabId - The list of tab IDs. To include multiple tab IDs, provide an ampersand-separated list. For example, `tabId=10000&tabId=10001`.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResult - The maximum number of items to return per page. The maximum number is 100,
     * @returns Returned if the request is successful.
     */
    getBulkScreenTabs(
      params?: {
        /** The list of screen IDs. To include multiple screen IDs, provide an ampersand-separated list. For example, `screenId=10000&screenId=10001`. */
        screenId?: number[];
        /** The list of tab IDs. To include multiple tab IDs, provide an ampersand-separated list. For example, `tabId=10000&tabId=10001`. */
        tabId?: number[];
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page. The maximum number is 100,
         *
         * @default 100
         * @format int32
         */
        maxResult?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return getBulkScreenTabs(ctx, params, options);
    },

    /**
     * Get all screen tabs
     *
     * Returns the list of tabs for a screen.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  *Administer projects* [project permission](https://confluence.atlassian.com/x/yodKLg) when the project key is specified, providing that the screen is associated with the project through a Screen Scheme and Issue Type Screen Scheme.
     *
     * @param params.screenId - The ID of the screen.
     * @param params.projectKey - The key of the project.
     * @returns Returned if the request is successful.
     */
    getAllScreenTabs(
      params: {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /** The key of the project. */
        projectKey?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenableTab[]> {
      return getAllScreenTabs(ctx, params, options);
    },

    /**
     * Create screen tab
     *
     * Creates a tab for a screen.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @returns Returned if the request is successful.
     */
    addScreenTab(
      params: ScreenableTab & {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenableTab> {
      return addScreenTab(ctx, params, options);
    },

    /**
     * Update screen tab
     *
     * Updates the name of a screen tab.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @returns Returned if the request is successful.
     */
    renameScreenTab(
      params: ScreenableTab & {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ScreenableTab> {
      return renameScreenTab(ctx, params, options);
    },

    /**
     * Delete screen tab
     *
     * Deletes a screen tab.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @returns Returned if the request is successful.
     */
    deleteScreenTab(
      params: {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteScreenTab(ctx, params, options);
    },

    /**
     * Move screen tab
     *
     * Moves a screen tab.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.screenId - The ID of the screen.
     * @param params.tabId - The ID of the screen tab.
     * @param params.pos - The position of tab. The base index is 0.
     * @returns Returned if the request is successful.
     */
    moveScreenTab(
      params: {
        /**
         * The ID of the screen.
         *
         * @format int64
         */
        screenId: number;
        /**
         * The ID of the screen tab.
         *
         * @format int64
         */
        tabId: number;
        /**
         * The position of tab. The base index is 0.
         *
         * @format int32
         */
        pos: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return moveScreenTab(ctx, params, options);
    },
  };
}
