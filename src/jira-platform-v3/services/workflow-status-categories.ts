// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { StatusCategory } from "../types/index.ts";

/**
 * Get all status categories
 *
 * Returns a list of all status categories.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @returns Returned if the request is successful.
 */
export function getStatusCategories(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusCategory[]> {
  return ctx.request({
    method: "get",
    path: "/rest/api/3/statuscategory",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get status category
 *
 * Returns a status category. Status categories provided a mechanism for categorizing [statuses](#api-rest-api-3-status-idOrName-get).
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @param params.idOrKey - The ID or key of the status category.
 * @returns Returned if the request is successful.
 */
export function getStatusCategory(
  ctx: ClientContext,
  params: {
    /** The ID or key of the status category. */
    idOrKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<StatusCategory> {
  const { idOrKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/statuscategory/{idOrKey}",
    pathParams: { idOrKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents status categories. Use it to obtain a list of all status categories and the details of a category. Status categories provided a mechanism for categorizing [statuses](#api-group-Workflow-statuses). */
export function createWorkflowStatusCategoriesService(ctx: ClientContext) {
  return {
    /**
     * Get all status categories
     *
     * Returns a list of all status categories.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @returns Returned if the request is successful.
     */
    getStatusCategories(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<StatusCategory[]> {
      return getStatusCategories(ctx, options);
    },

    /**
     * Get status category
     *
     * Returns a status category. Status categories provided a mechanism for categorizing [statuses](#api-rest-api-3-status-idOrName-get).
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @param params.idOrKey - The ID or key of the status category.
     * @returns Returned if the request is successful.
     */
    getStatusCategory(
      params: {
        /** The ID or key of the status category. */
        idOrKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<StatusCategory> {
      return getStatusCategory(ctx, params, options);
    },
  };
}
