// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { IssueIdOrKeysAssociation } from "../types/index.ts";

/**
 * Submit Feature Flag data
 *
 * Update / insert Feature Flag data.
 *
 * Feature Flags are identified by their ID, and existing Feature Flag data for the same ID will be replaced if it exists and the updateSequenceId of existing data is less than the incoming data.
 *
 * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getFeatureFlagById operation can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple Feature Flags being submitted in one request, each is validated individually prior to submission. Details of which Feature Flags failed submission (if any) are available in the response object.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Submission accepted. Each submitted Feature Flag that is of a valid format will be eventually available in Jira. Details of which Feature Flags were submitted and which failed submission (due to data format problems etc.) are available in the response object.
 */
export function submitFeatureFlags(
  ctx: ClientContext,
  params: {
    /**
     * Properties assigned to Feature Flag data that can then be used for delete / query operations.
     *
     * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
     *
     * Note that these properties will never be returned with Feature Flag data. They are not intended for use as metadata to associate with a Feature Flag. Internally they are stored as a hash so that personal information etc. is never stored within Jira.
     *
     * Properties are supplied as key/value pairs, a maximum of 5 properties can be supplied, and keys must not contain ':' or start with '_'.
     *
     * @example
     * ```json
     * {
     *   "accountId": "account-234",
     *   "projectId": "project-123"
     * }
     * ```
     */
    properties?: Record<string, string>;
    /**
     * A list of Feature Flags to submit to Jira.
     *
     * Each Feature Flag may be associated with 1 or more Jira issue keys, and will be associated with any properties included in this request.
     */
    flags: {
      /**
       * The FeatureFlagData schema version used for this flag data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
      /**
       * The identifier for the Feature Flag. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * The identifier that users would use to reference the Feature Flag in their source code etc.
       *
       * Will be made available via the UI for users to copy into their source code etc.
       *
       * @example
       * ```json
       * "my-awesome-feature"
       * ```
       */
      key: string;
      /**
       * An ID used to apply an ordering to updates for this Feature Flag in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Feature Flag and increment that on each update to Jira).
       *
       * Updates for a Feature Flag that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceId: number;
      /**
       * The human-readable name for the Feature Flag. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "Enable awesome feature"
       * ```
       */
      displayName?: string;
      /**
       * The Jira issue keys to associate the Feature Flag information with.
       *
       * @deprecated
       */
      issueKeys?: string[];
      /** The Jira issue keys or IDs to associate the feature flag with. */
      associations?: IssueIdOrKeysAssociation[];
      /**
       * Summary information for a single Feature Flag.
       *
       * Providers may elect to provide information from a specific environment, or they may choose to 'roll up' information from across multiple environments - whatever makes most sense in the Provider system.
       *
       * This is the summary information that will be presented to the user on e.g. the Jira issue screen.
       */
      summary: {
        /**
         * A URL users can use to link to a summary view of this flag, if appropriate.
         *
         * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific environment, it might make sense to link the user to the flag in that environment).
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/feature-123/summary"
         * ```
         */
        url?: string;
        /** Status information about a single Feature Flag. */
        status: {
          /**
           * Whether the Feature Flag is enabled in the given environment (or in summary).
           *
           * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
           */
          enabled: boolean;
          /**
           * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
           *
           * This value may be presented to the user in the UI.
           *
           * @example
           * ```json
           * "Disabled"
           * ```
           */
          defaultValue?: string;
          /**
           * Information about the rollout of a Feature Flag in an environment (or in summary).
           *
           * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
           *
           * This information may be presented to the user in the UI.
           *
           * @example
           * ```json
           * {
           *   "percentage": 80
           * }
           * ```
           */
          rollout?: {
            /** If the Feature Flag rollout is a simple percentage rollout */
            percentage?: number;
            /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
            text?: string;
            /** A count of the number of rules active for this Feature Flag in an environment. */
            rules?: number;
          };
        };
        /**
         * The last-updated timestamp to present to the user as a summary of the state of the Feature Flag.
         *
         * Providers may choose to supply the last-updated timestamp from a specific environment, or the 'most recent' last-updated timestamp across all environments - whatever makes sense in the Provider system.
         *
         * Expected format is an RFC3339 formatted string.
         *
         * @format date-time
         * @example
         * ```json
         * "2018-01-20T23:27:25.000Z"
         * ```
         */
        lastUpdated: string;
      };
      /**
       * Detail information for this Feature Flag.
       *
       * This may be information for each environment the Feature Flag is defined in or a selection of environments made by the user, as appropriate.
       */
      details: {
        /**
         * A URL users can use to link to this Feature Flag, in this environment.
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/feature-123/production"
         * ```
         */
        url: string;
        /**
         * The last-updated timestamp for this Feature Flag, in this environment.
         *
         * Expected format is an RFC3339 formatted string.
         *
         * @format date-time
         * @example
         * ```json
         * "2018-01-20T23:27:25.000Z"
         * ```
         */
        lastUpdated: string;
        /**
         * Details of a single environment.
         *
         * At the simplest this must be the name of the environment.
         *
         * Ideally there is also type information which may be used to group data from multiple Feature Flags and other entities for visualisation in the UI.
         *
         * @example
         * ```json
         * {
         *   "name": "prod-us-west",
         *   "type": "production"
         * }
         * ```
         */
        environment: {
          /** The name of the environment. */
          name: string;
          /** The 'type' or 'category' of environment this environment belongs to. */
          type?: "development" | "testing" | "staging" | "production";
        };
        /** Status information about a single Feature Flag. */
        status: {
          /**
           * Whether the Feature Flag is enabled in the given environment (or in summary).
           *
           * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
           */
          enabled: boolean;
          /**
           * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
           *
           * This value may be presented to the user in the UI.
           *
           * @example
           * ```json
           * "Disabled"
           * ```
           */
          defaultValue?: string;
          /**
           * Information about the rollout of a Feature Flag in an environment (or in summary).
           *
           * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
           *
           * This information may be presented to the user in the UI.
           *
           * @example
           * ```json
           * {
           *   "percentage": 80
           * }
           * ```
           */
          rollout?: {
            /** If the Feature Flag rollout is a simple percentage rollout */
            percentage?: number;
            /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
            text?: string;
            /** A count of the number of rules active for this Feature Flag in an environment. */
            rules?: number;
          };
        };
      }[];
    }[];
    /**
     * Information about the provider. This is useful for auditing, logging, debugging,
     * and other internal uses. It is not considered private information. Hence, it may not contain personally
     * identifiable information.
     */
    providerMetadata?: {
      /**
       * An optional name of the source of the feature flags.
       *
       * @example
       * ```json
       * "Atlassian Release Platform 2.1.0"
       * ```
       */
      product?: string;
    };
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Feature Flags that have been accepted for submission.
   *
   * A Feature Flag may be rejected if it was only associated with unknown issue keys.
   *
   * Note that a Feature Flag that isn't updated due to it's updateSequenceId being out of order is not considered a failed submission.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedFeatureFlags?: string[];
  /**
   * Details of Feature Flags that have not been accepted for submission, usually due to a problem with the request data.
   *
   * The object (if present) will be keyed by Feature Flag ID and include any errors associated with that Feature Flag that have prevented it being submitted.
   */
  failedFeatureFlags?: Record<
    string,
    {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[]
  >;
  /**
   * Issue keys that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
   *
   * If a Feature Flag has been associated with issue keys other than those in this array it will still be stored against those valid keys.
   * If a Feature Flag was only associated with issue keys deemed to be invalid it won't be persisted.
   */
  unknownIssueKeys?: string[];
  /**
   * Associations that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
   *
   * If a feature flag has been associated with any other association other than those in this array it will still be stored against those valid associations.
   * If a feature flag was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
   */
  unknownAssociations?: IssueIdOrKeysAssociation[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/featureflags/0.1/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete Feature Flags by Property
 *
 * Bulk delete all Feature Flags that match the given request.
 *
 * One or more query params must be supplied to specify Properties to delete by. Optional param `_updateSequenceId` is no longer supported.
 * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
 * See the documentation for the submitFeatureFlags operation for more details.
 *
 * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
 *
 * Deletion is performed asynchronously. The getFeatureFlagById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param params.updateSequenceId - This parameter usage is no longer supported. An optional `_updateSequenceId` to use to control deletion. Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteFeatureFlagsByProperty(
  ctx: ClientContext,
  params?: {
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `_updateSequenceId` to use to control deletion.
     *
     * Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * If not provided, all stored data that matches the request will be deleted.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceId?: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { updateSequenceId } = params ?? {};
  return ctx.request({
    method: "delete",
    path: "/rest/featureflags/0.1/bulkByProperties",
    query: { _updateSequenceId: updateSequenceId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Feature Flag by ID
 *
 * Retrieve the currently stored Feature Flag data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param params.featureFlagId - The ID of the Feature Flag to fetch.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns The Feature Flag data currently stored for the given ID.
 */
export function getFeatureFlagById(
  ctx: ClientContext,
  params: {
    /** The ID of the Feature Flag to fetch. */
    featureFlagId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The FeatureFlagData schema version used for this flag data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion?: "1.0";
  /**
   * The identifier for the Feature Flag. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * The identifier that users would use to reference the Feature Flag in their source code etc.
   *
   * Will be made available via the UI for users to copy into their source code etc.
   *
   * @example
   * ```json
   * "my-awesome-feature"
   * ```
   */
  key: string;
  /**
   * An ID used to apply an ordering to updates for this Feature Flag in the case of out-of-order receipt of update requests.
   *
   * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Feature Flag and increment that on each update to Jira).
   *
   * Updates for a Feature Flag that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceId: number;
  /**
   * The human-readable name for the Feature Flag. Will be shown in the UI.
   *
   * If not provided, will use the ID for display.
   *
   * @example
   * ```json
   * "Enable awesome feature"
   * ```
   */
  displayName?: string;
  /**
   * The Jira issue keys to associate the Feature Flag information with.
   *
   * @deprecated
   */
  issueKeys?: string[];
  /** The Jira issue keys or IDs to associate the feature flag with. */
  associations?: IssueIdOrKeysAssociation[];
  /**
   * Summary information for a single Feature Flag.
   *
   * Providers may elect to provide information from a specific environment, or they may choose to 'roll up' information from across multiple environments - whatever makes most sense in the Provider system.
   *
   * This is the summary information that will be presented to the user on e.g. the Jira issue screen.
   */
  summary: {
    /**
     * A URL users can use to link to a summary view of this flag, if appropriate.
     *
     * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific environment, it might make sense to link the user to the flag in that environment).
     *
     * @format uri
     * @example
     * ```json
     * "https://example.com/project/feature-123/summary"
     * ```
     */
    url?: string;
    /** Status information about a single Feature Flag. */
    status: {
      /**
       * Whether the Feature Flag is enabled in the given environment (or in summary).
       *
       * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
       */
      enabled: boolean;
      /**
       * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
       *
       * This value may be presented to the user in the UI.
       *
       * @example
       * ```json
       * "Disabled"
       * ```
       */
      defaultValue?: string;
      /**
       * Information about the rollout of a Feature Flag in an environment (or in summary).
       *
       * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
       *
       * This information may be presented to the user in the UI.
       *
       * @example
       * ```json
       * {
       *   "percentage": 80
       * }
       * ```
       */
      rollout?: {
        /** If the Feature Flag rollout is a simple percentage rollout */
        percentage?: number;
        /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
        text?: string;
        /** A count of the number of rules active for this Feature Flag in an environment. */
        rules?: number;
      };
    };
    /**
     * The last-updated timestamp to present to the user as a summary of the state of the Feature Flag.
     *
     * Providers may choose to supply the last-updated timestamp from a specific environment, or the 'most recent' last-updated timestamp across all environments - whatever makes sense in the Provider system.
     *
     * Expected format is an RFC3339 formatted string.
     *
     * @format date-time
     * @example
     * ```json
     * "2018-01-20T23:27:25.000Z"
     * ```
     */
    lastUpdated: string;
  };
  /**
   * Detail information for this Feature Flag.
   *
   * This may be information for each environment the Feature Flag is defined in or a selection of environments made by the user, as appropriate.
   */
  details: {
    /**
     * A URL users can use to link to this Feature Flag, in this environment.
     *
     * @format uri
     * @example
     * ```json
     * "https://example.com/project/feature-123/production"
     * ```
     */
    url: string;
    /**
     * The last-updated timestamp for this Feature Flag, in this environment.
     *
     * Expected format is an RFC3339 formatted string.
     *
     * @format date-time
     * @example
     * ```json
     * "2018-01-20T23:27:25.000Z"
     * ```
     */
    lastUpdated: string;
    /**
     * Details of a single environment.
     *
     * At the simplest this must be the name of the environment.
     *
     * Ideally there is also type information which may be used to group data from multiple Feature Flags and other entities for visualisation in the UI.
     *
     * @example
     * ```json
     * {
     *   "name": "prod-us-west",
     *   "type": "production"
     * }
     * ```
     */
    environment: {
      /** The name of the environment. */
      name: string;
      /** The 'type' or 'category' of environment this environment belongs to. */
      type?: "development" | "testing" | "staging" | "production";
    };
    /** Status information about a single Feature Flag. */
    status: {
      /**
       * Whether the Feature Flag is enabled in the given environment (or in summary).
       *
       * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
       */
      enabled: boolean;
      /**
       * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
       *
       * This value may be presented to the user in the UI.
       *
       * @example
       * ```json
       * "Disabled"
       * ```
       */
      defaultValue?: string;
      /**
       * Information about the rollout of a Feature Flag in an environment (or in summary).
       *
       * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
       *
       * This information may be presented to the user in the UI.
       *
       * @example
       * ```json
       * {
       *   "percentage": 80
       * }
       * ```
       */
      rollout?: {
        /** If the Feature Flag rollout is a simple percentage rollout */
        percentage?: number;
        /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
        text?: string;
        /** A count of the number of rules active for this Feature Flag in an environment. */
        rules?: number;
      };
    };
  }[];
}> {
  const { featureFlagId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/featureflags/0.1/flag/{featureFlagId}",
    pathParams: { featureFlagId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Feature Flag by ID
 *
 * Delete the Feature Flag data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The getFeatureFlagById operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param params.featureFlagId - The ID of the Feature Flag to delete.
 * @param params.updateSequenceId - This parameter usage is no longer supported. An optional `_updateSequenceId` to use to control deletion. Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteFeatureFlagById(
  ctx: ClientContext,
  params: {
    /** The ID of the Feature Flag to delete. */
    featureFlagId: string;
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `_updateSequenceId` to use to control deletion.
     *
     * Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceId?: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
       *
       * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { featureFlagId, updateSequenceId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/featureflags/0.1/flag/{featureFlagId}",
    pathParams: { featureFlagId },
    query: { _updateSequenceId: updateSequenceId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * APIs related to integrating feature flags with Jira Software.
 *
 * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraFeatureFlagInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/feature-flag/.
 *
 * These APIs are available to Forge apps with the `devops:featureFlagInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-feature-flag-info/.
 */
export function createFeatureFlagsService(ctx: ClientContext) {
  return {
    /**
     * Submit Feature Flag data
     *
     * Update / insert Feature Flag data.
     *
     * Feature Flags are identified by their ID, and existing Feature Flag data for the same ID will be replaced if it exists and the updateSequenceId of existing data is less than the incoming data.
     *
     * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The getFeatureFlagById operation can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple Feature Flags being submitted in one request, each is validated individually prior to submission. Details of which Feature Flags failed submission (if any) are available in the response object.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Submission accepted. Each submitted Feature Flag that is of a valid format will be eventually available in Jira. Details of which Feature Flags were submitted and which failed submission (due to data format problems etc.) are available in the response object.
     */
    submitFeatureFlags(
      params: {
        /**
         * Properties assigned to Feature Flag data that can then be used for delete / query operations.
         *
         * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
         *
         * Note that these properties will never be returned with Feature Flag data. They are not intended for use as metadata to associate with a Feature Flag. Internally they are stored as a hash so that personal information etc. is never stored within Jira.
         *
         * Properties are supplied as key/value pairs, a maximum of 5 properties can be supplied, and keys must not contain ':' or start with '_'.
         *
         * @example
         * ```json
         * {
         *   "accountId": "account-234",
         *   "projectId": "project-123"
         * }
         * ```
         */
        properties?: Record<string, string>;
        /**
         * A list of Feature Flags to submit to Jira.
         *
         * Each Feature Flag may be associated with 1 or more Jira issue keys, and will be associated with any properties included in this request.
         */
        flags: {
          /**
           * The FeatureFlagData schema version used for this flag data.
           *
           * Placeholder to support potential schema changes in the future.
           *
           * @default "1.0"
           * @example
           * ```json
           * "1.0"
           * ```
           */
          schemaVersion?: "1.0";
          /**
           * The identifier for the Feature Flag. Must be unique for a given Provider.
           *
           * @example
           * ```json
           * "111-222-333"
           * ```
           */
          id: string;
          /**
           * The identifier that users would use to reference the Feature Flag in their source code etc.
           *
           * Will be made available via the UI for users to copy into their source code etc.
           *
           * @example
           * ```json
           * "my-awesome-feature"
           * ```
           */
          key: string;
          /**
           * An ID used to apply an ordering to updates for this Feature Flag in the case of out-of-order receipt of update requests.
           *
           * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Feature Flag and increment that on each update to Jira).
           *
           * Updates for a Feature Flag that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
           *
           * @format int64
           * @example
           * ```json
           * 1523494301448
           * ```
           */
          updateSequenceId: number;
          /**
           * The human-readable name for the Feature Flag. Will be shown in the UI.
           *
           * If not provided, will use the ID for display.
           *
           * @example
           * ```json
           * "Enable awesome feature"
           * ```
           */
          displayName?: string;
          /**
           * The Jira issue keys to associate the Feature Flag information with.
           *
           * @deprecated
           */
          issueKeys?: string[];
          /** The Jira issue keys or IDs to associate the feature flag with. */
          associations?: IssueIdOrKeysAssociation[];
          /**
           * Summary information for a single Feature Flag.
           *
           * Providers may elect to provide information from a specific environment, or they may choose to 'roll up' information from across multiple environments - whatever makes most sense in the Provider system.
           *
           * This is the summary information that will be presented to the user on e.g. the Jira issue screen.
           */
          summary: {
            /**
             * A URL users can use to link to a summary view of this flag, if appropriate.
             *
             * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific environment, it might make sense to link the user to the flag in that environment).
             *
             * @format uri
             * @example
             * ```json
             * "https://example.com/project/feature-123/summary"
             * ```
             */
            url?: string;
            /** Status information about a single Feature Flag. */
            status: {
              /**
               * Whether the Feature Flag is enabled in the given environment (or in summary).
               *
               * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
               */
              enabled: boolean;
              /**
               * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
               *
               * This value may be presented to the user in the UI.
               *
               * @example
               * ```json
               * "Disabled"
               * ```
               */
              defaultValue?: string;
              /**
               * Information about the rollout of a Feature Flag in an environment (or in summary).
               *
               * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
               *
               * This information may be presented to the user in the UI.
               *
               * @example
               * ```json
               * {
               *   "percentage": 80
               * }
               * ```
               */
              rollout?: {
                /** If the Feature Flag rollout is a simple percentage rollout */
                percentage?: number;
                /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
                text?: string;
                /** A count of the number of rules active for this Feature Flag in an environment. */
                rules?: number;
              };
            };
            /**
             * The last-updated timestamp to present to the user as a summary of the state of the Feature Flag.
             *
             * Providers may choose to supply the last-updated timestamp from a specific environment, or the 'most recent' last-updated timestamp across all environments - whatever makes sense in the Provider system.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            lastUpdated: string;
          };
          /**
           * Detail information for this Feature Flag.
           *
           * This may be information for each environment the Feature Flag is defined in or a selection of environments made by the user, as appropriate.
           */
          details: {
            /**
             * A URL users can use to link to this Feature Flag, in this environment.
             *
             * @format uri
             * @example
             * ```json
             * "https://example.com/project/feature-123/production"
             * ```
             */
            url: string;
            /**
             * The last-updated timestamp for this Feature Flag, in this environment.
             *
             * Expected format is an RFC3339 formatted string.
             *
             * @format date-time
             * @example
             * ```json
             * "2018-01-20T23:27:25.000Z"
             * ```
             */
            lastUpdated: string;
            /**
             * Details of a single environment.
             *
             * At the simplest this must be the name of the environment.
             *
             * Ideally there is also type information which may be used to group data from multiple Feature Flags and other entities for visualisation in the UI.
             *
             * @example
             * ```json
             * {
             *   "name": "prod-us-west",
             *   "type": "production"
             * }
             * ```
             */
            environment: {
              /** The name of the environment. */
              name: string;
              /** The 'type' or 'category' of environment this environment belongs to. */
              type?: "development" | "testing" | "staging" | "production";
            };
            /** Status information about a single Feature Flag. */
            status: {
              /**
               * Whether the Feature Flag is enabled in the given environment (or in summary).
               *
               * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
               */
              enabled: boolean;
              /**
               * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
               *
               * This value may be presented to the user in the UI.
               *
               * @example
               * ```json
               * "Disabled"
               * ```
               */
              defaultValue?: string;
              /**
               * Information about the rollout of a Feature Flag in an environment (or in summary).
               *
               * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
               *
               * This information may be presented to the user in the UI.
               *
               * @example
               * ```json
               * {
               *   "percentage": 80
               * }
               * ```
               */
              rollout?: {
                /** If the Feature Flag rollout is a simple percentage rollout */
                percentage?: number;
                /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
                text?: string;
                /** A count of the number of rules active for this Feature Flag in an environment. */
                rules?: number;
              };
            };
          }[];
        }[];
        /**
         * Information about the provider. This is useful for auditing, logging, debugging,
         * and other internal uses. It is not considered private information. Hence, it may not contain personally
         * identifiable information.
         */
        providerMetadata?: {
          /**
           * An optional name of the source of the feature flags.
           *
           * @example
           * ```json
           * "Atlassian Release Platform 2.1.0"
           * ```
           */
          product?: string;
        };
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Feature Flags that have been accepted for submission.
       *
       * A Feature Flag may be rejected if it was only associated with unknown issue keys.
       *
       * Note that a Feature Flag that isn't updated due to it's updateSequenceId being out of order is not considered a failed submission.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedFeatureFlags?: string[];
      /**
       * Details of Feature Flags that have not been accepted for submission, usually due to a problem with the request data.
       *
       * The object (if present) will be keyed by Feature Flag ID and include any errors associated with that Feature Flag that have prevented it being submitted.
       */
      failedFeatureFlags?: Record<
        string,
        {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[]
      >;
      /**
       * Issue keys that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
       *
       * If a Feature Flag has been associated with issue keys other than those in this array it will still be stored against those valid keys.
       * If a Feature Flag was only associated with issue keys deemed to be invalid it won't be persisted.
       */
      unknownIssueKeys?: string[];
      /**
       * Associations that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
       *
       * If a feature flag has been associated with any other association other than those in this array it will still be stored against those valid associations.
       * If a feature flag was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
       */
      unknownAssociations?: IssueIdOrKeysAssociation[];
    }> {
      return submitFeatureFlags(ctx, params, options);
    },

    /**
     * Delete Feature Flags by Property
     *
     * Bulk delete all Feature Flags that match the given request.
     *
     * One or more query params must be supplied to specify Properties to delete by. Optional param `_updateSequenceId` is no longer supported.
     * If more than one Property is provided, data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
     * See the documentation for the submitFeatureFlags operation for more details.
     *
     * e.g. DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
     *
     * Deletion is performed asynchronously. The getFeatureFlagById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param params.updateSequenceId - This parameter usage is no longer supported. An optional `_updateSequenceId` to use to control deletion. Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteFeatureFlagsByProperty(
      params?: {
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `_updateSequenceId` to use to control deletion.
         *
         * Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * If not provided, all stored data that matches the request will be deleted.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceId?: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteFeatureFlagsByProperty(ctx, params, options);
    },

    /**
     * Get a Feature Flag by ID
     *
     * Retrieve the currently stored Feature Flag data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param params.featureFlagId - The ID of the Feature Flag to fetch.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns The Feature Flag data currently stored for the given ID.
     */
    getFeatureFlagById(
      params: {
        /** The ID of the Feature Flag to fetch. */
        featureFlagId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The FeatureFlagData schema version used for this flag data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
      /**
       * The identifier for the Feature Flag. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * The identifier that users would use to reference the Feature Flag in their source code etc.
       *
       * Will be made available via the UI for users to copy into their source code etc.
       *
       * @example
       * ```json
       * "my-awesome-feature"
       * ```
       */
      key: string;
      /**
       * An ID used to apply an ordering to updates for this Feature Flag in the case of out-of-order receipt of update requests.
       *
       * This can be any monotonically increasing number. A suggested implementation is to use epoch millis from the Provider system, but other alternatives are valid (e.g. a Provider could store a counter against each Feature Flag and increment that on each update to Jira).
       *
       * Updates for a Feature Flag that are received with an updateSqeuenceId lower than what is currently stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceId: number;
      /**
       * The human-readable name for the Feature Flag. Will be shown in the UI.
       *
       * If not provided, will use the ID for display.
       *
       * @example
       * ```json
       * "Enable awesome feature"
       * ```
       */
      displayName?: string;
      /**
       * The Jira issue keys to associate the Feature Flag information with.
       *
       * @deprecated
       */
      issueKeys?: string[];
      /** The Jira issue keys or IDs to associate the feature flag with. */
      associations?: IssueIdOrKeysAssociation[];
      /**
       * Summary information for a single Feature Flag.
       *
       * Providers may elect to provide information from a specific environment, or they may choose to 'roll up' information from across multiple environments - whatever makes most sense in the Provider system.
       *
       * This is the summary information that will be presented to the user on e.g. the Jira issue screen.
       */
      summary: {
        /**
         * A URL users can use to link to a summary view of this flag, if appropriate.
         *
         * This could be any location that makes sense in the Provider system (e.g. if the summary information comes from a specific environment, it might make sense to link the user to the flag in that environment).
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/feature-123/summary"
         * ```
         */
        url?: string;
        /** Status information about a single Feature Flag. */
        status: {
          /**
           * Whether the Feature Flag is enabled in the given environment (or in summary).
           *
           * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
           */
          enabled: boolean;
          /**
           * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
           *
           * This value may be presented to the user in the UI.
           *
           * @example
           * ```json
           * "Disabled"
           * ```
           */
          defaultValue?: string;
          /**
           * Information about the rollout of a Feature Flag in an environment (or in summary).
           *
           * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
           *
           * This information may be presented to the user in the UI.
           *
           * @example
           * ```json
           * {
           *   "percentage": 80
           * }
           * ```
           */
          rollout?: {
            /** If the Feature Flag rollout is a simple percentage rollout */
            percentage?: number;
            /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
            text?: string;
            /** A count of the number of rules active for this Feature Flag in an environment. */
            rules?: number;
          };
        };
        /**
         * The last-updated timestamp to present to the user as a summary of the state of the Feature Flag.
         *
         * Providers may choose to supply the last-updated timestamp from a specific environment, or the 'most recent' last-updated timestamp across all environments - whatever makes sense in the Provider system.
         *
         * Expected format is an RFC3339 formatted string.
         *
         * @format date-time
         * @example
         * ```json
         * "2018-01-20T23:27:25.000Z"
         * ```
         */
        lastUpdated: string;
      };
      /**
       * Detail information for this Feature Flag.
       *
       * This may be information for each environment the Feature Flag is defined in or a selection of environments made by the user, as appropriate.
       */
      details: {
        /**
         * A URL users can use to link to this Feature Flag, in this environment.
         *
         * @format uri
         * @example
         * ```json
         * "https://example.com/project/feature-123/production"
         * ```
         */
        url: string;
        /**
         * The last-updated timestamp for this Feature Flag, in this environment.
         *
         * Expected format is an RFC3339 formatted string.
         *
         * @format date-time
         * @example
         * ```json
         * "2018-01-20T23:27:25.000Z"
         * ```
         */
        lastUpdated: string;
        /**
         * Details of a single environment.
         *
         * At the simplest this must be the name of the environment.
         *
         * Ideally there is also type information which may be used to group data from multiple Feature Flags and other entities for visualisation in the UI.
         *
         * @example
         * ```json
         * {
         *   "name": "prod-us-west",
         *   "type": "production"
         * }
         * ```
         */
        environment: {
          /** The name of the environment. */
          name: string;
          /** The 'type' or 'category' of environment this environment belongs to. */
          type?: "development" | "testing" | "staging" | "production";
        };
        /** Status information about a single Feature Flag. */
        status: {
          /**
           * Whether the Feature Flag is enabled in the given environment (or in summary).
           *
           * Enabled may imply a partial rollout, which can be described using the 'rollout' field.
           */
          enabled: boolean;
          /**
           * The value served by this Feature Flag when it is disabled. This could be the actual value or an alias, as appropriate.
           *
           * This value may be presented to the user in the UI.
           *
           * @example
           * ```json
           * "Disabled"
           * ```
           */
          defaultValue?: string;
          /**
           * Information about the rollout of a Feature Flag in an environment (or in summary).
           *
           * Only one of 'percentage', 'text', or 'rules' should be provided. They will be used in that order if multiple are present.
           *
           * This information may be presented to the user in the UI.
           *
           * @example
           * ```json
           * {
           *   "percentage": 80
           * }
           * ```
           */
          rollout?: {
            /** If the Feature Flag rollout is a simple percentage rollout */
            percentage?: number;
            /** A text status to display that represents the rollout. This could be e.g. a named cohort. */
            text?: string;
            /** A count of the number of rules active for this Feature Flag in an environment. */
            rules?: number;
          };
        };
      }[];
    }> {
      return getFeatureFlagById(ctx, params, options);
    },

    /**
     * Delete a Feature Flag by ID
     *
     * Delete the Feature Flag data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The getFeatureFlagById operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param params.featureFlagId - The ID of the Feature Flag to delete.
     * @param params.updateSequenceId - This parameter usage is no longer supported. An optional `_updateSequenceId` to use to control deletion. Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira. If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteFeatureFlagById(
      params: {
        /** The ID of the Feature Flag to delete. */
        featureFlagId: string;
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `_updateSequenceId` to use to control deletion.
         *
         * Only stored data with an `updateSequenceId` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceId?: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to the Provider app installed in Jira.
           *
           * If the JWT token corresponds to an app that does not define Feature Flags module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteFeatureFlagById(ctx, params, options);
    },
  };
}
