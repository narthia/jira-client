// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  DefaultShareScope,
  SharePermission,
  SharePermissionInputBean,
} from "../types/index.ts";

/**
 * Get default share scope
 *
 * Returns the default sharing settings for new filters and dashboards for a user.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @returns Returned if the request is successful.
 */
export function getDefaultShareScope(
  ctx: ClientContext,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<DefaultShareScope> {
  return ctx.request({
    method: "get",
    path: "/rest/api/2/filter/defaultShareScope",
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set default share scope
 *
 * Sets the default sharing for new filters and dashboards for a user.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira.
 *
 * @returns Returned if the request is successful.
 */
export function setDefaultShareScope(
  ctx: ClientContext,
  params: DefaultShareScope,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<DefaultShareScope> {
  return ctx.request({
    method: "put",
    path: "/rest/api/2/filter/defaultShareScope",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get share permissions
 *
 * Returns the share permissions for a filter. A filter can be shared with groups, projects, all logged-in users, or the public. Sharing with all logged-in users or the public is known as a global share permission.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** None, however, share permissions are only returned for:
 *
 *  *  filters owned by the user.
 *  *  filters shared with a group that the user is a member of.
 *  *  filters shared with a private project that the user has *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.
 *  *  filters shared with a public project.
 *  *  filters shared with the public.
 *
 * @param params.id - The ID of the filter.
 * @returns Returned if the request is successful.
 */
export function getSharePermissions(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the filter.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SharePermission[]> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/filter/{id}/permission",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add share permission
 *
 * Add a share permissions to a filter. If you add a global share permission (one for all logged-in users or the public) it will overwrite all share permissions for the filter.
 *
 * Be aware that this operation uses different objects for updating share permissions compared to [Update filter](#api-rest-api-2-filter-id-put).
 *
 * **[Permissions](#permissions) required:** *Share dashboards and filters* [global permission](https://confluence.atlassian.com/x/x4dKLg) and the user must own the filter.
 *
 * @param params.id - The ID of the filter.
 * @returns Returned if the request is successful.
 */
export function addSharePermission(
  ctx: ClientContext,
  params: SharePermissionInputBean & {
    /**
     * The ID of the filter.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SharePermission[]> {
  const { id, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/filter/{id}/permission",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get share permission
 *
 * Returns a share permission for a filter. A filter can be shared with groups, projects, all logged-in users, or the public. Sharing with all logged-in users or the public is known as a global share permission.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** None, however, a share permission is only returned for:
 *
 *  *  filters owned by the user.
 *  *  filters shared with a group that the user is a member of.
 *  *  filters shared with a private project that the user has *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.
 *  *  filters shared with a public project.
 *  *  filters shared with the public.
 *
 * @param params.id - The ID of the filter.
 * @param params.permissionId - The ID of the share permission.
 * @returns Returned if the request is successful.
 */
export function getSharePermission(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the filter.
     *
     * @format int64
     */
    id: number;
    /**
     * The ID of the share permission.
     *
     * @format int64
     */
    permissionId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<SharePermission> {
  const { id, permissionId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/filter/{id}/permission/{permissionId}",
    pathParams: { id, permissionId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete share permission
 *
 * Deletes a share permission from a filter.
 *
 * **[Permissions](#permissions) required:** Permission to access Jira and the user must own the filter.
 *
 * @param params.id - The ID of the filter.
 * @param params.permissionId - The ID of the share permission.
 * @returns Returned if the request is successful.
 */
export function deleteSharePermission(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the filter.
     *
     * @format int64
     */
    id: number;
    /**
     * The ID of the share permission.
     *
     * @format int64
     */
    permissionId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, permissionId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/filter/{id}/permission/{permissionId}",
    pathParams: { id, permissionId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents options for sharing [filters](#api-group-Filters). Use it to get share scopes as well as add and remove share scopes from filters. */
export function createFilterSharingService(ctx: ClientContext) {
  return {
    /**
     * Get default share scope
     *
     * Returns the default sharing settings for new filters and dashboards for a user.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @returns Returned if the request is successful.
     */
    getDefaultShareScope(options?: {
      headers?: Record<string, string | number | boolean>;
      signal?: AbortSignal;
      extensions?: Record<string, unknown>;
    }): Promise<DefaultShareScope> {
      return getDefaultShareScope(ctx, options);
    },

    /**
     * Set default share scope
     *
     * Sets the default sharing for new filters and dashboards for a user.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira.
     *
     * @returns Returned if the request is successful.
     */
    setDefaultShareScope(
      params: DefaultShareScope,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<DefaultShareScope> {
      return setDefaultShareScope(ctx, params, options);
    },

    /**
     * Get share permissions
     *
     * Returns the share permissions for a filter. A filter can be shared with groups, projects, all logged-in users, or the public. Sharing with all logged-in users or the public is known as a global share permission.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** None, however, share permissions are only returned for:
     *
     *  *  filters owned by the user.
     *  *  filters shared with a group that the user is a member of.
     *  *  filters shared with a private project that the user has *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.
     *  *  filters shared with a public project.
     *  *  filters shared with the public.
     *
     * @param params.id - The ID of the filter.
     * @returns Returned if the request is successful.
     */
    getSharePermissions(
      params: {
        /**
         * The ID of the filter.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SharePermission[]> {
      return getSharePermissions(ctx, params, options);
    },

    /**
     * Add share permission
     *
     * Add a share permissions to a filter. If you add a global share permission (one for all logged-in users or the public) it will overwrite all share permissions for the filter.
     *
     * Be aware that this operation uses different objects for updating share permissions compared to [Update filter](#api-rest-api-2-filter-id-put).
     *
     * **[Permissions](#permissions) required:** *Share dashboards and filters* [global permission](https://confluence.atlassian.com/x/x4dKLg) and the user must own the filter.
     *
     * @param params.id - The ID of the filter.
     * @returns Returned if the request is successful.
     */
    addSharePermission(
      params: SharePermissionInputBean & {
        /**
         * The ID of the filter.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SharePermission[]> {
      return addSharePermission(ctx, params, options);
    },

    /**
     * Get share permission
     *
     * Returns a share permission for a filter. A filter can be shared with groups, projects, all logged-in users, or the public. Sharing with all logged-in users or the public is known as a global share permission.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** None, however, a share permission is only returned for:
     *
     *  *  filters owned by the user.
     *  *  filters shared with a group that the user is a member of.
     *  *  filters shared with a private project that the user has *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.
     *  *  filters shared with a public project.
     *  *  filters shared with the public.
     *
     * @param params.id - The ID of the filter.
     * @param params.permissionId - The ID of the share permission.
     * @returns Returned if the request is successful.
     */
    getSharePermission(
      params: {
        /**
         * The ID of the filter.
         *
         * @format int64
         */
        id: number;
        /**
         * The ID of the share permission.
         *
         * @format int64
         */
        permissionId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<SharePermission> {
      return getSharePermission(ctx, params, options);
    },

    /**
     * Delete share permission
     *
     * Deletes a share permission from a filter.
     *
     * **[Permissions](#permissions) required:** Permission to access Jira and the user must own the filter.
     *
     * @param params.id - The ID of the filter.
     * @param params.permissionId - The ID of the share permission.
     * @returns Returned if the request is successful.
     */
    deleteSharePermission(
      params: {
        /**
         * The ID of the filter.
         *
         * @format int64
         */
        id: number;
        /**
         * The ID of the share permission.
         *
         * @format int64
         */
        permissionId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteSharePermission(ctx, params, options);
    },
  };
}
