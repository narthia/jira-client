// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  BulkCustomFieldOptionCreateRequest,
  BulkCustomFieldOptionUpdateRequest,
  CustomFieldCreatedContextOptionsList,
  CustomFieldOption,
  CustomFieldUpdatedContextOptionsList,
  OrderOfCustomFieldOptions,
  PageBeanCustomFieldContextOption,
} from "../types/index.ts";

/**
 * Get custom field option
 *
 * Returns a custom field option. For example, an option in a select list.
 *
 * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required:** The custom field option is returned as follows:
 *
 *  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the custom field is used in, and the field is visible in at least one layout the user has permission to view.
 *
 * @param params.id - The ID of the custom field option.
 * @returns Returned if the request is successful.
 */
export function getCustomFieldOption(
  ctx: ClientContext,
  params: {
    /** The ID of the custom field option. */
    id: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomFieldOption> {
  const { id } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/customFieldOption/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get custom field options (context)
 *
 * Returns a [paginated](#pagination) list of all custom field option for a context. Options are returned first then cascading options, in the order they display in Jira.
 *
 * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg). *Edit Workflow* [edit workflow permission](https://support.atlassian.com/jira-cloud-administration/docs/permissions-for-company-managed-projects/#Edit-Workflows)
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.contextId - The ID of the context.
 * @param params.optionId - The ID of the option.
 * @param params.onlyOptions - Whether only options are returned.
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @returns Returned if the request is successful.
 */
export function getOptionsForContext(
  ctx: ClientContext,
  params: {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the context.
     *
     * @format int64
     */
    contextId: number;
    /**
     * The ID of the option.
     *
     * @format int64
     */
    optionId?: number;
    /**
     * Whether only options are returned.
     *
     * @default false
     */
    onlyOptions?: boolean;
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 100
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanCustomFieldContextOption> {
  const { fieldId, contextId, optionId, onlyOptions, startAt, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option",
    pathParams: { fieldId, contextId },
    query: { optionId, onlyOptions, startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update custom field options (context)
 *
 * Updates the options of a custom field.
 *
 * If any of the options are not found, no options are updated. Options where the values in the request match the current values aren't updated and aren't reported in the response.
 *
 * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.contextId - The ID of the context.
 * @returns Returned if the request is successful.
 */
export function updateCustomFieldOption(
  ctx: ClientContext,
  params: BulkCustomFieldOptionUpdateRequest & {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the context.
     *
     * @format int64
     */
    contextId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomFieldUpdatedContextOptionsList> {
  const { fieldId, contextId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option",
    pathParams: { fieldId, contextId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create custom field options (context)
 *
 * Creates options and, where the custom select field is of the type Select List (cascading), cascading options for a custom select field. The options are added to a context of the field.
 *
 * The maximum number of options that can be created per request is 1000 and each field can have a maximum of 10000 options.
 *
 * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.contextId - The ID of the context.
 * @returns Returned if the request is successful.
 */
export function createCustomFieldOption(
  ctx: ClientContext,
  params: BulkCustomFieldOptionCreateRequest & {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the context.
     *
     * @format int64
     */
    contextId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomFieldCreatedContextOptionsList> {
  const { fieldId, contextId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option",
    pathParams: { fieldId, contextId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Reorder custom field options (context)
 *
 * Changes the order of custom field options or cascading options in a context.
 *
 * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.contextId - The ID of the context.
 * @returns Returned if options are reordered.
 */
export function reorderCustomFieldOptions(
  ctx: ClientContext,
  params: OrderOfCustomFieldOptions & {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the context.
     *
     * @format int64
     */
    contextId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { fieldId, contextId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option/move",
    pathParams: { fieldId, contextId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete custom field options (context)
 *
 * Deletes a custom field option.
 *
 * Options with cascading options cannot be deleted without deleting the cascading options first.
 *
 * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.contextId - The ID of the context from which an option should be deleted.
 * @param params.optionId - The ID of the option to delete.
 * @returns Returned if the option is deleted.
 */
export function deleteCustomFieldOption(
  ctx: ClientContext,
  params: {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the context from which an option should be deleted.
     *
     * @format int64
     */
    contextId: number;
    /**
     * The ID of the option to delete.
     *
     * @format int64
     */
    optionId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { fieldId, contextId, optionId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option/{optionId}",
    pathParams: { fieldId, contextId, optionId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Replace custom field options
 *
 * Replaces the options of a custom field.
 *
 * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect or Forge apps.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.fieldId - The ID of the custom field.
 * @param params.optionId - The ID of the option to be deselected.
 * @param params.contextId - The ID of the context.
 * @param params.replaceWith - The ID of the option that will replace the currently selected option.
 * @param params.jql - A JQL query that specifies the issues to be updated. For example, *project=10000*.
 */
export function replaceCustomFieldOption(
  ctx: ClientContext,
  params: {
    /** The ID of the custom field. */
    fieldId: string;
    /**
     * The ID of the option to be deselected.
     *
     * @format int64
     */
    optionId: number;
    /**
     * The ID of the context.
     *
     * @format int64
     */
    contextId: number;
    /**
     * The ID of the option that will replace the currently selected option.
     *
     * @format int64
     */
    replaceWith?: number;
    /** A JQL query that specifies the issues to be updated. For example, *project=10000*. */
    jql?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { fieldId, optionId, contextId, replaceWith, jql } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/2/field/{fieldId}/context/{contextId}/option/{optionId}/issue",
    pathParams: { fieldId, optionId, contextId },
    query: { replaceWith, jql },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents custom issue field select list options created in Jira or using the REST API. This resource supports the following field types:
 *
 *  *  Checkboxes.
 *  *  Radio Buttons.
 *  *  Select List (single choice).
 *  *  Select List (multiple choices).
 *  *  Select List (cascading).
 *
 * See [Issue custom field options (apps)](#api-group-Issue-custom-field-options--apps-) to manipulate custom issue field select list options created by a Connect app.
 *
 * Use it to retrieve, create, update, order, and delete custom field options.
 */
export function createIssueCustomFieldOptionsService(ctx: ClientContext) {
  return {
    /**
     * Get custom field option
     *
     * Returns a custom field option. For example, an option in a select list.
     *
     * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required:** The custom field option is returned as follows:
     *
     *  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the custom field is used in, and the field is visible in at least one layout the user has permission to view.
     *
     * @param params.id - The ID of the custom field option.
     * @returns Returned if the request is successful.
     */
    getCustomFieldOption(
      params: {
        /** The ID of the custom field option. */
        id: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomFieldOption> {
      return getCustomFieldOption(ctx, params, options);
    },

    /**
     * Get custom field options (context)
     *
     * Returns a [paginated](#pagination) list of all custom field option for a context. Options are returned first then cascading options, in the order they display in Jira.
     *
     * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg). *Edit Workflow* [edit workflow permission](https://support.atlassian.com/jira-cloud-administration/docs/permissions-for-company-managed-projects/#Edit-Workflows)
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.contextId - The ID of the context.
     * @param params.optionId - The ID of the option.
     * @param params.onlyOptions - Whether only options are returned.
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @returns Returned if the request is successful.
     */
    getOptionsForContext(
      params: {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the context.
         *
         * @format int64
         */
        contextId: number;
        /**
         * The ID of the option.
         *
         * @format int64
         */
        optionId?: number;
        /**
         * Whether only options are returned.
         *
         * @default false
         */
        onlyOptions?: boolean;
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 100
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanCustomFieldContextOption> {
      return getOptionsForContext(ctx, params, options);
    },

    /**
     * Update custom field options (context)
     *
     * Updates the options of a custom field.
     *
     * If any of the options are not found, no options are updated. Options where the values in the request match the current values aren't updated and aren't reported in the response.
     *
     * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.contextId - The ID of the context.
     * @returns Returned if the request is successful.
     */
    updateCustomFieldOption(
      params: BulkCustomFieldOptionUpdateRequest & {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the context.
         *
         * @format int64
         */
        contextId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomFieldUpdatedContextOptionsList> {
      return updateCustomFieldOption(ctx, params, options);
    },

    /**
     * Create custom field options (context)
     *
     * Creates options and, where the custom select field is of the type Select List (cascading), cascading options for a custom select field. The options are added to a context of the field.
     *
     * The maximum number of options that can be created per request is 1000 and each field can have a maximum of 10000 options.
     *
     * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.contextId - The ID of the context.
     * @returns Returned if the request is successful.
     */
    createCustomFieldOption(
      params: BulkCustomFieldOptionCreateRequest & {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the context.
         *
         * @format int64
         */
        contextId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomFieldCreatedContextOptionsList> {
      return createCustomFieldOption(ctx, params, options);
    },

    /**
     * Reorder custom field options (context)
     *
     * Changes the order of custom field options or cascading options in a context.
     *
     * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.contextId - The ID of the context.
     * @returns Returned if options are reordered.
     */
    reorderCustomFieldOptions(
      params: OrderOfCustomFieldOptions & {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the context.
         *
         * @format int64
         */
        contextId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return reorderCustomFieldOptions(ctx, params, options);
    },

    /**
     * Delete custom field options (context)
     *
     * Deletes a custom field option.
     *
     * Options with cascading options cannot be deleted without deleting the cascading options first.
     *
     * This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.contextId - The ID of the context from which an option should be deleted.
     * @param params.optionId - The ID of the option to delete.
     * @returns Returned if the option is deleted.
     */
    deleteCustomFieldOption(
      params: {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the context from which an option should be deleted.
         *
         * @format int64
         */
        contextId: number;
        /**
         * The ID of the option to delete.
         *
         * @format int64
         */
        optionId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteCustomFieldOption(ctx, params, options);
    },

    /**
     * Replace custom field options
     *
     * Replaces the options of a custom field.
     *
     * Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect or Forge apps.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.fieldId - The ID of the custom field.
     * @param params.optionId - The ID of the option to be deselected.
     * @param params.contextId - The ID of the context.
     * @param params.replaceWith - The ID of the option that will replace the currently selected option.
     * @param params.jql - A JQL query that specifies the issues to be updated. For example, *project=10000*.
     */
    replaceCustomFieldOption(
      params: {
        /** The ID of the custom field. */
        fieldId: string;
        /**
         * The ID of the option to be deselected.
         *
         * @format int64
         */
        optionId: number;
        /**
         * The ID of the context.
         *
         * @format int64
         */
        contextId: number;
        /**
         * The ID of the option that will replace the currently selected option.
         *
         * @format int64
         */
        replaceWith?: number;
        /** A JQL query that specifies the issues to be updated. For example, *project=10000*. */
        jql?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return replaceCustomFieldOption(ctx, params, options);
    },
  };
}
