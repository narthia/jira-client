// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { IssueIdOrKeysAssociation, ServiceIdOrKeysAssociation } from "../types/index.ts";

/**
 * Submit Remote Link data
 *
 * Update / insert Remote Link data.
 *
 * Remote Links are identified by their ID, existing Remote Link data for the same ID will be replaced if it
 * exists and the updateSequenceId of existing data is less than the incoming data.
 *
 * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are
 * available within a short period of time, but may take some time during peak load and/or maintenance times.
 * The `getRemoteLinkById` operation can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple Remote Links being submitted in one request, each is validated individually prior to
 * submission. Details of which Remote LInk failed submission (if any) are available in the response object.
 *
 * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Submission accepted. Each submitted Remote Link that is of a valid format will be eventually available in Jira. Details of which Remote Links were submitted and which failed submission (due to data format problems etc.) are available in the response object.
 */
export function submitRemoteLinks(
  ctx: ClientContext,
  params: {
    /**
     * Properties assigned to Remote Link data that can then be used for delete / query operations.
     *
     * Examples might be an account or user ID that can then be used to clean up data if an account is removed from
     * the Provider system.
     *
     * Properties are supplied as key/value pairs, a maximum of 5 properties can be supplied, and keys must not
     * contain ':' or start with '_'.
     *
     * @example
     * ```json
     * {
     *   "accountId": "account-234",
     *   "projectId": "project-123"
     * }
     * ```
     */
    properties?: Record<string, string>;
    /**
     * A list of Remote Links to submit to Jira.
     *
     * Each Remote Link may be associated with one or more Jira issue keys, and will be associated with any properties
     * included in this request.
     */
    remoteLinks: {
      /**
       * The schema version used for this data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
      /**
       * The identifier for the Remote Link. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Remote Link in the case of out-of-order receipt of
       * update requests.
       *
       * It must be a monotonically increasing number. For example, epoch time could be one way to generate the
       * `updateSequenceNumber`.
       *
       * Updates for a Remote Link that is received with an `updateSqeuenceNumber` less than or equal to what is currently
       * stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The human-readable name for the Remote Link.
       *
       * Will be shown in the UI.
       *
       * @example
       * ```json
       * "Remote Link #42"
       * ```
       */
      displayName: string;
      /**
       * The URL to this Remote Link in your system.
       *
       * @format uri
       */
      url: string;
      /**
       * The type of the Remote Link. The current supported types are 'document', 'alert', 'test',
       * 'security', 'logFile', 'prototype', 'coverage', 'bugReport' and 'other'
       *
       * @example
       * ```json
       * "security"
       * ```
       */
      type:
        | "document"
        | "alert"
        | "test"
        | "security"
        | "logFile"
        | "prototype"
        | "coverage"
        | "bugReport"
        | "other";
      /**
       * An optional description to attach to this Remote Link.
       *
       * This may be anything that makes sense in your system.
       *
       * @example
       * ```json
       * "Remote Link #42 with more information in this description"
       * ```
       */
      description?: string;
      /**
       * The last-updated timestamp to present to the user as a summary of when Remote Link was last updated.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /** The entities to associate the Remote Link information with. */
      associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
      /** The status of a Remote Link. */
      status?: {
        /**
         * Appearance is a fixed set of appearance types affecting the colour
         * of the status lozenge in the UI. The colours they correspond to are
         * equivalent to atlaskit's [Lozenge](https://atlaskit.atlassian.com/packages/core/lozenge) component.
         *
         * @example
         * ```json
         * "inprogress"
         * ```
         */
        appearance:
          | "default"
          | "inprogress"
          | "moved"
          | "new"
          | "removed"
          | "prototype"
          | "success";
        /**
         * The human-readable description for the Remote Link status.
         *
         * Will be shown in the UI.
         *
         * @example
         * ```json
         * "ANOMALOUS"
         * ```
         */
        label: string;
      };
      /**
       * Optional list of actionIds. They are associated with the actions the provider is able to provide when they
       * registered. Indicates which actions this Remote Link has.
       *
       * If any actions have a templateUrl that requires string substitution, then `attributeMap` must be passed in.
       *
       * @example
       * ```json
       * [
       *   "action-111-222-333",
       *   "action-444-555-666"
       * ]
       * ```
       */
      actionIds?: string[];
      /**
       * Map of key/values (string to string mapping). This is used to build the urls for actions from the
       * templateUrl the provider registered their available actions with.
       */
      attributeMap?: Record<string, string>;
    }[];
    /**
     * Information about the provider. This is useful for auditing, logging, debugging, and other internal uses. It is
     * not considered private information. Hence, it may not contain personally identifiable information.
     */
    providerMetadata?: {
      /**
       * An optional name of the source of the Remote Links data.
       *
       * @example
       * ```json
       * "Opsgenie 6.10.2"
       * ```
       */
      product?: string;
    };
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with a Connect JWT token that corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The IDs of Remote Links that have been accepted for submission.
   *
   * A Remote Link may be rejected if it was only associated with unknown issue keys, unknown service IDs, or if
   * the submitted data for that Remote Link does not match the required schema.
   *
   * Note that a Remote Link that isn't updated due to it's `updateSequenceNumber` being out of order is not
   * considered a failed submission.
   *
   * @example
   * ```json
   * [
   *   "111-222-333",
   *   "444-555-666"
   * ]
   * ```
   */
  acceptedRemoteLinks?: string[];
  /**
   * Details of Remote Links that have not been accepted for submission, usually due to a problem with the request data.
   *
   * A Remote Link may be rejected if it was only associated with unknown issue keys, unknown service IDs, or
   * if the submitted data for the Remote Link does not match the required schema.
   *
   * The object (if present) will be keyed by Remote Link ID and include any errors associated with that
   * Remote Link that have prevented it being submitted.
   */
  rejectedRemoteLinks?: Record<
    string,
    {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[]
  >;
  /** Issue keys or services IDs or keys that are not known on this Jira instance (if any). */
  unknownAssociations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/remotelinks/1.0/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete Remote Links by Property
 *
 * Bulk delete all Remote Links data that match the given request.
 *
 * One or more query params must be supplied to specify Properties to delete by.
 * Optional param `_updateSequenceNumber` is no longer supported. If more than one Property is provided,
 * data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
 *
 * See the documentation for the `submitRemoteLinks` operation for more details.
 *
 * e.g. DELETE /bulkByProperties?accountId=account-123&repoId=repo-345
 *
 * Deletion is performed asynchronously. The `getRemoteLinkById` operation can be used to confirm that data has been
 * deleted successfully (if needed).
 *
 * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
 * @param params.params_query - Free-form query parameters to specify which properties to delete by. Properties refer to the arbitrary information the provider tagged Remote Links with previously. For example, if the provider previously tagged a remote link with accountId: "properties": { "accountId": "account-123" } And now they want to delete Remote Links in bulk by that specific accountId as follows: e.g. DELETE /bulkByProperties?accountId=account-123
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteRemoteLinksByProperty(
  ctx: ClientContext,
  params?: {
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `_updateSequenceNumber` to use to control deletion.
     *
     * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * If not provided, all stored data that matches the request will be deleted.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceNumber?: number;
    /**
     * Free-form query parameters to specify which properties to delete by. Properties refer to the arbitrary
     * information the provider tagged Remote Links with previously.
     *
     * For example, if the provider previously tagged a remote link with accountId:
     *   "properties": {
     *     "accountId": "account-123"
     *   }
     *
     * And now they want to delete Remote Links in bulk by that specific accountId as follows:
     * e.g. DELETE /bulkByProperties?accountId=account-123
     */
    params_query?: Record<string, unknown>;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
       * corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { updateSequenceNumber, params_query } = params ?? {};
  return ctx.request({
    method: "delete",
    path: "/rest/remotelinks/1.0/bulkByProperties",
    query: { _updateSequenceNumber: updateSequenceNumber, params: params_query },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a Remote Link by ID
 *
 * Retrieve the currently stored Remote Link data for the given ID.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param params.remoteLinkId - The ID of the Remote Link to fetch.
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns The Remote Link data currently stored for the given ID.
 */
export function getRemoteLinkById(
  ctx: ClientContext,
  params: {
    /** The ID of the Remote Link to fetch. */
    remoteLinkId: string;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
       * corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The schema version used for this data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion?: "1.0";
  /**
   * The identifier for the Remote Link. Must be unique for a given Provider.
   *
   * @example
   * ```json
   * "111-222-333"
   * ```
   */
  id: string;
  /**
   * An ID used to apply an ordering to updates for this Remote Link in the case of out-of-order receipt of
   * update requests.
   *
   * It must be a monotonically increasing number. For example, epoch time could be one way to generate the
   * `updateSequenceNumber`.
   *
   * Updates for a Remote Link that is received with an `updateSqeuenceNumber` less than or equal to what is currently
   * stored will be ignored.
   *
   * @format int64
   * @example
   * ```json
   * 1523494301448
   * ```
   */
  updateSequenceNumber: number;
  /**
   * The human-readable name for the Remote Link.
   *
   * Will be shown in the UI.
   *
   * @example
   * ```json
   * "Remote Link #42"
   * ```
   */
  displayName: string;
  /**
   * The URL to this Remote Link in your system.
   *
   * @format uri
   */
  url: string;
  /**
   * The type of the Remote Link. The current supported types are 'document', 'alert', 'test',
   * 'security', 'logFile', 'prototype', 'coverage', 'bugReport' and 'other'
   *
   * @example
   * ```json
   * "security"
   * ```
   */
  type:
    | "document"
    | "alert"
    | "test"
    | "security"
    | "logFile"
    | "prototype"
    | "coverage"
    | "bugReport"
    | "other";
  /**
   * An optional description to attach to this Remote Link.
   *
   * This may be anything that makes sense in your system.
   *
   * @example
   * ```json
   * "Remote Link #42 with more information in this description"
   * ```
   */
  description?: string;
  /**
   * The last-updated timestamp to present to the user as a summary of when Remote Link was last updated.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
  /** The entities to associate the Remote Link information with. */
  associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
  /** The status of a Remote Link. */
  status?: {
    /**
     * Appearance is a fixed set of appearance types affecting the colour
     * of the status lozenge in the UI. The colours they correspond to are
     * equivalent to atlaskit's [Lozenge](https://atlaskit.atlassian.com/packages/core/lozenge) component.
     *
     * @example
     * ```json
     * "inprogress"
     * ```
     */
    appearance: "default" | "inprogress" | "moved" | "new" | "removed" | "prototype" | "success";
    /**
     * The human-readable description for the Remote Link status.
     *
     * Will be shown in the UI.
     *
     * @example
     * ```json
     * "ANOMALOUS"
     * ```
     */
    label: string;
  };
  /**
   * Optional list of actionIds. They are associated with the actions the provider is able to provide when they
   * registered. Indicates which actions this Remote Link has.
   *
   * If any actions have a templateUrl that requires string substitution, then `attributeMap` must be passed in.
   *
   * @example
   * ```json
   * [
   *   "action-111-222-333",
   *   "action-444-555-666"
   * ]
   * ```
   */
  actionIds?: string[];
  /**
   * Map of key/values (string to string mapping). This is used to build the urls for actions from the
   * templateUrl the provider registered their available actions with.
   */
  attributeMap?: Record<string, string>;
}> {
  const { remoteLinkId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/remotelinks/1.0/remotelink/{remoteLinkId}",
    pathParams: { remoteLinkId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a Remote Link by ID
 *
 * Delete the Remote Link data currently stored for the given ID.
 *
 * Deletion is performed asynchronously. The `getRemoteLinkById` operation can be used to confirm that data has been
 * deleted successfully (if needed).
 *
 * @param params.remoteLinkId - The ID of the Remote Link to fetch.
 * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteRemoteLinkById(
  ctx: ClientContext,
  params: {
    /** The ID of the Remote Link to fetch. */
    remoteLinkId: string;
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `_updateSequenceNumber` to use to control deletion.
     *
     * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceNumber?: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
       * corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { remoteLinkId, updateSequenceNumber } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/remotelinks/1.0/remotelink/{remoteLinkId}",
    pathParams: { remoteLinkId },
    query: { _updateSequenceNumber: updateSequenceNumber },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * APIs related to integrating remote link data with Jira Software.
 *
 * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraRemoteLinkInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/remote-link/.
 *
 * These APIs are available to Forge apps with the `devops:remoteLinkInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-remote-link-info/.
 */
export function createRemoteLinksService(ctx: ClientContext) {
  return {
    /**
     * Submit Remote Link data
     *
     * Update / insert Remote Link data.
     *
     * Remote Links are identified by their ID, existing Remote Link data for the same ID will be replaced if it
     * exists and the updateSequenceId of existing data is less than the incoming data.
     *
     * Submissions are performed asynchronously. Submitted data will eventually be available in Jira; most updates are
     * available within a short period of time, but may take some time during peak load and/or maintenance times.
     * The `getRemoteLinkById` operation can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple Remote Links being submitted in one request, each is validated individually prior to
     * submission. Details of which Remote LInk failed submission (if any) are available in the response object.
     *
     * @param options.headers.Authorization - All requests must be signed with a Connect JWT token that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Submission accepted. Each submitted Remote Link that is of a valid format will be eventually available in Jira. Details of which Remote Links were submitted and which failed submission (due to data format problems etc.) are available in the response object.
     */
    submitRemoteLinks(
      params: {
        /**
         * Properties assigned to Remote Link data that can then be used for delete / query operations.
         *
         * Examples might be an account or user ID that can then be used to clean up data if an account is removed from
         * the Provider system.
         *
         * Properties are supplied as key/value pairs, a maximum of 5 properties can be supplied, and keys must not
         * contain ':' or start with '_'.
         *
         * @example
         * ```json
         * {
         *   "accountId": "account-234",
         *   "projectId": "project-123"
         * }
         * ```
         */
        properties?: Record<string, string>;
        /**
         * A list of Remote Links to submit to Jira.
         *
         * Each Remote Link may be associated with one or more Jira issue keys, and will be associated with any properties
         * included in this request.
         */
        remoteLinks: {
          /**
           * The schema version used for this data.
           *
           * Placeholder to support potential schema changes in the future.
           *
           * @default "1.0"
           * @example
           * ```json
           * "1.0"
           * ```
           */
          schemaVersion?: "1.0";
          /**
           * The identifier for the Remote Link. Must be unique for a given Provider.
           *
           * @example
           * ```json
           * "111-222-333"
           * ```
           */
          id: string;
          /**
           * An ID used to apply an ordering to updates for this Remote Link in the case of out-of-order receipt of
           * update requests.
           *
           * It must be a monotonically increasing number. For example, epoch time could be one way to generate the
           * `updateSequenceNumber`.
           *
           * Updates for a Remote Link that is received with an `updateSqeuenceNumber` less than or equal to what is currently
           * stored will be ignored.
           *
           * @format int64
           * @example
           * ```json
           * 1523494301448
           * ```
           */
          updateSequenceNumber: number;
          /**
           * The human-readable name for the Remote Link.
           *
           * Will be shown in the UI.
           *
           * @example
           * ```json
           * "Remote Link #42"
           * ```
           */
          displayName: string;
          /**
           * The URL to this Remote Link in your system.
           *
           * @format uri
           */
          url: string;
          /**
           * The type of the Remote Link. The current supported types are 'document', 'alert', 'test',
           * 'security', 'logFile', 'prototype', 'coverage', 'bugReport' and 'other'
           *
           * @example
           * ```json
           * "security"
           * ```
           */
          type:
            | "document"
            | "alert"
            | "test"
            | "security"
            | "logFile"
            | "prototype"
            | "coverage"
            | "bugReport"
            | "other";
          /**
           * An optional description to attach to this Remote Link.
           *
           * This may be anything that makes sense in your system.
           *
           * @example
           * ```json
           * "Remote Link #42 with more information in this description"
           * ```
           */
          description?: string;
          /**
           * The last-updated timestamp to present to the user as a summary of when Remote Link was last updated.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          lastUpdated: string;
          /** The entities to associate the Remote Link information with. */
          associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
          /** The status of a Remote Link. */
          status?: {
            /**
             * Appearance is a fixed set of appearance types affecting the colour
             * of the status lozenge in the UI. The colours they correspond to are
             * equivalent to atlaskit's [Lozenge](https://atlaskit.atlassian.com/packages/core/lozenge) component.
             *
             * @example
             * ```json
             * "inprogress"
             * ```
             */
            appearance:
              | "default"
              | "inprogress"
              | "moved"
              | "new"
              | "removed"
              | "prototype"
              | "success";
            /**
             * The human-readable description for the Remote Link status.
             *
             * Will be shown in the UI.
             *
             * @example
             * ```json
             * "ANOMALOUS"
             * ```
             */
            label: string;
          };
          /**
           * Optional list of actionIds. They are associated with the actions the provider is able to provide when they
           * registered. Indicates which actions this Remote Link has.
           *
           * If any actions have a templateUrl that requires string substitution, then `attributeMap` must be passed in.
           *
           * @example
           * ```json
           * [
           *   "action-111-222-333",
           *   "action-444-555-666"
           * ]
           * ```
           */
          actionIds?: string[];
          /**
           * Map of key/values (string to string mapping). This is used to build the urls for actions from the
           * templateUrl the provider registered their available actions with.
           */
          attributeMap?: Record<string, string>;
        }[];
        /**
         * Information about the provider. This is useful for auditing, logging, debugging, and other internal uses. It is
         * not considered private information. Hence, it may not contain personally identifiable information.
         */
        providerMetadata?: {
          /**
           * An optional name of the source of the Remote Links data.
           *
           * @example
           * ```json
           * "Opsgenie 6.10.2"
           * ```
           */
          product?: string;
        };
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with a Connect JWT token that corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The IDs of Remote Links that have been accepted for submission.
       *
       * A Remote Link may be rejected if it was only associated with unknown issue keys, unknown service IDs, or if
       * the submitted data for that Remote Link does not match the required schema.
       *
       * Note that a Remote Link that isn't updated due to it's `updateSequenceNumber` being out of order is not
       * considered a failed submission.
       *
       * @example
       * ```json
       * [
       *   "111-222-333",
       *   "444-555-666"
       * ]
       * ```
       */
      acceptedRemoteLinks?: string[];
      /**
       * Details of Remote Links that have not been accepted for submission, usually due to a problem with the request data.
       *
       * A Remote Link may be rejected if it was only associated with unknown issue keys, unknown service IDs, or
       * if the submitted data for the Remote Link does not match the required schema.
       *
       * The object (if present) will be keyed by Remote Link ID and include any errors associated with that
       * Remote Link that have prevented it being submitted.
       */
      rejectedRemoteLinks?: Record<
        string,
        {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[]
      >;
      /** Issue keys or services IDs or keys that are not known on this Jira instance (if any). */
      unknownAssociations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
    }> {
      return submitRemoteLinks(ctx, params, options);
    },

    /**
     * Delete Remote Links by Property
     *
     * Bulk delete all Remote Links data that match the given request.
     *
     * One or more query params must be supplied to specify Properties to delete by.
     * Optional param `_updateSequenceNumber` is no longer supported. If more than one Property is provided,
     * data will be deleted that matches ALL of the Properties (e.g. treated as an AND).
     *
     * See the documentation for the `submitRemoteLinks` operation for more details.
     *
     * e.g. DELETE /bulkByProperties?accountId=account-123&repoId=repo-345
     *
     * Deletion is performed asynchronously. The `getRemoteLinkById` operation can be used to confirm that data has been
     * deleted successfully (if needed).
     *
     * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
     * @param params.params_query - Free-form query parameters to specify which properties to delete by. Properties refer to the arbitrary information the provider tagged Remote Links with previously. For example, if the provider previously tagged a remote link with accountId: "properties": { "accountId": "account-123" } And now they want to delete Remote Links in bulk by that specific accountId as follows: e.g. DELETE /bulkByProperties?accountId=account-123
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteRemoteLinksByProperty(
      params?: {
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `_updateSequenceNumber` to use to control deletion.
         *
         * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * If not provided, all stored data that matches the request will be deleted.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceNumber?: number;
        /**
         * Free-form query parameters to specify which properties to delete by. Properties refer to the arbitrary
         * information the provider tagged Remote Links with previously.
         *
         * For example, if the provider previously tagged a remote link with accountId:
         *   "properties": {
         *     "accountId": "account-123"
         *   }
         *
         * And now they want to delete Remote Links in bulk by that specific accountId as follows:
         * e.g. DELETE /bulkByProperties?accountId=account-123
         */
        params_query?: Record<string, unknown>;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
           * corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteRemoteLinksByProperty(ctx, params, options);
    },

    /**
     * Get a Remote Link by ID
     *
     * Retrieve the currently stored Remote Link data for the given ID.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param params.remoteLinkId - The ID of the Remote Link to fetch.
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns The Remote Link data currently stored for the given ID.
     */
    getRemoteLinkById(
      params: {
        /** The ID of the Remote Link to fetch. */
        remoteLinkId: string;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
           * corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The schema version used for this data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
      /**
       * The identifier for the Remote Link. Must be unique for a given Provider.
       *
       * @example
       * ```json
       * "111-222-333"
       * ```
       */
      id: string;
      /**
       * An ID used to apply an ordering to updates for this Remote Link in the case of out-of-order receipt of
       * update requests.
       *
       * It must be a monotonically increasing number. For example, epoch time could be one way to generate the
       * `updateSequenceNumber`.
       *
       * Updates for a Remote Link that is received with an `updateSqeuenceNumber` less than or equal to what is currently
       * stored will be ignored.
       *
       * @format int64
       * @example
       * ```json
       * 1523494301448
       * ```
       */
      updateSequenceNumber: number;
      /**
       * The human-readable name for the Remote Link.
       *
       * Will be shown in the UI.
       *
       * @example
       * ```json
       * "Remote Link #42"
       * ```
       */
      displayName: string;
      /**
       * The URL to this Remote Link in your system.
       *
       * @format uri
       */
      url: string;
      /**
       * The type of the Remote Link. The current supported types are 'document', 'alert', 'test',
       * 'security', 'logFile', 'prototype', 'coverage', 'bugReport' and 'other'
       *
       * @example
       * ```json
       * "security"
       * ```
       */
      type:
        | "document"
        | "alert"
        | "test"
        | "security"
        | "logFile"
        | "prototype"
        | "coverage"
        | "bugReport"
        | "other";
      /**
       * An optional description to attach to this Remote Link.
       *
       * This may be anything that makes sense in your system.
       *
       * @example
       * ```json
       * "Remote Link #42 with more information in this description"
       * ```
       */
      description?: string;
      /**
       * The last-updated timestamp to present to the user as a summary of when Remote Link was last updated.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /** The entities to associate the Remote Link information with. */
      associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation)[];
      /** The status of a Remote Link. */
      status?: {
        /**
         * Appearance is a fixed set of appearance types affecting the colour
         * of the status lozenge in the UI. The colours they correspond to are
         * equivalent to atlaskit's [Lozenge](https://atlaskit.atlassian.com/packages/core/lozenge) component.
         *
         * @example
         * ```json
         * "inprogress"
         * ```
         */
        appearance:
          | "default"
          | "inprogress"
          | "moved"
          | "new"
          | "removed"
          | "prototype"
          | "success";
        /**
         * The human-readable description for the Remote Link status.
         *
         * Will be shown in the UI.
         *
         * @example
         * ```json
         * "ANOMALOUS"
         * ```
         */
        label: string;
      };
      /**
       * Optional list of actionIds. They are associated with the actions the provider is able to provide when they
       * registered. Indicates which actions this Remote Link has.
       *
       * If any actions have a templateUrl that requires string substitution, then `attributeMap` must be passed in.
       *
       * @example
       * ```json
       * [
       *   "action-111-222-333",
       *   "action-444-555-666"
       * ]
       * ```
       */
      actionIds?: string[];
      /**
       * Map of key/values (string to string mapping). This is used to build the urls for actions from the
       * templateUrl the provider registered their available actions with.
       */
      attributeMap?: Record<string, string>;
    }> {
      return getRemoteLinkById(ctx, params, options);
    },

    /**
     * Delete a Remote Link by ID
     *
     * Delete the Remote Link data currently stored for the given ID.
     *
     * Deletion is performed asynchronously. The `getRemoteLinkById` operation can be used to confirm that data has been
     * deleted successfully (if needed).
     *
     * @param params.remoteLinkId - The ID of the Remote Link to fetch.
     * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteRemoteLinkById(
      params: {
        /** The ID of the Remote Link to fetch. */
        remoteLinkId: string;
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `_updateSequenceNumber` to use to control deletion.
         *
         * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceNumber?: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that
           * corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraRemoteLinkInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteRemoteLinkById(ctx, params, options);
    },
  };
}
