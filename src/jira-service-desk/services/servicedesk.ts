// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  CustomerRequestCreateMetaDto,
  EntityProperty,
  MultipartFile,
  PagedDtoArticleDto,
  PagedDtoIssueBean,
  PagedDtoQueueDto,
  PagedDtoRequestTypeDto,
  PagedDtoRequestTypeGroupDto,
  PagedDtoServiceDeskDto,
  PagedDtoUserDto,
  PropertyKeys,
  QueueDto,
  RequestTypeCreateDto,
  RequestTypeDto,
  RequestTypePermissionCheckRequestDto,
  RequestTypePermissionCheckResponse,
  ServiceDeskCustomerDto,
  ServiceDeskCustomerInviteDto,
  ServiceDeskDto,
} from "../types/index.ts";

/**
 * Get service desks
 *
 * This method returns all the service desks in the Jira Service Management instance that the user has permission to access. Use this method where you need a list of service desks or need to locate a service desk by name or keyword.
 *
 * **Note:** This method will be slow if the instance has hundreds of service desks. If you want to fetch a single service desk by its ID, use [/rest/servicedeskapi/servicedesk/\{serviceDeskId\}](./#api-rest-servicedeskapi-servicedesk-servicedeskid-get) instead.
 *
 * **[Permissions](#permissions) required**: Any
 *
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the service desks, on the specified page of the results.
 */
export function getServiceDesks(
  ctx: ClientContext,
  params?: {
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoServiceDeskDto> {
  const { start, limit } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk",
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get service desk by id
 *
 * This method returns a service desk. Use this method to get service desk details whenever your application component is passed a service desk ID but needs to display other service desk details.
 *
 * **[Permissions](#permissions) required**: Permission to access the Service Desk. For example, being the Service Desk's Administrator or one of its Agents or Users.
 *
 * @param params.serviceDeskId - The ID of the service desk to return. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returns the requested service desk.
 */
export function getServiceDeskById(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk to return. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ServiceDeskDto> {
  const { serviceDeskId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Attach temporary file
 *
 * This method adds one or more temporary attachments to a service desk, which can then be permanently attached to a customer request using [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-post).
 *
 * **Note**: It is possible for a service desk administrator to turn off the ability to add attachments to a service desk.
 *
 * This method expects a multipart request. The media-type multipart/form-data is defined in RFC 1867. Most client libraries have classes that make dealing with multipart posts simple. For instance, in Java the Apache HTTP Components library provides [MultiPartEntity](http://hc.apache.org/httpcomponents-client-ga/httpmime/apidocs/org/apache/http/entity/mime/MultipartEntity.html).
 *
 * Because this method accepts multipart/form-data, it has XSRF protection on it. This means you must submit a header of X-Atlassian-Token: no-check with the request or it will be blocked.
 *
 * The name of the multipart/form-data parameter that contains the attachments must be `file`.
 *
 * For example, to upload a file called `myfile.txt` in the Service Desk with ID 10001 use
 *
 *     curl -D- -u customer:customer -X POST -H "X-ExperimentalApi: opt-in" -H "X-Atlassian-Token: no-check" -F "file=@myfile.txt" https://your-domain.atlassian.net/rest/servicedeskapi/servicedesk/10001/attachTemporaryFile
 *
 * **[Permissions](#permissions) required**: Permission to add attachments in this Service Desk.
 *
 * @param params.serviceDeskId - The ID of the Service Desk to which the file will be attached. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returns if the file(s) were attached.
 */
export function attachTemporaryFile(
  ctx: ClientContext,
  params: {
    /** The ID of the Service Desk to which the file will be attached. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    body: MultipartFile[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { serviceDeskId, body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/attachTemporaryFile",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    bodyType: "form-data",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get customers
 *
 * This method returns a list of the customers on a service desk.
 *
 * The returned list of customers can be filtered using the `query` parameter. The parameter is matched against customers' `displayName`, `name`, or `email`. For example, searching for "John", "Jo", "Smi", or "Smith" will match a user with display name "John Smith".
 *
 * **[Permissions](#permissions) required**: Permission to view this Service Desk's customers.
 *
 * @param params.serviceDeskId - The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.query - The string used to filter the customer list.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of users to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the service desk's customer list.
 */
export function getCustomers(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /** The string used to filter the customer list. */
    query?: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of users to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoUserDto> {
  const { serviceDeskId, query, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/customer",
    pathParams: { serviceDeskId },
    query: { query, start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add customers
 *
 * Adds one or more customers to a service desk. If any of the passed customers are associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
 *
 * **[Permissions](#permissions) required**: Service desk administrator
 *
 * @param params.serviceDeskId - The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returned if all the customers were added to the service desk or were already associated with the service desk.
 */
export function addCustomers(
  ctx: ClientContext,
  params: ServiceDeskCustomerDto & {
    /** The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { serviceDeskId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/customer",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove customers
 *
 * This method removes one or more customers from a service desk. The service desk must have closed access. If any of the passed customers are not associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
 *
 * **[Permissions](#permissions) required**: Services desk administrator
 *
 * @param params.serviceDeskId - The ID of the service desk the customers should be removed from. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returned if the customers were removed from the service desk, or any of the customers were not associated with the service desk.
 */
export function removeCustomers(
  ctx: ClientContext,
  params: ServiceDeskCustomerDto & {
    /** The ID of the service desk the customers should be removed from. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { serviceDeskId, ...body } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/customer",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Invite customer
 *
 * This method invites a customer to a specified service desk by sending them an email invitation, creating a new customer account if one does not already exist. The display name does not need to be unique. The record's identifiers, `name` and `key`, are automatically generated from the request details.
 *
 * **[Permissions](#permissions) required**: Jira Administrator Global permission & Service desk administrator
 *
 * @param params.serviceDeskId - The ID of the service desk to which the newly created customer should be added.
 * @param params.strictConflictStatusCode - Optional boolean flag to return 409 Conflict status code when a customer with the same email already exists.
 * @returns Returns the customer details.
 */
export function inviteCustomer(
  ctx: ClientContext,
  params: ServiceDeskCustomerInviteDto & {
    /** The ID of the service desk to which the newly created customer should be added. */
    serviceDeskId: string;
    /** Optional boolean flag to return 409 Conflict status code when a customer with the same email already exists. */
    strictConflictStatusCode?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { serviceDeskId, strictConflictStatusCode, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/customer/invite",
    pathParams: { serviceDeskId },
    query: { strictConflictStatusCode },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get articles
 *
 * Returns articles which match the given query and belong to the knowledge base linked to the service desk.
 *
 * **[Permissions](#permissions) required**: Permission to access the service desk.
 *
 * @param params.query - The string used to filter the articles (required).
 * @param params.highlight - If set to true matching query term in the title and excerpt will be highlighted using the `@@@hl@@@term@@@endhl@@@` syntax. Default: false.
 * @param params.start - (Deprecated) The starting index of the returned objects. Base index: 0.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the section for more details.
 * @param params.cursor - Pointer to a set of search results, returned as part of the next or prev URL from the previous search call.
 * @param params.prev - Should navigate to the previous page. Defaulted to false. Set to true as part of prev URL from the previous search call.
 * @returns Returns the articles, on the specified page of the results.
 */
export function getArticles(
  ctx: ClientContext,
  params: {
    serviceDeskId: string;
    /** The string used to filter the articles (required). */
    query: string;
    /**
     * If set to true matching query term in the title and excerpt will be highlighted using the `@@@hl@@@term@@@endhl@@@` syntax. Default: false.
     *
     * @default false
     */
    highlight?: boolean;
    /**
     * (Deprecated) The starting index of the returned objects. Base index: 0.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the section for more details.
     *
     * @format int32
     */
    limit?: number;
    /** Pointer to a set of search results, returned as part of the next or prev URL from the previous search call. */
    cursor?: string;
    /**
     * Should navigate to the previous page. Defaulted to false. Set to true as part of prev URL from the previous search call.
     *
     * @default false
     */
    prev?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoArticleDto> {
  const { serviceDeskId, query, highlight, start, limit, cursor, prev } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/knowledgebase/article",
    pathParams: { serviceDeskId },
    query: { query, highlight, start, limit, cursor, prev },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get queues
 *
 * This method returns the queues in a service desk. To include a customer request count for each queue (in the `issueCount` field) in the response, set the query parameter `includeCount` to true (its default is false).
 *
 * **[Permissions](#permissions) required**: service desk's Agent.
 *
 * @param params.serviceDeskId - ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.includeCount - Specifies whether to include each queue's customer request (issue) count in the response.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the queues of the service desk, on the specified page of the results.
 */
export function getQueues(
  ctx: ClientContext,
  params: {
    /** ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * Specifies whether to include each queue's customer request (issue) count in the response.
     *
     * @default false
     */
    includeCount?: boolean;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoQueueDto> {
  const { serviceDeskId, includeCount, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/queue",
    pathParams: { serviceDeskId },
    query: { includeCount, start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get queue
 *
 * This method returns a specific queues in a service desk. To include a customer request count for the queue (in the `issueCount` field) in the response, set the query parameter `includeCount` to true (its default is false).
 *
 * **[Permissions](#permissions) required**: service desk's Agent.
 *
 * @param params.serviceDeskId - ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.queueId - ID of the required queue.
 * @param params.includeCount - Specifies whether to include each queue's customer request (issue) count in the response.
 * @returns Returns the specific queue of the service desk.
 */
export function getQueue(
  ctx: ClientContext,
  params: {
    /** ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * ID of the required queue.
     *
     * @format int64
     */
    queueId: number;
    /**
     * Specifies whether to include each queue's customer request (issue) count in the response.
     *
     * @default false
     */
    includeCount?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<QueueDto> {
  const { serviceDeskId, queueId, includeCount } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/queue/{queueId}",
    pathParams: { serviceDeskId, queueId },
    query: { includeCount },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issues in queue
 *
 * This method returns the customer requests in a queue. Only fields that the queue is configured to show are returned. For example, if a queue is configured to show description and due date, then only those two fields are returned for each customer request in the queue.
 *
 * **[Permissions](#permissions) required**: Service desk's agent.
 *
 * @param params.serviceDeskId - The ID of the service desk containing the queue to be queried. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.queueId - The ID of the queue whose customer requests will be returned.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the customer requests belonging to the queue, on the specified page of the results.
 */
export function getIssuesInQueue(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk containing the queue to be queried. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The ID of the queue whose customer requests will be returned.
     *
     * @format int64
     */
    queueId: number;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoIssueBean> {
  const { serviceDeskId, queueId, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/queue/{queueId}/issue",
    pathParams: { serviceDeskId, queueId },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request types
 *
 * This method returns all customer request types from a service desk. There are two parameters for filtering the returned list:
 *
 *  *  `groupId` which filters the results to items in the customer request type group.
 *  *  `searchQuery` which is matched against request types' `name` or `description`. For example, the strings "Install", "Inst", "Equi", or "Equipment" will match a request type with the *name* "Equipment Installation Request".
 *
 * **Note:** This API by default will filter out request types hidden in the portal (i.e. request types without groups and request types where a user doesn't have permission) when `searchQuery` is provided, unless `includeHiddenRequestTypesInSearch` is set to true. Restricted request types will not be returned for those who aren't admins.
 *
 * **[Permissions](#permissions) required**: Permission to access the service desk.
 *
 * @param params.serviceDeskId - The ID of the service desk whose customer request types are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.groupId - Filters results to those in a customer request type group.
 * @param params.searchQuery - The string to be used to filter the results.
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @param params.includeHiddenRequestTypesInSearch - Whether to include hidden request types when searching with `searchQuery`.
 * @param params.restrictionStatus - Request type restriction status (`open` or `restricted`) used to filter the results.
 * @returns Returns the requested customer request types, on the specified page of the results.
 */
export function getRequestTypes(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk whose customer request types are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * Filters results to those in a customer request type group.
     *
     * @format int32
     */
    groupId?: number;
    expand?: string[];
    /** The string to be used to filter the results. */
    searchQuery?: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
    /**
     * Whether to include hidden request types when searching with `searchQuery`.
     *
     * @default false
     */
    includeHiddenRequestTypesInSearch?: boolean;
    /** Request type restriction status (`open` or `restricted`) used to filter the results. */
    restrictionStatus?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoRequestTypeDto> {
  const {
    serviceDeskId,
    groupId,
    expand,
    searchQuery,
    start,
    limit,
    includeHiddenRequestTypesInSearch,
    restrictionStatus,
  } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype",
    pathParams: { serviceDeskId },
    query: {
      groupId,
      expand,
      searchQuery,
      start,
      limit,
      includeHiddenRequestTypesInSearch,
      restrictionStatus,
    },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create request type
 *
 * This method enables a customer request type to be added to a service desk based on an issue type. Note that not all customer request type fields can be specified in the request and these fields are given the following default values:
 *
 *  *  Request type icon is given the headset icon.
 *  *  Request type groups is left empty, which means this customer request type will not be visible on the [customer portal](https://confluence.atlassian.com/servicedeskcloud/configuring-the-customer-portal-732528918.html).
 *  *  Request type status mapping is left empty, so the request type has no custom status mapping but inherits the status map from the issue type upon which it is based.
 *  *  Request type field mapping is set to show the required fields as specified by the issue type used to create the customer request type.
 *
 *
 * These fields can be updated by a service desk administrator using the **Request types** option in **Project settings**.
 * Request Types are created in next-gen projects by creating Issue Types. Please use the Jira Cloud Platform Create issue type endpoint instead.
 *
 * **[Permissions](#permissions) required**: Service desk's administrator
 *
 * @param params.serviceDeskId - The ID of the service desk where the customer request type is to be created. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returns the customer request type created.
 */
export function createRequestType(
  ctx: ClientContext,
  params: RequestTypeCreateDto & {
    /** The ID of the service desk where the customer request type is to be created. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RequestTypeDto> {
  const { serviceDeskId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Check request type permissions
 *
 * Returns:
 *
 *  *  a list of request type IDs where the given user has permission to administer.
 *  *  a list of request type IDs where the given user has permission to submit the request.
 *
 * If no account ID is provided, the operation returns details for the logged in user.
 *
 * Note that:
 *
 *  *  invalid request type IDs are ignored.
 *  *  a maximum of 50 request types can be checked.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* or *Project Administrator* to check the permissions for other users.
 *
 * However, Connect apps can make a call from the app server to the product to obtain permission details for any user, without admin permission. This Connect app ability doesn't apply to calls made using AP.request() in a browser.
 *
 * @returns Returned if the request is successful.
 */
export function checkRequestTypePermissions(
  ctx: ClientContext,
  params: RequestTypePermissionCheckRequestDto & {
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RequestTypePermissionCheckResponse> {
  const { serviceDeskId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/permissions/check",
    pathParams: { serviceDeskId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request type by id
 *
 * This method returns a customer request type from a service desk.
 *
 * This operation can be accessed anonymously.
 *
 * **[Permissions](#permissions) required**: Permission to access the service desk.
 *
 * @param params.serviceDeskId - The ID of the service desk whose customer request type is to be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.requestTypeId - The ID of the customer request type to be returned.
 * @returns Returns the customer request type item.
 */
export function getRequestTypeById(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk whose customer request type is to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /** The ID of the customer request type to be returned. */
    requestTypeId: string;
    expand?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RequestTypeDto> {
  const { serviceDeskId, requestTypeId, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}",
    pathParams: { serviceDeskId, requestTypeId },
    query: { expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete request type
 *
 * This method deletes a customer request type from a service desk, and removes it from all customer requests.
 * This only supports classic projects.
 *
 * **[Permissions](#permissions) required**: Service desk administrator.
 *
 * @param params.serviceDeskId - The ID or [project identifier](#project-identifiers) of the service desk.
 * @param params.requestTypeId - The ID of the request type.
 * @returns Returned if the request type is deleted.
 */
export function deleteRequestType(
  ctx: ClientContext,
  params: {
    /** The ID or [project identifier](#project-identifiers) of the service desk. */
    serviceDeskId: string;
    /**
     * The ID of the request type.
     *
     * @format int32
     */
    requestTypeId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { serviceDeskId, requestTypeId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}",
    pathParams: { serviceDeskId, requestTypeId },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request type fields
 *
 * This method returns the fields for a service desk's customer request type.
 *
 * Also, the following information about the user's permissions for the request type is returned:
 *
 *  *  `canRaiseOnBehalfOf` returns `true` if the user has permission to raise customer requests on behalf of other customers. Otherwise, returns `false`.
 *  *  `canAddRequestParticipants` returns `true` if the user can add customer request participants. Otherwise, returns `false`.
 *
 * **[Permissions](#permissions) required**: Permission to view the Service Desk. However, hidden fields would be visible to only Service desk's Administrator.
 *
 * @param params.serviceDeskId - The ID of the service desk containing the request types whose fields are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.requestTypeId - The ID of the request types whose fields are to be returned.
 * @param params.expand - Use [expand](#expansion) to include additional information in the response. This parameter accepts `hiddenFields` that returns hidden fields associated with the request type.
 * @returns Returns the request type's fields and user permission details, on the specified page of the results.
 */
export function getRequestTypeFields(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk containing the request types whose fields are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The ID of the request types whose fields are to be returned.
     *
     * @format int32
     */
    requestTypeId: number;
    /** Use [expand](#expansion) to include additional information in the response. This parameter accepts `hiddenFields` that returns hidden fields associated with the request type. */
    expand?: string[];
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<CustomerRequestCreateMetaDto> {
  const { serviceDeskId, requestTypeId, expand } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}/field",
    pathParams: { serviceDeskId, requestTypeId },
    query: { expand },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get properties keys
 *
 * Returns the keys of all properties for a request type.
 *
 * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore the keys of all properties for a request type are also available by calling the Jira Cloud Platform [Get issue type property keys](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-get) endpoint.
 *
 * **[Permissions](#permissions) required**: The user must have permission to view the request type.
 *
 * @param params.requestTypeId - The ID of the request type for which keys will be retrieved.
 * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
 * @returns Returned if the request type was found.
 */
export function getPropertiesKeys(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the request type for which keys will be retrieved.
     *
     * @format int32
     */
    requestTypeId: number;
    /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PropertyKeys> {
  const { requestTypeId, serviceDeskId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}/property",
    pathParams: { requestTypeId, serviceDeskId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get property
 *
 * Returns the value of the property from a request type.
 *
 * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore also available by calling the Jira Cloud Platform [Get issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-get) endpoint.
 *
 * **[Permissions](#permissions) required**: User must have permission to view the request type.
 *
 * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.requestTypeId - The ID of the request type from which the property will be retrieved.
 * @param params.propertyKey - The key of the property to return.
 * @returns Returned if the request type property was returned.
 */
export function getProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The ID of the request type from which the property will be retrieved.
     *
     * @format int32
     */
    requestTypeId: number;
    /** The key of the property to return. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<EntityProperty> {
  const { serviceDeskId, requestTypeId, propertyKey } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}/property/{propertyKey}",
    pathParams: { serviceDeskId, requestTypeId, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set property
 *
 * Sets the value of a request type property. Use this resource to store custom data against a request type.
 *
 * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore can also be set by calling the Jira Cloud Platform [Set issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-put) endpoint.
 *
 * **[Permissions](#permissions) required**: Jira project administrator with a Jira Service Management agent license.
 *
 * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.requestTypeId - The ID of the request type on which the property will be set.
 * @param params.propertyKey - The key of the request type property. The maximum length of the key is 255 bytes.
 * @returns Returned if the request type property is updated.
 */
export function setProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The ID of the request type on which the property will be set.
     *
     * @format int32
     */
    requestTypeId: number;
    /** The key of the request type property. The maximum length of the key is 255 bytes. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { serviceDeskId, requestTypeId, propertyKey } = params;
  return ctx.request({
    method: "put",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}/property/{propertyKey}",
    pathParams: { serviceDeskId, requestTypeId, propertyKey },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete property
 *
 * Removes a property from a request type.
 *
 * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore can also be deleted by calling the Jira Cloud Platform [Delete issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-delete) endpoint.
 *
 * **[Permissions](#permissions) required**: Jira project administrator with a Jira Service Management agent license.
 *
 * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.requestTypeId - The ID of the request type for which the property will be removed.
 * @param params.propertyKey - The key of the property to remove.
 * @returns Returned if the request type property was removed.
 */
export function deleteProperty(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The ID of the request type for which the property will be removed.
     *
     * @format int32
     */
    requestTypeId: number;
    /** The key of the property to remove. */
    propertyKey: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { serviceDeskId, requestTypeId, propertyKey } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttype/{requestTypeId}/property/{propertyKey}",
    pathParams: { serviceDeskId, requestTypeId, propertyKey },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get request type groups
 *
 * This method returns a service desk's customer request type groups. Jira Service Management administrators can arrange the customer request type groups in an arbitrary order for display on the customer portal; the groups are returned in this order.
 *
 * **[Permissions](#permissions) required**: Permission to view the service desk.
 *
 * @param params.serviceDeskId - The ID of the service desk whose customer request type groups are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
 * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
 * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
 * @returns Returns the service desk's customer request type groups, on the specified page of the results.
 */
export function getRequestTypeGroups(
  ctx: ClientContext,
  params: {
    /** The ID of the service desk whose customer request type groups are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
    serviceDeskId: string;
    /**
     * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    start?: number;
    /**
     * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     *
     * @format int32
     */
    limit?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PagedDtoRequestTypeGroupDto> {
  const { serviceDeskId, start, limit } = params;
  return ctx.request({
    method: "get",
    path: "/rest/servicedeskapi/servicedesk/{serviceDeskId}/requesttypegroup",
    pathParams: { serviceDeskId },
    query: { start, limit },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** Operations of the `servicedesk` service. */
export function createServicedeskService(ctx: ClientContext) {
  return {
    /**
     * Get service desks
     *
     * This method returns all the service desks in the Jira Service Management instance that the user has permission to access. Use this method where you need a list of service desks or need to locate a service desk by name or keyword.
     *
     * **Note:** This method will be slow if the instance has hundreds of service desks. If you want to fetch a single service desk by its ID, use [/rest/servicedeskapi/servicedesk/\{serviceDeskId\}](./#api-rest-servicedeskapi-servicedesk-servicedeskid-get) instead.
     *
     * **[Permissions](#permissions) required**: Any
     *
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the service desks, on the specified page of the results.
     */
    getServiceDesks(
      params?: {
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoServiceDeskDto> {
      return getServiceDesks(ctx, params, options);
    },

    /**
     * Get service desk by id
     *
     * This method returns a service desk. Use this method to get service desk details whenever your application component is passed a service desk ID but needs to display other service desk details.
     *
     * **[Permissions](#permissions) required**: Permission to access the Service Desk. For example, being the Service Desk's Administrator or one of its Agents or Users.
     *
     * @param params.serviceDeskId - The ID of the service desk to return. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returns the requested service desk.
     */
    getServiceDeskById(
      params: {
        /** The ID of the service desk to return. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ServiceDeskDto> {
      return getServiceDeskById(ctx, params, options);
    },

    /**
     * Attach temporary file
     *
     * This method adds one or more temporary attachments to a service desk, which can then be permanently attached to a customer request using [servicedeskapi/request/\{issueIdOrKey\}/attachment](#api-request-issueIdOrKey-attachment-post).
     *
     * **Note**: It is possible for a service desk administrator to turn off the ability to add attachments to a service desk.
     *
     * This method expects a multipart request. The media-type multipart/form-data is defined in RFC 1867. Most client libraries have classes that make dealing with multipart posts simple. For instance, in Java the Apache HTTP Components library provides [MultiPartEntity](http://hc.apache.org/httpcomponents-client-ga/httpmime/apidocs/org/apache/http/entity/mime/MultipartEntity.html).
     *
     * Because this method accepts multipart/form-data, it has XSRF protection on it. This means you must submit a header of X-Atlassian-Token: no-check with the request or it will be blocked.
     *
     * The name of the multipart/form-data parameter that contains the attachments must be `file`.
     *
     * For example, to upload a file called `myfile.txt` in the Service Desk with ID 10001 use
     *
     *     curl -D- -u customer:customer -X POST -H "X-ExperimentalApi: opt-in" -H "X-Atlassian-Token: no-check" -F "file=@myfile.txt" https://your-domain.atlassian.net/rest/servicedeskapi/servicedesk/10001/attachTemporaryFile
     *
     * **[Permissions](#permissions) required**: Permission to add attachments in this Service Desk.
     *
     * @param params.serviceDeskId - The ID of the Service Desk to which the file will be attached. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returns if the file(s) were attached.
     */
    attachTemporaryFile(
      params: {
        /** The ID of the Service Desk to which the file will be attached. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        body: MultipartFile[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return attachTemporaryFile(ctx, params, options);
    },

    /**
     * Get customers
     *
     * This method returns a list of the customers on a service desk.
     *
     * The returned list of customers can be filtered using the `query` parameter. The parameter is matched against customers' `displayName`, `name`, or `email`. For example, searching for "John", "Jo", "Smi", or "Smith" will match a user with display name "John Smith".
     *
     * **[Permissions](#permissions) required**: Permission to view this Service Desk's customers.
     *
     * @param params.serviceDeskId - The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.query - The string used to filter the customer list.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of users to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the service desk's customer list.
     */
    getCustomers(
      params: {
        /** The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /** The string used to filter the customer list. */
        query?: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of users to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoUserDto> {
      return getCustomers(ctx, params, options);
    },

    /**
     * Add customers
     *
     * Adds one or more customers to a service desk. If any of the passed customers are associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
     *
     * **[Permissions](#permissions) required**: Service desk administrator
     *
     * @param params.serviceDeskId - The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returned if all the customers were added to the service desk or were already associated with the service desk.
     */
    addCustomers(
      params: ServiceDeskCustomerDto & {
        /** The ID of the service desk the customer list should be returned from. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return addCustomers(ctx, params, options);
    },

    /**
     * Remove customers
     *
     * This method removes one or more customers from a service desk. The service desk must have closed access. If any of the passed customers are not associated with the service desk, no changes will be made for those customers and the resource returns a 204 success code.
     *
     * **[Permissions](#permissions) required**: Services desk administrator
     *
     * @param params.serviceDeskId - The ID of the service desk the customers should be removed from. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returned if the customers were removed from the service desk, or any of the customers were not associated with the service desk.
     */
    removeCustomers(
      params: ServiceDeskCustomerDto & {
        /** The ID of the service desk the customers should be removed from. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return removeCustomers(ctx, params, options);
    },

    /**
     * Invite customer
     *
     * This method invites a customer to a specified service desk by sending them an email invitation, creating a new customer account if one does not already exist. The display name does not need to be unique. The record's identifiers, `name` and `key`, are automatically generated from the request details.
     *
     * **[Permissions](#permissions) required**: Jira Administrator Global permission & Service desk administrator
     *
     * @param params.serviceDeskId - The ID of the service desk to which the newly created customer should be added.
     * @param params.strictConflictStatusCode - Optional boolean flag to return 409 Conflict status code when a customer with the same email already exists.
     * @returns Returns the customer details.
     */
    inviteCustomer(
      params: ServiceDeskCustomerInviteDto & {
        /** The ID of the service desk to which the newly created customer should be added. */
        serviceDeskId: string;
        /** Optional boolean flag to return 409 Conflict status code when a customer with the same email already exists. */
        strictConflictStatusCode?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return inviteCustomer(ctx, params, options);
    },

    /**
     * Get articles
     *
     * Returns articles which match the given query and belong to the knowledge base linked to the service desk.
     *
     * **[Permissions](#permissions) required**: Permission to access the service desk.
     *
     * @param params.query - The string used to filter the articles (required).
     * @param params.highlight - If set to true matching query term in the title and excerpt will be highlighted using the `@@@hl@@@term@@@endhl@@@` syntax. Default: false.
     * @param params.start - (Deprecated) The starting index of the returned objects. Base index: 0.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the section for more details.
     * @param params.cursor - Pointer to a set of search results, returned as part of the next or prev URL from the previous search call.
     * @param params.prev - Should navigate to the previous page. Defaulted to false. Set to true as part of prev URL from the previous search call.
     * @returns Returns the articles, on the specified page of the results.
     */
    getArticles(
      params: {
        serviceDeskId: string;
        /** The string used to filter the articles (required). */
        query: string;
        /**
         * If set to true matching query term in the title and excerpt will be highlighted using the `@@@hl@@@term@@@endhl@@@` syntax. Default: false.
         *
         * @default false
         */
        highlight?: boolean;
        /**
         * (Deprecated) The starting index of the returned objects. Base index: 0.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the section for more details.
         *
         * @format int32
         */
        limit?: number;
        /** Pointer to a set of search results, returned as part of the next or prev URL from the previous search call. */
        cursor?: string;
        /**
         * Should navigate to the previous page. Defaulted to false. Set to true as part of prev URL from the previous search call.
         *
         * @default false
         */
        prev?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoArticleDto> {
      return getArticles(ctx, params, options);
    },

    /**
     * Get queues
     *
     * This method returns the queues in a service desk. To include a customer request count for each queue (in the `issueCount` field) in the response, set the query parameter `includeCount` to true (its default is false).
     *
     * **[Permissions](#permissions) required**: service desk's Agent.
     *
     * @param params.serviceDeskId - ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.includeCount - Specifies whether to include each queue's customer request (issue) count in the response.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the queues of the service desk, on the specified page of the results.
     */
    getQueues(
      params: {
        /** ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * Specifies whether to include each queue's customer request (issue) count in the response.
         *
         * @default false
         */
        includeCount?: boolean;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoQueueDto> {
      return getQueues(ctx, params, options);
    },

    /**
     * Get queue
     *
     * This method returns a specific queues in a service desk. To include a customer request count for the queue (in the `issueCount` field) in the response, set the query parameter `includeCount` to true (its default is false).
     *
     * **[Permissions](#permissions) required**: service desk's Agent.
     *
     * @param params.serviceDeskId - ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.queueId - ID of the required queue.
     * @param params.includeCount - Specifies whether to include each queue's customer request (issue) count in the response.
     * @returns Returns the specific queue of the service desk.
     */
    getQueue(
      params: {
        /** ID of the service desk whose queues will be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * ID of the required queue.
         *
         * @format int64
         */
        queueId: number;
        /**
         * Specifies whether to include each queue's customer request (issue) count in the response.
         *
         * @default false
         */
        includeCount?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<QueueDto> {
      return getQueue(ctx, params, options);
    },

    /**
     * Get issues in queue
     *
     * This method returns the customer requests in a queue. Only fields that the queue is configured to show are returned. For example, if a queue is configured to show description and due date, then only those two fields are returned for each customer request in the queue.
     *
     * **[Permissions](#permissions) required**: Service desk's agent.
     *
     * @param params.serviceDeskId - The ID of the service desk containing the queue to be queried. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.queueId - The ID of the queue whose customer requests will be returned.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the customer requests belonging to the queue, on the specified page of the results.
     */
    getIssuesInQueue(
      params: {
        /** The ID of the service desk containing the queue to be queried. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The ID of the queue whose customer requests will be returned.
         *
         * @format int64
         */
        queueId: number;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoIssueBean> {
      return getIssuesInQueue(ctx, params, options);
    },

    /**
     * Get request types
     *
     * This method returns all customer request types from a service desk. There are two parameters for filtering the returned list:
     *
     *  *  `groupId` which filters the results to items in the customer request type group.
     *  *  `searchQuery` which is matched against request types' `name` or `description`. For example, the strings "Install", "Inst", "Equi", or "Equipment" will match a request type with the *name* "Equipment Installation Request".
     *
     * **Note:** This API by default will filter out request types hidden in the portal (i.e. request types without groups and request types where a user doesn't have permission) when `searchQuery` is provided, unless `includeHiddenRequestTypesInSearch` is set to true. Restricted request types will not be returned for those who aren't admins.
     *
     * **[Permissions](#permissions) required**: Permission to access the service desk.
     *
     * @param params.serviceDeskId - The ID of the service desk whose customer request types are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.groupId - Filters results to those in a customer request type group.
     * @param params.searchQuery - The string to be used to filter the results.
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @param params.includeHiddenRequestTypesInSearch - Whether to include hidden request types when searching with `searchQuery`.
     * @param params.restrictionStatus - Request type restriction status (`open` or `restricted`) used to filter the results.
     * @returns Returns the requested customer request types, on the specified page of the results.
     */
    getRequestTypes(
      params: {
        /** The ID of the service desk whose customer request types are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * Filters results to those in a customer request type group.
         *
         * @format int32
         */
        groupId?: number;
        expand?: string[];
        /** The string to be used to filter the results. */
        searchQuery?: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
        /**
         * Whether to include hidden request types when searching with `searchQuery`.
         *
         * @default false
         */
        includeHiddenRequestTypesInSearch?: boolean;
        /** Request type restriction status (`open` or `restricted`) used to filter the results. */
        restrictionStatus?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoRequestTypeDto> {
      return getRequestTypes(ctx, params, options);
    },

    /**
     * Create request type
     *
     * This method enables a customer request type to be added to a service desk based on an issue type. Note that not all customer request type fields can be specified in the request and these fields are given the following default values:
     *
     *  *  Request type icon is given the headset icon.
     *  *  Request type groups is left empty, which means this customer request type will not be visible on the [customer portal](https://confluence.atlassian.com/servicedeskcloud/configuring-the-customer-portal-732528918.html).
     *  *  Request type status mapping is left empty, so the request type has no custom status mapping but inherits the status map from the issue type upon which it is based.
     *  *  Request type field mapping is set to show the required fields as specified by the issue type used to create the customer request type.
     *
     *
     * These fields can be updated by a service desk administrator using the **Request types** option in **Project settings**.
     * Request Types are created in next-gen projects by creating Issue Types. Please use the Jira Cloud Platform Create issue type endpoint instead.
     *
     * **[Permissions](#permissions) required**: Service desk's administrator
     *
     * @param params.serviceDeskId - The ID of the service desk where the customer request type is to be created. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returns the customer request type created.
     */
    createRequestType(
      params: RequestTypeCreateDto & {
        /** The ID of the service desk where the customer request type is to be created. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RequestTypeDto> {
      return createRequestType(ctx, params, options);
    },

    /**
     * Check request type permissions
     *
     * Returns:
     *
     *  *  a list of request type IDs where the given user has permission to administer.
     *  *  a list of request type IDs where the given user has permission to submit the request.
     *
     * If no account ID is provided, the operation returns details for the logged in user.
     *
     * Note that:
     *
     *  *  invalid request type IDs are ignored.
     *  *  a maximum of 50 request types can be checked.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* or *Project Administrator* to check the permissions for other users.
     *
     * However, Connect apps can make a call from the app server to the product to obtain permission details for any user, without admin permission. This Connect app ability doesn't apply to calls made using AP.request() in a browser.
     *
     * @returns Returned if the request is successful.
     */
    checkRequestTypePermissions(
      params: RequestTypePermissionCheckRequestDto & {
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RequestTypePermissionCheckResponse> {
      return checkRequestTypePermissions(ctx, params, options);
    },

    /**
     * Get request type by id
     *
     * This method returns a customer request type from a service desk.
     *
     * This operation can be accessed anonymously.
     *
     * **[Permissions](#permissions) required**: Permission to access the service desk.
     *
     * @param params.serviceDeskId - The ID of the service desk whose customer request type is to be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.requestTypeId - The ID of the customer request type to be returned.
     * @returns Returns the customer request type item.
     */
    getRequestTypeById(
      params: {
        /** The ID of the service desk whose customer request type is to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /** The ID of the customer request type to be returned. */
        requestTypeId: string;
        expand?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RequestTypeDto> {
      return getRequestTypeById(ctx, params, options);
    },

    /**
     * Delete request type
     *
     * This method deletes a customer request type from a service desk, and removes it from all customer requests.
     * This only supports classic projects.
     *
     * **[Permissions](#permissions) required**: Service desk administrator.
     *
     * @param params.serviceDeskId - The ID or [project identifier](#project-identifiers) of the service desk.
     * @param params.requestTypeId - The ID of the request type.
     * @returns Returned if the request type is deleted.
     */
    deleteRequestType(
      params: {
        /** The ID or [project identifier](#project-identifiers) of the service desk. */
        serviceDeskId: string;
        /**
         * The ID of the request type.
         *
         * @format int32
         */
        requestTypeId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteRequestType(ctx, params, options);
    },

    /**
     * Get request type fields
     *
     * This method returns the fields for a service desk's customer request type.
     *
     * Also, the following information about the user's permissions for the request type is returned:
     *
     *  *  `canRaiseOnBehalfOf` returns `true` if the user has permission to raise customer requests on behalf of other customers. Otherwise, returns `false`.
     *  *  `canAddRequestParticipants` returns `true` if the user can add customer request participants. Otherwise, returns `false`.
     *
     * **[Permissions](#permissions) required**: Permission to view the Service Desk. However, hidden fields would be visible to only Service desk's Administrator.
     *
     * @param params.serviceDeskId - The ID of the service desk containing the request types whose fields are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.requestTypeId - The ID of the request types whose fields are to be returned.
     * @param params.expand - Use [expand](#expansion) to include additional information in the response. This parameter accepts `hiddenFields` that returns hidden fields associated with the request type.
     * @returns Returns the request type's fields and user permission details, on the specified page of the results.
     */
    getRequestTypeFields(
      params: {
        /** The ID of the service desk containing the request types whose fields are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The ID of the request types whose fields are to be returned.
         *
         * @format int32
         */
        requestTypeId: number;
        /** Use [expand](#expansion) to include additional information in the response. This parameter accepts `hiddenFields` that returns hidden fields associated with the request type. */
        expand?: string[];
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<CustomerRequestCreateMetaDto> {
      return getRequestTypeFields(ctx, params, options);
    },

    /**
     * Get properties keys
     *
     * Returns the keys of all properties for a request type.
     *
     * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore the keys of all properties for a request type are also available by calling the Jira Cloud Platform [Get issue type property keys](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-get) endpoint.
     *
     * **[Permissions](#permissions) required**: The user must have permission to view the request type.
     *
     * @param params.requestTypeId - The ID of the request type for which keys will be retrieved.
     * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
     * @returns Returned if the request type was found.
     */
    getPropertiesKeys(
      params: {
        /**
         * The ID of the request type for which keys will be retrieved.
         *
         * @format int32
         */
        requestTypeId: number;
        /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PropertyKeys> {
      return getPropertiesKeys(ctx, params, options);
    },

    /**
     * Get property
     *
     * Returns the value of the property from a request type.
     *
     * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore also available by calling the Jira Cloud Platform [Get issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-get) endpoint.
     *
     * **[Permissions](#permissions) required**: User must have permission to view the request type.
     *
     * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.requestTypeId - The ID of the request type from which the property will be retrieved.
     * @param params.propertyKey - The key of the property to return.
     * @returns Returned if the request type property was returned.
     */
    getProperty(
      params: {
        /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The ID of the request type from which the property will be retrieved.
         *
         * @format int32
         */
        requestTypeId: number;
        /** The key of the property to return. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<EntityProperty> {
      return getProperty(ctx, params, options);
    },

    /**
     * Set property
     *
     * Sets the value of a request type property. Use this resource to store custom data against a request type.
     *
     * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore can also be set by calling the Jira Cloud Platform [Set issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-put) endpoint.
     *
     * **[Permissions](#permissions) required**: Jira project administrator with a Jira Service Management agent license.
     *
     * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.requestTypeId - The ID of the request type on which the property will be set.
     * @param params.propertyKey - The key of the request type property. The maximum length of the key is 255 bytes.
     * @returns Returned if the request type property is updated.
     */
    setProperty(
      params: {
        /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The ID of the request type on which the property will be set.
         *
         * @format int32
         */
        requestTypeId: number;
        /** The key of the request type property. The maximum length of the key is 255 bytes. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return setProperty(ctx, params, options);
    },

    /**
     * Delete property
     *
     * Removes a property from a request type.
     *
     * Properties for a Request Type in next-gen are stored as Issue Type properties and therefore can also be deleted by calling the Jira Cloud Platform [Delete issue type property](https://developer.atlassian.com/cloud/jira/platform/rest/v3/#api-rest-api-3-issuetype-issueTypeId-properties-propertyKey-delete) endpoint.
     *
     * **[Permissions](#permissions) required**: Jira project administrator with a Jira Service Management agent license.
     *
     * @param params.serviceDeskId - The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.requestTypeId - The ID of the request type for which the property will be removed.
     * @param params.propertyKey - The key of the property to remove.
     * @returns Returned if the request type property was removed.
     */
    deleteProperty(
      params: {
        /** The ID of the service desk which contains the request type. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The ID of the request type for which the property will be removed.
         *
         * @format int32
         */
        requestTypeId: number;
        /** The key of the property to remove. */
        propertyKey: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteProperty(ctx, params, options);
    },

    /**
     * Get request type groups
     *
     * This method returns a service desk's customer request type groups. Jira Service Management administrators can arrange the customer request type groups in an arbitrary order for display on the customer portal; the groups are returned in this order.
     *
     * **[Permissions](#permissions) required**: Permission to view the service desk.
     *
     * @param params.serviceDeskId - The ID of the service desk whose customer request type groups are to be returned. This can alternatively be a [project identifier.](#project-identifiers)
     * @param params.start - The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
     * @param params.limit - The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
     * @returns Returns the service desk's customer request type groups, on the specified page of the results.
     */
    getRequestTypeGroups(
      params: {
        /** The ID of the service desk whose customer request type groups are to be returned. This can alternatively be a [project identifier.](#project-identifiers) */
        serviceDeskId: string;
        /**
         * The starting index of the returned objects. Base index: 0. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        start?: number;
        /**
         * The maximum number of items to return per page. Default: 50. See the [Pagination](#pagination) section for more details.
         *
         * @format int32
         */
        limit?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PagedDtoRequestTypeGroupDto> {
      return getRequestTypeGroups(ctx, params, options);
    },
  };
}
