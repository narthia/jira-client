// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { BulkRedactionRequest, RedactionJobStatusResponse } from "../types/index.ts";

/**
 * Redact
 *
 * Submit a job to redact issue field data. This will trigger the redaction of the data in the specified fields asynchronously.
 *
 * The redaction status can be polled using the job id.
 *
 * @returns Returned if the job submission is successful. The response contains the job id.
 */
export function redact(
  ctx: ClientContext,
  params: BulkRedactionRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<string> {
  return ctx.request({
    method: "post",
    path: "/rest/api/2/redact",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get redaction status
 *
 * Retrieves the current status of a redaction job ID.
 *
 * The jobStatus will be one of the following:
 *
 *  *  IN\_PROGRESS - The redaction job is currently in progress
 *  *  COMPLETED - The redaction job has completed successfully.
 *  *  PENDING - The redaction job has not started yet
 *
 * @param params.jobId - Redaction job id
 * @returns Returned if the job status is successfully retrieved.
 */
export function getRedactionStatus(
  ctx: ClientContext,
  params: {
    /** Redaction job id */
    jobId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<RedactionJobStatusResponse> {
  const { jobId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/redact/status/{jobId}",
    pathParams: { jobId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents Issue Redaction. Provides APIs to redact issue data. */
export function createIssueRedactionService(ctx: ClientContext) {
  return {
    /**
     * Redact
     *
     * Submit a job to redact issue field data. This will trigger the redaction of the data in the specified fields asynchronously.
     *
     * The redaction status can be polled using the job id.
     *
     * @returns Returned if the job submission is successful. The response contains the job id.
     */
    redact(
      params: BulkRedactionRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<string> {
      return redact(ctx, params, options);
    },

    /**
     * Get redaction status
     *
     * Retrieves the current status of a redaction job ID.
     *
     * The jobStatus will be one of the following:
     *
     *  *  IN\_PROGRESS - The redaction job is currently in progress
     *  *  COMPLETED - The redaction job has completed successfully.
     *  *  PENDING - The redaction job has not started yet
     *
     * @param params.jobId - Redaction job id
     * @returns Returned if the job status is successfully retrieved.
     */
    getRedactionStatus(
      params: {
        /** Redaction job id */
        jobId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<RedactionJobStatusResponse> {
      return getRedactionStatus(ctx, params, options);
    },
  };
}
