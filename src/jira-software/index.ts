// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientConfig, ClientContext, SdkConfig, Transport } from "./config.ts";
import { ApiError, createClient } from "./config.ts";
import { createBacklogService } from "./services/backlog.ts";
import { createBoardService } from "./services/board.ts";
import { createBuildsService } from "./services/builds.ts";
import { createDeploymentsService } from "./services/deployments.ts";
import { createDevOpsComponentsService } from "./services/dev-ops-components.ts";
import { createDevelopmentInformationService } from "./services/development-information.ts";
import { createEpicService } from "./services/epic.ts";
import { createFeatureFlagsService } from "./services/feature-flags.ts";
import { createIssueService } from "./services/issue.ts";
import { createOperationsService } from "./services/operations.ts";
import { createRemoteLinksService } from "./services/remote-links.ts";
import { createSecurityInformationService } from "./services/security-information.ts";
import { createSprintService } from "./services/sprint.ts";

export * from "./types/index.ts";
export { ApiError };
export type { ClientConfig, ClientContext, SdkConfig, Transport };

/**
 * Create a `Jira Software Cloud API` SDK client (API version 1001.0.0).
 *
 * Jira Software Cloud REST API documentation
 *
 * @param config - Transport plus cross-cutting options (headers, interceptors).
 */
export function createSoftwareSdk(config: SdkConfig) {
  const ctx = createClient(config);
  return {
    /** Apis related to the backlog */
    backlog: createBacklogService(ctx),
    /** Apis related to boards */
    board: createBoardService(ctx),
    /** Apis related to epics */
    epic: createEpicService(ctx),
    /** Apis related to issues */
    issue: createIssueService(ctx),
    /** Apis related to sprints */
    sprint: createSprintService(ctx),
    /**
     * APIs related to integrating development information (commits, branches and pull requests) with Jira.
     *
     * These APIs are available to Atlassian Connect apps and on-premise integrations using OAuth. Connect apps using these APIs must have the `jiraDevelopmentTool` module in the Connect app descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/development-tool/. For more details on integrating Jira Software Cloud with on-premises tools using OAuth 2.0 credentials, see https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/.
     *
     * These APIs are available to Forge apps with the `devops:developmentInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-development-info/.
     */
    developmentInformation: createDevelopmentInformationService(ctx),
    /**
     * APIs related to integrating feature flags with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraFeatureFlagInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/feature-flag/.
     *
     * These APIs are available to Forge apps with the `devops:featureFlagInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-feature-flag-info/.
     */
    featureFlags: createFeatureFlagsService(ctx),
    /**
     * APIs related to integrating deployment data with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraDeploymentInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/deployment/. They are also related to integrating Jira Software Cloud with on-premises tools using OAuth 2.0 credentials. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/.
     *
     * These APIs are available to Forge apps with the `devops:deploymentInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-deployment-info/.
     */
    deployments: createDeploymentsService(ctx),
    /**
     * APIs related to integrating build data with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraBuildInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/build/. They are also related to integrating Jira Software Cloud with on-premises tools using OAuth 2.0 credentials. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/.
     *
     * These APIs are available to Forge apps with the `devops:buildInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-build-info/.
     */
    builds: createBuildsService(ctx),
    /**
     * APIs related to integrating remote link data with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraRemoteLinkInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/remote-link/.
     *
     * These APIs are available to Forge apps with the `devops:remoteLinkInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-remote-link-info/.
     */
    remoteLinks: createRemoteLinksService(ctx),
    /**
     * Send security information to Jira Software and enable your teams to turn unplanned vulnerabilities into planned and tracked work.
     *
     * Security is everyone's responsibility, and the security feature in Jira lets you triage and track relevant vulnerabilities as a team. Discuss and prioritise issues, minimise errors and duplication, and plan security work to complete in your sprints.
     *
     * APIs related to integrating Security information with Jira Software are available to Atlassian Connect apps. To use these APIs you must have a security info provider module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/security-information/.
     *
     * These APIs are available to Forge apps with the `devops:securityInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-security-info/.
     */
    securityInformation: createSecurityInformationService(ctx),
    /**
     * APIs related to integrating Incident and Post-Incident Review (PIR) data with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraOperationsInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/operations-information/.
     */
    operations: createOperationsService(ctx),
    /**
     * APIs related to integrating Dev Ops Components affected by Incident data with Jira Software.
     *
     * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraDevOpsComponentProvider` in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/devops-components/.
     */
    devOpsComponents: createDevOpsComponentsService(ctx),
  };
}

export type CreateSoftwareSdkClient = ReturnType<typeof createSoftwareSdk>;
