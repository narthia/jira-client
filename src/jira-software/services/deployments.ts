// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  EntityAssociation,
  IssueIdOrKeysAssociation,
  ServiceIdOrKeysAssociation,
} from "../types/index.ts";

/**
 * Submit deployment data
 *
 * Update / insert deployment data.
 *
 * Deployments are identified by the combination of `pipelineId`, `environmentId` and `deploymentSequenceNumber`, and existing deployment data for the same deployment will be replaced if it exists and the `updateSequenceNumber` of existing data is less than the incoming data.
 *
 * Submissions are processed asynchronously. Submitted data will eventually be available in Jira. Most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The `getDeploymentByKey` operation can be used to confirm that data has been stored successfully (if needed).
 *
 * In the case of multiple deployments being submitted in one request, each is validated individually prior to submission. Details of which deployments failed submission (if any) are available in the response object.
 *
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Submission accepted. Each submitted deployment that is of a valid format will eventually be available in Jira. Details of which deployments were submitted and which failed submission (due to data format problems etc.) are available in the response object.
 */
export function submitDeployments(
  ctx: ClientContext,
  params: {
    /**
     * Properties assigned to deployment data that can then be used for delete / query operations.
     *
     * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
     *
     * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
     *
     * @example
     * ```json
     * {
     *   "accountId": "account-234",
     *   "projectId": "project-123"
     * }
     * ```
     */
    properties?: Record<string, string>;
    /**
     * A list of deployments to submit to Jira.
     *
     * Each deployment may be associated with one or more Jira issue keys, and will be associated with any properties included in this request.
     */
    deployments: {
      /**
       * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
       *
       * @format int64
       * @example
       * ```json
       * 100
       * ```
       */
      deploymentSequenceNumber: number;
      /**
       * A number used to apply an order to the updates to the deployment, as identified by the deploymentSequenceNumber, in the case of out-of-order receipt of update requests. It must be a monotonically increasing number. For example, epoch time could be one way to generate the updateSequenceNumber.
       *
       * @format int64
       * @example
       * ```json
       * 1
       * ```
       */
      updateSequenceNumber: number;
      /**
       * Deprecated. The Jira issue keys to associate the Deployment information with.
       * Should replace this field with the "associations" field to associate Deployment information with issueKeys or other types of associations.
       *
       * @deprecated
       */
      issueKeys?: string[];
      /** The entities to associate the Deployment information with. */
      associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation)[];
      /**
       * The human-readable name for the deployment. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Deployment number 16 of Data Depot"
       * ```
       */
      displayName: string;
      /**
       * A URL users can use to link to this deployment, in this environment.
       *
       * @format uri
       * @example
       * ```json
       * "http://mydeployer.com/project1/1111-222-333/prod-east"
       * ```
       */
      url: string;
      /**
       * A short description of the deployment
       *
       * @example
       * ```json
       * "The bits are being transferred"
       * ```
       */
      description: string;
      /**
       * The last-updated timestamp to present to the user as a summary of the state of the deployment.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * An (optional) additional label that may be displayed with deployment information. Can be used to display version information etc. for the deployment.
       *
       * @example
       * ```json
       * "Release 2018-01-20_08-47-bc2421a"
       * ```
       */
      label?: string;
      /**
       * The duration of the deployment (in seconds).
       *
       * @format int64
       * @example
       * ```json
       * 47
       * ```
       */
      duration?: number;
      /**
       * The state of the deployment
       *
       * @example
       * ```json
       * "in_progress"
       * ```
       */
      state:
        | "unknown"
        | "pending"
        | "in_progress"
        | "cancelled"
        | "failed"
        | "rolled_back"
        | "successful";
      /**
       * This object models the Continuous Delivery (CD) Pipeline concept, an automated process (usually comprised of multiple stages)
       *
       * for getting software from version control right through to the production environment.
       */
      pipeline: {
        /**
         * The identifier of this pipeline, must be unique for the provider.
         *
         * @example
         * ```json
         * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
         * ```
         */
        id: string;
        /**
         * The name of the pipeline to present to the user.
         *
         * @example
         * ```json
         * "Data Depot Deployment"
         * ```
         */
        displayName: string;
        /**
         * A URL users can use to link to this deployment pipeline.
         *
         * @format uri
         * @example
         * ```json
         * "http://mydeployer.com/project1"
         * ```
         */
        url: string;
      };
      /** The environment that the deployment is present in. */
      environment: {
        /**
         * The identifier of this environment, must be unique for the provider so that it can be shared across pipelines.
         *
         * @example
         * ```json
         * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
         * ```
         */
        id: string;
        /**
         * The name of the environment to present to the user.
         *
         * @example
         * ```json
         * "US East"
         * ```
         */
        displayName: string;
        /**
         * The type of the environment.
         *
         * @example
         * ```json
         * "production"
         * ```
         */
        type: "unmapped" | "development" | "testing" | "staging" | "production";
      };
      /** A list of commands to be actioned for this Deployment */
      commands?: {
        /**
         * The command name.
         *
         * @example
         * ```json
         * "initiate_deployment_gating"
         * ```
         */
        command?: string;
      }[];
      /**
       * The DeploymentData schema version used for this deployment data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
    }[];
    /**
     * Information about the provider. This is useful for auditing, logging, debugging,
     * and other internal uses. It is not considered private information. Hence, it may not contain personally
     * identifiable information.
     */
    providerMetadata?: {
      /**
       * An optional name of the source of the deployments data.
       *
       * @example
       * ```json
       * "Bamboo 6.10.2"
       * ```
       */
      product?: string;
    };
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * The keys of deployments that have been accepted for submission. A deployment key is a composite key that consists of `pipelineId`, `environmentId` and `deploymentSequenceNumber`.
   *
   * A deployment may be rejected if it was only associated with unknown issue keys.
   *
   * Note that a deployment that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
   */
  acceptedDeployments?: {
    /**
     * The identifier of a pipeline, must be unique for the provider.
     *
     * @example
     * ```json
     * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
     * ```
     */
    pipelineId: string;
    /**
     * The identifier of an environment, must be unique for the provider so that it can be shared across pipelines.
     *
     * @example
     * ```json
     * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
     * ```
     */
    environmentId: string;
    /**
     * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
     *
     * @format int64
     * @example
     * ```json
     * 100
     * ```
     */
    deploymentSequenceNumber: number;
  }[];
  /**
   * Details of deployments that have not been accepted for submission, usually due to a problem with the request data.
   *
   * The object will contain the deployment key and any errors associated with that deployment that have prevented it being submitted.
   */
  rejectedDeployments?: {
    /** Fields that uniquely reference a deployment. */
    key: {
      /**
       * The identifier of a pipeline, must be unique for the provider.
       *
       * @example
       * ```json
       * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
       * ```
       */
      pipelineId: string;
      /**
       * The identifier of an environment, must be unique for the provider so that it can be shared across pipelines.
       *
       * @example
       * ```json
       * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
       * ```
       */
      environmentId: string;
      /**
       * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
       *
       * @format int64
       * @example
       * ```json
       * 100
       * ```
       */
      deploymentSequenceNumber: number;
    };
    /** The error messages for the rejected deployment */
    errors: {
      /** A human-readable message describing the error. */
      message: string;
      /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
      errorTraceId?: string;
    }[];
  }[];
  /**
   * Issue keys that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
   *
   * If a deployment has been associated with issue keys other than those in this array it will still be stored against those valid keys.
   * If a deployment was only associated with issue keys deemed to be invalid it won't be persisted.
   */
  unknownIssueKeys?: string[];
  /**
   * Associations (e.g. Issue Keys or Service IDs) that are not known on this Jira instance (if any).
   *
   * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
   *
   * If a deployment has been associated with any other association other than those in this array it will still be stored against those valid associations.
   * If a deployment was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
   */
  unknownAssociations?: (
    | IssueIdOrKeysAssociation
    | ServiceIdOrKeysAssociation
    | EntityAssociation
  )[];
}> {
  return ctx.request({
    method: "post",
    path: "/rest/deployments/0.1/bulk",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete deployments by Property
 *
 * Bulk delete all deployments that match the given request.
 *
 * One or more query params must be supplied to specify the Properties to delete by. Optional param `_updateSequenceNumber` is no longer supported.
 * If more than one Property is provided, data will be deleted that matches ALL of the Properties (i.e. treated as AND).
 * See the documentation for the `submitDeployments` operation for more details.
 *
 * Example operation: DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
 *
 * Deletion is performed asynchronously. The `getDeploymentByKey` operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Delete accepted. Data will eventually be removed from Jira.
 */
export function deleteDeploymentsByProperty(
  ctx: ClientContext,
  params?: {
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `updateSequenceNumber` to use to control deletion.
     *
     * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * If not provided, all stored data that matches the request will be deleted.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceNumber?: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { updateSequenceNumber } = params ?? {};
  return ctx.request({
    method: "delete",
    path: "/rest/deployments/0.1/bulkByProperties",
    query: { _updateSequenceNumber: updateSequenceNumber },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get a deployment by key
 *
 * Retrieve the currently stored deployment data for the given `pipelineId`, `environmentId` and `deploymentSequenceNumber` combination.
 *
 * The result will be what is currently stored, ignoring any pending updates or deletes.
 *
 * @param params.pipelineId - The ID of the deployment's pipeline.
 * @param params.environmentId - The ID of the deployment's environment.
 * @param params.deploymentSequenceNumber - The deployment's deploymentSequenceNumber.
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns The deployment data currently stored for the given ID.
 */
export function getDeploymentByKey(
  ctx: ClientContext,
  params: {
    /** The ID of the deployment's pipeline. */
    pipelineId: string;
    /** The ID of the deployment's environment. */
    environmentId: string;
    /**
     * The deployment's deploymentSequenceNumber.
     *
     * @format int64
     */
    deploymentSequenceNumber: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
   *
   * @format int64
   * @example
   * ```json
   * 100
   * ```
   */
  deploymentSequenceNumber: number;
  /**
   * A number used to apply an order to the updates to the deployment, as identified by the deploymentSequenceNumber, in the case of out-of-order receipt of update requests. It must be a monotonically increasing number. For example, epoch time could be one way to generate the updateSequenceNumber.
   *
   * @format int64
   * @example
   * ```json
   * 1
   * ```
   */
  updateSequenceNumber: number;
  /**
   * Deprecated. The Jira issue keys to associate the Deployment information with.
   * Should replace this field with the "associations" field to associate Deployment information with issueKeys or other types of associations.
   *
   * @deprecated
   */
  issueKeys?: string[];
  /** The entities to associate the Deployment information with. */
  associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation)[];
  /**
   * The human-readable name for the deployment. Will be shown in the UI.
   *
   * @example
   * ```json
   * "Deployment number 16 of Data Depot"
   * ```
   */
  displayName: string;
  /**
   * A URL users can use to link to this deployment, in this environment.
   *
   * @format uri
   * @example
   * ```json
   * "http://mydeployer.com/project1/1111-222-333/prod-east"
   * ```
   */
  url: string;
  /**
   * A short description of the deployment
   *
   * @example
   * ```json
   * "The bits are being transferred"
   * ```
   */
  description: string;
  /**
   * The last-updated timestamp to present to the user as a summary of the state of the deployment.
   *
   * @format date-time
   * @example
   * ```json
   * "2018-01-20T23:27:25.000Z"
   * ```
   */
  lastUpdated: string;
  /**
   * An (optional) additional label that may be displayed with deployment information. Can be used to display version information etc. for the deployment.
   *
   * @example
   * ```json
   * "Release 2018-01-20_08-47-bc2421a"
   * ```
   */
  label?: string;
  /**
   * The duration of the deployment (in seconds).
   *
   * @format int64
   * @example
   * ```json
   * 47
   * ```
   */
  duration?: number;
  /**
   * The state of the deployment
   *
   * @example
   * ```json
   * "in_progress"
   * ```
   */
  state:
    | "unknown"
    | "pending"
    | "in_progress"
    | "cancelled"
    | "failed"
    | "rolled_back"
    | "successful";
  /**
   * This object models the Continuous Delivery (CD) Pipeline concept, an automated process (usually comprised of multiple stages)
   *
   * for getting software from version control right through to the production environment.
   */
  pipeline: {
    /**
     * The identifier of this pipeline, must be unique for the provider.
     *
     * @example
     * ```json
     * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
     * ```
     */
    id: string;
    /**
     * The name of the pipeline to present to the user.
     *
     * @example
     * ```json
     * "Data Depot Deployment"
     * ```
     */
    displayName: string;
    /**
     * A URL users can use to link to this deployment pipeline.
     *
     * @format uri
     * @example
     * ```json
     * "http://mydeployer.com/project1"
     * ```
     */
    url: string;
  };
  /** The environment that the deployment is present in. */
  environment: {
    /**
     * The identifier of this environment, must be unique for the provider so that it can be shared across pipelines.
     *
     * @example
     * ```json
     * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
     * ```
     */
    id: string;
    /**
     * The name of the environment to present to the user.
     *
     * @example
     * ```json
     * "US East"
     * ```
     */
    displayName: string;
    /**
     * The type of the environment.
     *
     * @example
     * ```json
     * "production"
     * ```
     */
    type: "unmapped" | "development" | "testing" | "staging" | "production";
  };
  /** A list of commands to be actioned for this Deployment */
  commands?: {
    /**
     * The command name.
     *
     * @example
     * ```json
     * "initiate_deployment_gating"
     * ```
     */
    command?: string;
  }[];
  /**
   * The DeploymentData schema version used for this deployment data.
   *
   * Placeholder to support potential schema changes in the future.
   *
   * @default "1.0"
   * @example
   * ```json
   * "1.0"
   * ```
   */
  schemaVersion?: "1.0";
}> {
  const { pipelineId, environmentId, deploymentSequenceNumber } = params;
  return ctx.request({
    method: "get",
    path: "/rest/deployments/0.1/pipelines/{pipelineId}/environments/{environmentId}/deployments/{deploymentSequenceNumber}",
    pathParams: { pipelineId, environmentId, deploymentSequenceNumber },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete a deployment by key
 *
 * Delete the currently stored deployment data for the given `pipelineId`, `environmentId` and `deploymentSequenceNumber` combination.
 *
 * Deletion is performed asynchronously. The `getDeploymentByKey` operation can be used to confirm that data has been deleted successfully (if needed).
 *
 * @param params.pipelineId - The ID of the deployment's pipeline.
 * @param params.environmentId - The ID of the deployment's environment.
 * @param params.deploymentSequenceNumber - The deployment's deploymentSequenceNumber.
 * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
 * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
 * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
 */
export function deleteDeploymentByKey(
  ctx: ClientContext,
  params: {
    /** The ID of the deployment's pipeline. */
    pipelineId: string;
    /** The ID of the deployment's environment. */
    environmentId: string;
    /**
     * The deployment's deploymentSequenceNumber.
     *
     * @format int64
     */
    deploymentSequenceNumber: number;
    /**
     * This parameter usage is no longer supported.
     *
     * An optional `_updateSequenceNumber` to use to control deletion.
     *
     * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
     * This can be used help ensure submit/delete requests are applied correctly if issued close together.
     *
     * @format int64
     * @deprecated
     */
    updateSequenceNumber?: number;
  },
  options?: {
    headers?: {
      /**
       * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
       *
       * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
       *
       * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
       * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
       */
      Authorization: string;
    } & Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { pipelineId, environmentId, deploymentSequenceNumber, updateSequenceNumber } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/deployments/0.1/pipelines/{pipelineId}/environments/{environmentId}/deployments/{deploymentSequenceNumber}",
    pathParams: { pipelineId, environmentId, deploymentSequenceNumber },
    query: { _updateSequenceNumber: updateSequenceNumber },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get deployment gating status by key
 *
 * Retrieve the  Deployment gating status for the given `pipelineId + environmentId + deploymentSequenceNumber` combination.
 * Only apps that define the `jiraDeploymentInfoProvider` module can access this resource. This resource requires the 'READ' scope.
 *
 * @param params.pipelineId - The ID of the Deployment's pipeline.
 * @param params.environmentId - The ID of the Deployment's environment.
 * @param params.deploymentSequenceNumber - The Deployment's deploymentSequenceNumber.
 * @returns The current gating status for the given Deployment
 */
export function getDeploymentGatingStatusByKey(
  ctx: ClientContext,
  params: {
    /** The ID of the Deployment's pipeline. */
    pipelineId: string;
    /** The ID of the Deployment's environment. */
    environmentId: string;
    /**
     * The Deployment's deploymentSequenceNumber.
     *
     * @format int64
     */
    deploymentSequenceNumber: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<{
  /**
   * This is the identifier for the Deployment.
   *
   * @format int64
   * @example
   * ```json
   * 100
   * ```
   */
  deploymentSequenceNumber?: number;
  /**
   * The ID of the Deployment's pipeline.
   *
   * @example
   * ```json
   * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
   * ```
   */
  pipelineId?: string;
  /**
   * The ID of the Deployment's environment.
   *
   * @example
   * ```json
   * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
   * ```
   */
  environmentId?: string;
  /**
   * Time the deployment gating status was updated.
   *
   * @format date-time
   * @example
   * ```json
   * "2020-08-25T06:04:50.239Z"
   * ```
   */
  updatedTimestamp?: string;
  /**
   * The gating status
   *
   * @example
   * ```json
   * "allowed"
   * ```
   */
  gatingStatus?: "allowed" | "prevented" | "awaiting" | "invalid";
  details?: {
    /**
     * The type of the gating status details.
     *
     * @example
     * ```json
     * "issue"
     * ```
     */
    type: "issue";
    /**
     * An issue key that references an issue in Jira.
     *
     * @example
     * ```json
     * "ABC-123"
     * ```
     */
    issueKey: string;
    /**
     * A full HTTPS link to the Jira issue for the change request gating this Deployment. This field is provided if the details type is issue.
     *
     * @format uri
     * @example
     * ```json
     * "https://your-domain.atlassian.net/servicedesk/customer/portal/1/ZAINA-123"
     * ```
     */
    issueLink: string;
  }[];
}> {
  const { pipelineId, environmentId, deploymentSequenceNumber } = params;
  return ctx.request({
    method: "get",
    path: "/rest/deployments/0.1/pipelines/{pipelineId}/environments/{environmentId}/deployments/{deploymentSequenceNumber}/gating-status",
    pathParams: { pipelineId, environmentId, deploymentSequenceNumber },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * APIs related to integrating deployment data with Jira Software.
 *
 * These APIs are available to Atlassian Connect apps. To use these APIs you must have the `jiraDeploymentInfoProvider` module in your Connect app's descriptor. See https://developer.atlassian.com/cloud/jira/software/modules/deployment/. They are also related to integrating Jira Software Cloud with on-premises tools using OAuth 2.0 credentials. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/.
 *
 * These APIs are available to Forge apps with the `devops:deploymentInfoProvider` module in the Forge app's manifest. See https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-software-deployment-info/.
 */
export function createDeploymentsService(ctx: ClientContext) {
  return {
    /**
     * Submit deployment data
     *
     * Update / insert deployment data.
     *
     * Deployments are identified by the combination of `pipelineId`, `environmentId` and `deploymentSequenceNumber`, and existing deployment data for the same deployment will be replaced if it exists and the `updateSequenceNumber` of existing data is less than the incoming data.
     *
     * Submissions are processed asynchronously. Submitted data will eventually be available in Jira. Most updates are available within a short period of time, but may take some time during peak load and/or maintenance times. The `getDeploymentByKey` operation can be used to confirm that data has been stored successfully (if needed).
     *
     * In the case of multiple deployments being submitted in one request, each is validated individually prior to submission. Details of which deployments failed submission (if any) are available in the response object.
     *
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Submission accepted. Each submitted deployment that is of a valid format will eventually be available in Jira. Details of which deployments were submitted and which failed submission (due to data format problems etc.) are available in the response object.
     */
    submitDeployments(
      params: {
        /**
         * Properties assigned to deployment data that can then be used for delete / query operations.
         *
         * Examples might be an account or user ID that can then be used to clean up data if an account is removed from the Provider system.
         *
         * Properties are supplied as key/value pairs, and a maximum of 5 properties can be supplied, keys cannot contain ':' or start with '_'.
         *
         * @example
         * ```json
         * {
         *   "accountId": "account-234",
         *   "projectId": "project-123"
         * }
         * ```
         */
        properties?: Record<string, string>;
        /**
         * A list of deployments to submit to Jira.
         *
         * Each deployment may be associated with one or more Jira issue keys, and will be associated with any properties included in this request.
         */
        deployments: {
          /**
           * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
           *
           * @format int64
           * @example
           * ```json
           * 100
           * ```
           */
          deploymentSequenceNumber: number;
          /**
           * A number used to apply an order to the updates to the deployment, as identified by the deploymentSequenceNumber, in the case of out-of-order receipt of update requests. It must be a monotonically increasing number. For example, epoch time could be one way to generate the updateSequenceNumber.
           *
           * @format int64
           * @example
           * ```json
           * 1
           * ```
           */
          updateSequenceNumber: number;
          /**
           * Deprecated. The Jira issue keys to associate the Deployment information with.
           * Should replace this field with the "associations" field to associate Deployment information with issueKeys or other types of associations.
           *
           * @deprecated
           */
          issueKeys?: string[];
          /** The entities to associate the Deployment information with. */
          associations?: (
            | IssueIdOrKeysAssociation
            | ServiceIdOrKeysAssociation
            | EntityAssociation
          )[];
          /**
           * The human-readable name for the deployment. Will be shown in the UI.
           *
           * @example
           * ```json
           * "Deployment number 16 of Data Depot"
           * ```
           */
          displayName: string;
          /**
           * A URL users can use to link to this deployment, in this environment.
           *
           * @format uri
           * @example
           * ```json
           * "http://mydeployer.com/project1/1111-222-333/prod-east"
           * ```
           */
          url: string;
          /**
           * A short description of the deployment
           *
           * @example
           * ```json
           * "The bits are being transferred"
           * ```
           */
          description: string;
          /**
           * The last-updated timestamp to present to the user as a summary of the state of the deployment.
           *
           * @format date-time
           * @example
           * ```json
           * "2018-01-20T23:27:25.000Z"
           * ```
           */
          lastUpdated: string;
          /**
           * An (optional) additional label that may be displayed with deployment information. Can be used to display version information etc. for the deployment.
           *
           * @example
           * ```json
           * "Release 2018-01-20_08-47-bc2421a"
           * ```
           */
          label?: string;
          /**
           * The duration of the deployment (in seconds).
           *
           * @format int64
           * @example
           * ```json
           * 47
           * ```
           */
          duration?: number;
          /**
           * The state of the deployment
           *
           * @example
           * ```json
           * "in_progress"
           * ```
           */
          state:
            | "unknown"
            | "pending"
            | "in_progress"
            | "cancelled"
            | "failed"
            | "rolled_back"
            | "successful";
          /**
           * This object models the Continuous Delivery (CD) Pipeline concept, an automated process (usually comprised of multiple stages)
           *
           * for getting software from version control right through to the production environment.
           */
          pipeline: {
            /**
             * The identifier of this pipeline, must be unique for the provider.
             *
             * @example
             * ```json
             * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
             * ```
             */
            id: string;
            /**
             * The name of the pipeline to present to the user.
             *
             * @example
             * ```json
             * "Data Depot Deployment"
             * ```
             */
            displayName: string;
            /**
             * A URL users can use to link to this deployment pipeline.
             *
             * @format uri
             * @example
             * ```json
             * "http://mydeployer.com/project1"
             * ```
             */
            url: string;
          };
          /** The environment that the deployment is present in. */
          environment: {
            /**
             * The identifier of this environment, must be unique for the provider so that it can be shared across pipelines.
             *
             * @example
             * ```json
             * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
             * ```
             */
            id: string;
            /**
             * The name of the environment to present to the user.
             *
             * @example
             * ```json
             * "US East"
             * ```
             */
            displayName: string;
            /**
             * The type of the environment.
             *
             * @example
             * ```json
             * "production"
             * ```
             */
            type: "unmapped" | "development" | "testing" | "staging" | "production";
          };
          /** A list of commands to be actioned for this Deployment */
          commands?: {
            /**
             * The command name.
             *
             * @example
             * ```json
             * "initiate_deployment_gating"
             * ```
             */
            command?: string;
          }[];
          /**
           * The DeploymentData schema version used for this deployment data.
           *
           * Placeholder to support potential schema changes in the future.
           *
           * @default "1.0"
           * @example
           * ```json
           * "1.0"
           * ```
           */
          schemaVersion?: "1.0";
        }[];
        /**
         * Information about the provider. This is useful for auditing, logging, debugging,
         * and other internal uses. It is not considered private information. Hence, it may not contain personally
         * identifiable information.
         */
        providerMetadata?: {
          /**
           * An optional name of the source of the deployments data.
           *
           * @example
           * ```json
           * "Bamboo 6.10.2"
           * ```
           */
          product?: string;
        };
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * The keys of deployments that have been accepted for submission. A deployment key is a composite key that consists of `pipelineId`, `environmentId` and `deploymentSequenceNumber`.
       *
       * A deployment may be rejected if it was only associated with unknown issue keys.
       *
       * Note that a deployment that isn't updated due to it's updateSequenceNumber being out of order is not considered a failed submission.
       */
      acceptedDeployments?: {
        /**
         * The identifier of a pipeline, must be unique for the provider.
         *
         * @example
         * ```json
         * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
         * ```
         */
        pipelineId: string;
        /**
         * The identifier of an environment, must be unique for the provider so that it can be shared across pipelines.
         *
         * @example
         * ```json
         * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
         * ```
         */
        environmentId: string;
        /**
         * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
         *
         * @format int64
         * @example
         * ```json
         * 100
         * ```
         */
        deploymentSequenceNumber: number;
      }[];
      /**
       * Details of deployments that have not been accepted for submission, usually due to a problem with the request data.
       *
       * The object will contain the deployment key and any errors associated with that deployment that have prevented it being submitted.
       */
      rejectedDeployments?: {
        /** Fields that uniquely reference a deployment. */
        key: {
          /**
           * The identifier of a pipeline, must be unique for the provider.
           *
           * @example
           * ```json
           * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
           * ```
           */
          pipelineId: string;
          /**
           * The identifier of an environment, must be unique for the provider so that it can be shared across pipelines.
           *
           * @example
           * ```json
           * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
           * ```
           */
          environmentId: string;
          /**
           * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
           *
           * @format int64
           * @example
           * ```json
           * 100
           * ```
           */
          deploymentSequenceNumber: number;
        };
        /** The error messages for the rejected deployment */
        errors: {
          /** A human-readable message describing the error. */
          message: string;
          /** An optional trace ID that can be used by Jira developers to locate the source of the error. */
          errorTraceId?: string;
        }[];
      }[];
      /**
       * Issue keys that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
       *
       * If a deployment has been associated with issue keys other than those in this array it will still be stored against those valid keys.
       * If a deployment was only associated with issue keys deemed to be invalid it won't be persisted.
       */
      unknownIssueKeys?: string[];
      /**
       * Associations (e.g. Issue Keys or Service IDs) that are not known on this Jira instance (if any).
       *
       * These may be invalid keys (e.g. `UTF-8` is sometimes incorrectly identified as a Jira issue key), or they may be for projects that no longer exist.
       *
       * If a deployment has been associated with any other association other than those in this array it will still be stored against those valid associations.
       * If a deployment was only associated with the associations in this array, it is deemed to be invalid and it won't be persisted.
       */
      unknownAssociations?: (
        | IssueIdOrKeysAssociation
        | ServiceIdOrKeysAssociation
        | EntityAssociation
      )[];
    }> {
      return submitDeployments(ctx, params, options);
    },

    /**
     * Delete deployments by Property
     *
     * Bulk delete all deployments that match the given request.
     *
     * One or more query params must be supplied to specify the Properties to delete by. Optional param `_updateSequenceNumber` is no longer supported.
     * If more than one Property is provided, data will be deleted that matches ALL of the Properties (i.e. treated as AND).
     * See the documentation for the `submitDeployments` operation for more details.
     *
     * Example operation: DELETE /bulkByProperties?accountId=account-123&createdBy=user-456
     *
     * Deletion is performed asynchronously. The `getDeploymentByKey` operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together. If not provided, all stored data that matches the request will be deleted.
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Delete accepted. Data will eventually be removed from Jira.
     */
    deleteDeploymentsByProperty(
      params?: {
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `updateSequenceNumber` to use to control deletion.
         *
         * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * If not provided, all stored data that matches the request will be deleted.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceNumber?: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteDeploymentsByProperty(ctx, params, options);
    },

    /**
     * Get a deployment by key
     *
     * Retrieve the currently stored deployment data for the given `pipelineId`, `environmentId` and `deploymentSequenceNumber` combination.
     *
     * The result will be what is currently stored, ignoring any pending updates or deletes.
     *
     * @param params.pipelineId - The ID of the deployment's pipeline.
     * @param params.environmentId - The ID of the deployment's environment.
     * @param params.deploymentSequenceNumber - The deployment's deploymentSequenceNumber.
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns The deployment data currently stored for the given ID.
     */
    getDeploymentByKey(
      params: {
        /** The ID of the deployment's pipeline. */
        pipelineId: string;
        /** The ID of the deployment's environment. */
        environmentId: string;
        /**
         * The deployment's deploymentSequenceNumber.
         *
         * @format int64
         */
        deploymentSequenceNumber: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * This is the identifier for the deployment. It must be unique for the specified pipeline and environment. It must be a monotonically increasing number, as this is used to sequence the deployments.
       *
       * @format int64
       * @example
       * ```json
       * 100
       * ```
       */
      deploymentSequenceNumber: number;
      /**
       * A number used to apply an order to the updates to the deployment, as identified by the deploymentSequenceNumber, in the case of out-of-order receipt of update requests. It must be a monotonically increasing number. For example, epoch time could be one way to generate the updateSequenceNumber.
       *
       * @format int64
       * @example
       * ```json
       * 1
       * ```
       */
      updateSequenceNumber: number;
      /**
       * Deprecated. The Jira issue keys to associate the Deployment information with.
       * Should replace this field with the "associations" field to associate Deployment information with issueKeys or other types of associations.
       *
       * @deprecated
       */
      issueKeys?: string[];
      /** The entities to associate the Deployment information with. */
      associations?: (IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation)[];
      /**
       * The human-readable name for the deployment. Will be shown in the UI.
       *
       * @example
       * ```json
       * "Deployment number 16 of Data Depot"
       * ```
       */
      displayName: string;
      /**
       * A URL users can use to link to this deployment, in this environment.
       *
       * @format uri
       * @example
       * ```json
       * "http://mydeployer.com/project1/1111-222-333/prod-east"
       * ```
       */
      url: string;
      /**
       * A short description of the deployment
       *
       * @example
       * ```json
       * "The bits are being transferred"
       * ```
       */
      description: string;
      /**
       * The last-updated timestamp to present to the user as a summary of the state of the deployment.
       *
       * @format date-time
       * @example
       * ```json
       * "2018-01-20T23:27:25.000Z"
       * ```
       */
      lastUpdated: string;
      /**
       * An (optional) additional label that may be displayed with deployment information. Can be used to display version information etc. for the deployment.
       *
       * @example
       * ```json
       * "Release 2018-01-20_08-47-bc2421a"
       * ```
       */
      label?: string;
      /**
       * The duration of the deployment (in seconds).
       *
       * @format int64
       * @example
       * ```json
       * 47
       * ```
       */
      duration?: number;
      /**
       * The state of the deployment
       *
       * @example
       * ```json
       * "in_progress"
       * ```
       */
      state:
        | "unknown"
        | "pending"
        | "in_progress"
        | "cancelled"
        | "failed"
        | "rolled_back"
        | "successful";
      /**
       * This object models the Continuous Delivery (CD) Pipeline concept, an automated process (usually comprised of multiple stages)
       *
       * for getting software from version control right through to the production environment.
       */
      pipeline: {
        /**
         * The identifier of this pipeline, must be unique for the provider.
         *
         * @example
         * ```json
         * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
         * ```
         */
        id: string;
        /**
         * The name of the pipeline to present to the user.
         *
         * @example
         * ```json
         * "Data Depot Deployment"
         * ```
         */
        displayName: string;
        /**
         * A URL users can use to link to this deployment pipeline.
         *
         * @format uri
         * @example
         * ```json
         * "http://mydeployer.com/project1"
         * ```
         */
        url: string;
      };
      /** The environment that the deployment is present in. */
      environment: {
        /**
         * The identifier of this environment, must be unique for the provider so that it can be shared across pipelines.
         *
         * @example
         * ```json
         * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
         * ```
         */
        id: string;
        /**
         * The name of the environment to present to the user.
         *
         * @example
         * ```json
         * "US East"
         * ```
         */
        displayName: string;
        /**
         * The type of the environment.
         *
         * @example
         * ```json
         * "production"
         * ```
         */
        type: "unmapped" | "development" | "testing" | "staging" | "production";
      };
      /** A list of commands to be actioned for this Deployment */
      commands?: {
        /**
         * The command name.
         *
         * @example
         * ```json
         * "initiate_deployment_gating"
         * ```
         */
        command?: string;
      }[];
      /**
       * The DeploymentData schema version used for this deployment data.
       *
       * Placeholder to support potential schema changes in the future.
       *
       * @default "1.0"
       * @example
       * ```json
       * "1.0"
       * ```
       */
      schemaVersion?: "1.0";
    }> {
      return getDeploymentByKey(ctx, params, options);
    },

    /**
     * Delete a deployment by key
     *
     * Delete the currently stored deployment data for the given `pipelineId`, `environmentId` and `deploymentSequenceNumber` combination.
     *
     * Deletion is performed asynchronously. The `getDeploymentByKey` operation can be used to confirm that data has been deleted successfully (if needed).
     *
     * @param params.pipelineId - The ID of the deployment's pipeline.
     * @param params.environmentId - The ID of the deployment's environment.
     * @param params.deploymentSequenceNumber - The deployment's deploymentSequenceNumber.
     * @param params.updateSequenceNumber - This parameter usage is no longer supported. An optional `_updateSequenceNumber` to use to control deletion. Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted. This can be used help ensure submit/delete requests are applied correctly if issued close together.
     * @param options.headers.Authorization - All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira. If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403. See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens. See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
     * @returns Delete has been accepted. Data will eventually be removed from Jira if it exists.
     */
    deleteDeploymentByKey(
      params: {
        /** The ID of the deployment's pipeline. */
        pipelineId: string;
        /** The ID of the deployment's environment. */
        environmentId: string;
        /**
         * The deployment's deploymentSequenceNumber.
         *
         * @format int64
         */
        deploymentSequenceNumber: number;
        /**
         * This parameter usage is no longer supported.
         *
         * An optional `_updateSequenceNumber` to use to control deletion.
         *
         * Only stored data with an `updateSequenceNumber` less than or equal to that provided will be deleted.
         * This can be used help ensure submit/delete requests are applied correctly if issued close together.
         *
         * @format int64
         * @deprecated
         */
        updateSequenceNumber?: number;
      },
      options?: {
        headers?: {
          /**
           * All requests must be signed with either a Connect JWT token or OAuth token for an on-premise integration that corresponds to an app installed in Jira.
           *
           * If the Connect JWT token corresponds to an app that does not define `jiraDeploymentInfoProvider` module it will be rejected with a 403.
           *
           * See https://developer.atlassian.com/blog/2015/01/understanding-jwt/ for more details about Connect JWT tokens.
           * See https://developer.atlassian.com/cloud/jira/software/integrate-jsw-cloud-with-onpremises-tools/ for details about on-premise integrations.
           */
          Authorization: string;
        } & Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteDeploymentByKey(ctx, params, options);
    },

    /**
     * Get deployment gating status by key
     *
     * Retrieve the  Deployment gating status for the given `pipelineId + environmentId + deploymentSequenceNumber` combination.
     * Only apps that define the `jiraDeploymentInfoProvider` module can access this resource. This resource requires the 'READ' scope.
     *
     * @param params.pipelineId - The ID of the Deployment's pipeline.
     * @param params.environmentId - The ID of the Deployment's environment.
     * @param params.deploymentSequenceNumber - The Deployment's deploymentSequenceNumber.
     * @returns The current gating status for the given Deployment
     */
    getDeploymentGatingStatusByKey(
      params: {
        /** The ID of the Deployment's pipeline. */
        pipelineId: string;
        /** The ID of the Deployment's environment. */
        environmentId: string;
        /**
         * The Deployment's deploymentSequenceNumber.
         *
         * @format int64
         */
        deploymentSequenceNumber: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<{
      /**
       * This is the identifier for the Deployment.
       *
       * @format int64
       * @example
       * ```json
       * 100
       * ```
       */
      deploymentSequenceNumber?: number;
      /**
       * The ID of the Deployment's pipeline.
       *
       * @example
       * ```json
       * "e9c906a7-451f-4fa6-ae1a-c389e2e2d87c"
       * ```
       */
      pipelineId?: string;
      /**
       * The ID of the Deployment's environment.
       *
       * @example
       * ```json
       * "8ec94d72-a4fc-4ac0-b31d-c5a595f373ba"
       * ```
       */
      environmentId?: string;
      /**
       * Time the deployment gating status was updated.
       *
       * @format date-time
       * @example
       * ```json
       * "2020-08-25T06:04:50.239Z"
       * ```
       */
      updatedTimestamp?: string;
      /**
       * The gating status
       *
       * @example
       * ```json
       * "allowed"
       * ```
       */
      gatingStatus?: "allowed" | "prevented" | "awaiting" | "invalid";
      details?: {
        /**
         * The type of the gating status details.
         *
         * @example
         * ```json
         * "issue"
         * ```
         */
        type: "issue";
        /**
         * An issue key that references an issue in Jira.
         *
         * @example
         * ```json
         * "ABC-123"
         * ```
         */
        issueKey: string;
        /**
         * A full HTTPS link to the Jira issue for the change request gating this Deployment. This field is provided if the details type is issue.
         *
         * @format uri
         * @example
         * ```json
         * "https://your-domain.atlassian.net/servicedesk/customer/portal/1/ZAINA-123"
         * ```
         */
        issueLink: string;
      }[];
    }> {
      return getDeploymentGatingStatusByKey(ctx, params, options);
    },
  };
}
