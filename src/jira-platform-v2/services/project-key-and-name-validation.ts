// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { ErrorCollection } from "../types/index.ts";

/**
 * Validate project key
 *
 * Validates a project key by confirming the key is a valid string and not in use.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.key - The project key.
 * @returns Returned if the request is successful.
 */
export function validateProjectKey(
  ctx: ClientContext,
  params?: {
    /**
     * The project key.
     *
     * @example
     * ```json
     * "HSP"
     * ```
     */
    key?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<ErrorCollection> {
  const { key } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/projectvalidate/key",
    query: { key },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get valid project key
 *
 * Validates a project key and, if the key is invalid or in use, generates a valid random string for the project key.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.key - The project key.
 * @returns Returned if the request is successful.
 */
export function getValidProjectKey(
  ctx: ClientContext,
  params?: {
    /**
     * The project key.
     *
     * @example
     * ```json
     * "HSP"
     * ```
     */
    key?: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<string> {
  const { key } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/2/projectvalidate/validProjectKey",
    query: { key },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get valid project name
 *
 * Checks that a project name isn't in use. If the name isn't in use, the passed string is returned. If the name is in use, this operation attempts to generate a valid project name based on the one supplied, usually by adding a sequence number. If a valid project name cannot be generated, a 404 response is returned.
 *
 * **[Permissions](#permissions) required:** None.
 *
 * @param params.name - The project name.
 * @returns Returned if the request is successful.
 */
export function getValidProjectName(
  ctx: ClientContext,
  params: {
    /** The project name. */
    name: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<string> {
  const { name } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/2/projectvalidate/validProjectName",
    query: { name },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource provides validation for project keys and names. */
export function createProjectKeyAndNameValidationService(ctx: ClientContext) {
  return {
    /**
     * Validate project key
     *
     * Validates a project key by confirming the key is a valid string and not in use.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.key - The project key.
     * @returns Returned if the request is successful.
     */
    validateProjectKey(
      params?: {
        /**
         * The project key.
         *
         * @example
         * ```json
         * "HSP"
         * ```
         */
        key?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<ErrorCollection> {
      return validateProjectKey(ctx, params, options);
    },

    /**
     * Get valid project key
     *
     * Validates a project key and, if the key is invalid or in use, generates a valid random string for the project key.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.key - The project key.
     * @returns Returned if the request is successful.
     */
    getValidProjectKey(
      params?: {
        /**
         * The project key.
         *
         * @example
         * ```json
         * "HSP"
         * ```
         */
        key?: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<string> {
      return getValidProjectKey(ctx, params, options);
    },

    /**
     * Get valid project name
     *
     * Checks that a project name isn't in use. If the name isn't in use, the passed string is returned. If the name is in use, this operation attempts to generate a valid project name based on the one supplied, usually by adding a sequence number. If a valid project name cannot be generated, a 404 response is returned.
     *
     * **[Permissions](#permissions) required:** None.
     *
     * @param params.name - The project name.
     * @returns Returned if the request is successful.
     */
    getValidProjectName(
      params: {
        /** The project name. */
        name: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<string> {
      return getValidProjectName(ctx, params, options);
    },
  };
}
