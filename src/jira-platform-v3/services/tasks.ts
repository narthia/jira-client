// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type { TaskProgressBeanObject } from "../types/index.ts";

/**
 * Get task
 *
 * Returns the status of a [long-running asynchronous task](#async).
 *
 * When a task has finished, this operation returns the JSON blob applicable to the task. See the documentation of the operation that created the task for details. Task details are not permanently retained. As of September 2019, details are retained for 14 days although this period may change without notice.
 *
 * **Deprecation notice:** The required OAuth 2.0 scopes will be updated on June 15, 2024.
 *
 *  *  `read:jira-work`
 *
 * **[Permissions](#permissions) required:** either of:
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  Creator of the task.
 *
 * @param params.taskId - The ID of the task.
 * @returns Returned if the request is successful.
 */
export function getTask(
  ctx: ClientContext,
  params: {
    /** The ID of the task. */
    taskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<TaskProgressBeanObject> {
  const { taskId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/task/{taskId}",
    pathParams: { taskId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Cancel task
 *
 * Cancels a task.
 *
 * **[Permissions](#permissions) required:** either of:
 *
 *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *  *  Creator of the task.
 *
 * @param params.taskId - The ID of the task.
 * @returns Returned if the request is successful.
 */
export function cancelTask(
  ctx: ClientContext,
  params: {
    /** The ID of the task. */
    taskId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { taskId } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/task/{taskId}/cancel",
    pathParams: { taskId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents a [long-running asynchronous tasks](#async-operations). Use it to obtain details about the progress of a long-running task or cancel a long-running task. */
export function createTasksService(ctx: ClientContext) {
  return {
    /**
     * Get task
     *
     * Returns the status of a [long-running asynchronous task](#async).
     *
     * When a task has finished, this operation returns the JSON blob applicable to the task. See the documentation of the operation that created the task for details. Task details are not permanently retained. As of September 2019, details are retained for 14 days although this period may change without notice.
     *
     * **Deprecation notice:** The required OAuth 2.0 scopes will be updated on June 15, 2024.
     *
     *  *  `read:jira-work`
     *
     * **[Permissions](#permissions) required:** either of:
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  Creator of the task.
     *
     * @param params.taskId - The ID of the task.
     * @returns Returned if the request is successful.
     */
    getTask(
      params: {
        /** The ID of the task. */
        taskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<TaskProgressBeanObject> {
      return getTask(ctx, params, options);
    },

    /**
     * Cancel task
     *
     * Cancels a task.
     *
     * **[Permissions](#permissions) required:** either of:
     *
     *  *  *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *  *  Creator of the task.
     *
     * @param params.taskId - The ID of the task.
     * @returns Returned if the request is successful.
     */
    cancelTask(
      params: {
        /** The ID of the task. */
        taskId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return cancelTask(ctx, params, options);
    },
  };
}
