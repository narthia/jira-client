// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  AddAtlassianTeamRequest,
  CreatePlanOnlyTeamRequest,
  GetAtlassianTeamResponse,
  GetPlanOnlyTeamResponse,
  PageWithCursorGetTeamResponseForPage,
} from "../types/index.ts";

/**
 * Get teams in plan paginated
 *
 * Returns a [paginated](#pagination) list of plan-only and Atlassian teams in a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.cursor - The cursor to start from. If not provided, the first page will be returned.
 * @param params.maxResults - The maximum number of plan teams to return per page. The maximum value is 50. The default value is 50.
 * @returns Returned if the request is successful.
 */
export function getTeams(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * The cursor to start from. If not provided, the first page will be returned.
     *
     * @default ""
     */
    cursor?: string;
    /**
     * The maximum number of plan teams to return per page. The maximum value is 50. The default value is 50.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageWithCursorGetTeamResponseForPage> {
  const { planId, cursor, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/plans/plan/{planId}/team",
    pathParams: { planId },
    query: { cursor, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Add Atlassian team to plan
 *
 * Adds an existing Atlassian team to a plan and configures their plannning settings.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @returns Returned if the request is successful.
 */
export function addAtlassianTeam(
  ctx: ClientContext,
  params: AddAtlassianTeamRequest & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/plans/plan/{planId}/team/atlassian",
    pathParams: { planId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get Atlassian team in plan
 *
 * Returns planning settings for an Atlassian team in a plan.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.atlassianTeamId - The ID of the Atlassian team.
 * @returns Returned if the request is successful.
 */
export function getAtlassianTeam(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /** The ID of the Atlassian team. */
    atlassianTeamId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<GetAtlassianTeamResponse> {
  const { planId, atlassianTeamId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}",
    pathParams: { planId, atlassianTeamId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update Atlassian team in plan
 *
 * Updates any of the following planning settings of an Atlassian team in a plan using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
 *
 *  *  planningStyle
 *  *  issueSourceId
 *  *  sprintLength
 *  *  capacity
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * *Note that "add" operations do not respect array indexes in target locations. Call the "Get Atlassian team in plan" endpoint to find out the order of array elements.*
 *
 * @param params.planId - The ID of the plan.
 * @param params.atlassianTeamId - The ID of the Atlassian team.
 * @returns Returned if the request is successful.
 */
export function updateAtlassianTeam(
  ctx: ClientContext,
  params: Record<string, unknown> & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /** The ID of the Atlassian team. */
    atlassianTeamId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, atlassianTeamId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}",
    pathParams: { planId, atlassianTeamId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Remove Atlassian team from plan
 *
 * Removes an Atlassian team from a plan and deletes their planning settings.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.atlassianTeamId - The ID of the Atlassian team.
 * @returns Returned if the request is successful.
 */
export function removeAtlassianTeam(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /** The ID of the Atlassian team. */
    atlassianTeamId: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, atlassianTeamId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/plans/plan/{planId}/team/atlassian/{atlassianTeamId}",
    pathParams: { planId, atlassianTeamId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create plan-only team
 *
 * Creates a plan-only team and configures their planning settings.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @returns Returned if the request is successful.
 */
export function createPlanOnlyTeam(
  ctx: ClientContext,
  params: CreatePlanOnlyTeamRequest & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<number> {
  const { planId, ...body } = params;
  return ctx.request({
    method: "post",
    path: "/rest/api/3/plans/plan/{planId}/team/planonly",
    pathParams: { planId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get plan-only team
 *
 * Returns planning settings for a plan-only team.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.planOnlyTeamId - The ID of the plan-only team.
 * @returns Returned if the request is successful.
 */
export function getPlanOnlyTeam(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * The ID of the plan-only team.
     *
     * @format int64
     */
    planOnlyTeamId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<GetPlanOnlyTeamResponse> {
  const { planId, planOnlyTeamId } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}",
    pathParams: { planId, planOnlyTeamId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update plan-only team
 *
 * Updates any of the following planning settings of a plan-only team using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
 *
 *  *  name
 *  *  planningStyle
 *  *  issueSourceId
 *  *  sprintLength
 *  *  capacity
 *  *  memberAccountIds
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * *Note that "add" operations do not respect array indexes in target locations. Call the "Get plan-only team" endpoint to find out the order of array elements.*
 *
 * @param params.planId - The ID of the plan.
 * @param params.planOnlyTeamId - The ID of the plan-only team.
 * @returns Returned if the request is successful.
 */
export function updatePlanOnlyTeam(
  ctx: ClientContext,
  params: Record<string, unknown> & {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * The ID of the plan-only team.
     *
     * @format int64
     */
    planOnlyTeamId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, planOnlyTeamId, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}",
    pathParams: { planId, planOnlyTeamId },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete plan-only team
 *
 * Deletes a plan-only team and their planning settings.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.planId - The ID of the plan.
 * @param params.planOnlyTeamId - The ID of the plan-only team.
 * @returns Returned if the request is successful.
 */
export function deletePlanOnlyTeam(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the plan.
     *
     * @format int64
     */
    planId: number;
    /**
     * The ID of the plan-only team.
     *
     * @format int64
     */
    planOnlyTeamId: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { planId, planOnlyTeamId } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/plans/plan/{planId}/team/planonly/{planOnlyTeamId}",
    pathParams: { planId, planOnlyTeamId },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/** This resource represents planning settings for plan-only and Atlassian teams in a plan. Use it to get, create, update and delete planning settings. */
export function createTeamsInPlanService(ctx: ClientContext) {
  return {
    /**
     * Get teams in plan paginated
     *
     * Returns a [paginated](#pagination) list of plan-only and Atlassian teams in a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.cursor - The cursor to start from. If not provided, the first page will be returned.
     * @param params.maxResults - The maximum number of plan teams to return per page. The maximum value is 50. The default value is 50.
     * @returns Returned if the request is successful.
     */
    getTeams(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * The cursor to start from. If not provided, the first page will be returned.
         *
         * @default ""
         */
        cursor?: string;
        /**
         * The maximum number of plan teams to return per page. The maximum value is 50. The default value is 50.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageWithCursorGetTeamResponseForPage> {
      return getTeams(ctx, params, options);
    },

    /**
     * Add Atlassian team to plan
     *
     * Adds an existing Atlassian team to a plan and configures their plannning settings.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @returns Returned if the request is successful.
     */
    addAtlassianTeam(
      params: AddAtlassianTeamRequest & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return addAtlassianTeam(ctx, params, options);
    },

    /**
     * Get Atlassian team in plan
     *
     * Returns planning settings for an Atlassian team in a plan.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.atlassianTeamId - The ID of the Atlassian team.
     * @returns Returned if the request is successful.
     */
    getAtlassianTeam(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /** The ID of the Atlassian team. */
        atlassianTeamId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<GetAtlassianTeamResponse> {
      return getAtlassianTeam(ctx, params, options);
    },

    /**
     * Update Atlassian team in plan
     *
     * Updates any of the following planning settings of an Atlassian team in a plan using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
     *
     *  *  planningStyle
     *  *  issueSourceId
     *  *  sprintLength
     *  *  capacity
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * *Note that "add" operations do not respect array indexes in target locations. Call the "Get Atlassian team in plan" endpoint to find out the order of array elements.*
     *
     * @param params.planId - The ID of the plan.
     * @param params.atlassianTeamId - The ID of the Atlassian team.
     * @returns Returned if the request is successful.
     */
    updateAtlassianTeam(
      params: Record<string, unknown> & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /** The ID of the Atlassian team. */
        atlassianTeamId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateAtlassianTeam(ctx, params, options);
    },

    /**
     * Remove Atlassian team from plan
     *
     * Removes an Atlassian team from a plan and deletes their planning settings.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.atlassianTeamId - The ID of the Atlassian team.
     * @returns Returned if the request is successful.
     */
    removeAtlassianTeam(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /** The ID of the Atlassian team. */
        atlassianTeamId: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return removeAtlassianTeam(ctx, params, options);
    },

    /**
     * Create plan-only team
     *
     * Creates a plan-only team and configures their planning settings.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @returns Returned if the request is successful.
     */
    createPlanOnlyTeam(
      params: CreatePlanOnlyTeamRequest & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<number> {
      return createPlanOnlyTeam(ctx, params, options);
    },

    /**
     * Get plan-only team
     *
     * Returns planning settings for a plan-only team.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.planOnlyTeamId - The ID of the plan-only team.
     * @returns Returned if the request is successful.
     */
    getPlanOnlyTeam(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * The ID of the plan-only team.
         *
         * @format int64
         */
        planOnlyTeamId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<GetPlanOnlyTeamResponse> {
      return getPlanOnlyTeam(ctx, params, options);
    },

    /**
     * Update plan-only team
     *
     * Updates any of the following planning settings of a plan-only team using [JSON Patch](https://datatracker.ietf.org/doc/html/rfc6902).
     *
     *  *  name
     *  *  planningStyle
     *  *  issueSourceId
     *  *  sprintLength
     *  *  capacity
     *  *  memberAccountIds
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * *Note that "add" operations do not respect array indexes in target locations. Call the "Get plan-only team" endpoint to find out the order of array elements.*
     *
     * @param params.planId - The ID of the plan.
     * @param params.planOnlyTeamId - The ID of the plan-only team.
     * @returns Returned if the request is successful.
     */
    updatePlanOnlyTeam(
      params: Record<string, unknown> & {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * The ID of the plan-only team.
         *
         * @format int64
         */
        planOnlyTeamId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updatePlanOnlyTeam(ctx, params, options);
    },

    /**
     * Delete plan-only team
     *
     * Deletes a plan-only team and their planning settings.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.planId - The ID of the plan.
     * @param params.planOnlyTeamId - The ID of the plan-only team.
     * @returns Returned if the request is successful.
     */
    deletePlanOnlyTeam(
      params: {
        /**
         * The ID of the plan.
         *
         * @format int64
         */
        planId: number;
        /**
         * The ID of the plan-only team.
         *
         * @format int64
         */
        planOnlyTeamId: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deletePlanOnlyTeam(ctx, params, options);
    },
  };
}
