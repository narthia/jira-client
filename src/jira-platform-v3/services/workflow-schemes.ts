// Generated by @narthia/openapi-sdk-generator. Do not edit manually.

import type { ClientContext } from "../../client/index.ts";
import type {
  DefaultWorkflow,
  IssueTypeWorkflowMapping,
  IssueTypesWorkflowMapping,
  PageBeanWorkflowScheme,
  WorkflowScheme,
  WorkflowSchemeProjectSwitchBean,
  WorkflowSchemeProjectUsageDto,
  WorkflowSchemeReadRequest,
  WorkflowSchemeReadResponse,
  WorkflowSchemeUpdateRequest,
  WorkflowSchemeUpdateRequiredMappingsRequest,
  WorkflowSchemeUpdateRequiredMappingsResponse,
} from "../types/index.ts";

/**
 * Get all workflow schemes
 *
 * Returns a [paginated](#pagination) list of all workflow schemes, not including draft workflow schemes.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.startAt - The index of the first item to return in a page of results (page offset).
 * @param params.maxResults - The maximum number of items to return per page.
 * @returns Returned if the request is successful.
 */
export function getAllWorkflowSchemes(
  ctx: ClientContext,
  params?: {
    /**
     * The index of the first item to return in a page of results (page offset).
     *
     * @default 0
     * @format int64
     */
    startAt?: number;
    /**
     * The maximum number of items to return per page.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<PageBeanWorkflowScheme> {
  const { startAt, maxResults } = params ?? {};
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme",
    query: { startAt, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Create workflow scheme
 *
 * Creates a workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @returns Returned if the request is successful.
 */
export function createWorkflowScheme(
  ctx: ClientContext,
  params: WorkflowScheme,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Switch workflow scheme for project
 *
 * Switches a workflow scheme for a project.
 *
 * Workflow schemes can only be assigned to classic projects.
 *
 * **Calculating required mappings:** If statuses from the current workflow scheme won't exist in the target workflow scheme, you must provide `mappingsByIssueTypeOverride` to specify how issues with those statuses should be migrated. Use [the required workflow scheme mappings API](#api-rest-api-3-workflowscheme-update-mappings-post) to determine which statuses and issue types require mappings.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 */
export function switchWorkflowSchemeForProject(
  ctx: ClientContext,
  params: WorkflowSchemeProjectSwitchBean,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/project/switch",
    headers: options?.headers,
    body: params,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Bulk get workflow schemes
 *
 * Returns a list of workflow schemes by providing workflow scheme IDs or project IDs.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* global permission to access all, including project-scoped, workflow schemes
 *  *  *Administer projects* project permissions to access project-scoped workflow schemes
 *
 * @returns Returned if the request is successful.
 */
export function readWorkflowSchemes(
  ctx: ClientContext,
  params: WorkflowSchemeReadRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowSchemeReadResponse[]> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/read",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update workflow scheme
 *
 * Updates company-managed and team-managed project workflow schemes. This API doesn't have a concept of draft, so any changes made to a workflow scheme are immediately available. When changing the available statuses for issue types, an [asynchronous task](#async) migrates the issues as defined in the provided mappings.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* project permission to update all, including global-scoped, workflow schemes.
 *  *  *Administer projects* project permission to update project-scoped workflow schemes.
 *
 * @returns Returned if the request is successful and there is no asynchronous task.
 */
export function updateSchemes(
  ctx: ClientContext,
  params: WorkflowSchemeUpdateRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/update",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get required status mappings for workflow scheme update
 *
 * Gets the required status mappings for the desired changes to a workflow scheme. The results are provided per issue type and workflow. When updating a workflow scheme, status mappings can be provided per issue type, per workflow, or both.
 *
 * **[Permissions](#permissions) required:**
 *
 *  *  *Administer Jira* permission to update all, including global-scoped, workflow schemes.
 *  *  *Administer projects* project permission to update project-scoped workflow schemes.
 *
 * @returns Returned if the request is successful.
 */
export function getRequiredWorkflowSchemeMappings(
  ctx: ClientContext,
  params: WorkflowSchemeUpdateRequiredMappingsRequest,
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowSchemeUpdateRequiredMappingsResponse> {
  return ctx.request({
    method: "post",
    path: "/rest/api/3/workflowscheme/update/mappings",
    headers: options?.headers,
    body: params,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get workflow scheme
 *
 * Returns a workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
 * @param params.returnDraftIfExists - Returns the workflow scheme's draft rather than scheme itself, if set to true. If the workflow scheme does not have a draft, then the workflow scheme is returned.
 * @returns Returned if the request is successful.
 */
export function getWorkflowScheme(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     *
     * @format int64
     */
    id: number;
    /**
     * Returns the workflow scheme's draft rather than scheme itself, if set to true. If the workflow scheme does not have a draft, then the workflow scheme is returned.
     *
     * @default false
     */
    returnDraftIfExists?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, returnDraftIfExists } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}",
    pathParams: { id },
    query: { returnDraftIfExists },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Classic update workflow scheme
 *
 * Updates a company-manged project workflow scheme, including the name, default workflow, issue type to project mappings, and more. If the workflow scheme is active (that is, being used by at least one project), then a draft workflow scheme is created or updated instead, provided that `updateDraftIfNeeded` is set to `true`.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id_path - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
 * @returns Returned if the request is successful.
 */
export function updateWorkflowScheme(
  ctx: ClientContext,
  params: WorkflowScheme & {
    /**
     * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     *
     * @format int64
     */
    id_path: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}",
    pathParams: { id: id_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete workflow scheme
 *
 * Deletes a workflow scheme. Note that a workflow scheme cannot be deleted if it is active (that is, being used by at least one project).
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
 * @returns Returned if the request is successful.
 */
export function deleteWorkflowScheme(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<unknown> {
  const { id } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}",
    pathParams: { id },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get default workflow
 *
 * Returns the default workflow for a workflow scheme. The default workflow is the workflow that is assigned any issue types that have not been mapped to any other workflow. The default workflow has *All Unassigned Issue Types* listed in its issue types for the workflow scheme in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.returnDraftIfExists - Set to `true` to return the default workflow for the workflow scheme's draft rather than scheme itself. If the workflow scheme does not have a draft, then the default workflow for the workflow scheme is returned.
 * @returns Returned if the request is successful.
 */
export function getDefaultWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /**
     * Set to `true` to return the default workflow for the workflow scheme's draft rather than scheme itself. If the workflow scheme does not have a draft, then the default workflow for the workflow scheme is returned.
     *
     * @default false
     */
    returnDraftIfExists?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<DefaultWorkflow> {
  const { id, returnDraftIfExists } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/default",
    pathParams: { id },
    query: { returnDraftIfExists },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Update default workflow
 *
 * Sets the default workflow for a workflow scheme.
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request object and a draft workflow scheme is created or updated with the new default workflow. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @returns Returned if the request is successful.
 */
export function updateDefaultWorkflow(
  ctx: ClientContext,
  params: DefaultWorkflow & {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/default",
    pathParams: { id },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete default workflow
 *
 * Resets the default workflow for a workflow scheme. That is, the default workflow is set to Jira's system workflow (the *jira* workflow).
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the default workflow reset. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
 * @returns Returned if the request is successful.
 */
export function deleteDefaultWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`. */
    updateDraftIfNeeded?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, updateDraftIfNeeded } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/default",
    pathParams: { id },
    query: { updateDraftIfNeeded },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get workflow for issue type in workflow scheme
 *
 * Returns the issue type-workflow mapping for an issue type in a workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.issueType - The ID of the issue type.
 * @param params.returnDraftIfExists - Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
 * @returns Returned if the request is successful.
 */
export function getWorkflowSchemeIssueType(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issueType: string;
    /**
     * Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
     *
     * @default false
     */
    returnDraftIfExists?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypeWorkflowMapping> {
  const { id, issueType, returnDraftIfExists } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}",
    pathParams: { id, issueType },
    query: { returnDraftIfExists },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set workflow for issue type in workflow scheme
 *
 * Sets the workflow for an issue type in a workflow scheme.
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request body and a draft workflow scheme is created or updated with the new issue type-workflow mapping. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.issue_type_path - The ID of the issue type.
 * @returns Returned if the request is successful.
 */
export function setWorkflowSchemeIssueType(
  ctx: ClientContext,
  params: IssueTypeWorkflowMapping & {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issue_type_path: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, issue_type_path, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}",
    pathParams: { id, issueType: issue_type_path },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete workflow for issue type in workflow scheme
 *
 * Deletes the issue type-workflow mapping for an issue type in a workflow scheme.
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the issue type-workflow mapping deleted. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.issueType - The ID of the issue type.
 * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and update the mapping in the draft, when the workflow scheme cannot be edited. Defaults to `false`.
 * @returns Returned if the request is successful.
 */
export function deleteWorkflowSchemeIssueType(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The ID of the issue type. */
    issueType: string;
    /**
     * Set to true to create or update the draft of a workflow scheme and update the mapping in the draft, when the workflow scheme cannot be edited. Defaults to `false`.
     *
     * @default false
     */
    updateDraftIfNeeded?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, issueType, updateDraftIfNeeded } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/issuetype/{issueType}",
    pathParams: { id, issueType },
    query: { updateDraftIfNeeded },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get issue types for workflows in workflow scheme
 *
 * Returns the workflow-issue type mappings for a workflow scheme.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.workflowName - The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow.
 * @param params.returnDraftIfExists - Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
 * @returns Returned if the request is successful.
 */
export function getWorkflow(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow. */
    workflowName?: string;
    /**
     * Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
     *
     * @default false
     */
    returnDraftIfExists?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<IssueTypesWorkflowMapping> {
  const { id, workflowName, returnDraftIfExists } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{id}/workflow",
    pathParams: { id },
    query: { workflowName, returnDraftIfExists },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Set issue types for workflow in workflow scheme
 *
 * Sets the issue types for a workflow in a workflow scheme. The workflow can also be set as the default workflow for the workflow scheme. Unmapped issues types are mapped to the default workflow.
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request body and a draft workflow scheme is created or updated with the new workflow-issue types mappings. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.workflowName - The name of the workflow.
 * @returns Returned if the request is successful.
 */
export function updateWorkflowMapping(
  ctx: ClientContext,
  params: IssueTypesWorkflowMapping & {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The name of the workflow. */
    workflowName: string;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowScheme> {
  const { id, workflowName, ...body } = params;
  return ctx.request({
    method: "put",
    path: "/rest/api/3/workflowscheme/{id}/workflow",
    pathParams: { id },
    query: { workflowName },
    headers: options?.headers,
    body: body,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Delete issue types for workflow in workflow scheme
 *
 * Deletes the workflow-issue type mapping for a workflow in a workflow scheme.
 *
 * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the workflow-issue type mapping deleted. The draft workflow scheme can be published in Jira.
 *
 * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
 *
 * @param params.id - The ID of the workflow scheme.
 * @param params.workflowName - The name of the workflow.
 * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
 * @returns Returned if the request is successful.
 */
export function deleteWorkflowMapping(
  ctx: ClientContext,
  params: {
    /**
     * The ID of the workflow scheme.
     *
     * @format int64
     */
    id: number;
    /** The name of the workflow. */
    workflowName: string;
    /**
     * Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
     *
     * @default false
     */
    updateDraftIfNeeded?: boolean;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<void> {
  const { id, workflowName, updateDraftIfNeeded } = params;
  return ctx.request({
    method: "delete",
    path: "/rest/api/3/workflowscheme/{id}/workflow",
    pathParams: { id },
    query: { workflowName, updateDraftIfNeeded },
    headers: options?.headers,
    responseType: "void",
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * Get projects which are using a given workflow scheme
 *
 * Returns a page of projects using a given workflow scheme.
 *
 * @param params.workflowSchemeId - The workflow scheme ID
 * @param params.nextPageToken - The cursor for pagination
 * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
 * @returns Returned if the request is successful.
 */
export function getProjectUsagesForWorkflowScheme(
  ctx: ClientContext,
  params: {
    /** The workflow scheme ID */
    workflowSchemeId: string;
    /** The cursor for pagination */
    nextPageToken?: string;
    /**
     * The maximum number of results to return. Must be an integer between 1 and 200.
     *
     * @default 50
     * @format int32
     */
    maxResults?: number;
  },
  options?: {
    headers?: Record<string, string | number | boolean>;
    signal?: AbortSignal;
    extensions?: Record<string, unknown>;
  }
): Promise<WorkflowSchemeProjectUsageDto> {
  const { workflowSchemeId, nextPageToken, maxResults } = params;
  return ctx.request({
    method: "get",
    path: "/rest/api/3/workflowscheme/{workflowSchemeId}/projectUsages",
    pathParams: { workflowSchemeId },
    query: { nextPageToken, maxResults },
    headers: options?.headers,
    signal: options?.signal,
    extensions: options?.extensions,
  });
}

/**
 * This resource represents workflow schemes. Use it to manage workflow schemes and the workflow scheme's workflows and issue types.
 *
 * A workflow scheme maps issue types to workflows. A workflow scheme can be associated with one or more projects, which enables the projects to use the workflow-issue type mappings.
 *
 * Active workflow schemes (workflow schemes that are used by projects) cannot be edited. When an active workflow scheme is edited, a draft copy of the scheme is created. The draft workflow scheme is then be edited and published (replacing the active scheme).
 *
 * See [Configuring workflow schemes](https://confluence.atlassian.com/x/tohKLg) for more information.
 */
export function createWorkflowSchemesService(ctx: ClientContext) {
  return {
    /**
     * Get all workflow schemes
     *
     * Returns a [paginated](#pagination) list of all workflow schemes, not including draft workflow schemes.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.startAt - The index of the first item to return in a page of results (page offset).
     * @param params.maxResults - The maximum number of items to return per page.
     * @returns Returned if the request is successful.
     */
    getAllWorkflowSchemes(
      params?: {
        /**
         * The index of the first item to return in a page of results (page offset).
         *
         * @default 0
         * @format int64
         */
        startAt?: number;
        /**
         * The maximum number of items to return per page.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<PageBeanWorkflowScheme> {
      return getAllWorkflowSchemes(ctx, params, options);
    },

    /**
     * Create workflow scheme
     *
     * Creates a workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @returns Returned if the request is successful.
     */
    createWorkflowScheme(
      params: WorkflowScheme,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return createWorkflowScheme(ctx, params, options);
    },

    /**
     * Switch workflow scheme for project
     *
     * Switches a workflow scheme for a project.
     *
     * Workflow schemes can only be assigned to classic projects.
     *
     * **Calculating required mappings:** If statuses from the current workflow scheme won't exist in the target workflow scheme, you must provide `mappingsByIssueTypeOverride` to specify how issues with those statuses should be migrated. Use [the required workflow scheme mappings API](#api-rest-api-3-workflowscheme-update-mappings-post) to determine which statuses and issue types require mappings.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     */
    switchWorkflowSchemeForProject(
      params: WorkflowSchemeProjectSwitchBean,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return switchWorkflowSchemeForProject(ctx, params, options);
    },

    /**
     * Bulk get workflow schemes
     *
     * Returns a list of workflow schemes by providing workflow scheme IDs or project IDs.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* global permission to access all, including project-scoped, workflow schemes
     *  *  *Administer projects* project permissions to access project-scoped workflow schemes
     *
     * @returns Returned if the request is successful.
     */
    readWorkflowSchemes(
      params: WorkflowSchemeReadRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowSchemeReadResponse[]> {
      return readWorkflowSchemes(ctx, params, options);
    },

    /**
     * Update workflow scheme
     *
     * Updates company-managed and team-managed project workflow schemes. This API doesn't have a concept of draft, so any changes made to a workflow scheme are immediately available. When changing the available statuses for issue types, an [asynchronous task](#async) migrates the issues as defined in the provided mappings.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* project permission to update all, including global-scoped, workflow schemes.
     *  *  *Administer projects* project permission to update project-scoped workflow schemes.
     *
     * @returns Returned if the request is successful and there is no asynchronous task.
     */
    updateSchemes(
      params: WorkflowSchemeUpdateRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return updateSchemes(ctx, params, options);
    },

    /**
     * Get required status mappings for workflow scheme update
     *
     * Gets the required status mappings for the desired changes to a workflow scheme. The results are provided per issue type and workflow. When updating a workflow scheme, status mappings can be provided per issue type, per workflow, or both.
     *
     * **[Permissions](#permissions) required:**
     *
     *  *  *Administer Jira* permission to update all, including global-scoped, workflow schemes.
     *  *  *Administer projects* project permission to update project-scoped workflow schemes.
     *
     * @returns Returned if the request is successful.
     */
    getRequiredWorkflowSchemeMappings(
      params: WorkflowSchemeUpdateRequiredMappingsRequest,
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowSchemeUpdateRequiredMappingsResponse> {
      return getRequiredWorkflowSchemeMappings(ctx, params, options);
    },

    /**
     * Get workflow scheme
     *
     * Returns a workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     * @param params.returnDraftIfExists - Returns the workflow scheme's draft rather than scheme itself, if set to true. If the workflow scheme does not have a draft, then the workflow scheme is returned.
     * @returns Returned if the request is successful.
     */
    getWorkflowScheme(
      params: {
        /**
         * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
         *
         * @format int64
         */
        id: number;
        /**
         * Returns the workflow scheme's draft rather than scheme itself, if set to true. If the workflow scheme does not have a draft, then the workflow scheme is returned.
         *
         * @default false
         */
        returnDraftIfExists?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return getWorkflowScheme(ctx, params, options);
    },

    /**
     * Classic update workflow scheme
     *
     * Updates a company-manged project workflow scheme, including the name, default workflow, issue type to project mappings, and more. If the workflow scheme is active (that is, being used by at least one project), then a draft workflow scheme is created or updated instead, provided that `updateDraftIfNeeded` is set to `true`.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id_path - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     * @returns Returned if the request is successful.
     */
    updateWorkflowScheme(
      params: WorkflowScheme & {
        /**
         * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
         *
         * @format int64
         */
        id_path: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateWorkflowScheme(ctx, params, options);
    },

    /**
     * Delete workflow scheme
     *
     * Deletes a workflow scheme. Note that a workflow scheme cannot be deleted if it is active (that is, being used by at least one project).
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
     * @returns Returned if the request is successful.
     */
    deleteWorkflowScheme(
      params: {
        /**
         * The ID of the workflow scheme. Find this ID by editing the desired workflow scheme in Jira. The ID is shown in the URL as `schemeId`. For example, *schemeId=10301*.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<unknown> {
      return deleteWorkflowScheme(ctx, params, options);
    },

    /**
     * Get default workflow
     *
     * Returns the default workflow for a workflow scheme. The default workflow is the workflow that is assigned any issue types that have not been mapped to any other workflow. The default workflow has *All Unassigned Issue Types* listed in its issue types for the workflow scheme in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.returnDraftIfExists - Set to `true` to return the default workflow for the workflow scheme's draft rather than scheme itself. If the workflow scheme does not have a draft, then the default workflow for the workflow scheme is returned.
     * @returns Returned if the request is successful.
     */
    getDefaultWorkflow(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /**
         * Set to `true` to return the default workflow for the workflow scheme's draft rather than scheme itself. If the workflow scheme does not have a draft, then the default workflow for the workflow scheme is returned.
         *
         * @default false
         */
        returnDraftIfExists?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<DefaultWorkflow> {
      return getDefaultWorkflow(ctx, params, options);
    },

    /**
     * Update default workflow
     *
     * Sets the default workflow for a workflow scheme.
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request object and a draft workflow scheme is created or updated with the new default workflow. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @returns Returned if the request is successful.
     */
    updateDefaultWorkflow(
      params: DefaultWorkflow & {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateDefaultWorkflow(ctx, params, options);
    },

    /**
     * Delete default workflow
     *
     * Resets the default workflow for a workflow scheme. That is, the default workflow is set to Jira's system workflow (the *jira* workflow).
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the default workflow reset. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
     * @returns Returned if the request is successful.
     */
    deleteDefaultWorkflow(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`. */
        updateDraftIfNeeded?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return deleteDefaultWorkflow(ctx, params, options);
    },

    /**
     * Get workflow for issue type in workflow scheme
     *
     * Returns the issue type-workflow mapping for an issue type in a workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.issueType - The ID of the issue type.
     * @param params.returnDraftIfExists - Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
     * @returns Returned if the request is successful.
     */
    getWorkflowSchemeIssueType(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issueType: string;
        /**
         * Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
         *
         * @default false
         */
        returnDraftIfExists?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypeWorkflowMapping> {
      return getWorkflowSchemeIssueType(ctx, params, options);
    },

    /**
     * Set workflow for issue type in workflow scheme
     *
     * Sets the workflow for an issue type in a workflow scheme.
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request body and a draft workflow scheme is created or updated with the new issue type-workflow mapping. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.issue_type_path - The ID of the issue type.
     * @returns Returned if the request is successful.
     */
    setWorkflowSchemeIssueType(
      params: IssueTypeWorkflowMapping & {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issue_type_path: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return setWorkflowSchemeIssueType(ctx, params, options);
    },

    /**
     * Delete workflow for issue type in workflow scheme
     *
     * Deletes the issue type-workflow mapping for an issue type in a workflow scheme.
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the issue type-workflow mapping deleted. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.issueType - The ID of the issue type.
     * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and update the mapping in the draft, when the workflow scheme cannot be edited. Defaults to `false`.
     * @returns Returned if the request is successful.
     */
    deleteWorkflowSchemeIssueType(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The ID of the issue type. */
        issueType: string;
        /**
         * Set to true to create or update the draft of a workflow scheme and update the mapping in the draft, when the workflow scheme cannot be edited. Defaults to `false`.
         *
         * @default false
         */
        updateDraftIfNeeded?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return deleteWorkflowSchemeIssueType(ctx, params, options);
    },

    /**
     * Get issue types for workflows in workflow scheme
     *
     * Returns the workflow-issue type mappings for a workflow scheme.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.workflowName - The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow.
     * @param params.returnDraftIfExists - Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
     * @returns Returned if the request is successful.
     */
    getWorkflow(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The name of a workflow in the scheme. Limits the results to the workflow-issue type mapping for the specified workflow. */
        workflowName?: string;
        /**
         * Returns the mapping from the workflow scheme's draft rather than the workflow scheme, if set to true. If no draft exists, the mapping from the workflow scheme is returned.
         *
         * @default false
         */
        returnDraftIfExists?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<IssueTypesWorkflowMapping> {
      return getWorkflow(ctx, params, options);
    },

    /**
     * Set issue types for workflow in workflow scheme
     *
     * Sets the issue types for a workflow in a workflow scheme. The workflow can also be set as the default workflow for the workflow scheme. Unmapped issues types are mapped to the default workflow.
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` in the request body and a draft workflow scheme is created or updated with the new workflow-issue types mappings. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.workflowName - The name of the workflow.
     * @returns Returned if the request is successful.
     */
    updateWorkflowMapping(
      params: IssueTypesWorkflowMapping & {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The name of the workflow. */
        workflowName: string;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowScheme> {
      return updateWorkflowMapping(ctx, params, options);
    },

    /**
     * Delete issue types for workflow in workflow scheme
     *
     * Deletes the workflow-issue type mapping for a workflow in a workflow scheme.
     *
     * Note that active workflow schemes cannot be edited. If the workflow scheme is active, set `updateDraftIfNeeded` to `true` and a draft workflow scheme is created or updated with the workflow-issue type mapping deleted. The draft workflow scheme can be published in Jira.
     *
     * **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
     *
     * @param params.id - The ID of the workflow scheme.
     * @param params.workflowName - The name of the workflow.
     * @param params.updateDraftIfNeeded - Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
     * @returns Returned if the request is successful.
     */
    deleteWorkflowMapping(
      params: {
        /**
         * The ID of the workflow scheme.
         *
         * @format int64
         */
        id: number;
        /** The name of the workflow. */
        workflowName: string;
        /**
         * Set to true to create or update the draft of a workflow scheme and delete the mapping from the draft, when the workflow scheme cannot be edited. Defaults to `false`.
         *
         * @default false
         */
        updateDraftIfNeeded?: boolean;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<void> {
      return deleteWorkflowMapping(ctx, params, options);
    },

    /**
     * Get projects which are using a given workflow scheme
     *
     * Returns a page of projects using a given workflow scheme.
     *
     * @param params.workflowSchemeId - The workflow scheme ID
     * @param params.nextPageToken - The cursor for pagination
     * @param params.maxResults - The maximum number of results to return. Must be an integer between 1 and 200.
     * @returns Returned if the request is successful.
     */
    getProjectUsagesForWorkflowScheme(
      params: {
        /** The workflow scheme ID */
        workflowSchemeId: string;
        /** The cursor for pagination */
        nextPageToken?: string;
        /**
         * The maximum number of results to return. Must be an integer between 1 and 200.
         *
         * @default 50
         * @format int32
         */
        maxResults?: number;
      },
      options?: {
        headers?: Record<string, string | number | boolean>;
        signal?: AbortSignal;
        extensions?: Record<string, unknown>;
      }
    ): Promise<WorkflowSchemeProjectUsageDto> {
      return getProjectUsagesForWorkflowScheme(ctx, params, options);
    },
  };
}
